# 🔒 ИСПРАВЛЕНИЕ: Персистентное хранение данных

## ❌ Проблема

После деплоя терялись данные:
- Балансы пользователей
- История генераций
- История пополнений
- Все JSON файлы данных

**Причина:** Данные хранились в контейнере, который пересоздавался при каждом деплое.

## ✅ Решение

### 1. Директория данных через переменную окружения

Добавлена поддержка переменной окружения `DATA_DIR`:
- По умолчанию: `./data`
- Можно настроить: `DATA_DIR=/app/data` или `DATA_DIR=/persistent/data`
- Все JSON файлы сохраняются в этой директории

### 2. Автоматическое создание директории

При старте бота:
- Автоматически создается директория данных, если её нет
- Проверяется доступность записи
- Логируется расположение директории

### 3. Обновлен Dockerfile

- Создается директория `/app/data` при сборке
- Устанавливается переменная окружения `DATA_DIR=/app/data`
- Директория готова для монтирования volume

## 🚀 Настройка персистентного хранилища

### Вариант 1: Render.com (рекомендуется)

1. **В панели Render:**
   - Откройте ваш сервис
   - Перейдите в "Environment"
   - Добавьте переменную: `DATA_DIR=/app/data`

2. **Настройте Volume (если доступно):**
   - В Render.com некоторые планы поддерживают volumes
   - Создайте volume и смонтируйте его в `/app/data`

3. **Альтернатива - внешнее хранилище:**
   - Используйте S3 или другой cloud storage
   - Настройте синхронизацию данных

### Вариант 2: Docker Compose (локально/сервер)

Создайте `docker-compose.yml`:

```yaml
version: '3.8'

services:
  bot:
    build: .
    environment:
      - DATA_DIR=/app/data
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - KIE_API_KEY=${KIE_API_KEY}
    volumes:
      - bot_data:/app/data  # Персистентное хранилище
    restart: unless-stopped

volumes:
  bot_data:  # Именованный volume сохраняется между перезапусками
```

### Вариант 3: Docker с volume

```bash
# Создайте volume
docker volume create bot_data

# Запустите контейнер с volume
docker run -d \
  -e DATA_DIR=/app/data \
  -v bot_data:/app/data \
  -e TELEGRAM_BOT_TOKEN=your_token \
  -e KIE_API_KEY=your_key \
  your-image-name
```

### Вариант 4: Bind mount (для разработки)

```bash
docker run -d \
  -e DATA_DIR=/app/data \
  -v $(pwd)/data:/app/data \
  -e TELEGRAM_BOT_TOKEN=your_token \
  -e KIE_API_KEY=your_key \
  your-image-name
```

## 📁 Структура данных

Все файлы данных сохраняются в `DATA_DIR`:

```
data/
├── user_balances.json          # Балансы пользователей
├── payments.json              # История платежей
├── generations_history.json    # История генераций
├── user_languages.json        # Языковые настройки
├── daily_free_generations.json # Бесплатные генерации
├── promocodes.json            # Промокоды
├── referrals.json             # Реферальная система
├── broadcasts.json            # Статистика рассылок
├── blocked_users.json         # Заблокированные пользователи
├── admin_limits.json          # Лимиты админов
├── currency_rate.json         # Курс валют
└── gift_claimed.json          # Подарки
```

## 🔍 Проверка работы

При старте бота в логах должно быть:

```
📁 Data directory: /app/data (from DATA_DIR env var or default)
📁 Data directory exists: True
📁 Data directory writable: True
✅ Created data file: /app/data/user_balances.json
✅ Created data file: /app/data/payments.json
...
🔒 Critical file: /app/data/user_balances.json (1234 bytes)
🔒 Critical file: /app/data/generations_history.json (5678 bytes)
🔒 Critical file: /app/data/payments.json (9012 bytes)
```

## ⚠️ Важно

1. **Резервное копирование:**
   - Регулярно делайте бэкапы директории `data/`
   - Можно настроить автоматическое копирование в S3

2. **Права доступа:**
   - Убедитесь, что директория данных доступна для записи
   - Проверьте права: `chmod 755 /app/data`

3. **Миграция существующих данных:**
   - Если данные были в корне проекта, скопируйте их в `data/`
   - Или установите `DATA_DIR=.` для обратной совместимости

## 🔄 Миграция данных

Если у вас уже есть данные в корне проекта:

```bash
# 1. Остановите бота
# 2. Создайте директорию данных
mkdir -p data

# 3. Скопируйте существующие файлы
cp user_balances.json data/
cp payments.json data/
cp generations_history.json data/
# ... и т.д. для всех JSON файлов

# 4. Установите DATA_DIR=/app/data
# 5. Перезапустите бота
```

## ✅ Гарантии

После настройки:
- ✅ Данные сохраняются между деплоями
- ✅ Балансы не теряются
- ✅ История генераций сохраняется
- ✅ История платежей сохраняется
- ✅ Все настройки пользователей сохраняются

## 🆘 Если данные все еще теряются

1. Проверьте логи при старте - должна быть строка с `Data directory`
2. Убедитесь, что `DATA_DIR` установлена правильно
3. Проверьте, что volume смонтирован (для Docker)
4. Проверьте права доступа к директории
5. Проверьте, что файлы действительно создаются в нужной директории

---

**После настройки персистентного хранилища данные будут сохраняться на 100%!**


