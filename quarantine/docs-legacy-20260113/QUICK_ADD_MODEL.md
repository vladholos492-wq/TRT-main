# ⚡ Быстрое добавление модели (Шпаргалка)

## 📋 Минимальные шаги

### 1. Добавить в `kie_models.py`:

```python
{
    "id": "model-id",                    # ID из KIE API
    "name": "Название",
    "description": "Описание",
    "category": "Фото",                  # Существующая категория
    "emoji": "🎨",
    "pricing": "X кредитов",
    "input_params": {
        "prompt": {
            "type": "string",
            "description": "Описание",
            "required": True,
            "max_length": 1000
        },
        "параметр": {
            "type": "string",
            "enum": ["вариант1", "вариант2"],  # Для кнопок
            "required": False
        }
    }
}
```

### 2. Добавить расчет цены в `bot_kie.py` (функция `calculate_price_rub`):

```python
elif model_id == "model-id":
    base_credits = 10.0  # Ваша цена в кредитах
    # Или с условием:
    if params.get("quality") == "high":
        base_credits = 20.0
    else:
        base_credits = 10.0
```

### 3. Добавить default_params (для минимальной цены):

```python
if model_id == "model-id":
    default_params = {"параметр": "самое_дешевое_значение"}
```

### 4. Готово! Перезапустить бота.

---

## 🎯 Пример: Простая модель за 5 кредитов

### `kie_models.py`:
```python
{
    "id": "simple-gen",
    "name": "Simple Generator",
    "description": "Простая генерация",
    "category": "Фото",
    "emoji": "🖼️",
    "pricing": "5 кредитов",
    "input_params": {
        "prompt": {
            "type": "string",
            "description": "Описание",
            "required": True,
            "max_length": 1000
        }
    }
}
```

### `bot_kie.py` (в `calculate_price_rub`):
```python
elif model_id == "simple-gen":
    base_credits = 5.0
```

### `bot_kie.py` (default_params):
```python
if model_id == "simple-gen":
    default_params = {}  # Нет параметров, влияющих на цену
```

---

## 📝 Где искать в коде:

- **Модели**: `kie_models.py` → `KIE_MODELS`
- **Цены**: `bot_kie.py` → `calculate_price_rub()` (строка ~109)
- **Форматирование**: `bot_kie.py` → `get_model_price_text()` (строка ~150)
- **Default params**: `bot_kie.py` → поиск `default_params = {}` (строки ~1744, 1171, 1233, 1256)

---

## ✅ Проверка:

1. Модель в меню "Все модели" ✓
2. Модель в категории ✓
3. Правильная цена ✓
4. Доступные генерации ✓
5. Ввод параметров работает ✓
6. Генерация работает ✓

---

**Подробная инструкция:** см. `ADD_NEW_MODEL.md`

