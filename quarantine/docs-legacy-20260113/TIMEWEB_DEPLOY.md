# 🚀 Развертывание на Timeweb

## 📋 Требования

- Аккаунт на Timeweb Cloud
- Telegram Bot Token от @BotFather
- KIE API Key

## 🔧 Шаги развертывания

### 1. Подготовка репозитория

1. Загрузите все файлы из папки `12-main-for-github` в ваш GitHub репозиторий
2. Убедитесь, что `.env` файл НЕ загружен (он в `.gitignore`)

### 2. Создание проекта на Timeweb

1. Войдите в панель управления Timeweb
2. Создайте новый проект (Docker)
3. Подключите ваш GitHub репозиторий
4. **ВАЖНО:** Оставьте поля "Build Command" и "Run Command" **ПУСТЫМИ**
   - Timeweb автоматически использует `CMD ["npm", "start"]` из Dockerfile

### 3. Настройка переменных окружения

В панели Timeweb добавьте следующие переменные окружения:

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
KIE_API_KEY=ваш_kie_api_key
ADMIN_ID=ваш_telegram_id
```

**Дополнительные переменные (опционально):**
```
NODE_ENV=production
PYTHONUNBUFFERED=1
```

### 4. Запуск

1. После настройки переменных окружения, Timeweb автоматически:
   - Соберет Docker образ
   - Установит все зависимости (Node.js и Python)
   - Запустит бота через `npm start`

### 5. Проверка работы

1. Откройте логи в панели Timeweb
2. Должны появиться сообщения:
   ```
   ============================================================
   KIE Telegram Bot - Starting...
   ============================================================
   Node.js version: v24.x.x
   Platform: linux
   Working directory: /app
   ============================================================
   ```

3. Проверьте бота в Telegram:
   - Отправьте `/start`
   - Бот должен ответить

## 🔍 Диагностика проблем

### Бот не отвечает

1. **Проверьте логи:**
   - Откройте логи в панели Timeweb
   - Ищите ошибки (красные сообщения)

2. **Проверьте переменные окружения:**
   - Убедитесь, что `TELEGRAM_BOT_TOKEN` и `KIE_API_KEY` установлены
   - Проверьте, что значения правильные (без лишних пробелов)

3. **Проверьте сборку:**
   - Убедитесь, что Docker образ собрался успешно
   - Проверьте, что нет ошибок при установке зависимостей

### Ошибка "Python not found"

- Dockerfile автоматически устанавливает Python 3
- Если ошибка все еще есть, проверьте логи сборки

### Ошибка "npm run build"

- **НЕ используйте** команду `npm run build`
- Оставьте поле "Build Command" **ПУСТЫМ** в панели Timeweb
- Dockerfile не содержит `RUN npm run build`

### Логи не отображаются

- Логи должны автоматически появляться в панели Timeweb
- Если не видно логов, попробуйте перезапустить контейнер

## 📝 Структура проекта

```
/app
├── index.js              # Node.js wrapper (запускает Python бота)
├── run_bot.py           # Python скрипт запуска
├── bot_kie.py           # Основная логика бота
├── kie_models.py        # Модели KIE AI
├── package.json         # Node.js зависимости
├── requirements.txt     # Python зависимости
├── Dockerfile           # Docker конфигурация
└── .env.example         # Пример переменных окружения
```

## 🔄 Обновление бота

1. Обновите код в GitHub репозитории
2. Timeweb автоматически пересоберет и перезапустит бота
3. Или вручную запустите пересборку в панели Timeweb

## ✅ Проверочный список

- [ ] Все файлы загружены в GitHub
- [ ] `.env` файл НЕ в репозитории
- [ ] Переменные окружения установлены в Timeweb
- [ ] Поля "Build Command" и "Run Command" **ПУСТЫЕ**
- [ ] Docker образ собрался успешно
- [ ] Бот отвечает на `/start` в Telegram
- [ ] Логи отображаются в панели Timeweb

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи в панели Timeweb
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что Dockerfile правильный (FROM node:24-slim)
4. Убедитесь, что package.json содержит правильный скрипт "start"

