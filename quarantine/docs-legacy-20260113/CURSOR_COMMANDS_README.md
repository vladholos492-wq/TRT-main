# 📋 ВСЕ КОМАНДЫ ДЛЯ CURSOR

Этот файл содержит все команды, которые ты можешь скопировать и отправить в Cursor.

---

## 🧪 1. АВТОМАТИЧЕСКОЕ ТЕСТИРОВАНИЕ ВСЕХ МОДЕЛЕЙ

**Файл с командой:** `CURSOR_COMMAND_AUTO_TEST.md`

**Что делает:**
- Создает автоматический тест для всех моделей KIE AI
- Проверяет, что все модели корректно работают с KIE API
- Генерирует тестовые параметры автоматически
- Отправляет запросы в KIE API
- Сохраняет результаты в JSON

**Как использовать:**
1. Открой `CURSOR_COMMAND_AUTO_TEST.md`
2. Скопируй всю команду
3. Вставь в Cursor
4. Дождись создания файла `auto_test_all_models_generation.py`
5. Запусти: `python auto_test_all_models_generation.py`

---

## 🔧 2. ПРОВЕРКА И ИСПРАВЛЕНИЕ ПРОБЛЕМ С RENDER

**Файл с командой:** `CURSOR_COMMAND_RENDER_FIX.md`

**Что делает:**
- Проверяет конфигурацию для деплоя на Render
- Исправляет проблемы с health check
- Проверяет переменные окружения
- Проверяет подключение к базе данных
- Создает скрипт проверки конфигурации

**Как использовать:**
1. Открой `CURSOR_COMMAND_RENDER_FIX.md`
2. Скопируй всю команду
3. Вставь в Cursor
4. Дождись исправлений
5. Запусти проверку: `python check_render_config.py`

---

## 📝 КАК РАБОТАТЬ С КОМАНДАМИ

### Шаг 1: Выбери нужную команду
- Для тестирования моделей → используй команду #1
- Для исправления проблем с Render → используй команду #2

### Шаг 2: Скопируй команду
- Открой соответствующий `.md` файл
- Скопируй ВСЮ команду (от начала до конца)
- Вставь в Cursor

### Шаг 3: Дождись выполнения
- Cursor прочитает команду
- Выполнит все инструкции
- Создаст/исправит файлы
- Покажет результат

### Шаг 4: Проверь результат
- Запусти созданные скрипты
- Проверь логи
- Убедись, что все работает

---

## 🚀 БЫСТРЫЙ СТАРТ

### Тестирование моделей:
```bash
# 1. Отправь команду из CURSOR_COMMAND_AUTO_TEST.md в Cursor
# 2. Запусти тест
python auto_test_all_models_generation.py

# 3. Проверь результаты
cat model_generation_test_results_*.json
```

### Проверка конфигурации Render:
```bash
# 1. Отправь команду из CURSOR_COMMAND_RENDER_FIX.md в Cursor
# 2. Запусти проверку
python check_render_config.py

# 3. Исправь найденные проблемы
# 4. Повтори проверку
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Всегда проверяй результаты** - не полагайся только на автоматику
2. **Читай логи** - там может быть важная информация
3. **Сохраняй результаты тестов** - они помогут найти проблемы
4. **Проверяй перед деплоем** - используй `check_render_config.py`

---

## 📊 ЧТО ДЕЛАТЬ ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Тесты не проходят:
1. Проверь `KIE_API_KEY` - должен быть установлен
2. Проверь логи - там будет описание ошибки
3. Проверь параметры модели - возможно, нужны другие значения
4. Проверь документацию KIE AI - возможно, API изменился

### Render не запускается:
1. Запусти `check_render_config.py` - найдет проблемы
2. Проверь переменные окружения в Render Dashboard
3. Проверь логи в Render - там будут ошибки
4. Проверь health check - должен отвечать на `/health`

### База данных не подключается:
1. Проверь `DATABASE_URL` - должен быть из Render Connections
2. Проверь connection pooling - должен использоваться
3. Проверь логи - там будет описание ошибки подключения

---

## ✅ ЧЕКЛИСТ ПЕРЕД ДЕПЛОЕМ

- [ ] Все модели протестированы (`auto_test_all_models_generation.py`)
- [ ] Конфигурация проверена (`check_render_config.py`)
- [ ] Все переменные окружения установлены в Render
- [ ] DATABASE_URL установлен (из Connections)
- [ ] Health check работает локально
- [ ] Код работает одинаково на всех инстансах
- [ ] Нет хардкода значений подключения
- [ ] Используется connection pooling для БД

---

## 📞 ПОДДЕРЖКА

Если что-то не работает:
1. Проверь логи
2. Запусти проверку конфигурации
3. Проверь документацию
4. Исправь найденные проблемы

**Помни:** Cursor выполняет команды автоматически, но ты должен проверить результат!
