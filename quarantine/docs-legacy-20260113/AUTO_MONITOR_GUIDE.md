# 🤖 Автоматический мониторинг и исправление бота на Render

## 🚀 Быстрый старт

### Вариант 1: Простой запуск (Windows)

1. **Установите переменные окружения:**
   ```cmd
   set RENDER_API_KEY=your_api_key
   set RENDER_SERVICE_ID=srv-xxxxx
   set TELEGRAM_BOT_TOKEN=your_bot_token
   ```

2. **Запустите скрипт:**
   ```cmd
   auto_fix_render_bot.bat
   ```

### Вариант 2: Python скрипт

```bash
python auto_fix_render_bot.py
```

---

## 📋 Что делает скрипт

### Автоматически:

1. ✅ **Мониторит логи** каждые 60 секунд
2. ✅ **Обнаруживает конфликты 409** - несколько экземпляров бота
3. ✅ **Удаляет webhook'и** автоматически
4. ✅ **Исправляет конфликты** - приостанавливает/возобновляет сервис
5. ✅ **Показывает статистику** - ошибки, предупреждения, конфликты

### Процесс исправления конфликта 409:

1. Проверяет наличие webhook
2. Удаляет webhook (если установлен)
3. Приостанавливает сервис на Render
4. Ждёт 15 секунд
5. Повторно проверяет webhook
6. Возобновляет сервис
7. Проверяет результат

---

## 🔑 Настройка

### 1. Получите Render API ключ

1. Откройте https://dashboard.render.com/
2. Settings → API Keys
3. Создайте новый ключ
4. Скопируйте его

### 2. Найдите Service ID

1. Откройте ваш сервис в Render Dashboard
2. Service ID в URL: `https://dashboard.render.com/web/srv-xxxxx`
3. Или используйте: `python get_render_logs.py --list-services`

### 3. Установите переменные окружения

**Windows (временно для сессии):**
```cmd
set RENDER_API_KEY=your_api_key
set RENDER_SERVICE_ID=srv-xxxxx
set TELEGRAM_BOT_TOKEN=your_bot_token
```

**Windows (постоянно):**
1. Откройте "Переменные среды"
2. Добавьте в "Переменные пользователя"
3. Или создайте файл `setup_env.bat`:
   ```batch
   @echo off
   set RENDER_API_KEY=your_api_key
   set RENDER_SERVICE_ID=srv-xxxxx
   set TELEGRAM_BOT_TOKEN=your_bot_token
   ```

**Linux/Mac:**
```bash
export RENDER_API_KEY=your_api_key
export RENDER_SERVICE_ID=srv-xxxxx
export TELEGRAM_BOT_TOKEN=your_bot_token
```

---

## 📊 Пример работы

```
================================================================================
🤖 АВТОМАТИЧЕСКИЙ МОНИТОРИНГ БОТА НА RENDER
================================================================================
📊 Интервал проверки: 60 секунд
🔧 Автоисправление: Включено
Нажмите Ctrl+C для остановки
================================================================================

================================================================================
🔄 Проверка #1 - 2025-12-19 10:30:00
================================================================================

📊 Статистика:
   Конфликты 409: 0
   Ошибки: 2
   Предупреждения: 5

✅ Webhook не установлен (OK)

⏳ Следующая проверка через 60 секунд...

================================================================================
🔄 Проверка #2 - 2025-12-19 10:31:00
================================================================================

📊 Статистика:
   Конфликты 409: 3
   Ошибки: 5
   Предупреждения: 8

⚠️  Webhook установлен: https://your-bot.onrender.com/webhook
   Ожидающих обновлений: 12

🚨 ОБНАРУЖЕН КОНФЛИКТ 409!
   Всего конфликтов за сессию: 3

   Примеры конфликтов:
   - Conflict: terminated by other getUpdates request...

🗑️  Удаляю webhook...
✅ Webhook успешно удалён

🔧 Запуск автоматического исправления...

================================================================================
🔧 АВТОМАТИЧЕСКОЕ ИСПРАВЛЕНИЕ КОНФЛИКТА 409
================================================================================

📋 Шаг 1: Проверка webhook...
⚠️  Обнаружен webhook: https://your-bot.onrender.com/webhook
   Ожидающих обновлений: 12
🗑️  Удаляю webhook...
✅ Webhook успешно удалён
⏳ Ожидание 2 секунды...

📋 Шаг 2: Приостановка сервиса...
✅ Сервис приостановлен
⏳ Ожидание 15 секунд...

📋 Шаг 3: Повторная проверка webhook...
✅ Webhook не установлен

📋 Шаг 4: Возобновление сервиса...
✅ Сервис возобновлён
⏳ Ожидание 10 секунд для запуска...

📋 Шаг 5: Проверка результата...
✅ Конфликт 409 исправлен!

✅ Автоисправление завершено успешно!
```

---

## ⚙️ Настройки

### Изменить интервал проверки

Отредактируйте `auto_fix_render_bot.py`:

```python
monitor.monitor_loop(interval=120, auto_fix=True)  # 120 секунд вместо 60
```

### Отключить автоисправление

```python
monitor.monitor_loop(interval=60, auto_fix=False)  # Только мониторинг
```

### Запуск в фоне (Linux/Mac)

```bash
nohup python auto_fix_render_bot.py > monitor.log 2>&1 &
```

---

## 🔄 Автозапуск

### Windows: Планировщик заданий

1. Откройте **Планировщик заданий**
2. Создайте новое задание
3. Триггер: **При запуске системы**
4. Действие: Запустить программу `auto_fix_render_bot.bat`
5. Параметры: `--minimized` (опционально)

### Linux: systemd service

Создайте `/etc/systemd/system/render-bot-monitor.service`:

```ini
[Unit]
Description=Render Bot Monitor
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/project
Environment="RENDER_API_KEY=your_key"
Environment="RENDER_SERVICE_ID=srv-xxxxx"
Environment="TELEGRAM_BOT_TOKEN=your_token"
ExecStart=/usr/bin/python3 /path/to/auto_fix_render_bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl enable render-bot-monitor
sudo systemctl start render-bot-monitor
```

---

## 📊 Статистика

Скрипт показывает:
- Количество обнаруженных конфликтов 409
- Количество удалённых webhook'ов
- Общее количество ошибок и предупреждений

При остановке (Ctrl+C) выводится итоговая статистика.

---

## ⚠️ Важно

1. **API ключ Render** безопаснее, чем логин/пароль
2. **Не делитесь** API ключами и токенами
3. **Скрипт работает автоматически** - не требует вмешательства
4. **Можно запускать 24/7** для постоянного мониторинга

---

## ❓ Частые вопросы

**Q: Можно ли использовать логин/пароль вместо API ключа?**
A: Нет, Render API требует только API ключ. Это безопаснее.

**Q: Как часто проверять логи?**
A: Рекомендуется 60 секунд. Можно увеличить до 120-300 для экономии API запросов.

**Q: Скрипт удалит все webhook'и?**
A: Да, скрипт автоматически удаляет webhook'и для вашего бота.

**Q: Что делать, если конфликт не исправляется?**
A: Проверьте, нет ли дублирующих сервисов на Render. Удалите лишние.

---

**Готово! 🚀**

Теперь ваш бот будет автоматически мониториться и исправляться!







