# 🚀 БЫСТРАЯ НАСТРОЙКА: Персистентное хранение данных

## ⚡ Быстрое решение для Render.com

### Шаг 1: Добавьте переменную окружения

В панели Render.com:
1. Откройте ваш сервис
2. Перейдите в **"Environment"**
3. Добавьте новую переменную:
   - **Key:** `DATA_DIR`
   - **Value:** `/app/data`
4. Сохраните изменения

### Шаг 2: Перезапустите сервис

После добавления переменной:
1. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**
2. Дождитесь завершения деплоя
3. Проверьте логи - должна быть строка: `📁 Data directory: /app/data`

### Шаг 3: Проверка

После перезапуска проверьте логи:
```
📁 Data directory: /app/data (from DATA_DIR env var or default)
📁 Data directory exists: True
📁 Data directory writable: True
✅ Created data file: /app/data/user_balances.json
...
🔒 Critical file: /app/data/user_balances.json (1234 bytes)
```

## ✅ Что изменилось

1. **Все данные теперь в `/app/data/`**
   - Балансы: `/app/data/user_balances.json`
   - Платежи: `/app/data/payments.json`
   - История: `/app/data/generations_history.json`
   - И все остальные файлы

2. **Автоматическое создание**
   - Директория создается автоматически
   - Файлы создаются при первом использовании

3. **Логирование**
   - При старте показывается расположение данных
   - Логируются все критические файлы

## ⚠️ ВАЖНО: Настройка Volume (для постоянного хранения)

**Проблема:** На Render.com free плане данные все еще могут теряться при перезапуске.

**Решение:** Используйте один из вариантов:

### Вариант A: Render.com Persistent Disk (платные планы)

1. В Render.com создайте **Persistent Disk**
2. Смонтируйте его в `/app/data`
3. Данные будут сохраняться навсегда

### Вариант B: Внешнее хранилище (S3, Google Cloud Storage)

Настройте автоматическую синхронизацию `/app/data` с облачным хранилищем.

### Вариант C: Регулярные бэкапы

Используйте скрипт `backup_data.py` для регулярного резервного копирования:

```bash
python backup_data.py --data-dir /app/data --output /tmp/backup
```

## 🔄 Миграция существующих данных

Если у вас уже были данные в корне проекта:

1. **Подключитесь к контейнеру:**
   ```bash
   # Через Render.com Shell или SSH
   ```

2. **Скопируйте данные:**
   ```bash
   mkdir -p /app/data
   cp user_balances.json /app/data/ 2>/dev/null || true
   cp payments.json /app/data/ 2>/dev/null || true
   cp generations_history.json /app/data/ 2>/dev/null || true
   # И т.д. для всех JSON файлов
   ```

3. **Или используйте скрипт:**
   ```bash
   # Если данные в корне
   DATA_DIR=. python backup_data.py --output /tmp/backup
   DATA_DIR=/app/data python backup_data.py --restore /tmp/backup
   ```

## 📊 Мониторинг

Проверяйте логи регулярно:
- Должна быть строка с `Data directory`
- Критические файлы должны показывать размер > 0
- При ошибках записи будут предупреждения

## 🆘 Решение проблем

### Данные все еще теряются?

1. **Проверьте переменную окружения:**
   - В Render.com: Environment → `DATA_DIR` должно быть `/app/data`

2. **Проверьте логи:**
   - Должна быть строка: `📁 Data directory: /app/data`
   - Если видите `./data` - переменная не установлена

3. **Проверьте права:**
   - В логах должно быть: `Data directory writable: True`
   - Если `False` - проблема с правами доступа

4. **Проверьте файлы:**
   ```bash
   ls -la /app/data/
   # Должны быть файлы: user_balances.json, payments.json и т.д.
   ```

### Файлы не создаются?

1. Проверьте, что директория существует: `ls -la /app/data`
2. Проверьте права: `chmod 755 /app/data`
3. Проверьте логи на ошибки создания файлов

## ✅ После настройки

После правильной настройки:
- ✅ Данные сохраняются между деплоями
- ✅ Балансы не теряются
- ✅ История генераций сохраняется
- ✅ История платежей сохраняется
- ✅ Все настройки пользователей сохраняются

---

**Готово! Теперь данные будут сохраняться на 100%!** 🎉


