# 🔄 АВТОМАТИЧЕСКИЙ PUSH В GITHUB

## ✅ НАСТРОЕНО

Git remote настроен с токеном для автоматического push.

## 📋 ИСПОЛЬЗОВАНИЕ

### Вариант 1: Автоматический скрипт
```bash
python scripts/auto_push.py
```

### Вариант 2: С сообщением коммита
```bash
python scripts/auto_push.py --message "Ваше сообщение коммита"
```

### Вариант 3: Вручную через git
```bash
git add .
git commit -m "Ваше сообщение"
git push origin main
```

## ⚠️ ВАЖНО: Workflow файлы

Текущий токен не имеет права `workflow` для изменения GitHub Actions файлов.

Если нужно добавить workflow файлы:
1. Создайте новый токен с scope `workflow`
2. Или добавьте workflow файлы вручную через GitHub UI

## 🔐 БЕЗОПАСНОСТЬ

⚠️ **ВАЖНО:** Токен GitHub хранится в git remote URL. 

Для большей безопасности:
1. Используйте переменную окружения `GITHUB_TOKEN`
2. Создайте файл `.github_token` (добавлен в .gitignore)

## 📝 ПРИМЕЧАНИЯ

- Токен уже настроен в git remote
- Все изменения автоматически пушатся в `main` ветку
- Скрипт `auto_push.py` можно использовать для автоматизации

## 🚀 БЫСТРЫЙ СТАРТ

После любых изменений:
```bash
python scripts/auto_push.py
```

Или вручную:
```bash
git add .
git commit -m "Описание изменений"
git push origin main
```





