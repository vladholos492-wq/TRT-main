# 📋 Инструкция по одноразовому парсингу

## 🎯 Цель

Парсинг должен быть выполнен **один раз локально**, результаты сохраняются в `kie_full_api.json`, и затем этот файл используется на Render без повторного парсинга.

## 📝 Шаги

### 1. Локальный парсинг (один раз)

Запустите парсер локально на вашем компьютере:

```bash
python kie_api_scraper.py
```

Или с принудительным перезаписью существующих данных:

```bash
python kie_api_scraper.py --force
```

### 2. Проверка результатов

После завершения парсинга проверьте:

- ✅ `kie_full_api.json` - файл с данными моделей
- ✅ `kie_scraper_stats.json` - статистика парсинга
- ✅ `kie_scraper.log` - лог файл

### 3. Коммит и push на GitHub

```bash
git add kie_full_api.json kie_scraper_stats.json
git commit -m "Add: Parsed Kie.ai models data"
git push origin main
```

### 4. Деплой на Render

После деплоя на Render:
- ✅ Парсер **НЕ будет запускаться автоматически**
- ✅ Будут использоваться уже спарсенные данные из `kie_full_api.json`
- ✅ Сервис просто покажет сообщение о готовности

## 🔄 Обновление данных

Если нужно обновить данные:

1. Локально запустите: `python kie_api_scraper.py --force`
2. Проверьте результаты
3. Закоммитьте и запушьте обновленный `kie_full_api.json`
4. Render автоматически получит обновления

## ⚙️ Переменные окружения

- `SKIP_SCRAPING=true` - пропустить парсинг (установлено на Render)
- `EXPORT_BY_CATEGORY=true` - экспорт по категориям (опционально)

## 📁 Структура файлов

```
.
├── kie_api_scraper.py      # Скрипт парсера
├── kie_full_api.json       # ⭐ Результаты парсинга (главный файл)
├── kie_scraper_stats.json  # Статистика
├── kie_scraper.log         # Логи
└── requirements.txt        # Зависимости
```

## ✅ Проверка

После деплоя на Render проверьте логи - должно быть сообщение:
```
✅ Сервис готов. Используйте данные из kie_full_api.json
```

Парсинг **НЕ должен запускаться** автоматически!

