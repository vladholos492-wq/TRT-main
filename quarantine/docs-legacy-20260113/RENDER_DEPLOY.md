# 🚀 Развертывание на Render.com

## 📋 Требования

- Аккаунт на Render.com (https://render.com/)
- GitHub репозиторий с вашим ботом
- Telegram Bot Token от @BotFather
- KIE API Key

## 🔧 Шаги развертывания

### 1. Подготовка репозитория

1. Убедитесь, что все файлы загружены в GitHub:
   - `Dockerfile` ✅
   - `index.js` ✅
   - `package.json` ✅
   - `run_bot.py` ✅
   - `bot_kie.py` ✅
   - `requirements.txt` ✅
   - Все остальные файлы ✅

2. Убедитесь, что `.env` файл **НЕ** в репозитории (он в `.gitignore`)

### 2. Создание сервиса на Render

1. **Войдите в Render:**
   - Откройте: https://dashboard.render.com/
   - Войдите или зарегистрируйтесь

2. **Создайте новый Web Service:**
   - Нажмите кнопку **"+ New"** в верхнем правом углу
   - Выберите **"Web Service"**

3. **Подключите GitHub репозиторий:**
   - Нажмите **"Connect GitHub"** (если еще не подключен)
   - Разрешите доступ Render к вашему GitHub
   - Выберите репозиторий: `ferixdi-png/5555555555`
   - Нажмите **"Connect"**

### 3. Настройка сервиса

**Основные настройки:**

- **Name:** `kie-telegram-bot` (или любое другое имя)
- **Region:** Выберите ближайший регион (например, Frankfurt)
- **Branch:** `main` (или `master`, если у вас другая ветка)
- **Root Directory:** Оставьте пустым (если проект в корне)

**Настройки сборки и запуска:**

Render автоматически определит Dockerfile, но можно указать явно:

- **Environment:** `Docker`
- **Dockerfile Path:** `Dockerfile` (или оставьте пустым)
- **Docker Context:** `.` (корень проекта)

**ИЛИ используйте прямую сборку (без Docker):**

- **Environment:** `Node`
- **Build Command:** `npm install && pip3 install -r requirements.txt` (если Python установлен)
- **Start Command:** `npm start`

⚠️ **РЕКОМЕНДУЕТСЯ:** Использовать Docker (Environment: Docker), так как у нас есть готовый Dockerfile.

### 4. Настройка переменных окружения

В разделе **"Environment Variables"** добавьте:

**Обязательные:**
```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
KIE_API_KEY=ваш_kie_api_key
ADMIN_ID=ваш_telegram_id
```

**Опциональные:**
```
NODE_ENV=production
PYTHONUNBUFFERED=1
KIE_API_URL=https://api.kie.ai
KIE_TIMEOUT_SECONDS=30
```

### 5. Настройки плана

- **Plan:** Выберите **Free** (для начала) или **Starter** (для постоянной работы)
- ⚠️ **ВАЖНО:** На бесплатном плане сервис "засыпает" после 15 минут бездействия
- Для постоянной работы нужен платный план (от $7/месяц)

### 6. Запуск развертывания

1. Нажмите **"Create Web Service"**
2. Render начнет сборку Docker образа
3. Дождитесь завершения (обычно 5-10 минут)
4. После успешной сборки сервис автоматически запустится

### 7. Проверка работы

1. **Проверьте логи:**
   - Откройте вкладку **"Logs"** в панели Render
   - Должны появиться сообщения:
     ```
     ============================================================
     KIE Telegram Bot - Starting...
     ============================================================
     Node.js version: v24.x.x
     Platform: linux
     ```

2. **Проверьте бота в Telegram:**
   - Найдите вашего бота
   - Отправьте команду `/start`
   - Бот должен ответить

## 🔍 Диагностика проблем

### Бот не отвечает

1. **Проверьте логи:**
   - Откройте вкладку "Logs" в Render
   - Ищите ошибки (красные сообщения)

2. **Проверьте переменные окружения:**
   - Убедитесь, что `TELEGRAM_BOT_TOKEN` и `KIE_API_KEY` установлены
   - Проверьте, что значения правильные (без лишних пробелов)

3. **Проверьте сборку:**
   - Откройте вкладку "Events" в Render
   - Убедитесь, что сборка прошла успешно
   - Если есть ошибки, проверьте Dockerfile

### Ошибка "Python not found"

- Dockerfile автоматически устанавливает Python 3
- Если ошибка все еще есть, проверьте логи сборки
- Убедитесь, что используется Environment: Docker

### Сервис "засыпает" (на бесплатном плане)

- Это нормально для бесплатного плана Render
- Сервис просыпается при первом запросе (но может быть задержка)
- Для постоянной работы нужен платный план

### Ошибка сборки Docker

1. Проверьте Dockerfile в репозитории
2. Убедитесь, что все файлы загружены
3. Проверьте логи сборки в Render

## 📝 Структура проекта

```
/app
├── index.js              # Node.js wrapper (запускает Python бота)
├── run_bot.py           # Python скрипт запуска
├── bot_kie.py           # Основная логика бота
├── kie_models.py        # Модели KIE AI
├── package.json         # Node.js зависимости
├── requirements.txt     # Python зависимости
├── Dockerfile           # Docker конфигурация
└── .env.example         # Пример переменных окружения
```

## 🔄 Обновление бота

1. **Автоматическое обновление:**
   - По умолчанию включено "Auto-Deploy"
   - При каждом push в ветку `main` Render автоматически пересоберет и перезапустит бота

2. **Ручное обновление:**
   - Откройте сервис в Render
   - Нажмите кнопку **"Manual Deploy"** → **"Deploy latest commit"**

## ✅ Проверочный список

- [ ] Все файлы загружены в GitHub
- [ ] `.env` файл НЕ в репозитории
- [ ] Репозиторий подключен к Render
- [ ] Environment: Docker выбран
- [ ] Переменные окружения установлены
- [ ] Сборка прошла успешно
- [ ] Бот отвечает на `/start` в Telegram
- [ ] Логи отображаются в Render

## 💰 Планы Render

- **Free:** Бесплатно, но сервис "засыпает" после 15 минут бездействия
- **Starter ($7/мес):** Постоянная работа, 512MB RAM
- **Standard ($25/мес):** Больше ресурсов, лучше производительность

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи в Render
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что Dockerfile правильный
4. Убедитесь, что используется Environment: Docker

---

**Удачного развертывания! 🚀**

