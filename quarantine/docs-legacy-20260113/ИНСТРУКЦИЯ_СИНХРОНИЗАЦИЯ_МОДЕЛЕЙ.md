# ИНСТРУКЦИЯ: Синхронизация моделей из KIE API

## Быстрый старт

### Шаг 1: Запуск синхронизации

```bash
python sync_models_from_api.py
```

### Шаг 2: Проверка результатов

После выполнения скрипт создаст:
- ✅ `kie_models.py.backup` — резервная копия файла
- ✅ `MODELS_SYNC_REPORT.json` — отчет о добавленных моделях
- ✅ Обновленный `kie_models.py` с новыми моделями

### Шаг 3: Проверка работы

После синхронизации:
1. ✅ Все новые модели автоматически получат кнопки
2. ✅ Все обработчики callback будут работать
3. ✅ Валидация параметров будет работать автоматически

---

## Что делает скрипт

1. **Получает модели из KIE API**
   - Использует `GET /api/v1/models`
   - Получает все модели с их параметрами

2. **Сравнивает с текущим списком**
   - Загружает модели из `kie_models.py`
   - Находит модели, отсутствующие в коде

3. **Нормализует модели**
   - Преобразует формат API в формат `kie_models.py`
   - Автоматически определяет `generation_type`
   - Генерирует `help` и `example_prompt`

4. **Добавляет модели в файл**
   - Создает backup перед изменением
   - Добавляет новые модели в `kie_models.py`
   - Сохраняет правильный формат Python

5. **Создает отчет**
   - Список добавленных моделей
   - Информация о параметрах каждой модели
   - Статистика синхронизации

---

## Формат отчета

Отчет `MODELS_SYNC_REPORT.json` содержит:

```json
{
  "total_missing": 5,
  "added_models": [
    {
      "id": "new-model-id",
      "name": "New Model Name",
      "category": "Фото",
      "generation_type": "text-to-image",
      "has_input_schema": true,
      "input_params_count": 3,
      "input_params": ["prompt", "aspect_ratio", "quality"]
    }
  ]
}
```

---

## Требования

- ✅ Python 3.7+
- ✅ Установлена переменная окружения `KIE_API_KEY`
- ✅ Доступ к KIE API

---

## Пример вывода

```
📡 Загрузка моделей из KIE API...
✅ Загружено 85 моделей из API
➕ Найдена новая модель: new-model-1 (New Model 1)
➕ Найдена новая модель: new-model-2 (New Model 2)
📊 Найдено 2 новых моделей:
  • new-model-1 - New Model 1 (Фото)
    Тип: text-to-image
    Параметров: 3
  • new-model-2 - New Model 2 (Видео)
    Тип: text-to-video
    Параметров: 4
💾 Создан backup: kie_models.py.backup
✅ Добавлено 2 новых моделей в kie_models.py
📄 Отчет сохранен в MODELS_SYNC_REPORT.json
```

---

## Обработка ошибок

### Ошибка: KIE_API_KEY не установлен
```
⚠️ Не удалось загрузить модели из API. Возможные причины:
  - KIE_API_KEY не установлен
  - API недоступен
  - Неверный endpoint
```

**Решение:** Установите переменную окружения `KIE_API_KEY`

### Ошибка: Не удалось найти место для вставки
```
❌ Не удалось найти место для вставки новых моделей
```

**Решение:** Скрипт создаст файл `NEW_MODELS_TO_ADD_MANUAL.py` для ручного добавления

### Ошибка: Не удалось сохранить файл
```
❌ Ошибка при сохранении kie_models.py
```

**Решение:** Проверьте права доступа к файлу. Backup будет создан в любом случае.

---

## Ручное добавление моделей

Если автоматическое добавление не сработало:

1. Откройте файл `NEW_MODELS_TO_ADD_MANUAL.py` (если создан)
2. Скопируйте код из файла
3. Откройте `kie_models.py`
4. Найдите строку `]` (конец списка `KIE_MODELS`, строка 2711)
5. Вставьте код **перед** закрывающей скобкой `]`
6. Сохраните файл

---

## Проверка после синхронизации

После синхронизации проверьте:

1. ✅ Все новые модели имеют правильную структуру
2. ✅ Все параметры корректны
3. ✅ Кнопки создаются автоматически
4. ✅ Обработчики callback работают

Для проверки запустите тесты:
```bash
python -m pytest tests/test_models_menu_coverage.py -v
```

---

## Регулярная синхронизация

Рекомендуется запускать синхронизацию:
- ✅ При добавлении новых моделей в KIE API
- ✅ Перед обновлением бота
- ✅ Раз в неделю для проверки новых моделей

---

## Безопасность

- ✅ Скрипт создает backup перед изменением файла
- ✅ Не удаляет существующие модели
- ✅ Не изменяет существующие модели
- ✅ Только добавляет новые модели

---

## Поддержка

Если возникли проблемы:
1. Проверьте отчет `MODELS_SYNC_REPORT.json`
2. Проверьте backup файл `kie_models.py.backup`
3. Проверьте логи скрипта
4. Убедитесь, что `KIE_API_KEY` установлен

