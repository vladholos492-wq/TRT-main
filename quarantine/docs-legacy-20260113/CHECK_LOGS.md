# 🔍 БОТ НЕ РЕАГИРУЕТ - ПРОВЕРКА ЛОГОВ

## ⚠️ ПРОБЛЕМА

Сборка прошла успешно, но бот не отвечает на команды.

**В логах видно:**
- ✅ Build succeeded
- ✅ Container started
- ⚠️ Warning: container is not running

## 🔍 ЧТО ПРОВЕРИТЬ

### 1. ПРОВЕРЬТЕ ЛОГИ КОНТЕЙНЕРА

**В панели Timeweb:**
1. Найдите раздел **"Логи"** или **"Logs"**
2. Откройте логи контейнера
3. Посмотрите последние строки

**Что искать в логах:**

#### ✅ ХОРОШИЕ СООБЩЕНИЯ (бот работает):
```
🚀 Starting KIE Telegram Bot...
✅ Environment variables checked
✅ Python found: python3
📝 Starting bot script: /app/run_bot.py
Starting KIE Telegram Bot...
Bot starting...
Application started
```

#### ❌ ПЛОХИЕ СООБЩЕНИЯ (бот не работает):

**Ошибка переменных:**
```
❌ Missing required environment variables: TELEGRAM_BOT_TOKEN
❌ Missing required environment variables: KIE_API_KEY
Error: TELEGRAM_BOT_TOKEN not set
```

**Ошибка Python:**
```
❌ Python not found. Please install Python 3.8+
Error: Python not found
```

**Ошибка модулей:**
```
ModuleNotFoundError: No module named 'telegram'
ModuleNotFoundError: No module named 'python-telegram-bot'
```

**Ошибка подключения:**
```
Error: Cannot connect to Telegram
ConnectionError: Failed to connect
```

**Ошибка скрипта:**
```
❌ Bot script not found: /app/run_bot.py
FileNotFoundError: run_bot.py
```

### 2. ПРОВЕРЬТЕ ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ

**В панели Timeweb → Переменные окружения:**

Убедитесь, что:
- ✅ `TELEGRAM_BOT_TOKEN` установлен (не пустой!)
- ✅ `KIE_API_KEY` установлен (не пустой!)

**Проверка:**
- Значения должны быть БЕЗ кавычек
- Значения должны быть БЕЗ пробелов в начале/конце
- Пример правильного: `TELEGRAM_BOT_TOKEN=1234567890:ABC...`
- Пример неправильного: `TELEGRAM_BOT_TOKEN="1234567890:ABC..."` (с кавычками)

### 3. ПРОВЕРЬТЕ СТАТУС КОНТЕЙНЕРА

**В панели Timeweb:**
1. Найдите раздел **"Статус"** или **"Status"**
2. Проверьте статус контейнера:
   - ✅ **Running** - контейнер работает
   - ❌ **Stopped** - контейнер остановлен
   - ❌ **Failed** - контейнер упал

### 4. ПЕРЕЗАПУСТИТЕ КОНТЕЙНЕР

1. В панели Timeweb найдите кнопку **"Перезапустить"** или **"Restart"**
2. Нажмите на неё
3. Дождитесь перезапуска
4. Проверьте логи снова

## 📋 ЧТО СДЕЛАТЬ СЕЙЧАС

### ШАГ 1: Откройте логи
1. Зайдите в панель Timeweb
2. Найдите раздел **"Логи"** или **"Logs"**
3. Скопируйте последние 20-30 строк логов
4. Посмотрите, есть ли там ошибки

### ШАГ 2: Проверьте переменные
1. Зайдите в **Переменные окружения**
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` и `KIE_API_KEY` установлены
3. Проверьте, что значения правильные (без кавычек, без пробелов)

### ШАГ 3: Перезапустите
1. Нажмите **"Перезапустить"**
2. Дождитесь перезапуска
3. Проверьте логи снова

## 🎯 ЧАСТЫЕ ПРОБЛЕМЫ

### Проблема 1: Переменные не передаются
**Симптомы:** В логах `Missing required environment variables`

**Решение:**
- Проверьте, что переменные установлены в панели
- Убедитесь, что значения правильные (без кавычек)
- Перезапустите контейнер

### Проблема 2: Бот падает при запуске
**Симптомы:** В логах `Error:` или `ModuleNotFoundError`

**Решение:**
- Проверьте логи на конкретную ошибку
- Убедитесь, что все зависимости установлены (они должны быть в Dockerfile)

### Проблема 3: Контейнер завершается сразу
**Симптомы:** `Container not running`, `Exit Code: 0`

**Решение:**
- Проверьте логи - бот должен работать постоянно
- Если бот завершается, значит есть ошибка в логах

## ⚠️ ВАЖНО

**Если в логах ничего нет:**
- Возможно, логи не отображаются
- Попробуйте перезапустить контейнер
- Подождите 1-2 минуты после перезапуска

**Если в логах есть ошибки:**
- Скопируйте текст ошибки
- Исправьте проблему согласно ошибке

---

**Главное: проверьте логи контейнера - там будет видно, в чём проблема!**

