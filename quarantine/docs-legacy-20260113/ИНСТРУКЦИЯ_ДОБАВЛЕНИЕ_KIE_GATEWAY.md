# 🔧 ИНСТРУКЦИЯ: Добавление kie_gateway.py в git и деплой на Render

## Дата: 2025-12-18

---

## ❌ ПРОБЛЕМА

На Render возникает ошибка:
```
ModuleNotFoundError: No module named 'kie_gateway'
```

**Причина:**
- Файл `kie_gateway.py` существует локально, но не добавлен в git репозиторий
- Или файл не запушен на удалённый репозиторий

---

## ✅ РЕШЕНИЕ

### ШАГ 1: Проверка наличия файла локально

Убедитесь, что файл существует:
```bash
# Windows
dir kie_gateway.py

# Linux/Mac
ls kie_gateway.py
```

Файл должен находиться в корне проекта (рядом с `bot_kie.py`).

---

### ШАГ 2: Проверка статуса файла в git

Проверьте, отслеживается ли файл git:

```bash
git status kie_gateway.py
```

**Если файл не отслеживается**, вы увидите:
```
Untracked files:
  kie_gateway.py
```

---

### ШАГ 3: Добавление файла в git

Добавьте файл в git:

```bash
git add kie_gateway.py
```

Проверьте статус:
```bash
git status
```

Должно быть:
```
Changes to be committed:
  new file:   kie_gateway.py
```

---

### ШАГ 4: Коммит изменений

Закоммитьте файл:

```bash
git commit -m "Add kie_gateway.py module for KIE API integration"
```

---

### ШАГ 5: Пуш в репозиторий

Запушьте изменения:

```bash
git push
```

**Если используете конкретную ветку:**
```bash
git push origin main
# или
git push origin master
```

---

### ШАГ 6: Проверка на Render

После push:

1. **Render автоматически начнёт деплой** (если настроен auto-deploy)
2. **Или выполните Manual Deploy** в Render Dashboard
3. **Проверьте логи** - должно быть:
   ```
   ✅ Bot started successfully
   ```
4. **НЕ должно быть:**
   ```
   ❌ ModuleNotFoundError: No module named 'kie_gateway'
   ```

---

## 🔍 ПРОВЕРКА ПЕРЕД ДЕПЛОЕМ

Запустите скрипт проверки:

```bash
python scripts/check_files_before_deploy.py
```

Скрипт проверит:
- ✅ Наличие всех необходимых файлов
- ✅ Наличие `kie_gateway.py`
- ✅ Импорт `kie_gateway` в `bot_kie.py`

---

## ⚠️ ВАЖНО

1. **Файл должен быть в корне проекта** (рядом с `bot_kie.py`)
2. **Файл должен быть добавлен в git** (`git add kie_gateway.py`)
3. **Файл должен быть закоммичен** (`git commit`)
4. **Файл должен быть запушен** (`git push`)
5. **Файл НЕ должен быть в `.gitignore`**

---

## 📋 ЧЕКЛИСТ

- [ ] Файл `kie_gateway.py` существует локально
- [ ] Файл добавлен в git (`git add kie_gateway.py`)
- [ ] Изменения закоммичены (`git commit`)
- [ ] Изменения запушены (`git push`)
- [ ] Render начал деплой
- [ ] Логи на Render показывают успешный запуск

---

## ✅ ПОСЛЕ ИСПРАВЛЕНИЯ

После выполнения всех шагов:

1. **Дождитесь завершения деплоя** на Render
2. **Проверьте логи** - должно быть:
   ```
   ✅ Bot started successfully
   ✅ Database initialized
   ✅ Ready to receive updates
   ```
3. **Протестируйте бота** - отправьте команду `/start`

---

**Готово! 🚀**

