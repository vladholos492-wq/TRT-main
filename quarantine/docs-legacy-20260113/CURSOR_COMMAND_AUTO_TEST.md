# 🧪 КОМАНДА ДЛЯ CURSOR: АВТОМАТИЧЕСКОЕ ТЕСТИРОВАНИЕ ВСЕХ МОДЕЛЕЙ

## 📋 ИНСТРУКЦИЯ ДЛЯ CURSOR

Скопируй и отправь эту команду в Cursor:

---

**Создай автоматический тест для всех моделей KIE AI, который:**

1. **Проверяет все модели из `kie_models.py`** - проходит по каждой модели в списке `KIE_MODELS`

2. **Генерирует тестовые параметры автоматически** - на основе `input_params` каждой модели создает валидные тестовые значения:
   - Для `prompt` - использует тестовый текст
   - Для `image_input`, `image_urls`, `image_url` - использует placeholder URL изображения
   - Для `video_url` - использует тестовый URL видео
   - Для `audio_url`, `audio` - использует тестовый URL аудио
   - Для `enum` параметров - берет первое значение из списка
   - Для `boolean` - использует `False`
   - Для `number`/`integer` - использует default или 1
   - Для остальных - использует default значения

3. **Применяет конвертации параметров** - точно так же, как в `bot_kie.py`:
   - `recraft/remove-background`, `recraft/crisp-upscale`: `image_input` → `image`
   - `ideogram/v3-reframe`, `topaz/image-upscale`: `image_input` → `image_url`
   - `seedream/4.5-edit`, `kling-2.6/image-to-video`: `image_input` → `image_urls`
   - `flux-2/pro-image-to-image`, `flux-2/flex-image-to-image`: `image_input` → `input_urls`
   - `kling/v2-5-turbo-image-to-video-pro`: `image_input` → `image_url` (первый элемент массива)

4. **Отправляет запросы в KIE API** - использует `kie_client.create_task()` для создания задач генерации

5. **Проверяет ответы API** - валидирует, что:
   - Задача успешно создана (`ok: true`)
   - Получен `taskId`
   - Опционально проверяет статус задачи через `get_task_status()`

6. **Логирует все детали**:
   - Тестовые параметры для каждой модели
   - Параметры после конвертации (для API)
   - Полный ответ от KIE API
   - Ошибки с полным контекстом

7. **Сохраняет результаты в JSON файл** - с временной меткой в имени файла

8. **Выводит итоговый отчет**:
   - Сколько моделей протестировано
   - Сколько успешно
   - Сколько с ошибками
   - Список всех ошибок с описанием

9. **Поддерживает опции командной строки**:
   - `--test-expensive` - тестировать дорогие модели (sora-2-pro, veo-3 и т.д.)
   - `--model MODEL_ID` - тестировать только одну модель

10. **Пропускает дорогие модели по умолчанию** - чтобы не тратить кредиты на тесты

**Файл должен называться:** `auto_test_all_models_generation.py`

**Используй существующий код как основу:**
- `kie_client.py` для работы с KIE API
- `kie_models.py` для списка моделей
- Логику конвертации параметров из `bot_kie.py` (функция `start_generation_directly`)

**Важно:**
- Все параметры должны строго соответствовать формату KIE API
- Используй async/await для всех API вызовов
- Логируй все детали для отладки
- Обрабатывай все исключения
- Сохраняй результаты в JSON для последующего анализа

---

## 🚀 КАК ИСПОЛЬЗОВАТЬ

После того как Cursor создаст файл, запусти тест:

```bash
# Тестировать все модели (пропуская дорогие)
python auto_test_all_models_generation.py

# Тестировать все модели включая дорогие
python auto_test_all_models_generation.py --test-expensive

# Тестировать только одну модель
python auto_test_all_models_generation.py --model z-image
```

## 📊 РЕЗУЛЬТАТЫ

Тест создаст:
- `model_generation_test.log` - лог файл с деталями
- `model_generation_test_results_YYYYMMDD_HHMMSS.json` - JSON с результатами

В JSON будет:
- Список всех протестированных моделей
- Статус каждой модели (success/failed/skipped)
- Параметры, которые использовались
- Ответы от API
- Ошибки с описанием

---

## ✅ ПРОВЕРКА

После запуска проверь:
1. Все модели протестированы (или пропущены с причиной)
2. Успешные модели получили `taskId` от KIE API
3. Ошибки логируются с полным контекстом
4. Результаты сохранены в JSON

Если какая-то модель не работает - в логах будет полная информация для исправления!
