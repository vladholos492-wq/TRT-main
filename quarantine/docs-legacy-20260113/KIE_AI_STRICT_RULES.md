# КРИТИЧЕСКИЕ ПРАВИЛА ГЕНЕРАЦИИ KIE AI - ЗАФИКСИРОВАНО НАВСЕГДА

## 🔴 ОБЯЗАТЕЛЬНОЕ ПРАВИЛО #0 (ГЛАВНОЕ):

**ВСЕ модели ДОЛЖНЫ использовать API Endpoints строго по официальной документации:**

**📚 ИСТОЧНИКИ:**

1. **https://docs.kie.ai/market** - Market Documentation (ОБЯЗАТЕЛЬНО!)
   - **Image Models** - все модели генерации изображений
   - **Video Models** - все модели генерации видео
   - **Audio Models** - все модели обработки аудио
   - Документация для каждой модели в Market
   - Unified API Structure для всех моделей

2. **https://docs.kie.ai/** - Comprehensive API Documentation
   - Полная документация всех API Endpoints
   - Quickstart guides для каждой модели
   - API Reference с полными параметрами
   - Code Samples и примеры
   - Interactive Examples

3. **https://kie.ai/ru** - Русская версия сайта
   - Информация о моделях и ценах
   - API Endpoints документация

4. **https://docs.kie.ai/file-upload-api** - File Upload API (ОБЯЗАТЕЛЬНО для загрузки файлов!)
   - URL File Upload - для загрузки файлов с удаленных URL
   - File Stream Upload - для загрузки локальных файлов (рекомендуется для больших файлов)
   - Base64 Upload - для загрузки файлов в формате Base64 (для маленьких файлов)
   - Все файлы автоматически удаляются через 3 дня

5. **https://docs.kie.ai/llms.txt** - Навигация по документации

**НИКАКИХ отклонений от официальной документации API Endpoints!**

---

## ⚠️ ВАЖНО: НИЧЕГО ОТ СЕБЯ НЕ ПРИДУМЫВАТЬ!

**ВСЕ ПАРАМЕТРЫ ДОЛЖНЫ СТРОГО СООТВЕТСТВОВАТЬ ИНСТРУКЦИЯМ KIE AI API**

**Используйте Interactive Examples и Code Samples из документации как эталон!**

---

## 📋 ОБЩИЕ ПРАВИЛА ДЛЯ ВСЕХ МОДЕЛЕЙ

### 1. ✅ ОБЯЗАТЕЛЬНЫЕ ПАРАМЕТРЫ
- **ВСЕ обязательные параметры ДОЛЖНЫ быть запрошены у пользователя**
- **ВСЕ обязательные параметры ДОЛЖНЫ быть валидированы перед отправкой**
- **ВСЕ обязательные параметры ДОЛЖНЫ быть отправлены в KIE API**

### 2. ✅ ФОРМАТ ПАРАМЕТРОВ
- **Строго по формату из валидационных файлов (validate_*.py)**
- **Строго по формату из документации KIE AI**
- **Никаких изменений формата без проверки документации**

### 3. ✅ ВАЛИДАЦИЯ ПАРАМЕТРОВ
- **Каждый параметр должен быть валидирован**
- **Проверка типов данных (string, number, boolean, array)**
- **Проверка диапазонов значений**
- **Проверка enum значений**
- **Проверка длины строк**
- **Округление чисел согласно шагу (step)**

### 4. ✅ КОНВЕРТАЦИЯ ПАРАМЕТРОВ
- **Только согласно инструкциям KIE API**
- **image_input → image (для recraft моделей)**
- **image_input → image_url (для некоторых моделей)**
- **image_input → image_urls (для некоторых моделей)**
- **image_input → input_urls (для flux моделей)**
- **Никаких произвольных конвертаций!**

### 5. ✅ ОТПРАВКА В KIE API
- **Формат: `{"model": "model-id", "input": {...}}`**
- **Только параметры из input_params модели**
- **Никаких дополнительных параметров**
- **Никаких параметров, которых нет в документации**

---

## 📝 ПРАВИЛА ДЛЯ КАЖДОЙ МОДЕЛИ

### Модели с полной валидацией (26 моделей):

#### 1. z-image
- **Обязательные:** prompt, aspect_ratio
- **Формат:** prompt (string, max 1000), aspect_ratio (enum: "1:1", "4:3", "3:4", "16:9", "9:16")
- **Валидация:** ✅ Есть

#### 2. nano-banana-pro
- **Обязательные:** prompt, image_input
- **Формат:** prompt (string, max 10000), image_input (array, max 8), aspect_ratio (optional), resolution (optional: "1K", "2K", "4K"), output_format (optional: "png", "jpg")
- **Валидация:** ✅ Есть
- **Конвертация:** image_input остается как массив (НЕ конвертируется)

#### 3. seedream/4.5-text-to-image
- **Обязательные:** prompt, aspect_ratio, quality
- **Формат:** prompt (string, max 3000), aspect_ratio (enum), quality (enum: "basic", "high")
- **Валидация:** ✅ Есть

#### 4. seedream/4.5-edit
- **Обязательные:** prompt, image_urls, aspect_ratio, quality
- **Формат:** prompt (string, max 3000), image_urls (array), aspect_ratio (enum), quality (enum: "basic", "high")
- **Валидация:** ✅ Есть
- **Конвертация:** image_input → image_urls

#### 5. kling-2.6/image-to-video
- **Обязательные:** prompt, image_urls, sound, duration
- **Формат:** prompt (string, max 1000), image_urls (array), sound (boolean), duration (enum: "5", "10")
- **Валидация:** ✅ Есть
- **Конвертация:** image_input → image_urls

#### 6. kling-2.6/text-to-video
- **Обязательные:** prompt, sound, aspect_ratio, duration
- **Формат:** prompt (string, max 1000), sound (boolean), aspect_ratio (enum: "1:1", "16:9", "9:16"), duration (enum: "5", "10")
- **Валидация:** ✅ Есть

#### 7. flux-2/pro-image-to-image
- **Обязательные:** prompt, input_urls, aspect_ratio, resolution
- **Формат:** prompt (string, min 3, max 5000), input_urls (array, 1-8), aspect_ratio (enum, включая "auto"), resolution (enum: "1K", "2K")
- **Валидация:** ✅ Есть
- **Конвертация:** image_input → input_urls

#### 8. flux-2/pro-text-to-image
- **Обязательные:** prompt, aspect_ratio, resolution
- **Формат:** prompt (string, min 3, max 5000), aspect_ratio (enum, включая "auto"), resolution (enum: "1K", "2K")
- **Валидация:** ✅ Есть

#### 9. flux-2/flex-image-to-image
- **Обязательные:** prompt, input_urls, aspect_ratio, resolution
- **Формат:** prompt (string, min 3, max 5000), input_urls (array, 1-8), aspect_ratio (enum, включая "auto"), resolution (enum: "1K", "2K")
- **Валидация:** ✅ Есть
- **Конвертация:** image_input → input_urls

#### 10. flux-2/flex-text-to-image
- **Обязательные:** prompt, aspect_ratio, resolution
- **Формат:** prompt (string, min 3, max 5000), aspect_ratio (enum, включая "auto"), resolution (enum: "1K", "2K")
- **Валидация:** ✅ Есть

#### 11. qwen/text-to-image
- **Обязательные:** prompt
- **Формат:** prompt (string), output_format (optional, lowercase: "png", "jpeg"), guidance_scale (optional, 0-20, step 0.1), acceleration (optional, lowercase: "none", "regular", "high")
- **Валидация:** ✅ Есть
- **КРИТИЧНО:** output_format в lowercase!

#### 12. qwen/image-to-image
- **Обязательные:** prompt, image_input
- **Формат:** prompt (string), image_input (array, 1 элемент), strength (optional, 0-1, step 0.01), output_format (optional, lowercase: "png", "jpeg"), guidance_scale (optional, 0-20, step 0.1)
- **Валидация:** ✅ Есть
- **КРИТИЧНО:** output_format в lowercase! strength округляется до 2 знаков!

#### 13. qwen/image-edit
- **Обязательные:** prompt, image_input
- **Формат:** prompt (string), image_input (array), output_format (optional, lowercase: "png", "jpeg"), guidance_scale (optional, 0-20, step 0.1)
- **Валидация:** ✅ Есть
- **КРИТИЧНО:** output_format в lowercase!

#### 14-26. Остальные модели с валидацией
(См. полный список в check_all_models_compliance.py)

---

### Модели без валидации (31 модель) - ТРЕБУЮТ ДОБАВЛЕНИЯ:

#### recraft/remove-background
- **Обязательные:** image_input
- **Формат:** image (string URL) - КРИТИЧНО: image_input → image
- **Конвертация:** image_input → image (первый элемент массива или строка)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### recraft/crisp-upscale
- **Обязательные:** image_input
- **Формат:** image (string URL) - КРИТИЧНО: image_input → image
- **Конвертация:** image_input → image (первый элемент массива или строка)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### topaz/image-upscale
- **Обязательные:** image_input, upscale_factor
- **Формат:** image_url (string), upscale_factor (enum: "1", "2", "4", "8")
- **Конвертация:** image_input → image_url (первый элемент массива или строка)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### sora-watermark-remover
- **Обязательные:** video_url
- **Формат:** video_url (string, должен начинаться с sora.chatgpt.com)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### sora-2-text-to-video
- **Обязательные:** prompt, aspect_ratio
- **Формат:** prompt (string, max 10000), aspect_ratio (enum: "portrait", "landscape"), n_frames (optional: "10", "15"), remove_watermark (optional, boolean)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### sora-2-pro-image-to-video
- **Обязательные:** image_urls, prompt
- **Формат:** image_urls (array, 1 элемент), prompt (string, max 10000), aspect_ratio (optional: "portrait", "landscape"), n_frames (optional: "10", "15"), size (optional: "standard", "high"), remove_watermark (optional, boolean)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### ideogram/v3-reframe
- **Обязательные:** image_input, image_size
- **Формат:** image_url (string), image_size (enum), prompt (optional)
- **Конвертация:** image_input → image_url (первый элемент массива или строка)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### ideogram/v3-text-to-image
- **Обязательные:** prompt
- **Формат:** prompt (string)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### ideogram/v3-edit
- **Обязательные:** prompt, image_input, mask_input
- **Формат:** prompt (string), image_input (array), mask_input (array)
- **Конвертация:** image_input и mask_input остаются как массивы
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### ideogram/v3-remix
- **Обязательные:** prompt, image_input
- **Формат:** prompt (string), image_input (array)
- **Конвертация:** image_input остается как массив
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### google/imagen4, google/imagen4-fast, google/imagen4-ultra
- **Обязательные:** prompt
- **Формат:** prompt (string)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### google/nano-banana, google/nano-banana-edit
- **Обязательные:** prompt (для nano-banana), prompt + image_urls (для nano-banana-edit)
- **Формат:** prompt (string), image_urls (array для edit)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### bytedance/seedream, bytedance/v1-pro-fast-image-to-video
- **Обязательные:** prompt (для seedream), prompt + image_input (для v1-pro-fast)
- **Формат:** prompt (string), image_url (string для v1-pro-fast)
- **Конвертация:** image_input → image_url (для v1-pro-fast)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### kling/v2-1-master-image-to-video, kling/v2-1-standard, kling/v2-1-pro
- **Обязательные:** prompt, image_input
- **Формат:** prompt (string), image_url (string)
- **Конвертация:** image_input → image_url
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### kling/v2-1-master-text-to-video
- **Обязательные:** prompt
- **Формат:** prompt (string)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### kling/v2-5-turbo-text-to-video-pro, kling/v2-5-turbo-image-to-video-pro
- **Обязательные:** prompt (для text-to-video), prompt + image_input (для image-to-video)
- **Формат:** prompt (string), image_url (string для image-to-video)
- **Конвертация:** image_input → image_url (для image-to-video)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### kling/v1-avatar-standard, kling/ai-avatar-v1-pro
- **Обязательные:** image_input, audio_input, prompt
- **Формат:** image_url (string), audio_url (string), prompt (string)
- **Конвертация:** image_input → image_url, audio_input → audio_url
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### wan/2-5-image-to-video, wan/2-5-text-to-video
- **Обязательные:** prompt, image_input (для image-to-video), prompt (для text-to-video)
- **Формат:** prompt (string), image_url (string для image-to-video)
- **Конвертация:** image_input → image_url (для image-to-video)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### wan/2-2-a14b-text-to-video-turbo, wan/2-2-a14b-image-to-video-turbo
- **Обязательные:** prompt (для text-to-video), prompt + image_input (для image-to-video)
- **Формат:** prompt (string), image_url (string для image-to-video)
- **Конвертация:** image_input → image_url (для image-to-video)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### wan/2-2-a14b-speech-to-video-turbo
- **Обязательные:** prompt, image_input, audio_input
- **Формат:** prompt (string), image_url (string), audio_url (string)
- **Конвертация:** image_input → image_url, audio_input → audio_url
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### wan/2-2-animate-move, wan/2-2-animate-replace
- **Обязательные:** video_input, image_input
- **Формат:** video_url (string), image_url (string)
- **Конвертация:** video_input → video_url, image_input → image_url
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### hailuo/02-image-to-video-standard, hailuo/02-text-to-video-standard
- **Обязательные:** prompt, image_input (для image-to-video), prompt (для text-to-video)
- **Формат:** prompt (string), image_url (string для image-to-video)
- **Конвертация:** image_input → image_url (для image-to-video)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### topaz/video-upscale
- **Обязательные:** video_input
- **Формат:** video_url (string)
- **Конвертация:** video_input → video_url
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### infinitalk/from-audio
- **Обязательные:** prompt
- **Формат:** prompt (string), image_url (optional, string), audio_url (optional, string)
- **Конвертация:** image_input → image_url, audio_input → audio_url (если есть)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### elevenlabs/speech-to-text
- **Обязательные:** audio_url
- **Формат:** audio_url (string)
- **Конвертация:** audio_input → audio_url
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

#### sora-2-pro-storyboard
- **Обязательные:** prompt
- **Формат:** prompt (string)
- **Валидация:** ⚠️ НЕТ - ТРЕБУЕТСЯ ДОБАВИТЬ

---

### Модели не найдены в коде (15 моделей) - ТРЕБУЮТ ДОБАВЛЕНИЯ:

1. elevenlabs/audio-isolation
2. elevenlabs/sound-effect
3. elevenlabs/text-to-speech
4. flux/kontext
5. google/nanobanana-gemini-2.5-flash
6. google/veo-3
7. google/veo-3.1
8. grok/imagine
9. hailuo/2.3
10. kling/v2-1-master-text-to-video
11. kling/v2-5-turbo
12. midjourney/api
13. openai/4o-image
14. runway/gen-4
15. suno/v5

**Эти модели требуют полной реализации:**
- Добавление в confirm_generation
- Добавление валидации параметров
- Добавление конвертации параметров (если нужно)
- Добавление обработки в input_parameters

---

## 🔒 КРИТИЧЕСКИЕ ПРАВИЛА - НАВСЕГДА ЗАФИКСИРОВАНО

### ❌ НИКОГДА НЕ ДЕЛАТЬ:
1. ❌ Не добавлять параметры, которых нет в документации KIE AI
2. ❌ Не изменять формат параметров без проверки документации
3. ❌ Не пропускать обязательные параметры
4. ❌ Не отправлять параметры в неправильном формате
5. ❌ Не придумывать свои правила валидации
6. ❌ Не изменять конвертацию параметров без проверки
7. ❌ Не использовать uppercase для output_format (где требуется lowercase)
8. ❌ Не забывать округлять числа согласно step

### ✅ ВСЕГДА ДЕЛАТЬ:
1. ✅ Проверять документацию KIE AI перед изменением
2. ✅ Использовать валидационные файлы (validate_*.py) как эталон
3. ✅ Валидировать все обязательные параметры
4. ✅ Проверять формат всех параметров
5. ✅ Округлять числа согласно step (0.01 для strength, 0.1 для guidance_scale)
6. ✅ Использовать правильный регистр (lowercase для output_format в qwen моделях)
7. ✅ Конвертировать параметры строго по инструкциям
8. ✅ Логировать все параметры перед отправкой в KIE API

---

## 📊 СТАТУС ПРОВЕРКИ

**Дата проверки:** 2025-12-16
**Всего моделей:** 72
**С валидацией:** 26
**Без валидации:** 31
**Не найдены в коде:** 15

**ТРЕБУЕТСЯ:**
1. Добавить валидацию для 31 модели
2. Добавить обработку для 15 моделей
3. Убедиться что все обязательные параметры запрашиваются

---

## 🔄 ПРОЦЕСС ОБНОВЛЕНИЯ

При добавлении новой модели:
1. ✅ Добавить модель в kie_models.py
2. ✅ Создать validate_*.py файл
3. ✅ Добавить обработку в input_parameters
4. ✅ Добавить валидацию в confirm_generation
5. ✅ Добавить конвертацию параметров (если нужно)
6. ✅ Протестировать с реальным KIE API
7. ✅ Обновить этот документ

---

**ЭТОТ ДОКУМЕНТ ЯВЛЯЕТСЯ ИСТОЧНИКОМ ИСТИНЫ ДЛЯ ВСЕХ ПРАВИЛ ГЕНЕРАЦИИ KIE AI**

**НИЧЕГО ОТ СЕБЯ НЕ ПРИДУМЫВАТЬ - ТОЛЬКО СТРОГО ПО ПРАВИЛАМ KIE AI!**


