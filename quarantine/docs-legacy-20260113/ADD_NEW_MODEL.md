# 📖 Инструкция по добавлению новых моделей

## 🎯 Обзор

Эта инструкция описывает процесс добавления новой модели в бота. Система автоматически обрабатывает:
- Отображение модели в меню
- Ввод параметров через кнопки или текст
- Расчет цен (с учетом x2 для обычных пользователей)
- Генерацию через KIE API
- Отображение доступных генераций на основе баланса

---

## 📝 Шаг 1: Добавление модели в `kie_models.py`

Откройте файл `kie_models.py` и добавьте новую модель в список `KIE_MODELS`.

### Структура модели:

```python
{
    "id": "model-id",                    # Уникальный ID модели (используется в API)
    "name": "Название модели",            # Отображаемое название
    "description": "Описание модели",     # Описание для пользователей
    "category": "Категория",             # Категория (например: "Фото", "Видео", "Текст")
    "emoji": "🎨",                       # Эмодзи для отображения
    "pricing": "Описание цены",          # Текстовое описание цены (для информации)
    "input_params": {                    # Параметры ввода
        "param_name": {
            "type": "string",             # Тип: "string", "array", "number"
            "description": "Описание",     # Описание параметра
            "required": True/False,        # Обязательный ли параметр
            "default": "значение",         # Значение по умолчанию (опционально)
            "max_length": 1000,           # Максимальная длина (для строк)
            "enum": ["вариант1", "вариант2"],  # Варианты выбора (для кнопок)
            "item_type": "string"         # Тип элементов массива (для type="array")
        }
    }
}
```

### Примеры:

#### Пример 1: Простая модель (как Z-Image)
```python
{
    "id": "simple-model",
    "name": "Simple Model",
    "description": "Простая модель генерации изображений",
    "category": "Фото",
    "emoji": "🖼️",
    "pricing": "1.0 кредитов за изображение",
    "input_params": {
        "prompt": {
            "type": "string",
            "description": "Текстовое описание изображения (макс. 1000 символов)",
            "required": True,
            "max_length": 1000
        },
        "aspect_ratio": {
            "type": "string",
            "description": "Соотношение сторон",
            "required": True,
            "default": "1:1",
            "enum": ["1:1", "4:3", "3:4", "16:9", "9:16"]
        }
    }
}
```

#### Пример 2: Сложная модель (как Nano Banana Pro)
```python
{
    "id": "advanced-model",
    "name": "Advanced Model",
    "description": "Продвинутая модель с множеством параметров",
    "category": "Фото",
    "emoji": "🚀",
    "pricing": "20 кредитов (1K/2K) или 30 кредитов (4K)",
    "input_params": {
        "prompt": {
            "type": "string",
            "description": "Текстовое описание (макс. 10000 символов)",
            "required": True,
            "max_length": 10000
        },
        "image_input": {
            "type": "array",
            "description": "Входные изображения (до 8, опционально)",
            "required": False,
            "item_type": "string"
        },
        "aspect_ratio": {
            "type": "string",
            "description": "Соотношение сторон",
            "required": False,
            "default": "1:1",
            "enum": ["1:1", "2:3", "3:2", "16:9", "9:16"]
        },
        "resolution": {
            "type": "string",
            "description": "Разрешение (1K/2K = 20 кредитов, 4K = 30 кредитов)",
            "required": False,
            "default": "1K",
            "enum": ["1K", "2K", "4K"]
        },
        "output_format": {
            "type": "string",
            "description": "Формат выходного файла",
            "required": False,
            "default": "png",
            "enum": ["png", "jpg"]
        }
    }
}
```

### ⚠️ Важные правила:

1. **Параметр `prompt`** - всегда должен быть первым, если он есть
2. **Параметр `image_input`** - обрабатывается специально (загрузка изображений)
3. **Параметры с `enum`** - отображаются как кнопки
4. **Параметры без `enum`** - вводятся текстом
5. **`required: True`** - обязательные параметры
6. **`required: False`** - опциональные параметры

---

## 💰 Шаг 2: Добавление расчета цены в `bot_kie.py`

Откройте файл `bot_kie.py` и найдите функцию `calculate_price_rub` (около строки 109).

### Добавьте логику для новой модели:

```python
def calculate_price_rub(model_id: str, params: dict = None, is_admin: bool = False) -> float:
    """Calculate price in rubles based on model and parameters."""
    if params is None:
        params = {}
    
    # Base prices in credits
    if model_id == "z-image":
        base_credits = 0.8
    elif model_id == "nano-banana-pro":
        resolution = params.get("resolution", "1K")
        if resolution == "4K":
            base_credits = 24
        else:  # 1K or 2K
            base_credits = 18
    elif model_id == "your-new-model-id":  # ← ДОБАВЬТЕ ЭТО
        # Логика расчета цены для вашей модели
        # Пример 1: Фиксированная цена
        base_credits = 10.0
        
        # Пример 2: Цена зависит от параметра
        quality = params.get("quality", "standard")
        if quality == "high":
            base_credits = 20.0
        else:
            base_credits = 10.0
        
        # Пример 3: Цена зависит от разрешения (как nano-banana-pro)
        resolution = params.get("resolution", "1K")
        if resolution == "4K":
            base_credits = 30
        else:
            base_credits = 20
    else:
        # Default fallback
        base_credits = 1.0
    
    # Convert credits to USD, then to RUB (no rounding)
    price_usd = base_credits * CREDIT_TO_USD
    price_rub = price_usd * USD_TO_RUB
    
    # For regular users, multiply by 2
    if not is_admin:
        price_rub *= 2
    
    # Return exact value without rounding
    return price_rub
```

### Константы для расчета:

- `CREDIT_TO_USD = 0.005` (1 кредит = $0.005)
- `USD_TO_RUB = 77.2222...` (1 USD = 77.22 ₽)
- Для обычных пользователей цена умножается на 2
- Для админов цена не умножается (но показывается как "Безлимит")

---

## 📊 Шаг 3: Добавление форматирования цены (опционально)

Найдите функцию `get_model_price_text` (около строки 150) и добавьте специальное форматирование, если нужно:

```python
def get_model_price_text(model_id: str, params: dict = None, is_admin: bool = False) -> str:
    """Get formatted price text for a model."""
    if model_id == "z-image":
        price = calculate_price_rub(model_id, params, is_admin)
        return format_price_rub(price, is_admin) + " за изображение"
    elif model_id == "nano-banana-pro":
        price_1k = calculate_price_rub(model_id, {"resolution": "1K"}, is_admin)
        price_4k = calculate_price_rub(model_id, {"resolution": "4K"}, is_admin)
        price_1k_str = f"{round(price_1k, 2):.2f}"
        price_4k_str = f"{round(price_4k, 2):.2f}"
        if is_admin:
            return f"💰 <b>Безлимит</b> (1K/2K: {price_1k_str} ₽, 4K: {price_4k_str} ₽)"
        else:
            return f"💰 <b>От {price_1k_str} ₽</b> (1K/2K: {price_1k_str} ₽, 4K: {price_4k_str} ₽)"
    elif model_id == "your-new-model-id":  # ← ДОБАВЬТЕ ЭТО (если нужно)
        # Специальное форматирование для вашей модели
        price = calculate_price_rub(model_id, params, is_admin)
        return format_price_rub(price, is_admin) + " за генерацию"
    else:
        # Общий формат (используется по умолчанию)
        price = calculate_price_rub(model_id, params, is_admin)
        return format_price_rub(price, is_admin)
```

**Примечание:** Если не добавите специальное форматирование, будет использован общий формат.

---

## 🔧 Шаг 4: Настройка default_params для расчета минимальной цены

Найдите места, где используется `default_params` для расчета минимальной цены (около строк 1744-1745, 1171-1172, и т.д.):

```python
# Пример для nano-banana-pro
default_params = {}
if model_id == "nano-banana-pro":
    default_params = {"resolution": "1K"}  # Самая дешевая опция

# Добавьте для вашей модели:
if model_id == "your-new-model-id":
    default_params = {"quality": "standard"}  # Самая дешевая опция
    # или
    default_params = {"resolution": "1K"}  # Если есть разрешение
```

Это нужно для правильного расчета минимальной цены и количества доступных генераций.

---

## ✅ Шаг 5: Проверка

После добавления модели:

1. **Перезапустите бота**
2. **Проверьте:**
   - Модель отображается в меню "Все модели"
   - Модель отображается в соответствующей категории
   - При выборе модели показывается правильная цена
   - Показывается количество доступных генераций
   - Параметры ввода работают корректно
   - Кнопки "Назад" работают
   - Генерация работает через KIE API

---

## 📋 Чеклист добавления новой модели

- [ ] Добавлена модель в `KIE_MODELS` в `kie_models.py`
- [ ] Указан правильный `id` (должен совпадать с ID в KIE API)
- [ ] Указана категория (существующая или новая)
- [ ] Добавлен эмодзи
- [ ] Определены все `input_params` с правильными типами
- [ ] Параметры с `enum` настроены для кнопок
- [ ] Добавлена логика расчета цены в `calculate_price_rub`
- [ ] Добавлено форматирование цены в `get_model_price_text` (если нужно)
- [ ] Настроены `default_params` для минимальной цены
- [ ] Проверена работа модели в боте

---

## 🎨 Примеры типов параметров

### 1. Текстовый параметр (prompt)
```python
"prompt": {
    "type": "string",
    "description": "Описание изображения",
    "required": True,
    "max_length": 1000
}
```

### 2. Параметр с выбором (кнопки)
```python
"aspect_ratio": {
    "type": "string",
    "description": "Соотношение сторон",
    "required": True,
    "default": "1:1",
    "enum": ["1:1", "4:3", "3:4", "16:9", "9:16"]
}
```

### 3. Массив изображений
```python
"image_input": {
    "type": "array",
    "description": "Входные изображения (до 8)",
    "required": False,
    "item_type": "string"
}
```

### 4. Числовой параметр
```python
"steps": {
    "type": "number",
    "description": "Количество шагов генерации",
    "required": False,
    "default": 50,
    "min": 10,
    "max": 100
}
```

---

## 🔍 Где что находится

### Файлы:
- **`kie_models.py`** - определение моделей
- **`bot_kie.py`** - логика бота, расчет цен, обработка параметров
- **`kie_client.py`** - клиент для работы с KIE API

### Функции в `bot_kie.py`:
- **`calculate_price_rub()`** - расчет цены в рублях
- **`format_price_rub()`** - форматирование цены для отображения
- **`get_model_price_text()`** - текст с ценой для модели
- **`select_model:`** - обработчик выбора модели (строка 1728)
- **`input_parameters()`** - обработка ввода параметров
- **`start_next_parameter()`** - переход к следующему параметру

### Состояния разговора:
- **`SELECTING_MODEL`** - выбор модели
- **`INPUTTING_PARAMS`** - ввод параметров
- **`CONFIRMING_GENERATION`** - подтверждение генерации

---

## 💡 Советы

1. **Всегда тестируйте** новую модель перед добавлением в продакшн
2. **Проверяйте цены** - они должны быть корректными для админов и пользователей
3. **Используйте понятные названия** параметров и описания
4. **Группируйте модели по категориям** для удобной навигации
5. **Добавляйте эмодзи** для визуального различия моделей

---

## 🚀 Готово!

После выполнения всех шагов ваша модель будет автоматически:
- Отображаться в меню
- Показывать правильные цены
- Обрабатывать параметры ввода
- Генерировать контент через KIE API
- Работать с балансом пользователей

Удачи! 🎉

