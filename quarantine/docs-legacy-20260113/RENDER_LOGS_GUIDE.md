# 📊 Руководство по получению логов с Render

## 🚀 Быстрый старт

### Вариант 1: Простой скрипт (Windows)

1. **Установите переменные окружения:**
   ```cmd
   set RENDER_API_KEY=your_api_key_here
   set RENDER_SERVICE_ID=your_service_id_here
   ```

2. **Запустите скрипт:**
   ```cmd
   get_render_logs_simple.bat
   ```

### Вариант 2: Python скрипт (кроссплатформенный)

```bash
python get_render_logs.py --service-id srv-xxxxx --lines 200 --analyze
```

---

## 🔑 Как получить API ключ Render

1. **Откройте Render Dashboard:**
   - https://dashboard.render.com/
   - Войдите в свой аккаунт

2. **Перейдите в настройки:**
   - Нажмите на ваш профиль (правый верхний угол)
   - Выберите **"Account Settings"**
   - Или перейдите напрямую: https://dashboard.render.com/account/api-keys

3. **Создайте API ключ:**
   - Нажмите **"New API Key"**
   - Введите название (например, "Logs Access")
   - Скопируйте ключ (он показывается только один раз!)

4. **Установите переменную окружения:**
   ```cmd
   # Windows
   set RENDER_API_KEY=your_api_key_here
   
   # Linux/Mac
   export RENDER_API_KEY=your_api_key_here
   ```

---

## 📋 Как найти Service ID

### Способ 1: Из URL

1. Откройте ваш сервис в Render Dashboard
2. Посмотрите на URL в адресной строке:
   ```
   https://dashboard.render.com/web/srv-xxxxxxxxxxxxx
   ```
3. `srv-xxxxxxxxxxxxx` - это ваш Service ID

### Способ 2: Из настроек сервиса

1. Откройте ваш сервис
2. Перейдите в **"Settings"**
3. Найдите **"Service ID"** в разделе "Service Details"

### Способ 3: Через скрипт

```bash
python get_render_logs.py --list-services
```

Это покажет все ваши сервисы с их ID.

---

## 📊 Использование скрипта

### Базовые команды

```bash
# Получить последние 100 строк логов
python get_render_logs.py --service-id srv-xxxxx

# Получить больше строк
python get_render_logs.py --service-id srv-xxxxx --lines 500

# Отслеживать логи в реальном времени
python get_render_logs.py --service-id srv-xxxxx --follow

# Проанализировать логи на ошибки
python get_render_logs.py --service-id srv-xxxxx --analyze

# Показать все сервисы
python get_render_logs.py --list-services
```

### Комбинированные команды

```bash
# Получить 200 строк и проанализировать
python get_render_logs.py --service-id srv-xxxxx --lines 200 --analyze

# Отслеживать с анализом (каждые 5 секунд)
python get_render_logs.py --service-id srv-xxxxx --follow
```

---

## 🔍 Анализ логов

Скрипт автоматически анализирует логи и показывает:

- 🚨 **Конфликты 409** - несколько экземпляров бота запущено
- ❌ **Ошибки** - критические проблемы
- ⚠️ **Предупреждения** - потенциальные проблемы

### Пример вывода анализа:

```
📊 АНАЛИЗ ЛОГОВ
================================================================================

🚨 КРИТИЧНО: Найдено 5 конфликтов 409!
Это означает, что запущено несколько экземпляров бота одновременно

💡 РЕШЕНИЕ:
1. Остановите все локальные экземпляры бота
2. Проверьте Render Dashboard - нет ли дублирующих сервисов
3. Выполните Restart на Render
4. Убедитесь, что webhook удалён:
   curl https://api.telegram.org/bot<TOKEN>/deleteWebhook?drop_pending_updates=true
```

---

## 🚨 Решение проблемы 409 Conflict

Если в логах видите ошибки 409:

### Шаг 1: Остановите все экземпляры

**На Render:**
1. Откройте ваш сервис
2. Нажмите **"Suspend"** (временно остановить)
3. Подождите 10 секунд
4. Нажмите **"Resume"**

**Локально (если запущен):**
```cmd
# Windows
taskkill /F /IM python.exe

# Linux/Mac
pkill -f bot_kie.py
```

### Шаг 2: Удалите webhook

```bash
curl https://api.telegram.org/bot<YOUR_TOKEN>/deleteWebhook?drop_pending_updates=true
```

### Шаг 3: Проверьте дублирующие сервисы

1. Откройте Render Dashboard
2. Проверьте список всех сервисов
3. Убедитесь, что есть **только ОДИН** сервис для бота
4. Если есть дубликаты - удалите их

### Шаг 4: Перезапустите

1. На Render нажмите **"Restart"**
2. Подождите полного перезапуска
3. Проверьте логи - не должно быть ошибок 409

---

## 📝 Постоянная настройка (Windows)

Чтобы не вводить ключи каждый раз, создайте файл `setup_render_env.bat`:

```batch
@echo off
set RENDER_API_KEY=your_api_key_here
set RENDER_SERVICE_ID=your_service_id_here
echo ✅ Переменные окружения установлены
```

Запускайте его перед использованием скрипта.

---

## 🔄 Автоматическое отслеживание

Для постоянного мониторинга можно использовать планировщик задач Windows:

1. Откройте **Планировщик заданий**
2. Создайте новое задание
3. Установите действие: `get_render_logs_simple.bat`
4. Установите расписание (например, каждые 5 минут)
5. Включите опцию "Запускать с наивысшими правами"

---

## 💡 Полезные советы

1. **Сохраняйте логи в файл:**
   ```bash
   python get_render_logs.py --service-id srv-xxxxx --lines 500 > logs.txt
   ```

2. **Фильтруйте ошибки:**
   ```bash
   python get_render_logs.py --service-id srv-xxxxx --analyze | findstr "ERROR"
   ```

3. **Мониторинг в реальном времени:**
   ```bash
   python get_render_logs.py --service-id srv-xxxxx --follow
   ```

---

## ❓ Частые вопросы

**Q: Скрипт не работает, говорит что API ключ неверный**
A: Проверьте, что ключ скопирован полностью, без пробелов в начале/конце

**Q: Не могу найти Service ID**
A: Используйте `--list-services` чтобы увидеть все ваши сервисы

**Q: Логи не обновляются**
A: Убедитесь, что сервис на Render запущен и работает

**Q: Как часто обновлять логи?**
A: Для мониторинга достаточно каждые 5-10 минут. Для отладки используйте `--follow`

---

**Готово! 🚀**

Теперь вы можете легко отслеживать логи вашего бота на Render!







