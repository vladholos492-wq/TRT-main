# Интеграция bytedance/seedream-v4-edit

## 📋 Обзор

Документация по интеграции модели `bytedance/seedream-v4-edit` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `bytedance/seedream-v4-edit`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"bytedance/seedream-v4-edit"` |
| `input.prompt` | string | Текстовый промпт для редактирования изображения | Обязательный, максимум 5000 символов |
| `input.image_urls` | array | Массив URL входных изображений для редактирования | Обязательный, массив, до 10 изображений, макс 10MB каждое, jpeg/png/webp |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.image_size` | string | Размер изображения (соотношение сторон) | `"square_hd"` |
| `input.image_resolution` | string | Разрешение изображения (пиксельный масштаб) | `"1K"` |
| `input.max_images` | number | Максимальное количество изображений (1-6) | `1` |
| `input.seed` | number | Случайное seed для контроля стохастичности | Не указан |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `image_urls`
- Тип: array
- Обязательный: да
- Максимум элементов: 10
- Максимальный размер файла: 10MB для каждого изображения
- Поддерживаемые форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Должен быть массивом URL (или одним URL, который будет преобразован в массив)! Не может быть пустым! Максимум 10 изображений!

#### `image_size`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"square"` - Квадрат
  - `"square_hd"` - Квадрат HD (default)
  - `"portrait_4_3"` - Портрет 3:4
  - `"portrait_3_2"` - Портрет 2:3
  - `"portrait_16_9"` - Портрет 9:16
  - `"landscape_4_3"` - Альбом 4:3
  - `"landscape_3_2"` - Альбом 3:2
  - `"landscape_16_9"` - Альбом 16:9
  - `"landscape_21_9"` - Альбом 21:9
- **Важно**: Опциональный параметр! Только указанные значения! Default: `"square_hd"`!

#### `image_resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"1K"` - 1K (default)
  - `"2K"` - 2K
  - `"4K"` - 4K
- **Важно**: Опциональный параметр! Только 3 значения! Default: `"1K"`!
- **Примечание**: Финальное разрешение определяется комбинацией `image_size` (соотношение сторон) и `image_resolution` (пиксельный масштаб). Например, 4:3 + 4K = 4096 × 3072px

#### `max_images`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 1-6 (шаг: 1)
- Default: `1`
- **Важно**: Опциональный параметр! Только числа от 1 до 6! Default: `1`!
- **Примечание**: Устанавливает максимальное количество изображений, которые может создать одна генерация. Изображения создаются одним запросом, поэтому нужно также указать точное количество в промпте, чтобы настройки совпадали.

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- **Важно**: Опциональный параметр! Любое число! Используется для контроля стохастичности генерации!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | bytedance/seedream-v4-edit | bytedance/seedream-v4-text-to-image | Другие i2i модели |
|----------|----------------------------|-------------------------------------|-------------------|
| `prompt` max | 5000 символов | 5000 символов | Обычно 1000-5000 |
| `image_urls` | ✅ Обязательный (массив, до 10) | ❌ Нет | ❌ Нет (используют image_url) |
| `image_size` | ✅ Есть (enum, 9 значений) | ✅ Есть (enum, 9 значений) | ❌ Нет |
| `image_resolution` | ✅ Есть (enum, 3 значения) | ✅ Есть (enum, 3 значения) | ❌ Нет |
| `max_images` | ✅ Есть (1-6) | ✅ Есть (1-6) | ❌ Нет |
| `seed` | ✅ Есть (number) | ✅ Есть (number) | ✅ Есть (number) |

**ВАЖНО**: 
- Это модель image-to-image (edit) с уникальными параметрами!
- Использует `image_urls` (массив, до 10 изображений) вместо стандартного `image_url`!
- Использует `image_size` и `image_resolution` вместо стандартных `width/height` или `aspect_ratio`!
- Использует `max_images` вместо стандартного `image_count`!
- Финальное разрешение = `image_size` (соотношение) + `image_resolution` (масштаб)!

## 🔧 Реализация

### Валидация

Функция `_validate_bytedance_seedream_v4_edit()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано и не пустое
2. **Длина `prompt`**: Максимум 5000 символов
3. **Обязательность `image_urls`**: Поле должно быть указано (массив или один URL)
4. **Количество `image_urls`**: От 1 до 10 изображений
5. **Значение `image_size`**: Должно быть одним из 9 допустимых значений (опциональный)
6. **Значение `image_resolution`**: Должно быть "1K", "2K" или "4K" (опциональный)
7. **Значение `max_images`**: Должно быть числом от 1 до 6 (опциональный)
8. **Значение `seed`**: Должно быть числом (опциональный)

### Нормализация

1. **`image_urls`**: Если передан один URL (строка), преобразуется в массив
2. **`_normalize_image_size_for_seedream_v4()`**: Нормализует image_size (9 значений) - переиспользуется из text-to-image
3. **`_normalize_image_resolution_for_seedream_v4()`**: Нормализует image_resolution ("1K" | "2K" | "4K") - переиспользуется из text-to-image

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'image_size' not in normalized_input:
    normalized_input['image_size'] = "square_hd"  # Default согласно документации
if 'image_resolution' not in normalized_input:
    normalized_input['image_resolution'] = "1K"  # Default согласно документации
if 'max_images' not in normalized_input:
    normalized_input['max_images'] = 1  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "bytedance/seedream-v4-edit",
  "input": {
    "prompt": "Refer to this logo and create a single visual showcase for an outdoor sports brand named 'KIE AI'. Display five branded items together in one image: a packaging bag, a hat, a carton box, a wristband, and a lanyard. Use blue as the main visual color, with a fun, simple, and modern style.",
    "image_urls": ["https://file.aiquickdraw.com/custom-page/akr/section-images/1757930552966e7f2on7s.png"],
    "image_size": "square_hd",
    "image_resolution": "1K",
    "max_images": 1,
    "seed": 42
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для редактирования |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'image_urls' обязательно...` | `image_urls` не указан | Загрузите хотя бы одно изображение |
| `Поле 'image_urls' не может быть пустым массивом` | `image_urls` пустой массив | Загрузите хотя бы одно изображение |
| `Поле 'image_urls' может содержать максимум 10 изображений` | `image_urls` содержит больше 10 изображений | Уменьшите количество до 10 или меньше |
| `Поле 'image_size' должно быть одним из...` | Неверное значение `image_size` | Используйте одно из 9 допустимых значений |
| `Поле 'image_resolution' должно быть одним из...` | Неверное значение `image_resolution` | Используйте "1K", "2K" или "4K" |
| `Поле 'max_images' должно быть в диапазоне...` | Неверное значение `max_images` | Используйте число от 1 до 6 |
| `Поле 'seed' должно быть числом...` | Неверное значение `seed` | Используйте любое число |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("bytedance/seedream-v4-edit")
user_payload = {
    "prompt": "Refer to this logo and create a single visual showcase...",
    "image_urls": ["https://example.com/image.png"],
    "image_size": "square_hd",
    "image_resolution": "1K",
    "max_images": 1,
    "seed": 42
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлены параметры `image_urls`, `image_size`, `image_resolution`, `max_images` в схему `i2i`
- [x] Реализована функция `_validate_bytedance_seedream_v4_edit()`
- [x] Переиспользованы функции нормализации из text-to-image версии
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=bytedance/seedream-v4-edit TYPE=i2i INPUT_KEYS=['prompt', 'image_urls', 'image_size', 'image_resolution', 'max_images', 'seed'] 
INPUT_PREVIEW={'prompt': 'Refer to this logo...', 'image_urls': ['https://...'], 'image_size': 'square_hd', 'image_resolution': '1K', 'max_images': 1, 'seed': 42}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для редактирования изображения (максимум 5000 символов)
2. **image_urls обязательный** - обязателен! Должен быть массивом URL (или одним URL, который будет преобразован)! Не может быть пустым! Максимум 10 изображений!
3. **image_size опциональный** - не обязательный! Только 9 значений, default "square_hd"!
4. **image_resolution опциональный** - не обязательный! Только 3 значения ("1K", "2K", "4K"), default "1K"!
5. **max_images опциональный** - не обязательный! Только числа от 1 до 6, default 1!
6. **seed опциональный** - не обязательный! Любое число для контроля стохастичности!
7. **Уникальные параметры** - модель использует `image_urls` (массив) вместо стандартного `image_url`! Использует `image_size` и `image_resolution` вместо стандартных `width/height` или `aspect_ratio`!
8. **Финальное разрешение** - определяется комбинацией `image_size` (соотношение) + `image_resolution` (масштаб)!

