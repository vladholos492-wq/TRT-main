# Интеграция flux-2/pro-image-to-image

## 📋 Обзор

Документация по интеграции модели `flux-2/pro-image-to-image` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `flux-2/pro-image-to-image`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"flux-2/pro-image-to-image"` |
| `input.input_urls` | array | Массив URL входных изображений | Обязательный, 1-8 изображений |
| `input.prompt` | string | Текстовое описание изображения | Обязательный, от 3 до 5000 символов |
| `input.aspect_ratio` | string | Соотношение сторон изображения | Обязательный, один из допустимых значений |
| `input.resolution` | string | Разрешение выходного изображения | Обязательный, "1K" или "2K" |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `input_urls`
- Тип: массив строк (array of strings)
- Обязательный: да
- Формат: `["https://example.com/image1.jpg", "https://example.com/image2.jpg"]`
- Количество: 1-8 изображений
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: JPEG, PNG, WebP
- **Важно**: Должен быть массивом, не строкой! Используется `input_urls`, а не `image_urls`!

#### `prompt`
- Тип: string
- Обязательный: да
- Минимум: 3 символа
- Максимум: 5000 символов
- **Важно**: Не может быть пустым! Должен быть от 3 до 5000 символов!

#### `aspect_ratio`
- Тип: string
- Обязательный: да
- Значения:
  - `"1:1"` - Квадрат (default)
  - `"4:3"` - Горизонтальный 4:3
  - `"3:4"` - Вертикальный 3:4
  - `"16:9"` - Горизонтальный 16:9
  - `"9:16"` - Вертикальный 9:16
  - `"3:2"` - Классический горизонтальный
  - `"2:3"` - Классический вертикальный
  - `"auto"` - Автоматически на основе первого входного изображения
- **Важно**: Обязательный параметр, не опциональный! 8 значений (включая "auto")!

#### `resolution`
- Тип: string
- Обязательный: да
- Значения:
  - `"1K"` - 1K разрешение (default)
  - `"2K"` - 2K разрешение
- **Важно**: Обязательный параметр, не опциональный! Только "1K" или "2K"!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_flux_2_pro_image_to_image(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для flux-2/pro-image-to-image согласно документации API.
    """
    if model_id != "flux-2/pro-image-to-image":
        return True, None
    
    # Валидация input_urls: обязательный массив (1-8 изображений)
    # ВАЖНО: Для Flux моделей используется input_urls, а не image_urls!
    input_urls = normalized_input.get('input_urls')
    if not input_urls:
        return False, "Поле 'input_urls' обязательно для генерации изображения"
    
    # Нормализуем input_urls
    normalized_input_urls = _normalize_input_urls_for_flux_2_pro(input_urls)
    if not normalized_input_urls:
        return False, "Поле 'input_urls' должно содержать хотя бы один валидный URL изображения"
    
    # Проверяем количество изображений (1-8)
    if len(normalized_input_urls) > 8:
        return False, f"Поле 'input_urls' содержит слишком много изображений: {len(normalized_input_urls)} (максимум 8)"
    
    # Валидация prompt: обязательный, от 3 до 5000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации изображения"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len < 3:
        return False, f"Поле 'prompt' слишком короткое: {prompt_len} символов (минимум 3)"
    if prompt_len > 5000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 5000)"
    
    # Валидация aspect_ratio и resolution
    ...
    
    return True, None
```

### Нормализация input_urls

Функция нормализации конвертирует строку в массив и проверяет количество:

```python
def _normalize_input_urls_for_flux_2_pro(value: Any) -> Optional[List[str]]:
    """
    Нормализует input_urls для flux-2/pro-image-to-image.
    ВАЖНО: Для Flux моделей используется input_urls, а не image_urls!
    """
    # Если это строка, конвертируем в массив
    if isinstance(value, str):
        if value.strip():
            return [value.strip()]
        return None
    
    # Если это массив, проверяем и нормализуем элементы
    if isinstance(value, list):
        normalized = []
        for item in value:
            if isinstance(item, str) and item.strip():
                normalized.append(item.strip())
        return normalized if normalized else None
    
    return None
```

### Нормализация aspect_ratio

Функция нормализации поддерживает 8 значений (включая "auto"):

```python
def _normalize_aspect_ratio_for_flux_2_pro(value: Any) -> Optional[str]:
    """
    Нормализует aspect_ratio для flux-2/pro-image-to-image.
    ВАЖНО: Для flux-2/pro поддерживаются "1:1", "4:3", "3:4", "16:9", "9:16", "3:2", "2:3", "auto"!
    """
    valid_values = ["1:1", "4:3", "3:4", "16:9", "9:16", "3:2", "2:3", "auto"]
    # Извлекает только соотношение сторон, если есть дополнительные символы (например, "1:1 (Square)")
    # Поддерживает алиасы (square, landscape, portrait и т.д.)
    ...
```

### Нормализация resolution

Функция нормализации конвертирует в верхний регистр:

```python
def _normalize_resolution_for_flux_2_pro(value: Any) -> Optional[str]:
    """
    Нормализует resolution для flux-2/pro-image-to-image.
    ВАЖНО: Для flux-2/pro поддерживаются только "1K" и "2K"!
    """
    # Конвертирует в верхний регистр и проверяет валидность
    valid_values = ["1K", "2K"]
    ...
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id == "flux-2/pro-image-to-image":
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "1:1"  # Default согласно документации
    if 'resolution' not in normalized_input:
        normalized_input['resolution'] = "1K"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "flux-2/pro-image-to-image",
  "input": {
    "input_urls": ["https://static.aiquickdraw.com/tools/example/1764235041265_kjJ2sTMR.png", "https://static.aiquickdraw.com/tools/example/1764235045490_9SjAUr4Z.png"],
    "prompt": "The jar in image 1 is filled with capsules exactly same as image 2 with the exact logo",
    "aspect_ratio": "1:1",
    "resolution": "1K"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация input_urls**: Проверяется что это массив с 1-8 валидными URL
2. **Нормализация input_urls**: Автоматически конвертирует строку в массив
3. **Валидация prompt**: Проверяется длина (от 3 до 5000 символов)
4. **Валидация aspect_ratio**: Проверяется что значение из допустимого списка (8 значений, включая "auto")
5. **Валидация resolution**: Проверяется что значение "1K" или "2K" (конвертируется в верхний регистр)
6. **Дефолтные значения**: Применяются если параметры не указаны (aspect_ratio="1:1", resolution="1K")
7. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=flux-2/pro-image-to-image TYPE=i2i INPUT_KEYS=['input_urls', 'prompt', 'aspect_ratio', 'resolution'] 
INPUT_PREVIEW={'input_urls': ['https://...'], 'prompt': 'The jar in image 1...', 'aspect_ratio': '1:1', 'resolution': '1K'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **input_urls (не image_urls!)** - Для Flux моделей используется `input_urls`, а не `image_urls`!
2. **prompt от 3 до 5000 символов** - проверяется минимум и максимум!
3. **input_urls 1-8 изображений** - проверяется количество изображений
4. **aspect_ratio обязательный** - не опциональный! 8 значений (включая "auto")!
5. **resolution обязательный** - не опциональный! Только "1K" или "2K"!
6. **Дефолтные значения**: aspect_ratio="1:1", resolution="1K"
7. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими моделями

### Сравнение с seedream/4.5-edit

| Параметр | seedream/4.5-edit | flux-2/pro-image-to-image |
|----------|-------------------|---------------------------|
| `prompt` | Максимум 3000 символов | От 3 до 5000 символов |
| `image_urls` | Обязательный массив | Не используется |
| `input_urls` | Не используется | Обязательный массив (1-8) |
| `aspect_ratio` | 8 значений (без "auto") | 8 значений (с "auto") |
| `quality` | Обязательный, "basic"\|"high" | Не требуется |
| `resolution` | Не требуется | Обязательный, "1K"\|"2K" |
| Тип модели | i2i | i2i |

### Сравнение с z-image

| Параметр | z-image | flux-2/pro-image-to-image |
|----------|---------|---------------------------|
| `prompt` | Максимум 1000 символов | От 3 до 5000 символов |
| `input_urls` | Не требуется | Обязательный массив (1-8) |
| `aspect_ratio` | 5 значений | 8 значений (с "auto") |
| `resolution` | Не требуется | Обязательный, "1K"\|"2K" |
| Тип модели | t2i | i2i |

## 🎯 Ключевые отличия

1. **input_urls вместо image_urls**: Для Flux моделей используется специфичное имя параметра
2. **Диапазон prompt**: От 3 до 5000 символов (не просто максимум!)
3. **Множественные изображения**: Поддерживает 1-8 входных изображений
4. **aspect_ratio с "auto"**: Включает опцию "auto" для автоматического определения соотношения сторон
5. **resolution обязательный**: В отличие от других моделей, требует явного указания разрешения
6. **Дефолтные значения**: aspect_ratio="1:1", resolution="1K"

