# Интеграция ideogram/v3-edit

## 📋 Обзор

Документация по интеграции модели `ideogram/v3-edit` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `ideogram/v3-edit`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"ideogram/v3-edit"` |
| `input.prompt` | string | Промпт для заполнения замаскированной части изображения | Обязательный, максимум 5000 символов |
| `input.image_url` | string | URL изображения для генерации изображения. Должен соответствовать размерам маски | Обязательный, максимум 10MB, форматы: jpeg/png/webp |
| `input.mask_url` | string | URL маски для инпейнтинга изображения. Должен соответствовать размерам входного изображения | Обязательный, максимум 10MB, форматы: jpeg/png/webp |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.rendering_speed` | string | Скорость рендеринга | `"BALANCED"` |
| `input.expand_prompt` | boolean | Определяет, должен ли использоваться MagicPrompt при генерации запроса | `true` |
| `input.seed` | number | Seed для генератора случайных чисел | Не указан |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов! Промпт для заполнения замаскированной части изображения!

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp! Должен соответствовать размерам маски!

#### `mask_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp! Должен соответствовать размерам входного изображения! **Уникальный параметр для inpainting!**

#### `rendering_speed`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"TURBO"`, `"BALANCED"`, `"QUALITY"`
- Default: `"BALANCED"`
- **Важно**: Опциональный параметр! Только 3 указанных значения (enum)! Default: `"BALANCED"`! Скорость рендеринга!

#### `expand_prompt`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean! Default: `true`! Определяет, должен ли использоваться MagicPrompt при генерации запроса!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- **Важно**: Опциональный параметр! Число! Seed для генератора случайных чисел!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | ideogram/v3-edit | ideogram/v3-text-to-image | ideogram/v3-reframe |
|----------|-------------------|---------------------------|---------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный (max 5000) | ❌ Нет |
| `image_url` | ✅ Обязательный (max 10MB) | ❌ Нет | ✅ Обязательный (max 10MB) |
| `mask_url` | ✅ Обязательный (max 10MB) | ❌ Нет | ❌ Нет |
| `rendering_speed` | ✅ Есть (enum, default "BALANCED") | ✅ Есть (enum, default "BALANCED") | ✅ Есть (enum, default "BALANCED") |
| `expand_prompt` | ✅ Есть (boolean, default true) | ✅ Есть (boolean, default true) | ❌ Нет |
| `seed` | ✅ Есть (number) | ✅ Есть (number) | ✅ Есть (number) |
| `style` | ❌ Нет | ✅ Есть (enum, default "AUTO") | ✅ Есть (enum, default "AUTO") |
| `image_size` | ❌ Нет | ✅ Есть (enum, default "square_hd") | ✅ Есть (enum, default "square_hd") |
| `negative_prompt` | ❌ Нет | ✅ Есть (max 5000, default "") | ❌ Нет |
| `num_images` | ❌ Нет | ❌ Нет | ✅ Есть (enum, default "1") |

**ВАЖНО**: 
- Это модель для редактирования изображений с использованием маски (inpainting) - V3 версия!
- Использует `mask_url` для указания маски для инпейнтинга (обязательный параметр!) - уникальный параметр для inpainting!
- Использует `rendering_speed` для управления скоростью рендеринга (enum: TURBO/BALANCED/QUALITY, default BALANCED)!
- Использует `expand_prompt` для управления MagicPrompt (boolean, default true)!
- `prompt` имеет максимум 5000 символов!
- НЕТ параметров `style`, `image_size`, `negative_prompt`, `num_images` (есть в других ideogram v3 моделях)!
- **Отличается от ideogram/v3-text-to-image наличием обязательных `image_url` и `mask_url` (для inpainting)!**

## 🔧 Реализация

### Валидация

Функция `_validate_ideogram_v3_edit()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Обязательность `image_url`**: Поле должно быть указано, не пустое, string
3. **Обязательность `mask_url`**: Поле должно быть указано, не пустое, string - уникальный параметр для inpainting!
4. **Значение `rendering_speed`**: Должно быть одним из 3 допустимых значений (опциональный, enum)
5. **Значение `expand_prompt`**: Должно быть boolean (опциональный)
6. **Значение `seed`**: Должно быть числом (опциональный)

### Нормализация

1. **`rendering_speed`**: Нормализуется через `_normalize_rendering_speed_for_ideogram_character_edit()`
2. **`expand_prompt`**: Нормализуется через `_normalize_boolean()`
3. **`seed`**: Нормализуется в число (int если целое, float если дробное)

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'rendering_speed' not in normalized_input:
    normalized_input['rendering_speed'] = "BALANCED"  # Default согласно документации
if 'expand_prompt' not in normalized_input:
    normalized_input['expand_prompt'] = True  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "ideogram/v3-edit",
  "input": {
    "prompt": "A dog wearing a cowboy hat",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755076859801ryyol1du.webp",
    "mask_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755076871089hx9uonhc.webp",
    "rendering_speed": "BALANCED",
    "expand_prompt": true,
    "seed": 42
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для заполнения замаскированной части изображения |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения для редактирования |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите непустой URL изображения |
| `Поле 'mask_url' обязательно...` | `mask_url` не указан | Укажите URL маски для инпейнтинга |
| `Поле 'mask_url' не может быть пустым` | `mask_url` пустой | Укажите непустой URL маски |
| `Поле 'rendering_speed' должно быть одним из...` | Неверное значение `rendering_speed` | Используйте одно из: TURBO, BALANCED, QUALITY |
| `Поле 'expand_prompt' должно быть boolean...` | Неверное значение `expand_prompt` | Используйте true или false |
| `Поле 'seed' должно быть числом...` | Неверное значение `seed` | Используйте число |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("ideogram/v3-edit")
user_payload = {
    "prompt": "A dog wearing a cowboy hat",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755076859801ryyol1du.webp",
    "mask_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755076871089hx9uonhc.webp",
    "rendering_speed": "BALANCED",
    "expand_prompt": True,
    "seed": 42
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Параметры уже есть в схеме `i2i` (mask_url, rendering_speed, expand_prompt, seed)
- [x] Реализована функция `_validate_ideogram_v3_edit()`
- [x] Переиспользованы функции нормализации из других ideogram моделей
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=ideogram/v3-edit TYPE=i2i INPUT_KEYS=['prompt', 'image_url', 'mask_url', 'rendering_speed', 'expand_prompt', 'seed'] 
INPUT_PREVIEW={'prompt': 'A dog wearing a cowboy hat', 'image_url': 'https://...', 'mask_url': 'https://...', 'rendering_speed': 'BALANCED', 'expand_prompt': True, 'seed': 42}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для редактирования изображения (максимум 5000 символов)! Промпт для заполнения замаскированной части изображения!
2. **image_url обязательный** - обязателен для редактирования изображения! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp! Должен соответствовать размерам маски!
3. **mask_url обязательный** - обязателен для редактирования изображения! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp! Должен соответствовать размерам входного изображения! **Уникальный параметр для inpainting!**
4. **rendering_speed опциональный** - не обязательный! Только 3 значения (enum: TURBO, BALANCED, QUALITY), default "BALANCED"! Скорость рендеринга!
5. **expand_prompt опциональный** - не обязательный! Boolean, default true! Определяет, должен ли использоваться MagicPrompt при генерации запроса!
6. **seed опциональный** - не обязательный! Число! Seed для генератора случайных чисел!
7. **Уникальные параметры** - модель использует `mask_url` (уникальный для inpainting!), `rendering_speed`, `expand_prompt` вместо стандартных `style`, `image_size`, `negative_prompt`, `num_images`!
8. **НЕТ стандартных параметров** - модель НЕ поддерживает `style`, `image_size`, `negative_prompt`, `num_images` (есть в других ideogram v3 моделях)!
9. **prompt max 5000** - `prompt` имеет максимум 5000 символов!
10. **Отличается от других ideogram v3 моделей** - имеет обязательные `image_url` и `mask_url` (для inpainting), которых нет в ideogram/v3-text-to-image!

