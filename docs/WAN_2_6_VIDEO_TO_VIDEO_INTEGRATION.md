# Интеграция wan/2-6-video-to-video

## 📋 Обзор

Документация по интеграции модели `wan/2-6-video-to-video` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `wan/2-6-video-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"wan/2-6-video-to-video"` |
| `input.prompt` | string | Текстовое описание видео | 2-5000 символов, обязательный |
| `input.video_urls` | array | Массив URL видео | Обязательный, минимум 1 URL |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.duration` | string | Длительность видео в секундах | `"5"` |
| `input.resolution` | string | Разрешение видео | `"1080p"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Минимум: 2 символа
- Максимум: 5000 символов
- Поддерживает китайский и английский языки
- **Важно**: Минимум 2 символа (не 1!)

#### `video_urls`
- Тип: массив строк (array of strings)
- Обязательный: да
- Формат: `["https://example.com/video.mp4"]`
- Поддерживает множественные видео
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: MP4, QuickTime, Matroska
- **Важно**: Должен быть массивом, не строкой!

#### `duration`
- `"5"` - 5 секунд
- `"10"` - 10 секунд
- **Важно**: Только "5" и "10"! "15" НЕ поддерживается для v2v!
- **Важно**: Должно быть строкой, не числом!

#### `resolution`
- `"720p"` - 720p разрешение
- `"1080p"` - 1080p разрешение
- **Важно**: Должно быть строкой с суффиксом "p"!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_wan_2_6_video_to_video(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для wan/2-6-video-to-video согласно документации API.
    """
    if model_id != "wan/2-6-video-to-video":
        return True, None
    
    # Валидация prompt: обязательный, 2-5000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации видео"
    
    prompt_len = len(prompt.strip())
    if prompt_len < 2:
        return False, "Поле 'prompt' должно содержать минимум 2 символа"
    if prompt_len > 5000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 5000)"
    
    # Валидация video_urls: обязательный массив
    video_urls = normalized_input.get('video_urls')
    if not video_urls:
        return False, "Поле 'video_urls' обязательно для генерации видео из видео"
    
    # Нормализуем video_urls (конвертируем строку в массив если нужно)
    normalized_video_urls = _normalize_video_urls_for_wan_2_6(video_urls)
    if not normalized_video_urls:
        return False, "Поле 'video_urls' должно содержать хотя бы один валидный URL видео"
    
    # Проверяем что все URL начинаются с http:// или https://
    for idx, url in enumerate(normalized_video_urls):
        if not (url.startswith('http://') or url.startswith('https://')):
            return False, f"URL видео #{idx + 1} должен начинаться с http:// или https://"
    
    # Сохраняем нормализованное значение
    normalized_input['video_urls'] = normalized_video_urls
    
    # Валидация duration: опциональный, "5" | "10" (НЕ "15"!), default "5"
    duration = normalized_input.get('duration')
    if duration is not None:
        normalized_duration = _normalize_duration_for_wan_2_6_v2v(duration)
        if normalized_duration is None:
            return False, f"Поле 'duration' должно быть '5' или '10' (получено: {duration})"
        normalized_input['duration'] = normalized_duration
    
    # Валидация resolution: опциональный, "720p" | "1080p", default "1080p"
    resolution = normalized_input.get('resolution')
    if resolution is not None:
        normalized_resolution = _normalize_resolution_for_wan_2_6(resolution)
        if normalized_resolution is None:
            return False, f"Поле 'resolution' должно быть '720p' или '1080p' (получено: {resolution})"
        normalized_input['resolution'] = normalized_resolution
    
    return True, None
```

### Нормализация video_urls

Функция нормализации автоматически конвертирует строку в массив:

```python
def _normalize_video_urls_for_wan_2_6(value: Any) -> Optional[List[str]]:
    """
    Нормализует video_urls для wan/2-6-video-to-video.
    Принимает строку, массив строк или None и возвращает массив строк.
    """
    # Если это строка, конвертируем в массив
    if isinstance(value, str):
        if value.strip():
            return [value.strip()]
        return None
    
    # Если это массив, проверяем и нормализуем элементы
    if isinstance(value, list):
        normalized = []
        for item in value:
            if isinstance(item, str) and item.strip():
                normalized.append(item.strip())
        return normalized if normalized else None
    
    return None
```

### Нормализация duration (только для v2v)

Для `wan/2-6-video-to-video` поддерживаются только "5" и "10":

```python
def _normalize_duration_for_wan_2_6_v2v(value: Any) -> Optional[str]:
    """
    Нормализует duration для wan/2-6-video-to-video.
    ВАЖНО: Для v2v поддерживаются только "5" и "10", не "15"!
    """
    # Проверяем что это валидное значение (только 5 и 10 для v2v!)
    valid_values = ["5", "10"]
    if str_value in valid_values:
        return str_value
    ...
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id == "wan/2-6-video-to-video":
    if 'duration' not in normalized_input:
        normalized_input['duration'] = "5"  # Default согласно документации
    if 'resolution' not in normalized_input:
        normalized_input['resolution'] = "1080p"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "wan/2-6-video-to-video",
  "input": {
    "prompt": "The video drinks milk tea while doing some improvised dance moves to the music.",
    "video_urls": ["https://static.aiquickdraw.com/tools/example/1765957777782_cNJpvhRx.mp4"],
    "duration": "5",
    "resolution": "1080p"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (2-5000 символов)
2. **Валидация video_urls**: Проверяется что это массив с хотя бы одним валидным URL
3. **Нормализация video_urls**: Автоматически конвертирует строку в массив
4. **Нормализация duration**: Автоматически конвертируется в строку ("5" или "10", НЕ "15"!)
5. **Нормализация resolution**: Автоматически добавляется суффикс "p" если отсутствует
6. **Дефолтные значения**: Применяются если параметры не указаны
7. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=wan/2-6-video-to-video TYPE=v2v INPUT_KEYS=['prompt', 'video_urls', 'duration', 'resolution'] 
INPUT_PREVIEW={'prompt': 'The video drinks...', 'video_urls': ['https://...'], 'duration': '5', 'resolution': '1080p'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt минимум 2 символа** (не 1!) - проверяется при валидации
2. **video_urls должен быть массивом** - автоматически конвертируется из строки если нужно
3. **duration только "5" или "10"** - "15" НЕ поддерживается для v2v!
4. **duration и resolution должны быть строками**, не числами
5. **Дефолтные значения**: duration="5", resolution="1080p"
6. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими моделями Wan 2.6

| Параметр | wan/2-6-text-to-video | wan/2-6-image-to-video | wan/2-6-video-to-video |
|----------|----------------------|------------------------|------------------------|
| `prompt` | 1-5000 символов | 2-5000 символов | 2-5000 символов |
| `image_urls` | Не требуется | Обязательный массив | Не требуется |
| `video_urls` | Не требуется | Не требуется | Обязательный массив |
| `duration` | "5"\|"10"\|"15" | "5"\|"10"\|"15" | "5"\|"10" (НЕ "15"!) |
| `resolution` | "720p"\|"1080p" | "720p"\|"1080p" | "720p"\|"1080p" |

## 🎯 Ключевые отличия

1. **duration ограничен**: Только "5" и "10" секунд (не "15"!)
2. **video_urls вместо image_urls**: Используется массив URL видео
3. **prompt минимум 2 символа**: Как и для i2v, не 1 символ

