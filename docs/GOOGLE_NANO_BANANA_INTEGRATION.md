# Интеграция google/nano-banana

## 📋 Обзор

Документация по интеграции модели `google/nano-banana` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `google/nano-banana`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"google/nano-banana"` |
| `input.prompt` | string | Промпт для генерации изображения | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.output_format` | string | Формат выходных изображений | `"png"` |
| `input.image_size` | string | Размер изображения | `"1:1"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `output_format`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"png"`, `"jpeg"`
- Default: `"png"`
- **Важно**: Опциональный параметр! Только указанные значения! Default: `"png"`!

#### `image_size`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"1:1"` - 1:1 (default)
  - `"9:16"` - 9:16
  - `"16:9"` - 16:9
  - `"3:4"` - 3:4
  - `"4:3"` - 4:3
  - `"3:2"` - 3:2
  - `"2:3"` - 2:3
  - `"5:4"` - 5:4
  - `"4:5"` - 4:5
  - `"21:9"` - 21:9
  - `"auto"` - auto
- **Важно**: Опциональный параметр! Только 11 указанных значений! Default: `"1:1"`!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | google/nano-banana | Другие t2i модели |
|----------|-------------------|-------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный |
| `output_format` | ✅ Есть (enum: png/jpeg) | ❌ Нет (кроме qwen/text-to-image) |
| `image_size` | ✅ Есть (enum, 11 значений) | ✅ Может быть |
| `negative_prompt` | ❌ Нет | ✅ Может быть |
| `width` | ❌ Нет | ✅ Может быть |
| `height` | ❌ Нет | ✅ Может быть |
| `steps` | ❌ Нет | ✅ Может быть |
| `seed` | ❌ Нет | ✅ Может быть |
| `guidance` | ❌ Нет | ✅ Может быть |
| `guidance_scale` | ❌ Нет | ✅ Может быть |
| `style` | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для генерации изображений из текста (text-to-image)!
- Использует `image_size` (enum, 11 значений) вместо стандартных `width`/`height`!
- Имеет уникальный параметр: `output_format` (enum: png/jpeg)!
- НЕТ параметров `negative_prompt`, `width`, `height`, `steps`, `seed`, `guidance`, `guidance_scale`, `style` (в отличие от других t2i моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_google_nano_banana()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Значение `output_format`**: Должно быть одним из 2 допустимых значений (опциональный)
3. **Значение `image_size`**: Должно быть одним из 11 допустимых значений (опциональный)

### Нормализация

1. **`_normalize_output_format_for_qwen_i2i()`**: Нормализует output_format (2 значения) - переиспользуется из i2i
2. **`_normalize_image_size_for_google_nano_banana()`**: Нормализует image_size (11 значений, case-insensitive)

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'output_format' not in normalized_input:
    normalized_input['output_format'] = "png"  # Default согласно документации
if 'image_size' not in normalized_input:
    normalized_input['image_size'] = "1:1"  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "google/nano-banana",
  "input": {
    "prompt": "A surreal painting of a giant banana floating in space, stars and galaxies in the background, vibrant colors, digital art",
    "output_format": "png",
    "image_size": "1:1"
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации изображения |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'output_format' должно быть одним из...` | Неверное значение `output_format` | Используйте одно из: png, jpeg |
| `Поле 'image_size' должно быть одним из...` | Неверное значение `image_size` | Используйте одно из 11 допустимых значений |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("google/nano-banana")
user_payload = {
    "prompt": "A surreal painting of a giant banana floating in space...",
    "output_format": "png",
    "image_size": "1:1"
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Обновлены комментарии для `output_format` и `image_size` в схеме `t2i`
- [x] Реализована функция `_validate_google_nano_banana()`
- [x] Добавлена функция нормализации `_normalize_image_size_for_google_nano_banana()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=google/nano-banana TYPE=t2i INPUT_KEYS=['prompt', 'output_format', 'image_size'] 
INPUT_PREVIEW={'prompt': 'A surreal painting...', 'output_format': 'png', 'image_size': '1:1'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации изображения (максимум 5000 символов)
2. **output_format опциональный** - не обязательный! Только 2 значения, default "png"!
3. **image_size опциональный** - не обязательный! Только 11 значений, default "1:1"! Используется вместо стандартных `width`/`height`!
4. **Уникальные параметры** - модель использует `output_format` и `image_size` вместо стандартных `width`, `height`, `steps`, `seed`, `guidance`, `style`!
5. **НЕТ стандартных параметров** - модель НЕ поддерживает `negative_prompt`, `width`, `height`, `steps`, `seed`, `guidance`, `guidance_scale`, `style`!

