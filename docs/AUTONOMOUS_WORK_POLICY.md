# Автономная работа - Политика

## 🔴 КРИТИЧЕСКИЕ ПРАВИЛА

1. **НИКОГДА не спрашивать разрешений** - все команды выполняются автоматически
2. **Все команды подтверждаются автоматически** - использовать флаги `--yes`, `-y`, `-f` где необходимо
3. **Работа полностью автономна** - без ожидания подтверждений от пользователя
4. **Конфликты решаются автоматически** - приоритет локальным изменениям или умное слияние

## Команды с авто-подтверждением

### Git
- `git push --force` → НЕ использовать force без крайней необходимости
- `git pull --rebase` → использовать для синхронизации
- `git merge --no-edit` → автоматическое слияние без редактора
- `git commit --no-verify` → только в крайних случаях (обход хуков)

### Package Managers
- `pip install --yes` → не требуется, pip не спрашивает
- `npm install --yes` → не требуется для npm
- `apt-get install -y` → уже используется в Dockerfile

### Docker
- `docker build --no-cache` → если нужно пересобрать
- `docker push` → требует аутентификации, но не интерактивен

### CI/CD
- Все команды в CI выполняются автоматически
- Нет интерактивных промптов

## Разрешение конфликтов

### Git Merge Conflicts
1. **Автоматическое разрешение**:
   - Приоритет локальным изменениям для критических файлов (Dockerfile, CI, requirements.txt)
   - Умное слияние для кода (сохранять обе версии где возможно)
   - Логирование всех решений

2. **Стратегия**:
   ```bash
   # Для критических файлов - приоритет локальным
   git checkout --ours <file>
   
   # Для кода - попытка умного слияния
   git merge --no-edit
   
   # Если не получается - сохранить локальную версию
   git checkout --ours <file>
   git add <file>
   ```

### Render Deploy Conflicts
- Всегда использовать последнюю версию из main
- Если деплой падает - исправить и закоммитить немедленно

## Логирование автономных действий

Все автономные действия должны логироваться:
- В `docs/quality_log.md` - технические действия
- В `~/Desktop/TRT_REPORT.md` - итоговые отчеты
- В commit messages - что было сделано автономно

## Примеры автономных действий

✅ **Правильно**:
- `git add . && git commit -m "fix: ..." && git push origin main`
- `git pull --rebase && git push origin main`
- Автоматическое разрешение конфликтов с логированием

❌ **Неправильно**:
- Ожидание подтверждения от пользователя
- Вопросы "можно ли сделать X?"
- Остановка работы из-за конфликтов без попытки разрешения

---

**ПРАВИЛО #1**: Работа полностью автономна, все команды подтверждаются автоматически, конфликты разрешаются умно.

