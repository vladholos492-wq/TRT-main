# Интеграция qwen/image-edit

## 📋 Обзор

Документация по интеграции модели `qwen/image-edit` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `qwen/image-edit`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"qwen/image-edit"` |
| `input.prompt` | string | Промпт для генерации изображения | Обязательный, максимум 2000 символов |
| `input.image_url` | string | URL изображения для редактирования | Обязательный, максимум 10MB, форматы: jpeg/png/webp |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.acceleration` | string | Уровень ускорения для генерации изображения | `"none"` |
| `input.image_size` | string | Размер сгенерированного изображения | `"landscape_4_3"` |
| `input.num_inference_steps` | number | Количество шагов вывода для выполнения | `25` |
| `input.seed` | number | Одинаковое зерно и одинаковый промпт для одной версии модели будут выдавать одно и то же изображение каждый раз | Не указан |
| `input.guidance_scale` | number | Масштаб CFG (Classifier Free Guidance) | `4` |
| `input.sync_mode` | boolean | Если установлено в true, функция будет ждать генерации и загрузки изображения перед возвратом ответа | `false` |
| `input.num_images` | string | Количество изображений для генерации | Не указан |
| `input.enable_safety_checker` | boolean | Если установлено в true, проверщик безопасности будет включен | `true` |
| `input.output_format` | string | Формат сгенерированного изображения | `"png"` |
| `input.negative_prompt` | string | Негативный промпт для генерации | `"blurry, ugly"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 2000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 2000 символов (меньше чем у qwen/image-to-image, где 5000)!

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp!

#### `acceleration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"none"`, `"regular"`, `"high"`
- Default: `"none"`
- **Важно**: Опциональный параметр! Только 3 указанных значения! Default: `"none"`! 'regular' балансирует скорость и качество!

#### `image_size`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"square"` - Square
  - `"square_hd"` - Square HD
  - `"portrait_4_3"` - Portrait 3:4
  - `"portrait_16_9"` - Portrait 9:16
  - `"landscape_4_3"` - Landscape 4:3 (default)
  - `"landscape_16_9"` - Landscape 16:9
- **Важно**: Опциональный параметр! Только 6 указанных значений! Default: `"landscape_4_3"`!

#### `num_inference_steps`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 2-49 (шаг: 1)
- Default: `25`
- **Важно**: Опциональный параметр! Только числа от 2 до 49! Default: `25`! (меньше чем у qwen/image-to-image, где 2-250 и default 30)!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- **Важно**: Опциональный параметр! Одинаковое зерно и одинаковый промпт для одной версии модели будут выдавать одно и то же изображение каждый раз!

#### `guidance_scale`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0-20 (шаг: 0.1)
- Default: `4`
- **Важно**: Опциональный параметр! Только числа от 0 до 20! Default: `4`! (больше чем у qwen/image-to-image, где default 2.5)!

#### `sync_mode`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `false`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `false`! Если установлено в true, функция будет ждать генерации и загрузки изображения перед возвратом ответа!

#### `num_images`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"1"`, `"2"`, `"3"`, `"4"`
- **Важно**: Опциональный параметр! Только 4 указанных значения! String enum (не number)!

#### `enable_safety_checker`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `true`!

#### `output_format`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"png"`, `"jpeg"`
- Default: `"png"`
- **Важно**: Опциональный параметр! Только указанные значения! Default: `"png"`! Используется `jpeg`, а не `jpg`!

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 500 символов
- Default: `"blurry, ugly"`
- **Важно**: Опциональный параметр! Максимум 500 символов! Default: `"blurry, ugly"`!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | qwen/image-edit | qwen/image-to-image |
|----------|----------------|---------------------|
| `prompt` | ✅ Обязательный (max 2000) | ✅ Обязательный (max 5000) |
| `image_url` | ✅ Обязательный | ✅ Обязательный |
| `acceleration` | ✅ Есть (enum: none/regular/high) | ✅ Есть (enum: none/regular/high) |
| `image_size` | ✅ Есть (enum, 6 значений) | ❌ Нет |
| `num_inference_steps` | ✅ Есть (2-49, default 25) | ✅ Есть (2-250, default 30) |
| `seed` | ✅ Есть | ✅ Есть |
| `guidance_scale` | ✅ Есть (0-20, default 4) | ✅ Есть (0-20, default 2.5) |
| `sync_mode` | ✅ Есть (boolean) | ❌ Нет |
| `num_images` | ✅ Есть (string enum: 1/2/3/4) | ❌ Нет |
| `enable_safety_checker` | ✅ Есть (boolean) | ✅ Есть (boolean) |
| `output_format` | ✅ Есть (enum: png/jpeg) | ✅ Есть (enum: png/jpeg) |
| `negative_prompt` | ✅ Есть (max 500, default "blurry, ugly") | ✅ Есть (max 500, default "blurry, ugly") |
| `strength` | ❌ Нет | ✅ Есть (0-1, default 0.8) |

**ВАЖНО**: 
- Это модель для редактирования изображений (image-to-image)!
- `prompt` имеет максимум 2000 символов (меньше чем у qwen/image-to-image, где 5000)!
- `num_inference_steps` имеет диапазон 2-49 и default 25 (меньше чем у qwen/image-to-image, где 2-250 и default 30)!
- `guidance_scale` имеет default 4 (больше чем у qwen/image-to-image, где default 2.5)!
- Использует `image_size` (enum, 6 значений, default "landscape_4_3") вместо стандартных `width`/`height`!
- Имеет уникальные параметры: `sync_mode` (boolean), `num_images` (string enum)!
- НЕТ параметра `strength` (есть в qwen/image-to-image)!

## 🔧 Реализация

### Валидация

Функция `_validate_qwen_image_edit()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 2000 символов
2. **Обязательность `image_url`**: Поле должно быть указано, не пустое, string
3. **Значение `acceleration`**: Должно быть одним из 3 допустимых значений (опциональный)
4. **Значение `image_size`**: Должно быть одним из 6 допустимых значений (опциональный)
5. **Значение `num_inference_steps`**: Должно быть числом от 2 до 49 (опциональный)
6. **Значение `seed`**: Должно быть числом (опциональный)
7. **Значение `guidance_scale`**: Должно быть числом от 0 до 20 (опциональный)
8. **Значение `sync_mode`**: Должно быть boolean (опциональный)
9. **Значение `num_images`**: Должно быть одним из 4 допустимых значений (опциональный)
10. **Значение `enable_safety_checker`**: Должно быть boolean (опциональный)
11. **Значение `output_format`**: Должно быть одним из 2 допустимых значений (опциональный)
12. **Длина `negative_prompt`**: Максимум 500 символов (опциональный)

### Нормализация

1. **`_normalize_acceleration_for_qwen_i2i()`**: Нормализует acceleration (3 значения) - переиспользуется из i2i
2. **`_normalize_image_size_for_qwen_image_edit()`**: Нормализует image_size (6 значений, case-insensitive)
3. **`_normalize_num_images_for_qwen_image_edit()`**: Нормализует num_images (4 значения: "1", "2", "3", "4")
4. **`_normalize_boolean()`**: Нормализует sync_mode и enable_safety_checker (boolean) - переиспользуется из других моделей

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'acceleration' not in normalized_input:
    normalized_input['acceleration'] = "none"  # Default согласно документации
if 'image_size' not in normalized_input:
    normalized_input['image_size'] = "landscape_4_3"  # Default согласно документации
if 'num_inference_steps' not in normalized_input:
    normalized_input['num_inference_steps'] = 25  # Default согласно документации
if 'guidance_scale' not in normalized_input:
    normalized_input['guidance_scale'] = 4  # Default согласно документации
if 'sync_mode' not in normalized_input:
    normalized_input['sync_mode'] = False  # Default согласно документации
if 'enable_safety_checker' not in normalized_input:
    normalized_input['enable_safety_checker'] = True  # Default согласно документации
if 'output_format' not in normalized_input:
    normalized_input['output_format'] = "png"  # Default согласно документации
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = "blurry, ugly"  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "qwen/image-edit",
  "input": {
    "prompt": "",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755603225969i6j87xnw.jpg",
    "acceleration": "none",
    "image_size": "landscape_4_3",
    "num_inference_steps": 25,
    "seed": 42,
    "guidance_scale": 4,
    "sync_mode": false,
    "enable_safety_checker": true,
    "output_format": "png",
    "negative_prompt": "blurry, ugly"
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для редактирования изображения |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 2000 символов | Сократите описание до 2000 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения для редактирования |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите непустой URL изображения |
| `Поле 'acceleration' должно быть одним из...` | Неверное значение `acceleration` | Используйте одно из: none, regular, high |
| `Поле 'image_size' должно быть одним из...` | Неверное значение `image_size` | Используйте одно из 6 допустимых значений |
| `Поле 'num_inference_steps' должно быть в диапазоне...` | Неверное значение `num_inference_steps` | Используйте число от 2 до 49 |
| `Поле 'guidance_scale' должно быть в диапазоне...` | Неверное значение `guidance_scale` | Используйте число от 0 до 20 |
| `Поле 'sync_mode' должно быть boolean...` | Неверное значение `sync_mode` | Используйте true/false |
| `Поле 'num_images' должно быть одним из...` | Неверное значение `num_images` | Используйте одно из: 1, 2, 3, 4 |
| `Поле 'enable_safety_checker' должно быть boolean...` | Неверное значение `enable_safety_checker` | Используйте true/false |
| `Поле 'output_format' должно быть одним из...` | Неверное значение `output_format` | Используйте одно из: png, jpeg |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 500 символов | Сократите до 500 символов |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("qwen/image-edit")
user_payload = {
    "prompt": "",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755603225969i6j87xnw.jpg",
    "acceleration": "none",
    "image_size": "landscape_4_3",
    "num_inference_steps": 25,
    "seed": 42,
    "guidance_scale": 4,
    "sync_mode": False,
    "enable_safety_checker": True,
    "output_format": "png",
    "negative_prompt": "blurry, ugly"
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Обновлены комментарии для `image_size`, `num_images`, `output_format`, `acceleration`, `num_inference_steps`, `guidance_scale`, `enable_safety_checker`, `sync_mode` в схеме `i2i`
- [x] Реализована функция `_validate_qwen_image_edit()`
- [x] Добавлены функции нормализации `_normalize_image_size_for_qwen_image_edit()` и `_normalize_num_images_for_qwen_image_edit()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=qwen/image-edit TYPE=i2i INPUT_KEYS=['prompt', 'image_url', 'acceleration', 'image_size', 'num_inference_steps', 'seed', 'guidance_scale', 'sync_mode', 'num_images', 'enable_safety_checker', 'output_format', 'negative_prompt'] 
INPUT_PREVIEW={'prompt': '', 'image_url': 'https://...', 'acceleration': 'none', 'image_size': 'landscape_4_3', 'num_inference_steps': 25, 'seed': 42, 'guidance_scale': 4, 'sync_mode': False, 'enable_safety_checker': True, 'output_format': 'png', 'negative_prompt': 'blurry, ugly'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для редактирования изображения (максимум 2000 символов, меньше чем у qwen/image-to-image, где 5000)!
2. **image_url обязательный** - обязателен для редактирования изображения! Максимум 10MB! Форматы: jpeg/png/webp!
3. **acceleration опциональный** - не обязательный! Только 3 значения, default "none"!
4. **image_size опциональный** - не обязательный! Только 6 значений, default "landscape_4_3"! Используется вместо стандартных `width`/`height`!
5. **num_inference_steps опциональный** - не обязательный! Только числа от 2 до 49, default 25! (меньше чем у qwen/image-to-image, где 2-250 и default 30)!
6. **guidance_scale опциональный** - не обязательный! Только числа от 0 до 20, default 4! (больше чем у qwen/image-to-image, где default 2.5)!
7. **sync_mode опциональный** - не обязательный! Boolean (true/false), default false! Если установлено в true, функция будет ждать генерации и загрузки изображения перед возвратом ответа!
8. **num_images опциональный** - не обязательный! Только 4 значения (string enum: "1", "2", "3", "4")!
9. **enable_safety_checker опциональный** - не обязательный! Boolean (true/false), default true!
10. **output_format опциональный** - не обязательный! Только 2 значения (png, jpeg), default "png"! Используется `jpeg`, а не `jpg`!
11. **negative_prompt опциональный** - не обязательный! Максимум 500 символов, default "blurry, ugly"!
12. **НЕТ параметра strength** - модель НЕ поддерживает `strength` (есть в qwen/image-to-image)!

