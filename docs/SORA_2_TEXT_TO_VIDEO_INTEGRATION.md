# Интеграция sora-2-text-to-video

## 📋 Обзор

Документация по интеграции модели `sora-2-text-to-video` согласно официальной документации KIE AI API.

**ВАЖНО**: Отличается от `sora-2-pro-text-to-video`:
- **НЕТ параметра `size`** (в pro версии есть параметр `size` с значениями "standard" | "high")
- Только: `prompt`, `aspect_ratio`, `n_frames`, `remove_watermark`

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `sora-2-text-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"sora-2-text-to-video"` |
| `input.prompt` | string | Текстовое описание движения видео | Обязательный, максимум 10000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.aspect_ratio` | string | Соотношение сторон видео | `"landscape"` |
| `input.n_frames` | string | Количество кадров для генерации | `"10"` |
| `input.remove_watermark` | boolean | Удаление водяных знаков из сгенерированного видео | `true` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 10000 символов
- **Важно**: Не может быть пустым! Максимум 10000 символов!

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"portrait"` - Портретная ориентация
  - `"landscape"` - Альбомная ориентация (default)
- **Важно**: Опциональный параметр! Только 2 значения!

#### `n_frames`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"10"` - 10 секунд (default)
  - `"15"` - 15 секунд
- **Важно**: Опциональный параметр! Только 2 значения!

#### `remove_watermark`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Значения:
  - `true` - Удалять водяные знаки (default)
  - `false` - Не удалять водяные знаки
- **Важно**: Опциональный параметр! Boolean значение!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Отличия от Pro версии

| Параметр | sora-2-text-to-video | sora-2-pro-text-to-video |
|----------|----------------------|--------------------------|
| `prompt` | ✅ Обязательный | ✅ Обязательный |
| `aspect_ratio` | ✅ Опциональный | ✅ Опциональный |
| `n_frames` | ✅ Опциональный | ✅ Опциональный |
| `size` | ❌ **НЕТ** | ✅ Опциональный ("standard" \| "high") |
| `remove_watermark` | ✅ Опциональный | ✅ Опциональный |

**ВАЖНО**: Если параметр `size` передан для `sora-2-text-to-video`, он будет автоматически удалён с предупреждением в логах!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_sora_2_text_to_video(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для sora-2-text-to-video согласно документации API.
    
    ВАЖНО: Отличается от sora-2-pro-text-to-video:
    - НЕТ параметра size (в pro версии есть)
    - Только: prompt, aspect_ratio, n_frames, remove_watermark
    """
    # Проверяем оба возможных ID модели
    if model_id not in ["sora-2-text-to-video", "sora-2/t2v", "openai/sora-2-text-to-video"]:
        return True, None
    
    # Валидация prompt: обязательный, максимум 10000 символов
    # Валидация aspect_ratio: опциональный, enum ("portrait" или "landscape")
    # Валидация n_frames: опциональный, enum ("10" или "15")
    # Валидация remove_watermark: опциональный boolean
    
    # ВАЖНО: Удаляем параметр size если он был передан
    if 'size' in normalized_input:
        logger.warning(f"Parameter 'size' is not supported for sora-2-text-to-video (only for pro version), removing it")
        del normalized_input['size']
```

### Дефолтные значения

```python
# Применяем дефолты для sora-2-text-to-video
if model_id in ["sora-2-text-to-video", "sora-2/t2v", "openai/sora-2-text-to-video"]:
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "landscape"  # Default согласно документации
    if 'n_frames' not in normalized_input:
        normalized_input['n_frames'] = "10"  # Default согласно документации
    if 'remove_watermark' not in normalized_input:
        normalized_input['remove_watermark'] = True  # Default согласно документации
    # ВАЖНО: НЕТ параметра size в sora-2-text-to-video (только в pro версии!)
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "sora-2-text-to-video",
  "input": {
    "prompt": "A professor stands at the front of a lively classroom, enthusiastically giving a lecture.",
    "aspect_ratio": "landscape",
    "n_frames": "10",
    "remove_watermark": true
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Укажите текстовое описание видео |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 10000 символов | Сократите описание до 10000 символов |
| `Поле 'aspect_ratio' должно быть одним из...` | Неверное значение `aspect_ratio` | Используйте "portrait" или "landscape" |
| `Поле 'n_frames' должно быть одним из...` | Неверное значение `n_frames` | Используйте "10" или "15" |
| `Поле 'remove_watermark' должно быть boolean...` | Неверное значение `remove_watermark` | Используйте true или false |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("sora-2-text-to-video")
user_payload = {
    "prompt": "A professor stands at the front of a lively classroom...",
    "aspect_ratio": "landscape",
    "n_frames": "10",
    "remove_watermark": True
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлена схема `t2v` с необходимыми полями
- [x] Реализована функция `_validate_sora_2_text_to_video()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Реализовано автоматическое удаление параметра `size` (если передан)
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=sora-2-text-to-video TYPE=t2v INPUT_KEYS=['prompt', 'aspect_ratio', 'n_frames', 'remove_watermark'] 
INPUT_PREVIEW={'prompt': 'A professor...', 'aspect_ratio': 'landscape', 'n_frames': '10', 'remove_watermark': True}
```

Если передан параметр `size`, будет предупреждение:
```
Parameter 'size' is not supported for sora-2-text-to-video (only for pro version), removing it
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео
2. **prompt максимум 10000 символов** - проверяется при валидации
3. **aspect_ratio опциональный** - не обязательный! Только 2 значения: "portrait" или "landscape"!
4. **n_frames опциональный** - не обязательный! Только 2 значения: "10" или "15"!
5. **remove_watermark опциональный** - не обязательный! Boolean значение!
6. **НЕТ параметра size** - в отличие от pro версии, параметр `size` не поддерживается и будет удалён если передан!

