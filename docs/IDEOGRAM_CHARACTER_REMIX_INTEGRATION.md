# Интеграция ideogram/character-remix

## 📋 Обзор

Документация по интеграции модели `ideogram/character-remix` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `ideogram/character-remix`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"ideogram/character-remix"` |
| `input.prompt` | string | Промпт для ремикса изображения | Обязательный, максимум 5000 символов |
| `input.image_url` | string | URL изображения для ремикса | Обязательный, максимум 10MB, форматы: jpeg/png/webp |
| `input.reference_image_urls` | array | Набор изображений для использования в качестве референсов персонажа | Обязательный, массив, максимум 10MB общий размер, форматы: jpeg/png/webp, только 1 изображение поддерживается |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.rendering_speed` | string | Скорость рендеринга | `"BALANCED"` |
| `input.style` | string | Тип стиля для генерации | `"AUTO"` |
| `input.expand_prompt` | boolean | Определяет, должен ли использоваться MagicPrompt | `true` |
| `input.image_size` | string | Размер сгенерированного изображения | `"square_hd"` |
| `input.num_images` | string | Количество изображений для генерации | `"1"` |
| `input.seed` | number | Зерно для генератора случайных чисел | Не указан |
| `input.strength` | number | Сила входного изображения в ремиксе | `0.8` |
| `input.negative_prompt` | string | Описание того, что исключить из изображения | `""` (пустая строка) |
| `input.image_urls` | array | Набор изображений для использования в качестве референсов стиля | `[]` (пустой массив) |
| `input.reference_mask_urls` | string | Набор масок для применения к референсам персонажа | `""` (пустая строка) |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp!

#### `reference_image_urls`
- Тип: array
- Обязательный: да
- Максимальный размер файла: 10MB общий размер
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- Количество: только 1 изображение поддерживается, остальные игнорируются
- **Важно**: Обязательный параметр! Должен быть массивом URL изображений! Минимум 1 изображение! Максимум 10MB общий размер! Форматы: jpeg/png/webp! Только 1 изображение поддерживается, остальные игнорируются!

#### `rendering_speed`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"TURBO"`, `"BALANCED"`, `"QUALITY"`
- Default: `"BALANCED"`
- **Важно**: Опциональный параметр! Только 3 указанных значения! Default: `"BALANCED"`!

#### `style`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"AUTO"`, `"REALISTIC"`, `"FICTION"`
- Default: `"AUTO"`
- **Важно**: Опциональный параметр! Только 3 указанных значения! Default: `"AUTO"`! Не может использоваться с style_codes!

#### `expand_prompt`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `true`! Определяет, должен ли использоваться MagicPrompt в генерации запроса!

#### `image_size`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"square"` - Square
  - `"square_hd"` - Square HD (default)
  - `"portrait_4_3"` - Portrait 3:4
  - `"portrait_16_9"` - Portrait 9:16
  - `"landscape_4_3"` - Landscape 4:3
  - `"landscape_16_9"` - Landscape 16:9
- **Важно**: Опциональный параметр! Только 6 указанных значений! Default: `"square_hd"`!

#### `num_images`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"1"`, `"2"`, `"3"`, `"4"`
- Default: `"1"`
- **Важно**: Опциональный параметр! Только 4 указанных значения! Default: `"1"`! String enum (не number)!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- **Важно**: Опциональный параметр! Зерно для генератора случайных чисел!

#### `strength`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0.1-1 (шаг: 0.1)
- Default: `0.8`
- **Важно**: Опциональный параметр! Только числа от 0.1 до 1! Default: `0.8`! Сила входного изображения в ремиксе!

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 500 символов
- Default: `""` (пустая строка)
- **Важно**: Опциональный параметр! Максимум 500 символов! Default: пустая строка `""`! Описания в prompt имеют приоритет над описаниями в negative_prompt!

#### `image_urls`
- Тип: array
- Обязательный: нет (опциональный!)
- Максимальный размер файла: 10MB общий размер
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- Default: `[]` (пустой массив)
- **Важно**: Опциональный параметр! Должен быть массивом URL изображений! Максимум 10MB общий размер! Форматы: jpeg/png/webp! Default: пустой массив `[]`! Набор изображений для использования в качестве референсов стиля!

#### `reference_mask_urls`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- Default: `""` (пустая строка)
- **Важно**: Опциональный параметр! String (не массив!)! Максимум 10MB! Форматы: jpeg/png/webp! Default: пустая строка `""`! Набор масок для применения к референсам персонажа (только 1 маска поддерживается, остальные игнорируются)!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | ideogram/character-remix | ideogram/character-edit |
|----------|------------------------|------------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный (max 5000) |
| `image_url` | ✅ Обязательный | ✅ Обязательный |
| `reference_image_urls` | ✅ Обязательный (только 1 поддерживается) | ✅ Обязательный (только 1 поддерживается) |
| `mask_url` | ❌ Нет | ✅ Обязательный |
| `rendering_speed` | ✅ Есть (enum: TURBO/BALANCED/QUALITY) | ✅ Есть (enum: TURBO/BALANCED/QUALITY) |
| `style` | ✅ Есть (enum: AUTO/REALISTIC/FICTION) | ✅ Есть (enum: AUTO/REALISTIC/FICTION) |
| `expand_prompt` | ✅ Есть (boolean) | ✅ Есть (boolean) |
| `image_size` | ✅ Есть (enum, 6 значений) | ❌ Нет |
| `num_images` | ✅ Есть (string enum: 1/2/3/4) | ✅ Есть (string enum: 1/2/3/4) |
| `seed` | ✅ Есть | ✅ Есть |
| `strength` | ✅ Есть (0.1-1, default 0.8) | ❌ Нет |
| `negative_prompt` | ✅ Есть (max 500, default "") | ❌ Нет |
| `image_urls` | ✅ Есть (массив, default []) | ❌ Нет |
| `reference_mask_urls` | ✅ Есть (string, default "") | ❌ Нет |

**ВАЖНО**: 
- Это модель для ремикса персонажей (image-to-image с character reference)!
- Использует `reference_image_urls` для указания референсов персонажа (только 1 изображение поддерживается, остальные игнорируются)!
- Имеет уникальные параметры: `strength`, `negative_prompt`, `image_urls`, `reference_mask_urls`!
- НЕТ параметра `mask_url` (есть в ideogram/character-edit)!
- Использует `image_size` (enum, 6 значений, default "square_hd") вместо стандартных `width`/`height`!

## 🔧 Реализация

### Валидация

Функция `_validate_ideogram_character_remix()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Обязательность `image_url`**: Поле должно быть указано, не пустое, string
3. **Обязательность `reference_image_urls`**: Поле должно быть массивом URL изображений, минимум 1 изображение (только 1 поддерживается, остальные игнорируются)
4. **Значение `rendering_speed`**: Должно быть одним из 3 допустимых значений (опциональный)
5. **Значение `style`**: Должно быть одним из 3 допустимых значений (опциональный)
6. **Значение `expand_prompt`**: Должно быть boolean (опциональный)
7. **Значение `image_size`**: Должно быть одним из 6 допустимых значений (опциональный)
8. **Значение `num_images`**: Должно быть одним из 4 допустимых значений (опциональный)
9. **Значение `seed`**: Должно быть числом (опциональный)
10. **Значение `strength`**: Должно быть числом от 0.1 до 1 (опциональный)
11. **Длина `negative_prompt`**: Максимум 500 символов (опциональный)
12. **Значение `image_urls`**: Должно быть массивом URL изображений (опциональный)
13. **Значение `reference_mask_urls`**: Должно быть string (опциональный)

### Нормализация

1. **`_normalize_rendering_speed_for_ideogram_character_edit()`**: Нормализует rendering_speed (3 значения) - переиспользуется из character-edit
2. **`_normalize_style_for_ideogram_character_edit()`**: Нормализует style (3 значения) - переиспользуется из character-edit
3. **`_normalize_image_size_for_qwen_image_edit()`**: Нормализует image_size (6 значений) - переиспользуется из qwen/image-edit
4. **`_normalize_num_images_for_qwen_image_edit()`**: Нормализует num_images (4 значения) - переиспользуется из qwen/image-edit
5. **`_normalize_boolean()`**: Нормализует expand_prompt (boolean) - переиспользуется из других моделей

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'rendering_speed' not in normalized_input:
    normalized_input['rendering_speed'] = "BALANCED"  # Default согласно документации
if 'style' not in normalized_input:
    normalized_input['style'] = "AUTO"  # Default согласно документации
if 'expand_prompt' not in normalized_input:
    normalized_input['expand_prompt'] = True  # Default согласно документации
if 'image_size' not in normalized_input:
    normalized_input['image_size'] = "square_hd"  # Default согласно документации
if 'num_images' not in normalized_input:
    normalized_input['num_images'] = "1"  # Default согласно документации
if 'strength' not in normalized_input:
    normalized_input['strength'] = 0.8  # Default согласно документации
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = ""  # Default согласно документации (пустая строка!)
if 'image_urls' not in normalized_input:
    normalized_input['image_urls'] = []  # Default согласно документации
if 'reference_mask_urls' not in normalized_input:
    normalized_input['reference_mask_urls'] = ""  # Default согласно документации (пустая строка!)
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "ideogram/character-remix",
  "input": {
    "prompt": "A fisheye lens selfie photograph taken at night on an urban street...",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755768466167d0tiuc6e.webp",
    "reference_image_urls": ["https://file.aiquickdraw.com/custom-page/akr/section-images/1755768479029sugx0g6f.webp"],
    "rendering_speed": "BALANCED",
    "style": "AUTO",
    "expand_prompt": true,
    "image_size": "square_hd",
    "num_images": "1",
    "seed": 42,
    "strength": 0.8,
    "negative_prompt": "",
    "image_urls": [],
    "reference_mask_urls": ""
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для ремикса персонажа |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения для ремикса |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите непустой URL изображения |
| `Поле 'reference_image_urls' обязательно...` | `reference_image_urls` не указан | Укажите массив URL изображений-референсов |
| `Поле 'reference_image_urls' должно быть массивом...` | `reference_image_urls` не массив | Укажите массив URL изображений |
| `Поле 'reference_image_urls' не может быть пустым...` | `reference_image_urls` пустой массив | Укажите хотя бы одно изображение-референс |
| `Элемент X в 'reference_image_urls' должен быть строкой...` | Неверный тип элемента | Укажите строку (URL) для каждого элемента |
| `Поле 'rendering_speed' должно быть одним из...` | Неверное значение `rendering_speed` | Используйте одно из: TURBO, BALANCED, QUALITY |
| `Поле 'style' должно быть одним из...` | Неверное значение `style` | Используйте одно из: AUTO, REALISTIC, FICTION |
| `Поле 'expand_prompt' должно быть boolean...` | Неверное значение `expand_prompt` | Используйте true/false |
| `Поле 'image_size' должно быть одним из...` | Неверное значение `image_size` | Используйте одно из 6 допустимых значений |
| `Поле 'num_images' должно быть одним из...` | Неверное значение `num_images` | Используйте одно из: 1, 2, 3, 4 |
| `Поле 'strength' должно быть в диапазоне...` | Неверное значение `strength` | Используйте число от 0.1 до 1 |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 500 символов | Сократите до 500 символов |
| `Поле 'image_urls' должно быть массивом...` | `image_urls` не массив | Укажите массив URL изображений |
| `Элемент X в 'image_urls' должен быть строкой...` | Неверный тип элемента | Укажите строку (URL) для каждого элемента |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("ideogram/character-remix")
user_payload = {
    "prompt": "A fisheye lens selfie photograph...",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755768466167d0tiuc6e.webp",
    "reference_image_urls": ["https://file.aiquickdraw.com/custom-page/akr/section-images/1755768479029sugx0g6f.webp"],
    "rendering_speed": "BALANCED",
    "style": "AUTO",
    "expand_prompt": True,
    "image_size": "square_hd",
    "num_images": "1",
    "seed": 42,
    "strength": 0.8,
    "negative_prompt": "",
    "image_urls": [],
    "reference_mask_urls": ""
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлен параметр `reference_mask_urls` в схему `i2i`
- [x] Обновлены комментарии для `reference_image_urls`, `rendering_speed`, `num_images`, `image_size`, `expand_prompt` в схеме `i2i`
- [x] Реализована функция `_validate_ideogram_character_remix()`
- [x] Переиспользованы функции нормализации из других моделей
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=ideogram/character-remix TYPE=i2i INPUT_KEYS=['prompt', 'image_url', 'reference_image_urls', 'rendering_speed', 'style', 'expand_prompt', 'image_size', 'num_images', 'seed', 'strength', 'negative_prompt', 'image_urls', 'reference_mask_urls'] 
INPUT_PREVIEW={'prompt': 'A fisheye lens...', 'image_url': 'https://...', 'reference_image_urls': ['https://...'], 'rendering_speed': 'BALANCED', 'style': 'AUTO', 'expand_prompt': True, 'image_size': 'square_hd', 'num_images': '1', 'seed': 42, 'strength': 0.8, 'negative_prompt': '', 'image_urls': [], 'reference_mask_urls': ''}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для ремикса персонажа (максимум 5000 символов)
2. **image_url обязательный** - обязателен для ремикса персонажа! Максимум 10MB! Форматы: jpeg/png/webp!
3. **reference_image_urls обязательный** - обязателен для ремикса персонажа! Должен быть массивом URL изображений! Минимум 1 изображение! Максимум 10MB общий размер! Форматы: jpeg/png/webp! Только 1 изображение поддерживается, остальные игнорируются!
4. **rendering_speed опциональный** - не обязательный! Только 3 значения, default "BALANCED"!
5. **style опциональный** - не обязательный! Только 3 значения, default "AUTO"! Не может использоваться с style_codes!
6. **expand_prompt опциональный** - не обязательный! Boolean (true/false), default true! Определяет, должен ли использоваться MagicPrompt в генерации запроса!
7. **image_size опциональный** - не обязательный! Только 6 значений, default "square_hd"! Используется вместо стандартных `width`/`height`!
8. **num_images опциональный** - не обязательный! Только 4 значения (string enum: "1", "2", "3", "4"), default "1"!
9. **seed опциональный** - не обязательный! Зерно для генератора случайных чисел!
10. **strength опциональный** - не обязательный! Только числа от 0.1 до 1, default 0.8! Сила входного изображения в ремиксе!
11. **negative_prompt опциональный** - не обязательный! Максимум 500 символов, default пустая строка `""`! Описания в prompt имеют приоритет над описаниями в negative_prompt!
12. **image_urls опциональный** - не обязательный! Должен быть массивом URL изображений! Максимум 10MB общий размер! Форматы: jpeg/png/webp! Default: пустой массив `[]`! Набор изображений для использования в качестве референсов стиля!
13. **reference_mask_urls опциональный** - не обязательный! String (не массив!)! Максимум 10MB! Форматы: jpeg/png/webp! Default: пустая строка `""`! Набор масок для применения к референсам персонажа (только 1 маска поддерживается, остальные игнорируются)!
14. **Уникальные параметры** - модель использует `strength`, `negative_prompt`, `image_urls`, `reference_mask_urls` вместо стандартных `width`, `height`, `steps`, `guidance`!
15. **НЕТ параметра mask_url** - модель НЕ поддерживает `mask_url` (есть в ideogram/character-edit)!

