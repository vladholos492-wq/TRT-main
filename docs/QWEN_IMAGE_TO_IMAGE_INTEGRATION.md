# Интеграция qwen/image-to-image

## 📋 Обзор

Документация по интеграции модели `qwen/image-to-image` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `qwen/image-to-image`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"qwen/image-to-image"` |
| `input.prompt` | string | Промпт для генерации изображения | Обязательный, максимум 5000 символов |
| `input.image_url` | string | Референсное изображение для направления генерации | Обязательный, макс 10MB, jpeg/png/webp |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.strength` | number | Сила шумоподавления. 1.0 = полностью переделать; 0.0 = сохранить оригинал | `0.8` |
| `input.output_format` | string | Формат сгенерированного изображения | `"png"` |
| `input.acceleration` | string | Уровень ускорения для генерации изображения. 'regular' балансирует скорость и качество. 'high' рекомендуется для изображений без текста | `"none"` |
| `input.negative_prompt` | string | Негативный промпт для генерации | `"blurry, ugly"` |
| `input.seed` | number | Одинаковое зерно и одинаковый промпт для одной версии модели будут выдавать одно и то же изображение каждый раз | Не указан |
| `input.num_inference_steps` | number | Количество шагов вывода для выполнения | `30` |
| `input.guidance_scale` | number | Масштаб CFG (Classifier Free Guidance) - мера того, насколько близко модель должна следовать вашему промпту | `2.5` |
| `input.enable_safety_checker` | boolean | Проверщик безопасности всегда включен в Playground. Может быть отключен только установкой false через API | `true` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Должен быть валидным URL изображения!

#### `strength`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0-1 (шаг: 0.01)
- Default: `0.8`
- **Важно**: Опциональный параметр! Только числа от 0 до 1! Default: `0.8`! 1.0 = полностью переделать; 0.0 = сохранить оригинал!

#### `output_format`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"png"`, `"jpeg"`
- Default: `"png"`
- **Важно**: Опциональный параметр! Только указанные значения! Default: `"png"`!

#### `acceleration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"none"` - Нет (default)
  - `"regular"` - Обычное (балансирует скорость и качество)
  - `"high"` - Высокое (рекомендуется для изображений без текста)
- Default: `"none"`
- **Важно**: Опциональный параметр! Только 3 указанных значения! Default: `"none"`! Более высокое ускорение увеличивает скорость!

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 500 символов
- Default: `"blurry, ugly"`
- **Важно**: Опциональный параметр! Максимум 500 символов! Default: `"blurry, ugly"`!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- **Важно**: Опциональный параметр! Одинаковое зерно и одинаковый промпт для одной версии модели будут выдавать одно и то же изображение каждый раз!

#### `num_inference_steps`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 2-250 (шаг: 1)
- Default: `30`
- **Важно**: Опциональный параметр! Только числа от 2 до 250! Default: `30`!

#### `guidance_scale`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0-20 (шаг: 0.1)
- Default: `2.5`
- **Важно**: Опциональный параметр! Только числа от 0 до 20! Default: `2.5`! Мера того, насколько близко модель должна следовать вашему промпту!

#### `enable_safety_checker`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `true`! Проверщик безопасности всегда включен в Playground. Может быть отключен только установкой false через API!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | qwen/image-to-image | Другие i2i модели |
|----------|---------------------|------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный |
| `image_url` | ✅ Обязательный (макс 10MB) | ✅ Обязательный |
| `strength` | ✅ Есть (0-1) | ✅ Может быть |
| `output_format` | ✅ Есть (enum: png/jpeg) | ❌ Нет |
| `acceleration` | ✅ Есть (enum: none/regular/high) | ❌ Нет |
| `negative_prompt` | ✅ Есть (max 500) | ✅ Может быть |
| `seed` | ✅ Есть | ✅ Может быть |
| `num_inference_steps` | ✅ Есть (2-250) | ❌ Нет (используют `steps`) |
| `guidance_scale` | ✅ Есть (0-20) | ❌ Нет (используют `guidance`) |
| `enable_safety_checker` | ✅ Есть (boolean) | ❌ Нет |
| `width` | ❌ Нет | ✅ Может быть |
| `height` | ❌ Нет | ✅ Может быть |
| `steps` | ❌ Нет | ✅ Может быть |
| `guidance` | ❌ Нет | ✅ Может быть |
| `style` | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для генерации изображений из изображений (image-to-image)!
- Использует `num_inference_steps` (2-250) вместо стандартного `steps`!
- Использует `guidance_scale` (0-20) вместо стандартного `guidance`!
- Имеет уникальные параметры: `output_format`, `acceleration`, `enable_safety_checker`!
- НЕТ параметров `width`, `height`, `steps`, `guidance`, `style` (в отличие от других i2i моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_qwen_image_to_image()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Обязательность `image_url`**: Поле должно быть указано, не пустое (макс 10MB, jpeg/png/webp)
3. **Значение `strength`**: Должно быть числом от 0 до 1 (опциональный)
4. **Значение `output_format`**: Должно быть одним из 2 допустимых значений (опциональный)
5. **Значение `acceleration`**: Должно быть одним из 3 допустимых значений (опциональный)
6. **Длина `negative_prompt`**: Максимум 500 символов (опциональный)
7. **Значение `seed`**: Должно быть числом (опциональный)
8. **Значение `num_inference_steps`**: Должно быть числом от 2 до 250 (опциональный)
9. **Значение `guidance_scale`**: Должно быть числом от 0 до 20 (опциональный)
10. **Значение `enable_safety_checker`**: Должно быть boolean (опциональный)

### Нормализация

1. **`_normalize_output_format_for_qwen_i2i()`**: Нормализует output_format (2 значения, case-insensitive)
2. **`_normalize_acceleration_for_qwen_i2i()`**: Нормализует acceleration (3 значения, case-insensitive)
3. **`_normalize_boolean()`**: Нормализует enable_safety_checker (boolean) - переиспользуется из других моделей

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'strength' not in normalized_input:
    normalized_input['strength'] = 0.8  # Default согласно документации
if 'output_format' not in normalized_input:
    normalized_input['output_format'] = "png"  # Default согласно документации
if 'acceleration' not in normalized_input:
    normalized_input['acceleration'] = "none"  # Default согласно документации
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = "blurry, ugly"  # Default согласно документации
if 'num_inference_steps' not in normalized_input:
    normalized_input['num_inference_steps'] = 30  # Default согласно документации
if 'guidance_scale' not in normalized_input:
    normalized_input['guidance_scale'] = 2.5  # Default согласно документации
if 'enable_safety_checker' not in normalized_input:
    normalized_input['enable_safety_checker'] = True  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "qwen/image-to-image",
  "input": {
    "prompt": "Enter your prompt here...",
    "image_url": "",
    "strength": 0.8,
    "output_format": "png",
    "acceleration": "none",
    "negative_prompt": "blurry, ugly",
    "seed": 42,
    "num_inference_steps": 30,
    "guidance_scale": 2.5,
    "enable_safety_checker": true
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации изображения |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите валидный URL изображения |
| `Поле 'strength' должно быть в диапазоне...` | Неверное значение `strength` | Используйте число от 0 до 1 |
| `Поле 'output_format' должно быть одним из...` | Неверное значение `output_format` | Используйте одно из: png, jpeg |
| `Поле 'acceleration' должно быть одним из...` | Неверное значение `acceleration` | Используйте одно из: none, regular, high |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 500 символов | Сократите до 500 символов |
| `Поле 'num_inference_steps' должно быть в диапазоне...` | Неверное значение `num_inference_steps` | Используйте число от 2 до 250 |
| `Поле 'guidance_scale' должно быть в диапазоне...` | Неверное значение `guidance_scale` | Используйте число от 0 до 20 |
| `Поле 'enable_safety_checker' должно быть boolean...` | Неверное значение `enable_safety_checker` | Используйте true/false |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("qwen/image-to-image")
user_payload = {
    "prompt": "Enter your prompt here...",
    "image_url": "https://example.com/image.png",
    "strength": 0.8,
    "output_format": "png",
    "acceleration": "none",
    "negative_prompt": "blurry, ugly",
    "seed": 42,
    "num_inference_steps": 30,
    "guidance_scale": 2.5,
    "enable_safety_checker": True
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлены параметры `output_format`, `acceleration`, `num_inference_steps`, `guidance_scale`, `enable_safety_checker` в схему `i2i`
- [x] Реализована функция `_validate_qwen_image_to_image()`
- [x] Добавлены функции нормализации `_normalize_output_format_for_qwen_i2i()` и `_normalize_acceleration_for_qwen_i2i()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=qwen/image-to-image TYPE=i2i INPUT_KEYS=['prompt', 'image_url', 'strength', 'output_format', 'acceleration', 'negative_prompt', 'seed', 'num_inference_steps', 'guidance_scale', 'enable_safety_checker'] 
INPUT_PREVIEW={'prompt': 'Enter your prompt here...', 'image_url': 'https://...', 'strength': 0.8, 'output_format': 'png', 'acceleration': 'none', 'negative_prompt': 'blurry, ugly', 'seed': 42, 'num_inference_steps': 30, 'guidance_scale': 2.5, 'enable_safety_checker': True}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **ДВА обязательных параметра** - `prompt` и `image_url` обязательны для генерации изображения!
2. **strength опциональный** - не обязательный! Только числа от 0 до 1, default 0.8! 1.0 = полностью переделать; 0.0 = сохранить оригинал!
3. **output_format опциональный** - не обязательный! Только 2 значения, default "png"!
4. **acceleration опциональный** - не обязательный! Только 3 значения, default "none"! 'regular' балансирует скорость и качество. 'high' рекомендуется для изображений без текста!
5. **negative_prompt опциональный** - не обязательный! Максимум 500 символов, default "blurry, ugly"!
6. **num_inference_steps опциональный** - не обязательный! Только числа от 2 до 250, default 30! Используется вместо стандартного `steps`!
7. **guidance_scale опциональный** - не обязательный! Только числа от 0 до 20, default 2.5! Используется вместо стандартного `guidance`!
8. **enable_safety_checker опциональный** - не обязательный! Boolean (true/false), default true! Проверщик безопасности всегда включен в Playground. Может быть отключен только установкой false через API!
9. **Уникальные параметры** - модель использует `output_format`, `acceleration`, `num_inference_steps`, `guidance_scale`, `enable_safety_checker` вместо стандартных `width`, `height`, `steps`, `guidance`, `style`!

