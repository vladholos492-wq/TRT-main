# Интеграция bytedance/v1-pro-fast-image-to-video

## 📋 Обзор

Документация по интеграции модели `bytedance/v1-pro-fast-image-to-video` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `bytedance/v1-pro-fast-image-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"bytedance/v1-pro-fast-image-to-video"` |
| `input.prompt` | string | Текстовый промпт для генерации видео | Обязательный, максимум 10000 символов |
| `input.image_url` | string | URL изображения для генерации видео | Обязательный, максимум 10MB, форматы: jpeg/png/webp |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.resolution` | string | Разрешение видео - 720p для баланса, 1080p для более высокого качества | `"720p"` |
| `input.duration` | string | Длительность видео в секундах | `"5"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 10000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10000 символов!

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp!

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"720p"`, `"1080p"`
- Default: `"720p"`
- **Важно**: Опциональный параметр! Только 2 указанных значения (НЕТ 480p!)! Default: `"720p"`! 720p для баланса, 1080p для более высокого качества!

#### `duration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"5"`, `"10"`
- Default: `"5"`
- **Важно**: Опциональный параметр! Только 2 указанных значения (string enum)! Default: `"5"`! Длительность видео в секундах!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | bytedance/v1-pro-fast-image-to-video | bytedance/v1-pro-image-to-video | bytedance/v1-lite-image-to-video |
|----------|--------------------------------------|----------------------------------|----------------------------------|
| `prompt` | ✅ Обязательный (max 10000) | ✅ Обязательный (max 10000) | ✅ Обязательный (max 10000) |
| `image_url` | ✅ Обязательный (max 10MB) | ✅ Обязательный (max 10MB) | ✅ Обязательный (max 10MB) |
| `resolution` | ✅ Есть (enum: 720p/1080p, НЕТ 480p!) | ✅ Есть (enum: 480p/720p/1080p) | ✅ Есть (enum: 480p/720p/1080p) |
| `duration` | ✅ Есть (enum: "5"/"10") | ✅ Есть (enum: "5"/"10") | ✅ Есть (enum: "5"/"10") |
| `camera_fixed` | ❌ Нет | ✅ Есть (boolean) | ✅ Есть (boolean) |
| `seed` | ❌ Нет | ✅ Есть (может быть -1, default -1) | ✅ Есть (может быть -1, default -1) |
| `enable_safety_checker` | ❌ Нет | ✅ Есть (boolean) | ✅ Есть (boolean) |
| `end_image_url` | ❌ Нет | ❌ Нет | ✅ Есть (string, default "") |
| `fps` | ❌ Нет | ❌ Нет | ❌ Нет |
| `with_audio` | ❌ Нет | ❌ Нет | ❌ Нет |
| `width` | ❌ Нет | ❌ Нет | ❌ Нет |
| `height` | ❌ Нет | ❌ Нет | ❌ Нет |
| `guidance` | ❌ Нет | ❌ Нет | ❌ Нет |
| `steps` | ❌ Нет | ❌ Нет | ❌ Нет |
| `negative_prompt` | ❌ Нет | ❌ Нет | ❌ Нет |
| `motion` | ❌ Нет | ❌ Нет | ❌ Нет |
| `style` | ❌ Нет | ❌ Нет | ❌ Нет |
| `strength` | ❌ Нет | ❌ Нет | ❌ Нет |

**ВАЖНО**: 
- Это модель для генерации видео из изображения (image-to-video) - Pro Fast версия!
- Использует `duration` как string enum ("5" или "10")!
- `resolution` только 2 значения (720p, 1080p), НЕТ 480p (в отличие от других v1 моделей)!
- НЕТ параметров `camera_fixed`, `seed`, `enable_safety_checker`, `end_image_url` (есть в других v1 моделях)!
- `prompt` имеет максимум 10000 символов (больше, чем у большинства других моделей)!
- НЕТ параметров `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `negative_prompt`, `motion`, `style`, `strength` (в отличие от других i2v моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_bytedance_v1_pro_fast_image_to_video()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 10000 символов
2. **Обязательность `image_url`**: Поле должно быть указано, не пустое, string
3. **Значение `resolution`**: Должно быть одним из 2 допустимых значений (опциональный, только 720p/1080p, НЕТ 480p!)
4. **Значение `duration`**: Должно быть одним из 2 допустимых значений (опциональный, string enum)

### Нормализация

1. **`duration`**: Нормализуется в строку ("5" или "10") если передан как число
2. **`resolution`**: Проверяется что это одно из 2 допустимых значений (720p, 1080p)

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'resolution' not in normalized_input:
    normalized_input['resolution'] = "720p"  # Default согласно документации
if 'duration' not in normalized_input:
    normalized_input['duration'] = "5"  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "bytedance/v1-pro-fast-image-to-video",
  "input": {
    "prompt": "A cinematic close-up sequence of a single elegant ceramic coffee cup with saucer on a rustic wooden table near a sunlit window, hot rich espresso poured in a thin golden stream from above, gradually filling the cup in distinct stages: empty with faint steam, 1/4 filled with dark crema, half-filled with swirling coffee and rising steam, 3/4 filled nearing the rim, perfectly full just below overflow with glossy surface and soft bokeh highlights; ultra-realistic, warm golden-hour light, shallow depth of field, photorealism, detailed textures, subtle steam wisps, serene inviting atmosphere --ar 16:9 --q 2 --style raw",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1762340693669m6sey187.webp",
    "resolution": "720p",
    "duration": "5"
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации видео |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 10000 символов | Сократите описание до 10000 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения для генерации видео |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите непустой URL изображения |
| `Поле 'resolution' должно быть одним из...` | Неверное значение `resolution` | Используйте одно из: 720p, 1080p (НЕТ 480p!) |
| `Поле 'duration' должно быть одним из...` | Неверное значение `duration` | Используйте одно из: 5, 10 |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("bytedance/v1-pro-fast-image-to-video")
user_payload = {
    "prompt": "A cinematic close-up sequence of a single elegant ceramic coffee cup...",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1762340693669m6sey187.webp",
    "resolution": "720p",
    "duration": "5"
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Параметры уже есть в схеме `i2v`
- [x] Обновлена функция `_validate_bytedance_v1_pro_fast_image_to_video()`
- [x] Переиспользованы функции нормализации из других моделей
- [x] Добавлена валидация в `build_input()` (уже есть)
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=bytedance/v1-pro-fast-image-to-video TYPE=i2v INPUT_KEYS=['prompt', 'image_url', 'resolution', 'duration'] 
INPUT_PREVIEW={'prompt': 'A cinematic close-up sequence...', 'image_url': 'https://...', 'resolution': '720p', 'duration': '5'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео (максимум 10000 символов)
2. **image_url обязательный** - обязателен для генерации видео! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp!
3. **resolution опциональный** - не обязательный! Только 2 значения (720p, 1080p), НЕТ 480p! Default: "720p"! 720p для баланса, 1080p для более высокого качества!
4. **duration опциональный** - не обязательный! Только 2 значения (string enum: "5", "10"), default "5"! Длительность видео в секундах!
5. **Уникальные параметры** - модель использует только `prompt`, `image_url`, `resolution`, `duration` вместо стандартных `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `negative_prompt`, `motion`, `style`, `strength`!
6. **НЕТ стандартных параметров** - модель НЕ поддерживает `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `negative_prompt`, `motion`, `style`, `strength` (в отличие от других i2v моделей)!
7. **prompt max 10000** - `prompt` имеет максимум 10000 символов (больше, чем у большинства других моделей)!
8. **Отличия от других v1 моделей** - НЕТ параметров `camera_fixed`, `seed`, `enable_safety_checker`, `end_image_url` (есть в других v1 моделях)! `resolution` только 2 значения (720p, 1080p), НЕТ 480p (в отличие от других v1 моделей)!
