# Интеграция google/imagen4-ultra

## 📋 Обзор

Документация по интеграции модели `google/imagen4-ultra` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `google/imagen4-ultra`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"google/imagen4-ultra"` |
| `input.prompt` | string | Текстовый промпт, описывающий то, что вы хотите увидеть | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.negative_prompt` | string | Описание того, что следует избегать в сгенерированных изображениях | `""` (пустая строка) |
| `input.aspect_ratio` | string | Соотношение сторон сгенерированного изображения | `"1:1"` |
| `input.seed` | string | Случайный seed для воспроизводимой генерации | `""` (пустая строка) |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов! Текстовый промпт, описывающий то, что вы хотите увидеть!

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 5000 символов
- Default: `""` (пустая строка!)
- **Важно**: Опциональный параметр! Максимум 5000 символов! Default: `""` (пустая строка!)! Описание того, что следует избегать в сгенерированных изображениях!

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"1:1"`, `"16:9"`, `"9:16"`, `"3:4"`, `"4:3"`
- Default: `"1:1"`
- **Важно**: Опциональный параметр! Только 5 указанных значений (enum)! Default: `"1:1"`! Соотношение сторон сгенерированного изображения! ВАЖНО: Default "1:1" (а не "16:9" как в imagen4-fast)!

#### `seed`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 500 символов
- Default: `""` (пустая строка!)
- **Важно**: Опциональный параметр! String (не number!)! Максимум 500 символов! Default: `""` (пустая строка!)! Случайный seed для воспроизводимой генерации! ВАЖНО: seed здесь - это string, а не number (как в других моделях)!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | google/imagen4-ultra | google/imagen4-fast | Другие t2i модели |
|----------|----------------------|---------------------|------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный (max 5000) | ✅ Может быть |
| `negative_prompt` | ✅ Есть (max 5000, default "") | ✅ Есть (max 5000, default "") | ✅ Может быть |
| `aspect_ratio` | ✅ Есть (enum: 1:1/16:9/9:16/3:4/4:3, default "1:1") | ✅ Есть (enum: 1:1/16:9/9:16/3:4/4:3, default "16:9") | ✅ Может быть |
| `seed` | ✅ Есть (string, max 500, default "") | ✅ Есть (number) | ✅ Может быть |
| `num_images` | ❌ Нет | ✅ Есть (enum: "1"/"2"/"3"/"4", default "1") | ✅ Может быть |
| `width` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `height` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `guidance` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `steps` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `style` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `output_format` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `image_size` | ❌ Нет | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для генерации изображений из текста (text-to-image) - Ultra версия Imagen4!
- Использует `prompt` для управления генерацией (обязательный параметр, максимум 5000 символов)!
- Использует `negative_prompt` для управления тем, что следует избегать (опциональный параметр, максимум 5000 символов, default "")!
- Использует `aspect_ratio` для управления соотношением сторон (enum: 1:1/16:9/9:16/3:4/4:3, default 1:1)! ВАЖНО: Default "1:1" (а не "16:9" как в imagen4-fast)!
- Использует `seed` для управления воспроизводимостью (string, максимум 500 символов, default "")! ВАЖНО: seed здесь - это string, а не number (как в других моделях)!
- `prompt` имеет максимум 5000 символов!
- `negative_prompt` имеет максимум 5000 символов и default "" (пустая строка!)!
- НЕТ параметра `num_images` (который есть в imagen4-fast)!
- НЕТ параметров `width`, `height`, `guidance`, `steps`, `style`, `output_format`, `image_size` (в отличие от других t2i моделей)!
- **Отличается от google/imagen4-fast тем, что `aspect_ratio` имеет default "1:1" (вместо "16:9"), `seed` - это string (а не number), и НЕТ параметра `num_images`!**

## 🔧 Реализация

### Валидация

Функция `_validate_google_imagen4_ultra()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Длина `negative_prompt`**: Максимум 5000 символов (опциональный)
3. **Значение `aspect_ratio`**: Должно быть одним из 5 допустимых значений (опциональный, enum)
4. **Значение `seed`**: Должно быть string, максимум 500 символов (опциональный, ВАЖНО: string, а не number!)

### Нормализация

1. **`prompt`**: Обрезается до 5000 символов
2. **`negative_prompt`**: Обрезается до 5000 символов, default ""
3. **`aspect_ratio`**: Проверяется что это одно из 5 допустимых значений
4. **`seed`**: Нормализуется в string, обрезается до 500 символов, default ""

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = ""  # Default согласно документации (пустая строка!)
if 'aspect_ratio' not in normalized_input:
    normalized_input['aspect_ratio'] = "1:1"  # Default согласно документации (ВАЖНО: "1:1", а не "16:9"!)
if 'seed' not in normalized_input:
    normalized_input['seed'] = ""  # Default согласно документации (пустая строка! ВАЖНО: string, а не number!)
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "google/imagen4-ultra",
  "input": {
    "prompt": "A lively comic scene where two colleagues are in an office. The first person says, 'Have you heard about Google Imagen 4 Ultra?' The second person responds with excitement, 'It's the best text-to-image tool out there!' The first person asks again, 'Do you know where to get the API?' The second person smiles and says, 'Kie.ai has it!' In the final panel, the two look at a screen showing Kie.ai's interface with an API option, with bright and colorful comic-style illustrations.",
    "negative_prompt": "Enter your prompt here...",
    "aspect_ratio": "1:1",
    "seed": ""
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации изображения |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 5000 символов | Сократите до 5000 символов |
| `Поле 'aspect_ratio' должно быть одним из...` | Неверное значение `aspect_ratio` | Используйте одно из: 1:1, 16:9, 9:16, 3:4, 4:3 |
| `Поле 'seed' слишком длинное...` | `seed` больше 500 символов | Сократите до 500 символов |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("google/imagen4-ultra")
user_payload = {
    "prompt": "A lively comic scene where two colleagues are in an office...",
    "negative_prompt": "Enter your prompt here...",
    "aspect_ratio": "1:1",
    "seed": ""  # ВАЖНО: string, а не number!
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Параметры уже есть в схеме `t2i` (prompt, negative_prompt, aspect_ratio, seed)
- [x] Реализована функция `_validate_google_imagen4_ultra()`
- [x] Переиспользованы функции нормализации из других моделей
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=google/imagen4-ultra TYPE=t2i INPUT_KEYS=['prompt', 'negative_prompt', 'aspect_ratio', 'seed'] 
INPUT_PREVIEW={'prompt': 'A lively comic scene...', 'negative_prompt': 'Enter your prompt here...', 'aspect_ratio': '1:1', 'seed': ''}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации изображения (максимум 5000 символов)! Текстовый промпт, описывающий то, что вы хотите увидеть!
2. **negative_prompt опциональный** - не обязательный! Максимум 5000 символов, default "" (пустая строка!)! Описание того, что следует избегать в сгенерированных изображениях!
3. **aspect_ratio опциональный** - не обязательный! Только 5 значений (enum: 1:1, 16:9, 9:16, 3:4, 4:3), default "1:1"! Соотношение сторон сгенерированного изображения! ВАЖНО: Default "1:1" (а не "16:9" как в imagen4-fast)!
4. **seed опциональный** - не обязательный! String (не number!)! Максимум 500 символов, default "" (пустая строка!)! Случайный seed для воспроизводимой генерации! ВАЖНО: seed здесь - это string, а не number (как в других моделях)!
5. **Уникальные параметры** - модель использует `aspect_ratio` (default "1:1"!), `seed` (string!) вместо стандартных `width`, `height`, `guidance`, `steps`, `style`, `output_format`, `image_size`, `num_images`!
6. **НЕТ стандартных параметров** - модель НЕ поддерживает `width`, `height`, `guidance`, `steps`, `style`, `output_format`, `image_size`, `num_images` (в отличие от других t2i моделей)!
7. **prompt max 5000** - `prompt` имеет максимум 5000 символов!
8. **negative_prompt max 5000** - `negative_prompt` имеет максимум 5000 символов и default "" (пустая строка!)!
9. **seed max 500** - `seed` имеет максимум 500 символов (ВАЖНО: string, а не number!)!
10. **Отличается от google/imagen4-fast** - `aspect_ratio` имеет default "1:1" (вместо "16:9"), `seed` - это string (а не number), и НЕТ параметра `num_images`!













