# Интеграция nano-banana-pro

## 📋 Обзор

Документация по интеграции модели `nano-banana-pro` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `nano-banana-pro` (или `google/nano-banana-pro` в каталоге)

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"nano-banana-pro"` |
| `input.prompt` | string | Текстовое описание изображения для генерации | Обязательный, максимум 10000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.image_input` | array | Входные изображения для трансформации или использования в качестве референса | `[]` (пустой массив) |
| `input.aspect_ratio` | string | Соотношение сторон сгенерированного изображения | `"1:1"` |
| `input.resolution` | string | Разрешение сгенерированного изображения | `"1K"` |
| `input.output_format` | string | Формат выходного изображения | `"png"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 10000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10000 символов!

#### `image_input`
- Тип: array
- Обязательный: нет (опциональный!)
- Количество: до 8 изображений
- Максимальный размер файла: 30MB каждое
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- Default: `[]` (пустой массив)
- **Важно**: Опциональный параметр! Должен быть массивом URL изображений! До 8 изображений! Максимум 30MB каждое! Форматы: jpeg/png/webp!

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"1:1"` - 1:1 (default)
  - `"2:3"` - 2:3
  - `"3:2"` - 3:2
  - `"3:4"` - 3:4
  - `"4:3"` - 4:3
  - `"4:5"` - 4:5
  - `"5:4"` - 5:4
  - `"9:16"` - 9:16
  - `"16:9"` - 16:9
  - `"21:9"` - 21:9
  - `"auto"` - Auto
- **Важно**: Опциональный параметр! Только 11 указанных значений! Default: `"1:1"`!

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"1K"`, `"2K"`, `"4K"`
- Default: `"1K"`
- **Важно**: Опциональный параметр! Только 3 указанных значения! Default: `"1K"`!

#### `output_format`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"png"`, `"jpg"`
- Default: `"png"`
- **Важно**: Опциональный параметр! Только 2 указанных значения! Default: `"png"`! (не `jpeg`, а `jpg`!)

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | nano-banana-pro | Другие t2i модели |
|----------|----------------|-------------------|
| `prompt` | ✅ Обязательный (max 10000) | ✅ Обязательный (обычно max 5000) |
| `image_input` | ✅ Есть (массив, до 8) | ❌ Нет (кроме некоторых i2i) |
| `aspect_ratio` | ✅ Есть (enum, 11 значений) | ✅ Может быть |
| `resolution` | ✅ Есть (enum: 1K/2K/4K) | ✅ Может быть |
| `output_format` | ✅ Есть (enum: png/jpg) | ❌ Нет (кроме qwen/text-to-image) |
| `negative_prompt` | ❌ Нет | ✅ Может быть |
| `width` | ❌ Нет | ✅ Может быть |
| `height` | ❌ Нет | ✅ Может быть |
| `steps` | ❌ Нет | ✅ Может быть |
| `seed` | ❌ Нет | ✅ Может быть |
| `guidance` | ❌ Нет | ✅ Может быть |
| `guidance_scale` | ❌ Нет | ✅ Может быть |
| `style` | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для генерации изображений из текста (text-to-image)!
- Может работать как image-to-image если указан `image_input` (массив, до 8 изображений)!
- Использует `aspect_ratio` (enum, 11 значений) вместо стандартных `width`/`height`!
- Использует `resolution` (enum: 1K/2K/4K) вместо стандартных значений!
- Имеет уникальный параметр: `output_format` (enum: png/jpg, не jpeg!)!
- `prompt` имеет максимум 10000 символов (в отличие от других моделей, где обычно 5000)!
- НЕТ параметров `negative_prompt`, `width`, `height`, `steps`, `seed`, `guidance`, `guidance_scale`, `style` (в отличие от других t2i моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_nano_banana_pro()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 10000 символов
2. **Значение `image_input`**: Должно быть массивом URL изображений, до 8 изображений (опциональный)
3. **Значение `aspect_ratio`**: Должно быть одним из 11 допустимых значений (опциональный)
4. **Значение `resolution`**: Должно быть одним из 3 допустимых значений (опциональный)
5. **Значение `output_format`**: Должно быть одним из 2 допустимых значений (опциональный)

### Нормализация

1. **`_normalize_aspect_ratio_for_nano_banana_pro()`**: Нормализует aspect_ratio (11 значений, case-insensitive, поддерживает варианты написания)
2. **`_normalize_resolution_for_nano_banana_pro()`**: Нормализует resolution (3 значения: 1K, 2K, 4K, case-insensitive)
3. **`_normalize_output_format_for_nano_banana_pro()`**: Нормализует output_format (2 значения: png, jpg, case-insensitive)
4. **`_normalize_input_urls_for_flux_2_pro()`**: Нормализует image_input (массив URL изображений) - переиспользуется из Flux

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'image_input' not in normalized_input:
    normalized_input['image_input'] = []  # Default согласно документации
if 'aspect_ratio' not in normalized_input:
    normalized_input['aspect_ratio'] = "1:1"  # Default согласно документации
if 'resolution' not in normalized_input:
    normalized_input['resolution'] = "1K"  # Default согласно документации
if 'output_format' not in normalized_input:
    normalized_input['output_format'] = "png"  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "nano-banana-pro",
  "input": {
    "prompt": "Comic poster: cool banana hero in shades leaps from sci-fi pad. Six panels: 1) 4K mountain landscape, 2) banana holds page of long multilingual text with auto translation, 3) Gemini 3 hologram for search/knowledge/reasoning, 4) camera UI sliders for angle focus color, 5) frame trio 1:1-9:16, 6) consistent banana poses. Footer shows Google icons. Tagline: Nano Banana Pro now on Kie AI.",
    "image_input": [],
    "aspect_ratio": "1:1",
    "resolution": "1K",
    "output_format": "png"
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации изображения |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 10000 символов | Сократите описание до 10000 символов |
| `Поле 'image_input' содержит слишком много изображений...` | Больше 8 изображений | Используйте максимум 8 изображений |
| `URL изображения #X должен начинаться с http:// или https://` | Неверный формат URL | Укажите корректный URL изображения |
| `Поле 'aspect_ratio' должно быть одним из...` | Неверное значение `aspect_ratio` | Используйте одно из 11 допустимых значений |
| `Поле 'resolution' должно быть '1K', '2K' или '4K'...` | Неверное значение `resolution` | Используйте одно из: 1K, 2K, 4K |
| `Поле 'output_format' должно быть 'png' или 'jpg'...` | Неверное значение `output_format` | Используйте одно из: png, jpg |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("google/nano-banana-pro")  # В каталоге может быть google/nano-banana-pro
user_payload = {
    "prompt": "Comic poster: cool banana hero...",
    "image_input": [],
    "aspect_ratio": "1:1",
    "resolution": "1K",
    "output_format": "png"
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Обновлены комментарии для `output_format`, `image_input` и `resolution` в схеме `t2i`
- [x] Реализована функция `_validate_nano_banana_pro()`
- [x] Добавлены функции нормализации для всех параметров
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=nano-banana-pro TYPE=t2i INPUT_KEYS=['prompt', 'image_input', 'aspect_ratio', 'resolution', 'output_format'] 
INPUT_PREVIEW={'prompt': 'Comic poster...', 'image_input': [], 'aspect_ratio': '1:1', 'resolution': '1K', 'output_format': 'png'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации изображения (максимум 10000 символов, больше чем у других моделей!)
2. **image_input опциональный** - не обязательный! Должен быть массивом URL изображений! До 8 изображений! Максимум 30MB каждое! Форматы: jpeg/png/webp! Default: пустой массив `[]`!
3. **aspect_ratio опциональный** - не обязательный! Только 11 значений, default "1:1"! Используется вместо стандартных `width`/`height`!
4. **resolution опциональный** - не обязательный! Только 3 значения (1K, 2K, 4K), default "1K"!
5. **output_format опциональный** - не обязательный! Только 2 значения (png, jpg), default "png"! ВАЖНО: используется `jpg`, а не `jpeg`!
6. **Уникальные параметры** - модель использует `image_input`, `aspect_ratio`, `resolution`, `output_format` вместо стандартных `width`, `height`, `steps`, `seed`, `guidance`, `style`!
7. **НЕТ стандартных параметров** - модель НЕ поддерживает `negative_prompt`, `width`, `height`, `steps`, `seed`, `guidance`, `guidance_scale`, `style`!
8. **Модель ID** - в документации указан `nano-banana-pro`, но в каталоге может быть `google/nano-banana-pro` (оба ID поддерживаются)!
