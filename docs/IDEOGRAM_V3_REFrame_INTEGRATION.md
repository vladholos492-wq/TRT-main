# Интеграция ideogram/v3-reframe

## 📋 Обзор

Документация по интеграции модели `ideogram/v3-reframe` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `ideogram/v3-reframe`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"ideogram/v3-reframe"` |
| `input.image_url` | string | URL изображения для рефрейминга | Обязательный, макс 10MB, jpeg/png/webp |
| `input.image_size` | string | Разрешение для рефреймленного выходного изображения | Обязательный, enum (6 значений) |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.rendering_speed` | string | Скорость рендеринга | `"BALANCED"` |
| `input.style` | string | Тип стиля для генерации. Нельзя использовать вместе с style_codes | `"AUTO"` |
| `input.num_images` | string | Количество изображений | `"1"` |
| `input.seed` | number | Seed для генератора случайных чисел | `0` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Должен быть валидным URL изображения!

#### `image_size`
- Тип: string
- Обязательный: да
- Значения:
  - `"square"` - Квадрат
  - `"square_hd"` - Квадрат HD (default)
  - `"portrait_4_3"` - Портрет 3:4
  - `"portrait_16_9"` - Портрет 9:16
  - `"landscape_4_3"` - Альбом 4:3
  - `"landscape_16_9"` - Альбом 16:9
- **Важно**: Обязательный параметр! Только 6 значений! Default: `"square_hd"`!

#### `rendering_speed`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"TURBO"` - Turbo
  - `"BALANCED"` - Balanced (default)
  - `"QUALITY"` - Quality
- **Важно**: Опциональный параметр! Только 3 значения! Default: `"BALANCED"`!

#### `style`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"AUTO"` - Auto (default)
  - `"GENERAL"` - General
  - `"REALISTIC"` - Realistic
  - `"DESIGN"` - Design
- **Важно**: Опциональный параметр! Только 4 значения! Default: `"AUTO"`! Нельзя использовать вместе с style_codes!

#### `num_images`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"1"` - 1 изображение (default)
  - `"2"` - 2 изображения
  - `"3"` - 3 изображения
  - `"4"` - 4 изображения
- **Важно**: Опциональный параметр! Только 4 значения! Default: `"1"`! Тип - string (не number)!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- Default: `0`
- **Важно**: Опциональный параметр! Любое число! Default: `0`!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | ideogram/v3-reframe | Другие i2i модели |
|----------|---------------------|-------------------|
| `image_url` | ✅ Обязательный | ✅ Обязательный |
| `image_size` | ✅ Обязательный (enum, 6 значений) | ❌ Нет (используют width/height или aspect_ratio) |
| `rendering_speed` | ✅ Есть (enum, 3 значения) | ❌ Нет |
| `style` | ✅ Есть (enum, 4 значения) | ✅ Может быть (но другие значения) |
| `num_images` | ✅ Есть (enum, string, 4 значения) | ❌ Нет (используют image_count) |
| `seed` | ✅ Есть (number) | ✅ Есть (number) |
| `prompt` | ❌ Нет | ✅ Может быть обязательным |

**ВАЖНО**: 
- Это модель для рефрейминга изображений!
- Использует `image_size` (обязательный) вместо стандартных `width/height` или `aspect_ratio`!
- Использует `num_images` (string, не number) вместо стандартного `image_count`!
- НЕТ параметра `prompt` (в отличие от других i2i моделей)!
- `image_size` обязательный (в отличие от других моделей, где он опциональный)!

## 🔧 Реализация

### Валидация

Функция `_validate_ideogram_v3_reframe()` проверяет:

1. **Обязательность `image_url`**: Поле должно быть указано и не пустое
2. **Обязательность `image_size`**: Поле должно быть указано и не пустое
3. **Значение `image_size`**: Должно быть одним из 6 допустимых значений
4. **Значение `rendering_speed`**: Должно быть "TURBO", "BALANCED" или "QUALITY" (опциональный)
5. **Значение `style`**: Должно быть "AUTO", "GENERAL", "REALISTIC" или "DESIGN" (опциональный)
6. **Значение `num_images`**: Должно быть "1", "2", "3" или "4" (опциональный, string)
7. **Значение `seed`**: Должно быть числом (опциональный)

### Нормализация

1. **`_normalize_image_size_for_ideogram_v3_reframe()`**: Нормализует image_size (6 значений)
2. **`_normalize_rendering_speed_for_ideogram_v3_reframe()`**: Нормализует rendering_speed ("TURBO" | "BALANCED" | "QUALITY")
3. **`_normalize_style_for_ideogram_v3_reframe()`**: Нормализует style ("AUTO" | "GENERAL" | "REALISTIC" | "DESIGN")
4. **`_normalize_num_images_for_ideogram_v3_reframe()`**: Нормализует num_images ("1" | "2" | "3" | "4")

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'image_size' not in normalized_input:
    normalized_input['image_size'] = "square_hd"  # Default согласно документации
if 'rendering_speed' not in normalized_input:
    normalized_input['rendering_speed'] = "BALANCED"  # Default согласно документации
if 'style' not in normalized_input:
    normalized_input['style'] = "AUTO"  # Default согласно документации
if 'num_images' not in normalized_input:
    normalized_input['num_images'] = "1"  # Default согласно документации
if 'seed' not in normalized_input:
    normalized_input['seed'] = 0  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "ideogram/v3-reframe",
  "input": {
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1757168087001amxesd6e.webp",
    "image_size": "square_hd",
    "rendering_speed": "BALANCED",
    "style": "AUTO",
    "num_images": "1",
    "seed": 42
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'image_url' обязательно...` | `image_url` не указан | Загрузите изображение для рефрейминга |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите валидный URL изображения |
| `Поле 'image_size' обязательно...` | `image_size` не указан | Укажите размер выходного изображения |
| `Поле 'image_size' должно быть одним из...` | Неверное значение `image_size` | Используйте одно из 6 допустимых значений |
| `Поле 'rendering_speed' должно быть одним из...` | Неверное значение `rendering_speed` | Используйте "TURBO", "BALANCED" или "QUALITY" |
| `Поле 'style' должно быть одним из...` | Неверное значение `style` | Используйте "AUTO", "GENERAL", "REALISTIC" или "DESIGN" |
| `Поле 'num_images' должно быть одним из...` | Неверное значение `num_images` | Используйте "1", "2", "3" или "4" |
| `Поле 'seed' должно быть числом...` | Неверное значение `seed` | Используйте любое число |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("ideogram/v3-reframe")
user_payload = {
    "image_url": "https://example.com/image.webp",
    "image_size": "square_hd",
    "rendering_speed": "BALANCED",
    "style": "AUTO",
    "num_images": "1",
    "seed": 42
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлены параметры `image_size`, `rendering_speed`, `num_images` в схему `i2i`
- [x] Реализована функция `_validate_ideogram_v3_reframe()`
- [x] Добавлены функции нормализации для всех enum параметров
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=ideogram/v3-reframe TYPE=i2i INPUT_KEYS=['image_url', 'image_size', 'rendering_speed', 'style', 'num_images', 'seed'] 
INPUT_PREVIEW={'image_url': 'https://...', 'image_size': 'square_hd', 'rendering_speed': 'BALANCED', 'style': 'AUTO', 'num_images': '1', 'seed': 42}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **image_url обязательный** - обязателен для рефрейминга изображения (макс 10MB, jpeg/png/webp)
2. **image_size обязательный** - обязателен! Только 6 значений, default "square_hd"!
3. **rendering_speed опциональный** - не обязательный! Только 3 значения ("TURBO", "BALANCED", "QUALITY"), default "BALANCED"!
4. **style опциональный** - не обязательный! Только 4 значения ("AUTO", "GENERAL", "REALISTIC", "DESIGN"), default "AUTO"! Нельзя использовать вместе с style_codes!
5. **num_images опциональный** - не обязательный! Только 4 значения ("1", "2", "3", "4"), default "1"! Тип - string (не number)!
6. **seed опциональный** - не обязательный! Любое число, default 0!
7. **НЕТ параметра prompt** - в отличие от других i2i моделей, параметр `prompt` не используется!
8. **Уникальные параметры** - модель использует `image_size` (обязательный) и `num_images` (string) вместо стандартных параметров!

