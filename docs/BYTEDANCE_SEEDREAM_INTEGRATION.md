# Интеграция bytedance/seedream

## 📋 Обзор

Документация по интеграции модели `bytedance/seedream` (Seedream 3.0) согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `bytedance/seedream`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"bytedance/seedream"` |
| `input.prompt` | string | Текстовый промпт для генерации изображения | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.image_size` | string | Размер изображения | `"square_hd"` |
| `input.guidance_scale` | number | Контролирует, насколько тесно выходное изображение соответствует входному промпту. Более высокие значения означают более сильную корреляцию с промптом | `2.5` |
| `input.seed` | number | Случайное зерно для управления стохастичностью генерации изображения | Не указан |
| `input.enable_safety_checker` | boolean | Если установлено в true, будет включен проверщик безопасности | `true` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `image_size`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"square"` - Квадрат
  - `"square_hd"` - Квадрат HD (default)
  - `"portrait_4_3"` - Портрет 3:4
  - `"portrait_16_9"` - Портрет 9:16
  - `"landscape_4_3"` - Пейзаж 4:3
  - `"landscape_16_9"` - Пейзаж 16:9
- **Важно**: Опциональный параметр! Только 6 указанных значений! Default: `"square_hd"`!

#### `guidance_scale`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 1-10 (шаг: 0.1)
- Default: `2.5`
- **Важно**: Опциональный параметр! Только числа от 1 до 10! Default: `2.5`! Более высокие значения означают более сильную корреляцию с промптом!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- **Важно**: Опциональный параметр! Если не указан, выбирается случайное зерно!

#### `enable_safety_checker`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `true`! Если установлено в true, будет включен проверщик безопасности!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | bytedance/seedream | Другие t2i модели |
|----------|-------------------|-------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный |
| `image_size` | ✅ Есть (enum, 6 значений) | ✅ Может быть |
| `guidance_scale` | ✅ Есть (1-10) | ❌ Нет (используют `guidance`) |
| `seed` | ✅ Есть | ✅ Может быть |
| `enable_safety_checker` | ✅ Есть (boolean) | ❌ Нет |
| `negative_prompt` | ❌ Нет | ✅ Может быть |
| `width` | ❌ Нет | ✅ Может быть |
| `height` | ❌ Нет | ✅ Может быть |
| `steps` | ❌ Нет | ✅ Может быть |
| `guidance` | ❌ Нет | ✅ Может быть |
| `style` | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель Seedream 3.0 для генерации изображений из текста!
- Использует `guidance_scale` (1-10) вместо стандартного `guidance`!
- Использует `image_size` (enum, 6 значений) вместо стандартных `width`/`height`!
- Имеет уникальный параметр: `enable_safety_checker`!
- НЕТ параметров `negative_prompt`, `width`, `height`, `steps`, `guidance`, `style` (в отличие от других t2i моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_bytedance_seedream()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Значение `image_size`**: Должно быть одним из 6 допустимых значений (опциональный)
3. **Значение `guidance_scale`**: Должно быть числом от 1 до 10 (опциональный)
4. **Значение `seed`**: Должно быть числом (опциональный)
5. **Значение `enable_safety_checker`**: Должно быть boolean (опциональный)

### Нормализация

1. **`_normalize_image_size_for_bytedance_seedream()`**: Нормализует image_size (6 значений, case-insensitive)
2. **`_normalize_boolean()`**: Нормализует enable_safety_checker (boolean) - переиспользуется из других моделей

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'image_size' not in normalized_input:
    normalized_input['image_size'] = "square_hd"  # Default согласно документации
if 'guidance_scale' not in normalized_input:
    normalized_input['guidance_scale'] = 2.5  # Default согласно документации
if 'enable_safety_checker' not in normalized_input:
    normalized_input['enable_safety_checker'] = True  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "bytedance/seedream",
  "input": {
    "prompt": "A 2D flat art style campsite poster with the text \"Kie AI Seedream 3.0 API\". The scene includes mountains, brown tents under a sunny sky, a rabbit, a deer, some birds, green grass, and a flowing river, all illustrated in clean vector art.",
    "image_size": "square_hd",
    "guidance_scale": 2.5,
    "seed": 42,
    "enable_safety_checker": true
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации изображения |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'image_size' должно быть одним из...` | Неверное значение `image_size` | Используйте одно из 6 допустимых значений |
| `Поле 'guidance_scale' должно быть в диапазоне...` | Неверное значение `guidance_scale` | Используйте число от 1 до 10 |
| `Поле 'seed' должно быть числом...` | Неверное значение `seed` | Используйте число |
| `Поле 'enable_safety_checker' должно быть boolean...` | Неверное значение `enable_safety_checker` | Используйте true/false |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("bytedance/seedream")
user_payload = {
    "prompt": "A 2D flat art style campsite poster...",
    "image_size": "square_hd",
    "guidance_scale": 2.5,
    "seed": 42,
    "enable_safety_checker": True
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлены параметры `guidance_scale`, `enable_safety_checker` в схему `t2i`
- [x] Реализована функция `_validate_bytedance_seedream()`
- [x] Добавлена функция нормализации `_normalize_image_size_for_bytedance_seedream()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=bytedance/seedream TYPE=t2i INPUT_KEYS=['prompt', 'image_size', 'guidance_scale', 'seed', 'enable_safety_checker'] 
INPUT_PREVIEW={'prompt': 'A 2D flat art style...', 'image_size': 'square_hd', 'guidance_scale': 2.5, 'seed': 42, 'enable_safety_checker': True}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации изображения (максимум 5000 символов)
2. **image_size опциональный** - не обязательный! Только 6 значений, default "square_hd"!
3. **guidance_scale опциональный** - не обязательный! Только числа от 1 до 10, default 2.5! Более высокие значения означают более сильную корреляцию с промптом!
4. **seed опциональный** - не обязательный! Если не указан, выбирается случайное зерно!
5. **enable_safety_checker опциональный** - не обязательный! Boolean (true/false), default true! Если установлено в true, будет включен проверщик безопасности!
6. **Уникальные параметры** - модель использует `guidance_scale` вместо стандартного `guidance`! Использует `image_size` вместо стандартных `width`/`height`!
7. **НЕТ стандартных параметров** - модель НЕ поддерживает `negative_prompt`, `width`, `height`, `steps`, `guidance`, `style`!

