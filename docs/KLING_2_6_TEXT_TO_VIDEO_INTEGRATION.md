# Интеграция kling-2.6/text-to-video

## 📋 Обзор

Документация по интеграции модели `kling-2.6/text-to-video` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `kling-2.6/text-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"kling-2.6/text-to-video"` |
| `input.prompt` | string | Текстовое описание видео | Обязательный, максимум 1000 символов |
| `input.sound` | boolean | Включить звук в видео | Обязательный boolean |
| `input.aspect_ratio` | string | Соотношение сторон видео | Обязательный, один из: "1:1", "16:9", "9:16" |
| `input.duration` | string | Длительность видео в секундах | Обязательный, "5" или "10" |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 1000 символов
- **Важно**: Не может быть пустым! Максимум 1000 символов (не 3000!)

#### `sound`
- Тип: boolean
- Обязательный: да
- Значения: `true` или `false`
- Default: `false`
- **Важно**: Обязательный параметр, не опциональный! Принимает boolean, строки конвертируются автоматически.

#### `aspect_ratio`
- Тип: string
- Обязательный: да
- Значения:
  - `"1:1"` - Квадрат (default)
  - `"16:9"` - Горизонтальный 16:9
  - `"9:16"` - Вертикальный 9:16
- **Важно**: Обязательный параметр, не опциональный! Только 3 значения (не 8 как у seedream)!

#### `duration`
- Тип: string
- Обязательный: да
- Значения:
  - `"5"` - 5 секунд (default)
  - `"10"` - 10 секунд
- **Важно**: Обязательный параметр, не опциональный! Только "5" и "10" (не "15"!)
- **Важно**: Должно быть строкой, не числом!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_kling_2_6_text_to_video(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для kling-2.6/text-to-video согласно документации API.
    """
    if model_id != "kling-2.6/text-to-video":
        return True, None
    
    # Валидация prompt: обязательный, максимум 1000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации видео"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len > 1000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 1000)"
    
    # Валидация sound: обязательный boolean
    sound = normalized_input.get('sound')
    if sound is None:
        return False, "Поле 'sound' обязательно для генерации видео"
    
    normalized_sound = _normalize_sound_for_kling_2_6(sound)
    if normalized_sound is None:
        return False, f"Поле 'sound' должно быть boolean (true/false)"
    normalized_input['sound'] = normalized_sound
    
    # Валидация aspect_ratio: обязательный, "1:1" | "16:9" | "9:16"
    aspect_ratio = normalized_input.get('aspect_ratio')
    if not aspect_ratio:
        return False, "Поле 'aspect_ratio' обязательно для генерации видео"
    
    normalized_aspect_ratio = _normalize_aspect_ratio_for_kling_2_6(aspect_ratio)
    if normalized_aspect_ratio is None:
        valid_values = ["1:1", "16:9", "9:16"]
        return False, f"Поле 'aspect_ratio' должно быть одним из: {', '.join(valid_values)}"
    normalized_input['aspect_ratio'] = normalized_aspect_ratio
    
    # Валидация duration: обязательный, "5" | "10", default "5"
    duration = normalized_input.get('duration')
    if not duration:
        return False, "Поле 'duration' обязательно для генерации видео"
    
    normalized_duration = _normalize_duration_for_kling_2_6(duration)
    if normalized_duration is None:
        return False, f"Поле 'duration' должно быть '5' или '10'"
    normalized_input['duration'] = normalized_duration
    
    return True, None
```

### Нормализация sound

Используется та же функция нормализации, что и для `kling-2.6/image-to-video`:

```python
def _normalize_sound_for_kling_2_6(value: Any) -> Optional[bool]:
    """
    Нормализует sound для kling-2.6/text-to-video.
    Принимает boolean, строку или число и возвращает boolean.
    """
    # Конвертирует строки и числа в boolean
    ...
```

### Нормализация aspect_ratio

Функция нормализации для kling-2.6 поддерживает только 3 значения (в отличие от seedream с 8):

```python
def _normalize_aspect_ratio_for_kling_2_6(value: Any) -> Optional[str]:
    """
    Нормализует aspect_ratio для kling-2.6/text-to-video.
    ВАЖНО: Для kling-2.6 поддерживаются только "1:1", "16:9", "9:16"!
    """
    valid_values = ["1:1", "16:9", "9:16"]
    if str_value in valid_values:
        return str_value
    
    # Пробуем нормализовать варианты написания
    str_lower = str_value.lower()
    if str_lower in ["1:1", "1/1", "1x1", "square"]:
        return "1:1"
    elif str_lower in ["16:9", "16/9", "16x9", "landscape", "wide"]:
        return "16:9"
    elif str_lower in ["9:16", "9/16", "9x16", "portrait", "vertical"]:
        return "9:16"
    
    return None
```

### Нормализация duration

Используется та же функция нормализации, что и для `kling-2.6/image-to-video`:

```python
def _normalize_duration_for_kling_2_6(value: Any) -> Optional[str]:
    """
    Нормализует duration для kling-2.6/text-to-video.
    ВАЖНО: Для kling-2.6 поддерживаются только "5" и "10"!
    """
    # Проверяет что это валидное значение (только 5 и 10!)
    ...
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id == "kling-2.6/text-to-video":
    if 'sound' not in normalized_input:
        normalized_input['sound'] = False  # Default согласно документации
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "1:1"  # Default согласно документации
    if 'duration' not in normalized_input:
        normalized_input['duration'] = "5"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "kling-2.6/text-to-video",
  "input": {
    "prompt": "Visual: In a fashion live-streaming room, clothes hang on a rack, and a full-length mirror reflects the host's figure. Dialog: [African-American female host] turns to show off the sweatshirt fit.",
    "sound": false,
    "aspect_ratio": "1:1",
    "duration": "5"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (максимум 1000 символов)
2. **Валидация sound**: Проверяется что это boolean (конвертируется из строки/числа если нужно)
3. **Валидация aspect_ratio**: Проверяется что значение из допустимого списка (только 3 значения!)
4. **Валидация duration**: Проверяется что значение "5" или "10" (конвертируется в строку)
5. **Дефолтные значения**: Применяются если параметры не указаны (sound=false, aspect_ratio="1:1", duration="5")
6. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=kling-2.6/text-to-video TYPE=t2v INPUT_KEYS=['prompt', 'sound', 'aspect_ratio', 'duration'] 
INPUT_PREVIEW={'prompt': 'Visual: In a fashion...', 'sound': False, 'aspect_ratio': '1:1', 'duration': '5'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt максимум 1000 символов** - проверяется при валидации (не 3000!)
2. **sound обязательный boolean** - не опциональный! Конвертируется из строки/числа автоматически
3. **aspect_ratio обязательный** - не опциональный! Только 3 значения: "1:1", "16:9", "9:16" (не 8 как у seedream!)
4. **duration обязательный** - не опциональный! Только "5" и "10" (не "15"!)
5. **duration должно быть строкой**, не числом
6. **Дефолтные значения**: sound=false, aspect_ratio="1:1", duration="5"
7. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими моделями

### Сравнение с kling-2.6/image-to-video

| Параметр | kling-2.6/image-to-video | kling-2.6/text-to-video |
|----------|--------------------------|--------------------------|
| `prompt` | Максимум 1000 символов | Максимум 1000 символов |
| `image_urls` | Обязательный массив | Не требуется |
| `sound` | Обязательный boolean | Обязательный boolean |
| `aspect_ratio` | Не требуется | Обязательный, 3 значения |
| `duration` | "5"\|"10" | "5"\|"10" |
| Тип модели | i2v | t2v |

### Сравнение с seedream/4.5-text-to-image

| Параметр | seedream/4.5-text-to-image | kling-2.6/text-to-video |
|----------|----------------------------|--------------------------|
| `prompt` | Максимум 3000 символов | Максимум 1000 символов |
| `aspect_ratio` | 8 значений | 3 значения |
| `quality` | "basic"\|"high" | Не требуется |
| `sound` | Не требуется | Обязательный boolean |
| `duration` | Не требуется | Обязательный, "5"\|"10" |
| Тип модели | t2i | t2v |

## 🎯 Ключевые отличия

1. **Нет image_urls**: В отличие от image-to-video, text-to-video не требует изображения
2. **aspect_ratio обязательный**: В отличие от других t2v моделей, kling-2.6 требует явного указания aspect_ratio
3. **Ограничение prompt**: Максимум 1000 символов (не 3000 или 5000)
4. **Ограничение aspect_ratio**: Только 3 значения (не 8 как у seedream)
5. **sound обязательный**: Должен быть указан явно













