# Интеграция seedream/4.5-edit

## 📋 Обзор

Документация по интеграции модели `seedream/4.5-edit` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `seedream/4.5-edit`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"seedream/4.5-edit"` |
| `input.prompt` | string | Текстовое описание редактирования | Обязательный, максимум 3000 символов |
| `input.image_urls` | array | Массив URL изображений | Обязательный, минимум 1 URL |
| `input.aspect_ratio` | string | Соотношение сторон изображения | Обязательный, один из допустимых значений |
| `input.quality` | string | Качество изображения | Обязательный, "basic" или "high" |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 3000 символов
- **Важно**: Не может быть пустым!

#### `image_urls`
- Тип: массив строк (array of strings)
- Обязательный: да
- Формат: `["https://example.com/image.jpg"]`
- Поддерживает множественные изображения
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: JPEG, PNG, WebP
- **Важно**: Должен быть массивом, не строкой!

#### `aspect_ratio`
- Тип: string
- Обязательный: да
- Значения:
  - `"1:1"` - Квадрат (default)
  - `"4:3"` - Горизонтальный 4:3
  - `"3:4"` - Вертикальный 3:4
  - `"16:9"` - Горизонтальный 16:9
  - `"9:16"` - Вертикальный 9:16
  - `"2:3"` - Вертикальный 2:3
  - `"3:2"` - Горизонтальный 3:2
  - `"21:9"` - Широкий 21:9
- **Важно**: Обязательный параметр, не опциональный!

#### `quality`
- Тип: string
- Обязательный: да
- Значения:
  - `"basic"` - Basic (2K изображения, default)
  - `"high"` - High (4K изображения)
- **Важно**: Обязательный параметр, не опциональный! Принимается в нижнем регистре.

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_seedream_4_5_edit(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для seedream/4.5-edit согласно документации API.
    """
    if model_id != "seedream/4.5-edit":
        return True, None
    
    # Валидация prompt: обязательный, максимум 3000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для редактирования изображения"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len > 3000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 3000)"
    
    # Валидация image_urls: обязательный массив
    image_urls = normalized_input.get('image_urls')
    if not image_urls:
        return False, "Поле 'image_urls' обязательно для редактирования изображения"
    
    # Нормализуем image_urls (конвертируем строку в массив если нужно)
    normalized_image_urls = _normalize_image_urls_for_wan_2_6(image_urls)
    if not normalized_image_urls:
        return False, "Поле 'image_urls' должно содержать хотя бы один валидный URL изображения"
    
    # Проверяем что все URL начинаются с http:// или https://
    for idx, url in enumerate(normalized_image_urls):
        if not (url.startswith('http://') or url.startswith('https://')):
            return False, f"URL изображения #{idx + 1} должен начинаться с http:// или https://"
    
    # Сохраняем нормализованное значение
    normalized_input['image_urls'] = normalized_image_urls
    
    # Валидация aspect_ratio и quality (аналогично seedream/4.5-text-to-image)
    ...
    
    return True, None
```

### Нормализация image_urls

Используется та же функция нормализации, что и для `wan/2-6-image-to-video`:

```python
def _normalize_image_urls_for_wan_2_6(value: Any) -> Optional[List[str]]:
    """
    Нормализует image_urls для seedream/4.5-edit.
    Принимает строку, массив строк или None и возвращает массив строк.
    """
    # Если это строка, конвертируем в массив
    if isinstance(value, str):
        if value.strip():
            return [value.strip()]
        return None
    
    # Если это массив, проверяем и нормализуем элементы
    if isinstance(value, list):
        normalized = []
        for item in value:
            if isinstance(item, str) and item.strip():
                normalized.append(item.strip())
        return normalized if normalized else None
    
    return None
```

### Нормализация aspect_ratio и quality

Используются те же функции нормализации, что и для `seedream/4.5-text-to-image`:

```python
def _normalize_aspect_ratio_for_seedream_4_5(value: Any) -> Optional[str]:
    """Нормализует aspect_ratio для seedream/4.5-edit."""
    # Проверяет валидность значения из списка: ["1:1", "4:3", "3:4", "16:9", "9:16", "2:3", "3:2", "21:9"]
    ...

def _normalize_quality_for_seedream_4_5(value: Any) -> Optional[str]:
    """Нормализует quality для seedream/4.5-edit (конвертирует в нижний регистр)."""
    # Проверяет валидность значения: "basic" или "high"
    ...
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id == "seedream/4.5-edit":
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "1:1"  # Default согласно документации
    if 'quality' not in normalized_input:
        normalized_input['quality'] = "basic"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "seedream/4.5-edit",
  "input": {
    "prompt": "Keep the model's pose and the flowing shape of the liquid dress unchanged. Change the clothing material from silver metal to completely transparent clear water (or glass). Through the liquid water, the model's skin details are visible. Lighting changes from reflection to refraction.",
    "image_urls": ["https://static.aiquickdraw.com/tools/example/1764851484363_ScV1s2aq.webp"],
    "aspect_ratio": "1:1",
    "quality": "basic"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (максимум 3000 символов)
2. **Валидация image_urls**: Проверяется что это массив с хотя бы одним валидным URL
3. **Нормализация image_urls**: Автоматически конвертирует строку в массив
4. **Валидация aspect_ratio**: Проверяется что значение из допустимого списка
5. **Валидация quality**: Проверяется что значение "basic" или "high" (конвертируется в нижний регистр)
6. **Дефолтные значения**: Применяются если параметры не указаны (aspect_ratio="1:1", quality="basic")
7. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=seedream/4.5-edit TYPE=i2i INPUT_KEYS=['prompt', 'image_urls', 'aspect_ratio', 'quality'] 
INPUT_PREVIEW={'prompt': 'Keep the model's pose...', 'image_urls': ['https://...'], 'aspect_ratio': '1:1', 'quality': 'basic'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt максимум 3000 символов** - проверяется при валидации
2. **image_urls обязательный массив** - автоматически конвертируется из строки если нужно
3. **aspect_ratio обязательный** - не опциональный! Должен быть указан явно или будет применён дефолт "1:1"
4. **quality обязательный** - не опциональный! Должен быть указан явно или будет применён дефолт "basic"
5. **quality конвертируется в нижний регистр** - "Basic" → "basic", "High" → "high"
6. **Дефолтные значения**: aspect_ratio="1:1", quality="basic"
7. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с seedream/4.5-text-to-image

| Параметр | seedream/4.5-text-to-image | seedream/4.5-edit |
|----------|----------------------------|-------------------|
| `prompt` | Обязательный, max 3000 | Обязательный, max 3000 |
| `image_urls` | Не требуется | Обязательный массив |
| `aspect_ratio` | Обязательный, 8 значений | Обязательный, 8 значений |
| `quality` | Обязательный, "basic"\|"high" | Обязательный, "basic"\|"high" |
| Тип модели | t2i | i2i |

## 🎯 Ключевые отличия

1. **Требует image_urls**: В отличие от text-to-image, edit требует исходное изображение
2. **Тип модели**: i2i (image-to-image), а не t2i
3. **Все параметры обязательны**: prompt, image_urls, aspect_ratio, quality - все обязательны

