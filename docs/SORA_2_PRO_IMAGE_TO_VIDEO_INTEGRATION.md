# Интеграция sora-2-pro-image-to-video

## 📋 Обзор

Документация по интеграции модели `sora-2-pro-image-to-video` согласно официальной документации KIE AI API.

**ВАЖНО**: Параметры похожи на `sora-2-pro-text-to-video`, но:
- `image_urls` обязательный (в text-to-video его нет)
- `size` default "standard" (в text-to-video default "high"!)

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `sora-2-pro-image-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"sora-2-pro-image-to-video"` |
| `input.prompt` | string | Текстовое описание движения видео | Обязательный, максимум 10000 символов |
| `input.image_urls` | array | URL изображения для использования в качестве первого кадра | Обязательный массив, максимум 10MB на файл, форматы: JPEG, PNG, WebP |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.aspect_ratio` | string | Соотношение сторон видео | `"landscape"` |
| `input.n_frames` | string | Количество кадров для генерации | `"10"` |
| `input.size` | string | Качество или размер сгенерированного видео | `"standard"` |
| `input.remove_watermark` | boolean | Удаление водяных знаков из сгенерированного видео | `true` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 10000 символов
- **Важно**: Не может быть пустым! Максимум 10000 символов!

#### `image_urls`
- Тип: array
- Обязательный: да
- **Важно**: Массив URL изображений (может быть несколько файлов!)
- **Важно**: Максимум 10MB на файл, форматы: JPEG, PNG, WebP
- **Важно**: Каждый URL должен начинаться с `http://` или `https://`

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"portrait"` - Портретная ориентация
  - `"landscape"` - Альбомная ориентация (default)
- **Важно**: Опциональный параметр! Только 2 значения!

#### `n_frames`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"10"` - 10 секунд (default)
  - `"15"` - 15 секунд
- **Важно**: Опциональный параметр! Только 2 значения!

#### `size`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"standard"` - Стандартное качество (default)
  - `"high"` - Высокое качество
- **Важно**: Опциональный параметр! Только 2 значения!
- **КРИТИЧНО**: Default "standard" (отличается от text-to-video, где default "high"!)

#### `remove_watermark`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Значения:
  - `true` - Удалять водяные знаки (default)
  - `false` - Не удалять водяные знаки
- **Важно**: Опциональный параметр! Boolean значение!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_sora_2_pro_image_to_video(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для sora-2-pro-image-to-video согласно документации API.
    
    ВАЖНО: Параметры похожи на sora-2-pro-text-to-video, но:
    - image_urls обязательный (в text-to-video его нет)
    - size default "standard" (в text-to-video default "high")
    """
    # Проверяем оба возможных ID модели
    if model_id not in ["sora-2-pro-image-to-video", "sora-2-pro/i2v", "openai/sora-2-pro-image-to-video"]:
        return True, None
    
    # Валидация prompt: обязательный, максимум 10000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации видео"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len > 10000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 10000)"
    
    # Валидация image_urls: обязательный массив
    image_urls = normalized_input.get('image_urls')
    if not image_urls:
        return False, "Поле 'image_urls' обязательно для генерации видео. Загрузите изображение"
    
    # Переиспользуем функцию нормализации из wan/2-6-image-to-video
    normalized_image_urls = _normalize_image_urls_for_wan_2_6(image_urls)
    if normalized_image_urls is None:
        return False, "Поле 'image_urls' должно быть массивом валидных URL изображений"
    normalized_input['image_urls'] = normalized_image_urls
    
    # Валидация aspect_ratio: опциональный, enum ("portrait" или "landscape")
    # Переиспользуем функцию нормализации из sora-2-pro-storyboard
    aspect_ratio = normalized_input.get('aspect_ratio')
    if aspect_ratio is not None:
        normalized_aspect_ratio = _normalize_aspect_ratio_for_sora_2_pro_storyboard(aspect_ratio)
        if normalized_aspect_ratio is None:
            valid_values = ["portrait", "landscape"]
            return False, f"Поле 'aspect_ratio' должно быть одним из: {', '.join(valid_values)}"
        normalized_input['aspect_ratio'] = normalized_aspect_ratio
    
    # Валидация n_frames: опциональный, enum ("10" или "15")
    # Переиспользуем функцию нормализации из sora-2-pro-text-to-video
    n_frames = normalized_input.get('n_frames')
    if n_frames is not None:
        normalized_n_frames = _normalize_n_frames_for_sora_2_pro_text_to_video(n_frames)
        if normalized_n_frames is None:
            valid_values = ["10", "15"]
            return False, f"Поле 'n_frames' должно быть одним из: {', '.join(valid_values)}"
        normalized_input['n_frames'] = normalized_n_frames
    
    # Валидация size: опциональный, enum ("standard" или "high")
    # Переиспользуем функцию нормализации из sora-2-pro-text-to-video
    size = normalized_input.get('size')
    if size is not None:
        normalized_size = _normalize_size_for_sora_2_pro_text_to_video(size)
        if normalized_size is None:
            valid_values = ["standard", "high"]
            return False, f"Поле 'size' должно быть одним из: {', '.join(valid_values)}"
        normalized_input['size'] = normalized_size
    
    # Валидация remove_watermark: опциональный boolean
    # Переиспользуем функцию нормализации из sora-2-pro-text-to-video
    remove_watermark = normalized_input.get('remove_watermark')
    if remove_watermark is not None:
        normalized_remove_watermark = _normalize_boolean(remove_watermark)
        if normalized_remove_watermark is None:
            return False, f"Поле 'remove_watermark' должно быть boolean (true/false)"
        normalized_input['remove_watermark'] = normalized_remove_watermark
    
    return True, None
```

### Нормализация параметров

#### image_urls

Переиспользуется функция нормализации из `wan/2-6-image-to-video`:

```python
# Переиспользуем функцию нормализации из wan/2-6-image-to-video
_normalize_image_urls_for_wan_2_6(image_urls)
```

#### aspect_ratio

Переиспользуется функция нормализации из `sora-2-pro-storyboard`:

```python
# Переиспользуем функцию нормализации из sora-2-pro-storyboard
_normalize_aspect_ratio_for_sora_2_pro_storyboard(aspect_ratio)
```

#### n_frames, size, remove_watermark

Переиспользуются функции нормализации из `sora-2-pro-text-to-video`:

```python
# Переиспользуем функции нормализации из sora-2-pro-text-to-video
_normalize_n_frames_for_sora_2_pro_text_to_video(n_frames)
_normalize_size_for_sora_2_pro_text_to_video(size)
_normalize_boolean(remove_watermark)
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id in ["sora-2-pro-image-to-video", "sora-2-pro/i2v", "openai/sora-2-pro-image-to-video"]:
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "landscape"  # Default согласно документации
    if 'n_frames' not in normalized_input:
        normalized_input['n_frames'] = "10"  # Default согласно документации
    if 'size' not in normalized_input:
        normalized_input['size'] = "standard"  # Default согласно документации (отличается от text-to-video, где "high"!)
    if 'remove_watermark' not in normalized_input:
        normalized_input['remove_watermark'] = True  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "sora-2-pro-image-to-video",
  "input": {
    "prompt": "Enter your prompt here...",
    "image_urls": ["https://file.aiquickdraw.com/custom-page/akr/section-images/example.png"],
    "aspect_ratio": "landscape",
    "n_frames": "10",
    "size": "standard",
    "remove_watermark": true
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (максимум 10000 символов) - обязательный!
2. **Валидация image_urls**: Проверяется что это массив валидных URL - обязательный!
3. **Валидация aspect_ratio**: Проверяется что значение "portrait" или "landscape" (опциональный!)
4. **Валидация n_frames**: Проверяется что значение "10" или "15" (опциональный!)
5. **Валидация size**: Проверяется что значение "standard" или "high" (опциональный!)
6. **Валидация remove_watermark**: Проверяется что значение boolean (опциональный!)
7. **Дефолтные значения**: Применяются если параметры не указаны (aspect_ratio="landscape", n_frames="10", size="standard", remove_watermark=true)
8. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=sora-2-pro-image-to-video TYPE=i2v INPUT_KEYS=['prompt', 'image_urls', 'aspect_ratio', 'n_frames', 'size', 'remove_watermark'] 
INPUT_PREVIEW={'prompt': 'Enter your prompt...', 'image_urls': ['https://...'], 'aspect_ratio': 'landscape', 'n_frames': '10', 'size': 'standard', 'remove_watermark': True}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео
2. **prompt максимум 10000 символов** - проверяется при валидации
3. **image_urls обязательный** - обязателен для генерации видео, массив URL
4. **image_urls может быть несколько** - может быть несколько изображений
5. **aspect_ratio опциональный** - не обязательный! Только 2 значения: "portrait" или "landscape"!
6. **n_frames опциональный** - не обязательный! Только 2 значения: "10" или "15"!
7. **size опциональный** - не обязательный! Только 2 значения: "standard" или "high"!
8. **КРИТИЧНО: size default "standard"** - отличается от text-to-video, где default "high"!
9. **remove_watermark опциональный** - не обязательный! Boolean значение!
10. **Дефолтные значения**: aspect_ratio="landscape", n_frames="10", size="standard", remove_watermark=true
11. **Переиспользование кода**: Использует функции нормализации из sora-2-pro-text-to-video и wan/2-6-image-to-video
12. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими моделями

### Сравнение с sora-2-pro-text-to-video

| Параметр | sora-2-pro-text-to-video | sora-2-pro-image-to-video |
|----------|--------------------------|---------------------------|
| `prompt` | Обязательный, max 10000 | Обязательный, max 10000 |
| `image_urls` | Не требуется | Обязательный массив |
| `aspect_ratio` | Опциональный, "portrait"\|"landscape" | Опциональный, "portrait"\|"landscape" |
| `n_frames` | Опциональный, "10"\|"15" | Опциональный, "10"\|"15" |
| `size` | Опциональный, "standard"\|"high" | Опциональный, "standard"\|"high" |
| `size` default | `"high"` | `"standard"` (отличается!) |
| `remove_watermark` | Опциональный boolean | Опциональный boolean |
| Тип модели | t2v | i2v |

### Сравнение с sora-2-pro-storyboard

| Параметр | sora-2-pro-storyboard | sora-2-pro-image-to-video |
|----------|----------------------|---------------------------|
| `prompt` | Не требуется | Обязательный, max 10000 |
| `image_urls` | Опциональный массив | Обязательный массив |
| `n_frames` | Обязательный, "10"\|"15"\|"25" | Опциональный, "10"\|"15" (только 2 значения!) |
| `aspect_ratio` | Опциональный, "portrait"\|"landscape" | Опциональный, "portrait"\|"landscape" |
| `size` | Не требуется | Опциональный, "standard"\|"high" |
| `remove_watermark` | Не требуется | Опциональный boolean |
| Тип модели | i2v (storyboard) | i2v |

### Сравнение с kling-2.6/image-to-video

| Параметр | kling-2.6/image-to-video | sora-2-pro-image-to-video |
|----------|-------------------------|---------------------------|
| `prompt` | Обязательный, max 1000 | Обязательный, max 10000 |
| `image_urls` | Обязательный массив | Обязательный массив |
| `sound` | Обязательный boolean | Не требуется |
| `duration` | Обязательный, "5"\|"10" | Не требуется |
| `aspect_ratio` | Не требуется | Опциональный, "portrait"\|"landscape" |
| `n_frames` | Не требуется | Опциональный, "10"\|"15" |
| `size` | Не требуется | Опциональный, "standard"\|"high" |
| `remove_watermark` | Не требуется | Опциональный boolean |
| Тип модели | i2v | i2v |

## 🎯 Ключевые отличия

1. **prompt обязательный**: Обязателен для генерации видео, максимум 10000 символов
2. **image_urls обязательный**: Обязателен для генерации видео, массив URL (может быть несколько изображений)
3. **aspect_ratio опциональный**: Только 2 значения ("portrait" или "landscape"), default "landscape"
4. **n_frames опциональный**: Только 2 значения ("10" или "15"), default "10"
5. **size опциональный**: Только 2 значения ("standard" или "high"), default "standard" (отличается от text-to-video!)
6. **remove_watermark опциональный**: Boolean значение, default true
7. **Переиспользование кода**: Использует функции нормализации из sora-2-pro-text-to-video и wan/2-6-image-to-video
8. **Нормализация типов**: Все параметры конвертируются с поддержкой алиасов
9. **Дефолтные значения**: Применяются автоматически
10. **Сообщения об ошибках**: Понятные сообщения для пользователя

## 💡 Особенности Pro версии

Модель `sora-2-pro-image-to-video` является Pro версией image-to-video модели:

- **Больше параметров**: Добавлены `size` и `remove_watermark`
- **Гибкость**: `aspect_ratio`, `n_frames`, `size`, `remove_watermark` опциональны
- **Качество**: Поддержка выбора качества через параметр `size`
- **Водяные знаки**: Возможность удаления водяных знаков через параметр `remove_watermark`
- **Отличие от text-to-video**: `size` default "standard" (в text-to-video default "high")

