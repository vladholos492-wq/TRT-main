# Интеграция elevenlabs/audio-isolation

## 📋 Обзор

Документация по интеграции модели `elevenlabs/audio-isolation` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `elevenlabs/audio-isolation`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"elevenlabs/audio-isolation"` |
| `input.audio_url` | string | URL аудио файла для изоляции голоса | Обязательный, макс 10MB, mpeg/wav/aac/mp4/ogg |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `audio_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: `audio/mpeg`, `audio/wav`, `audio/x-wav`, `audio/aac`, `audio/mp4`, `audio/ogg`
- **Важно**: Обязательный параметр! Должен быть валидным URL аудио файла!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | elevenlabs/audio-isolation | Другие audio_isolation модели |
|----------|----------------------------|------------------------------|
| `audio_url` | ✅ Обязательный | ✅ Обязательный |
| `audio` | ❌ Не используется (нормализуется в `audio_url`) | ✅ Может быть обязательным |
| `mode` | ❌ Нет | ✅ Может быть |
| `strength` | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для изоляции голоса из аудио!
- Только один параметр - `audio_url` (обязательный)!
- НЕТ дополнительных параметров (mode, strength и т.д.)!
- Максимальный размер файла: 10MB!

## 🔧 Реализация

### Валидация

Функция `_validate_elevenlabs_audio_isolation()` проверяет:

1. **Обязательность `audio_url`**: Поле должно быть указано (может быть передано как `audio_url` или `audio`)
2. **Нормализация параметра**: Если передан `audio`, нормализуется в `audio_url`
3. **Удаление лишних параметров**: Удаляет `audio`, если `audio_url` уже установлен

### Нормализация

1. **Параметр `audio_url`**: Если передан `audio`, нормализуется в `audio_url`
2. **Удаление дубликатов**: Удаляет `audio`, если `audio_url` уже установлен

### Дефолтные значения

Дефолтные значения не применяются, так как параметр обязательный.

## 📊 Пример использования

### Запрос

```json
{
  "model": "elevenlabs/audio-isolation",
  "input": {
    "audio_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1756964657418ljw1jbzr.mp3"
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'audio_url' обязательно...` | `audio_url` не указан | Загрузите аудио файл для изоляции голоса |
| `Поле 'audio_url' не может быть пустым` | `audio_url` пустой | Укажите валидный URL аудио файла |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("elevenlabs/audio-isolation")
user_payload = {
    "audio_url": "https://example.com/audio.mp3"
    # Или можно использовать:
    # "audio": "https://example.com/audio.mp3"
    # Будет автоматически нормализовано в "audio_url"
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Реализована функция `_validate_elevenlabs_audio_isolation()`
- [x] Добавлена валидация в `build_input()`
- [x] Реализована нормализация параметра `audio_url` из `audio`
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=elevenlabs/audio-isolation TYPE=audio_isolation INPUT_KEYS=['audio_url'] 
INPUT_PREVIEW={'audio_url': 'https://...'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **audio_url обязательный** - обязателен для изоляции голоса (макс 10MB, mpeg/wav/aac/mp4/ogg)
2. **Только один параметр** - модель принимает только `audio_url`, нет дополнительных параметров (mode, strength и т.д.)!
3. **Нормализация** - если передан `audio`, будет автоматически нормализован в `audio_url`!
4. **Форматы файлов** - поддерживаются только указанные форматы (валидация формата на стороне KIE API)
5. **Размер файла** - максимальный размер файла: 10MB (валидация размера на стороне KIE API)

