# Интеграция ideogram/v3-remix

## 📋 Обзор

Документация по интеграции модели `ideogram/v3-remix` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `ideogram/v3-remix`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"ideogram/v3-remix"` |
| `input.prompt` | string | Промпт для ремикса изображения | Обязательный, максимум 5000 символов |
| `input.image_url` | string | URL изображения для ремикса | Обязательный, максимум 10MB, форматы: jpeg/png/webp |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.rendering_speed` | string | Скорость рендеринга | `"BALANCED"` |
| `input.style` | string | Тип стиля для генерации. Не может использоваться вместе с style_codes | `"AUTO"` |
| `input.expand_prompt` | boolean | Определяет, должен ли использоваться MagicPrompt при генерации запроса | `true` |
| `input.image_size` | string | Разрешение сгенерированного изображения | `"square_hd"` |
| `input.num_images` | string | Radio description | `"1"` |
| `input.seed` | number | Seed для генератора случайных чисел | Не указан |
| `input.strength` | number | Сила входного изображения в ремиксе | `0.8` |
| `input.negative_prompt` | string | Описание того, что следует исключить из изображения. Описания в prompt имеют приоритет над описаниями в negative_prompt | `""` (пустая строка) |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов! Промпт для ремикса изображения!

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp! URL изображения для ремикса!

#### `rendering_speed`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"TURBO"`, `"BALANCED"`, `"QUALITY"`
- Default: `"BALANCED"`
- **Важно**: Опциональный параметр! Только 3 указанных значения (enum)! Default: `"BALANCED"`! Скорость рендеринга!

#### `style`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"AUTO"`, `"GENERAL"`, `"REALISTIC"`, `"DESIGN"`
- Default: `"AUTO"`
- **Важно**: Опциональный параметр! Только 4 указанных значения (enum)! Default: `"AUTO"`! Тип стиля для генерации! Не может использоваться вместе с style_codes!

#### `expand_prompt`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean! Default: `true`! Определяет, должен ли использоваться MagicPrompt при генерации запроса!

#### `image_size`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"square"`, `"square_hd"`, `"portrait_4_3"`, `"portrait_16_9"`, `"landscape_4_3"`, `"landscape_16_9"`
- Default: `"square_hd"`
- **Важно**: Опциональный параметр! Только 6 указанных значений (enum)! Default: `"square_hd"`! Разрешение сгенерированного изображения!

#### `num_images`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"1"`, `"2"`, `"3"`, `"4"`
- Default: `"1"`
- **Важно**: Опциональный параметр! Только 4 указанных значения (string enum)! Default: `"1"`! Radio description!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- **Важно**: Опциональный параметр! Число! Seed для генератора случайных чисел!

#### `strength`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0.01-1 (шаг: 0.01)
- Default: `0.8`
- **Важно**: Опциональный параметр! Число от 0.01 до 1 (шаг 0.01)! Default: `0.8`! Сила входного изображения в ремиксе!

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 5000 символов
- Default: `""` (пустая строка!)
- **Важно**: Опциональный параметр! Максимум 5000 символов! Default: `""` (пустая строка!)! Описание того, что следует исключить из изображения! Описания в prompt имеют приоритет над описаниями в negative_prompt!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | ideogram/v3-remix | ideogram/v3-text-to-image | ideogram/v3-edit |
|----------|-------------------|---------------------------|------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный (max 5000) | ✅ Обязательный (max 5000) |
| `image_url` | ✅ Обязательный (max 10MB) | ❌ Нет | ✅ Обязательный (max 10MB) |
| `mask_url` | ❌ Нет | ❌ Нет | ✅ Обязательный (max 10MB) |
| `rendering_speed` | ✅ Есть (enum, default "BALANCED") | ✅ Есть (enum, default "BALANCED") | ✅ Есть (enum, default "BALANCED") |
| `style` | ✅ Есть (enum, default "AUTO") | ✅ Есть (enum, default "AUTO") | ❌ Нет |
| `expand_prompt` | ✅ Есть (boolean, default true) | ✅ Есть (boolean, default true) | ✅ Есть (boolean, default true) |
| `image_size` | ✅ Есть (enum, default "square_hd") | ✅ Есть (enum, default "square_hd") | ❌ Нет |
| `num_images` | ✅ Есть (enum, default "1") | ❌ Нет | ❌ Нет |
| `seed` | ✅ Есть (number) | ✅ Есть (number) | ✅ Есть (number) |
| `strength` | ✅ Есть (0.01-1, шаг 0.01, default 0.8) | ❌ Нет | ❌ Нет |
| `negative_prompt` | ✅ Есть (max 5000, default "") | ✅ Есть (max 5000, default "") | ❌ Нет |

**ВАЖНО**: 
- Это модель для ремикса изображений - V3 версия!
- Использует `rendering_speed` для управления скоростью рендеринга (enum: TURBO/BALANCED/QUALITY, default BALANCED)!
- Использует `style` для управления типом стиля (enum: AUTO/GENERAL/REALISTIC/DESIGN, default AUTO)! Не может использоваться вместе с style_codes!
- Использует `expand_prompt` для управления MagicPrompt (boolean, default true)!
- Использует `image_size` для управления разрешением (enum, 6 значений, default square_hd)!
- Использует `num_images` для управления количеством изображений (string enum: "1", "2", "3", "4", default "1")!
- Использует `strength` для управления силой входного изображения (number, диапазон 0.01-1, шаг 0.01, default 0.8)!
- `negative_prompt` имеет максимум 5000 символов (больше, чем у большинства других моделей!) и default "" (пустая строка!)!
- `prompt` имеет максимум 5000 символов!
- НЕТ параметра `mask_url` (есть в ideogram/v3-edit)!
- **Отличается от ideogram/v3-text-to-image наличием обязательного `image_url` и параметров `num_images`, `strength`!**

## 🔧 Реализация

### Валидация

Функция `_validate_ideogram_v3_remix()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Обязательность `image_url`**: Поле должно быть указано, не пустое, string
3. **Значение `rendering_speed`**: Должно быть одним из 3 допустимых значений (опциональный, enum)
4. **Значение `style`**: Должно быть одним из 4 допустимых значений (опциональный, enum)
5. **Значение `expand_prompt`**: Должно быть boolean (опциональный)
6. **Значение `image_size`**: Должно быть одним из 6 допустимых значений (опциональный, enum)
7. **Значение `num_images`**: Должно быть одним из 4 допустимых значений (опциональный, string enum)
8. **Значение `seed`**: Должно быть числом (опциональный)
9. **Значение `strength`**: Должно быть числом в диапазоне от 0.01 до 1, округляется до 2 знаков после запятой (опциональный)
10. **Длина `negative_prompt`**: Максимум 5000 символов (опциональный)

### Нормализация

1. **`rendering_speed`**: Нормализуется через `_normalize_rendering_speed_for_ideogram_character_edit()`
2. **`style`**: Нормализуется через `_normalize_style_for_ideogram_character_edit()`
3. **`expand_prompt`**: Нормализуется через `_normalize_boolean()`
4. **`image_size`**: Нормализуется через `_normalize_image_size_for_qwen_image_edit()`
5. **`num_images`**: Нормализуется через `_normalize_num_images_for_qwen_image_edit()`
6. **`seed`**: Нормализуется в число (int если целое, float если дробное)
7. **`strength`**: Нормализуется в число с округлением до 2 знаков после запятой (шаг 0.01)
8. **`negative_prompt`**: Обрезается до 5000 символов, default ""

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'rendering_speed' not in normalized_input:
    normalized_input['rendering_speed'] = "BALANCED"  # Default согласно документации
if 'style' not in normalized_input:
    normalized_input['style'] = "AUTO"  # Default согласно документации
if 'expand_prompt' not in normalized_input:
    normalized_input['expand_prompt'] = True  # Default согласно документации
if 'image_size' not in normalized_input:
    normalized_input['image_size'] = "square_hd"  # Default согласно документации
if 'num_images' not in normalized_input:
    normalized_input['num_images'] = "1"  # Default согласно документации
if 'strength' not in normalized_input:
    normalized_input['strength'] = 0.8  # Default согласно документации
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = ""  # Default согласно документации (пустая строка!)
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "ideogram/v3-remix",
  "input": {
    "prompt": "Change the cube into a sphere",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17550782013854ykfihxv.webp",
    "rendering_speed": "BALANCED",
    "style": "AUTO",
    "expand_prompt": true,
    "image_size": "square_hd",
    "num_images": "1",
    "seed": 42,
    "strength": 0.8,
    "negative_prompt": ""
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для ремикса изображения |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения для ремикса |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите непустой URL изображения |
| `Поле 'rendering_speed' должно быть одним из...` | Неверное значение `rendering_speed` | Используйте одно из: TURBO, BALANCED, QUALITY |
| `Поле 'style' должно быть одним из...` | Неверное значение `style` | Используйте одно из: AUTO, GENERAL, REALISTIC, DESIGN |
| `Поле 'expand_prompt' должно быть boolean...` | Неверное значение `expand_prompt` | Используйте true или false |
| `Поле 'image_size' должно быть одним из...` | Неверное значение `image_size` | Используйте одно из: square, square_hd, portrait_4_3, portrait_16_9, landscape_4_3, landscape_16_9 |
| `Поле 'num_images' должно быть одним из...` | Неверное значение `num_images` | Используйте одно из: 1, 2, 3, 4 |
| `Поле 'seed' должно быть числом...` | Неверное значение `seed` | Используйте число |
| `Поле 'strength' должно быть в диапазоне...` | Неверное значение `strength` | Используйте число от 0.01 до 1 |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 5000 символов | Сократите до 5000 символов |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("ideogram/v3-remix")
user_payload = {
    "prompt": "Change the cube into a sphere",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17550782013854ykfihxv.webp",
    "rendering_speed": "BALANCED",
    "style": "AUTO",
    "expand_prompt": True,
    "image_size": "square_hd",
    "num_images": "1",
    "seed": 42,
    "strength": 0.8,
    "negative_prompt": ""
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Параметры уже есть в схеме `i2i` (rendering_speed, style, expand_prompt, image_size, num_images, seed, strength, negative_prompt)
- [x] Реализована функция `_validate_ideogram_v3_remix()`
- [x] Переиспользованы функции нормализации из других ideogram моделей
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=ideogram/v3-remix TYPE=i2i INPUT_KEYS=['prompt', 'image_url', 'rendering_speed', 'style', 'expand_prompt', 'image_size', 'num_images', 'seed', 'strength', 'negative_prompt'] 
INPUT_PREVIEW={'prompt': 'Change the cube into a sphere', 'image_url': 'https://...', 'rendering_speed': 'BALANCED', 'style': 'AUTO', 'expand_prompt': True, 'image_size': 'square_hd', 'num_images': '1', 'seed': 42, 'strength': 0.8, 'negative_prompt': ''}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для ремикса изображения (максимум 5000 символов)! Промпт для ремикса изображения!
2. **image_url обязательный** - обязателен для ремикса изображения! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp! URL изображения для ремикса!
3. **rendering_speed опциональный** - не обязательный! Только 3 значения (enum: TURBO, BALANCED, QUALITY), default "BALANCED"! Скорость рендеринга!
4. **style опциональный** - не обязательный! Только 4 значения (enum: AUTO, GENERAL, REALISTIC, DESIGN), default "AUTO"! Тип стиля для генерации! Не может использоваться вместе с style_codes!
5. **expand_prompt опциональный** - не обязательный! Boolean, default true! Определяет, должен ли использоваться MagicPrompt при генерации запроса!
6. **image_size опциональный** - не обязательный! Только 6 значений (enum), default "square_hd"! Разрешение сгенерированного изображения!
7. **num_images опциональный** - не обязательный! Только 4 значения (string enum: "1", "2", "3", "4"), default "1"! Radio description!
8. **seed опциональный** - не обязательный! Число! Seed для генератора случайных чисел!
9. **strength опциональный** - не обязательный! Число от 0.01 до 1 (шаг 0.01), default 0.8! Сила входного изображения в ремиксе!
10. **negative_prompt опциональный** - не обязательный! Максимум 5000 символов, default "" (пустая строка!)! Описание того, что следует исключить из изображения! Описания в prompt имеют приоритет над описаниями в negative_prompt!
11. **Уникальные параметры** - модель использует `rendering_speed`, `style`, `expand_prompt`, `image_size`, `num_images`, `strength` вместо стандартных `width`, `height`, `guidance`, `steps`!
12. **НЕТ стандартных параметров** - модель НЕ поддерживает `mask_url` (есть в ideogram/v3-edit)!
13. **prompt max 5000** - `prompt` имеет максимум 5000 символов!
14. **negative_prompt max 5000** - `negative_prompt` имеет максимум 5000 символов (больше, чем у большинства других моделей!)!
15. **Отличается от других ideogram v3 моделей** - имеет обязательный `image_url` и параметры `num_images`, `strength` (которых нет в ideogram/v3-text-to-image)!

