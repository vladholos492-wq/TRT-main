# Интеграция hailuo/2-3-image-to-video-standard

## 📋 Обзор

Документация по интеграции модели `hailuo/2-3-image-to-video-standard` согласно официальной документации KIE AI API.

**ВАЖНО**: Параметры идентичны `hailuo/2-3-image-to-video-pro`, но это другая модель (Standard версия)!

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `hailuo/2-3-image-to-video-standard`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"hailuo/2-3-image-to-video-standard"` |
| `input.prompt` | string | Текстовое описание анимации видео | Обязательный, максимум 5000 символов |
| `input.image_url` | string | Входное изображение для анимации | Обязательный, строка (не массив!), максимум 10MB, форматы: JPEG, PNG, WebP |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.duration` | string | Длительность видео в секундах | `"6"` |
| `input.resolution` | string | Разрешение сгенерированного видео | `"768P"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Не может быть пустым! Максимум 5000 символов!

#### `image_url`
- Тип: string
- Обязательный: да
- **Важно**: Строка (не массив!), максимум 10MB, форматы: JPEG, PNG, WebP
- **Важно**: Должен начинаться с `http://` или `https://`

#### `duration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"6"` - 6 секунд (default)
  - `"10"` - 10 секунд
- **Важно**: Опциональный параметр! Только 2 значения!
- **КРИТИЧНО**: Если `resolution="1080P"`, то `duration` не может быть `"10"`! Используйте `duration="6"` или `resolution="768P"`!

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"768P"` - 768P (default)
  - `"1080P"` - 1080P
- **Важно**: Опциональный параметр! Только 2 значения!
- **КРИТИЧНО**: Если `resolution="1080P"`, то `duration` не может быть `"10"`! Используйте `duration="6"` или `resolution="768P"`!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_hailuo_2_3_image_to_video_standard(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для hailuo/2-3-image-to-video-standard согласно документации API.
    
    ВАЖНО: Параметры идентичны hailuo/2-3-image-to-video-pro, но это другая модель!
    """
    # Проверяем оба возможных ID модели
    if model_id not in ["hailuo/2-3-image-to-video-standard", "hailuo/2-3-i2v-standard"]:
        return True, None
    
    # Валидация prompt: обязательный, максимум 5000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации видео"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len > 5000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 5000)"
    
    # Валидация image_url: обязательный, строка (не массив!)
    image_url = normalized_input.get('image_url')
    if not image_url:
        return False, "Поле 'image_url' обязательно для генерации видео. Загрузите изображение"
    
    # Если передан массив, берем первый элемент
    if isinstance(image_url, list) and len(image_url) > 0:
        image_url = str(image_url[0])
    else:
        image_url = str(image_url)
    
    image_url = image_url.strip()
    if len(image_url) == 0:
        return False, "Поле 'image_url' не может быть пустым"
    
    # Базовая валидация URL
    if not (image_url.startswith('http://') or image_url.startswith('https://')):
        return False, f"Поле 'image_url' должно быть валидным URL (начинаться с http:// или https://)"
    
    normalized_input['image_url'] = image_url
    
    # Валидация duration: опциональный, enum ("6" или "10")
    # Переиспользуем функцию нормализации из hailuo/2-3-image-to-video-pro
    duration = normalized_input.get('duration')
    if duration is not None:
        normalized_duration = _normalize_duration_for_hailuo_2_3_pro(duration)
        if normalized_duration is None:
            return False, f"Поле 'duration' должно быть '6' или '10'"
        normalized_input['duration'] = normalized_duration
    
    # Валидация resolution: опциональный, enum ("768P" или "1080P")
    # Переиспользуем функцию нормализации из hailuo/2-3-image-to-video-pro
    resolution = normalized_input.get('resolution')
    if resolution is not None:
        normalized_resolution = _normalize_resolution_for_hailuo_2_3_pro(resolution)
        if normalized_resolution is None:
            valid_values = ["768P", "1080P"]
            return False, f"Поле 'resolution' должно быть одним из: {', '.join(valid_values)}"
        normalized_input['resolution'] = normalized_resolution
    
    # ВАЖНО: Проверяем взаимоисключающие значения
    # Если resolution="1080P", то duration не может быть "10"!
    final_resolution = normalized_input.get('resolution', "768P")  # Default "768P"
    final_duration = normalized_input.get('duration', "6")  # Default "6"
    
    if final_resolution == "1080P" and final_duration == "10":
        return False, "Для разрешения '1080P' не поддерживается длительность '10' секунд. Используйте duration='6' или resolution='768P'"
    
    return True, None
```

### Нормализация duration и resolution

Переиспользуются функции нормализации из `hailuo/2-3-image-to-video-pro`:

```python
# Переиспользуем функции нормализации из hailuo/2-3-image-to-video-pro
_normalize_duration_for_hailuo_2_3_pro(duration)
_normalize_resolution_for_hailuo_2_3_pro(resolution)
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id in ["hailuo/2-3-image-to-video-standard", "hailuo/2-3-i2v-standard"]:
    if 'duration' not in normalized_input:
        normalized_input['duration'] = "6"  # Default согласно документации
    if 'resolution' not in normalized_input:
        normalized_input['resolution'] = "768P"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "hailuo/2-3-image-to-video-standard",
  "input": {
    "prompt": "Two armored medieval knights clash in an intense duel at sunset, cinematic lighting. Metal armor reflects warm golden light from the sun and the glowing swords. Sparks explode as the swords collide. Dynamic camera movement, shallow depth of field, dramatic slow motion. The scene takes place in an open desert battlefield, dust in the air, warm orange sun behind them, epic atmosphere. Highly detailed armor textures, realistic reflections, volumetric lighting, cinematic quality.",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1761736401898mpm67du5.webp",
    "duration": "6",
    "resolution": "768P"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (максимум 5000 символов) - обязательный!
2. **Валидация image_url**: Проверяется что это строка (не массив!), валидный URL - обязательный!
3. **Валидация duration**: Проверяется что значение "6" или "10" (опциональный!)
4. **Валидация resolution**: Проверяется что значение "768P" или "1080P" (опциональный!)
5. **Взаимоисключающие значения**: Проверяется что если resolution="1080P", то duration не может быть "10"!
6. **Дефолтные значения**: Применяются если параметры не указаны (duration="6", resolution="768P")
7. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=hailuo/2-3-image-to-video-standard TYPE=i2v INPUT_KEYS=['prompt', 'image_url', 'duration', 'resolution'] 
INPUT_PREVIEW={'prompt': 'Two armored medieval knights...', 'image_url': 'https://...', 'duration': '6', 'resolution': '768P'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео
2. **prompt максимум 5000 символов** - проверяется при валидации
3. **image_url обязательный** - обязателен для генерации видео
4. **image_url строка (не массив!)** - в отличие от других моделей, здесь image_url - это строка, а не массив!
5. **image_url валидация URL** - должен начинаться с `http://` или `https://`
6. **duration опциональный** - не обязательный! Только 2 значения: "6" или "10"!
7. **resolution опциональный** - не обязательный! Только 2 значения: "768P" или "1080P"!
8. **КРИТИЧНО: взаимоисключающие значения** - если `resolution="1080P"`, то `duration` не может быть `"10"`! Используйте `duration="6"` или `resolution="768P"`!
9. **Дефолтные значения**: duration="6", resolution="768P"
10. **Callback URL опционален** - если не указан, уведомления не отправляются
11. **Переиспользование кода** - использует те же функции нормализации, что и `hailuo/2-3-image-to-video-pro`

## 🔄 Сравнение с другими моделями

### Сравнение с hailuo/2-3-image-to-video-pro

| Параметр | hailuo/2-3-image-to-video-pro | hailuo/2-3-image-to-video-standard |
|----------|-------------------------------|-----------------------------------|
| `prompt` | Обязательный, max 5000 | Обязательный, max 5000 |
| `image_url` | Обязательный строка (не массив!) | Обязательный строка (не массив!) |
| `duration` | Опциональный, "6"\|"10" | Опциональный, "6"\|"10" |
| `resolution` | Опциональный, "768P"\|"1080P" | Опциональный, "768P"\|"1080P" |
| **Взаимоисключающие значения** | Да (1080P + 10s не поддерживается) | Да (1080P + 10s не поддерживается) |
| **Дефолтные значения** | duration="6", resolution="768P" | duration="6", resolution="768P" |
| **Нормализация** | Собственные функции | Переиспользует функции из Pro версии |
| Тип модели | i2v | i2v |
| **Отличия** | Pro версия | Standard версия (возможно, разное качество/скорость) |

### Сравнение с kling-2.6/image-to-video

| Параметр | kling-2.6/image-to-video | hailuo/2-3-image-to-video-standard |
|----------|-------------------------|-----------------------------------|
| `prompt` | Обязательный, max 1000 | Обязательный, max 5000 |
| `image_urls` | Обязательный массив | Не используется |
| `image_url` | Не используется | Обязательный строка (не массив!) |
| `sound` | Обязательный boolean | Не требуется |
| `duration` | Обязательный, "5"\|"10" | Опциональный, "6"\|"10" |
| `resolution` | Не требуется | Опциональный, "768P"\|"1080P" |
| Тип модели | i2v | i2v |

### Сравнение с bytedance/v1-pro-fast-image-to-video

| Параметр | bytedance/v1-pro-fast-image-to-video | hailuo/2-3-image-to-video-standard |
|----------|--------------------------------------|-----------------------------------|
| `prompt` | Обязательный, max 10000 | Обязательный, max 5000 |
| `image_url` | Обязательный строка (не массив!) | Обязательный строка (не массив!) |
| `resolution` | Опциональный, "720p"\|"1080p" | Опциональный, "768P"\|"1080P" |
| `duration` | Опциональный, "5"\|"10" | Опциональный, "6"\|"10" |
| **Взаимоисключающие значения** | Нет | Да (1080P + 10s не поддерживается) |
| Тип модели | i2v | i2v |

## 🎯 Ключевые отличия

1. **prompt обязательный**: Обязателен для генерации видео, максимум 5000 символов
2. **image_url обязательный**: Обязателен для генерации видео, строка (не массив!)
3. **duration опциональный**: Только 2 значения ("6" или "10"), default "6"
4. **resolution опциональный**: Только 2 значения ("768P" или "1080P"), default "768P"
5. **КРИТИЧНО: взаимоисключающие значения**: Если `resolution="1080P"`, то `duration` не может быть `"10"`!
6. **Переиспользование кода**: Использует те же функции нормализации, что и `hailuo/2-3-image-to-video-pro`
7. **Нормализация типов**: duration и resolution конвертируются с поддержкой алиасов
8. **Дефолтные значения**: Применяются автоматически (duration="6", resolution="768P")
9. **Сообщения об ошибках**: Понятные сообщения для пользователя с указанием на взаимоисключающие значения

## 🔄 Взаимоисключающие значения

### Правильные комбинации:

✅ **Разрешено:**
- `duration="6"` + `resolution="768P"` (default)
- `duration="6"` + `resolution="1080P"`
- `duration="10"` + `resolution="768P"`

❌ **Запрещено:**
- `duration="10"` + `resolution="1080P"` - **НЕ поддерживается!**

### Обработка ошибок:

Если пользователь пытается использовать недопустимую комбинацию, система вернёт ошибку:

```
Для разрешения '1080P' не поддерживается длительность '10' секунд. Используйте duration='6' или resolution='768P'
```

## 💡 Отличия от Pro версии

Модели `hailuo/2-3-image-to-video-pro` и `hailuo/2-3-image-to-video-standard` имеют **идентичные параметры**, но это **разные модели**:

- **Pro версия**: Возможно, более высокое качество или дополнительные возможности
- **Standard версия**: Возможно, более быстрая генерация или более низкая цена

Обе модели используют:
- Одинаковые параметры (`prompt`, `image_url`, `duration`, `resolution`)
- Одинаковые ограничения (взаимоисключающие значения)
- Одинаковые дефолтные значения
- Переиспользуют одни и те же функции нормализации

