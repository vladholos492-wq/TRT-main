# Интеграция topaz/image-upscale

## 📋 Обзор

Документация по интеграции модели `topaz/image-upscale` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `topaz/image-upscale`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"topaz/image-upscale"` |
| `input.image_url` | string | URL изображения для увеличения | Обязательный, максимум 10MB, форматы: JPEG, PNG, WebP |
| `input.upscale_factor` | string | Коэффициент увеличения | Обязательный, только "1", "2", "4", или "8" |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `image_url`
- **Тип**: string
- **Обязательный**: да
- **Описание**: URL изображения для увеличения
- **Ограничения**:
  - Максимум 10MB на файл
  - Форматы: JPEG, PNG, WebP
  - Должен быть валидным URL (начинается с `http://` или `https://`)
- **Default**: `"https://static.aiquickdraw.com/tools/example/1762752805607_mErUj1KR.png"`
- **Примеры**:
  - ✅ `https://example.com/image.jpg`
  - ✅ `http://example.com/image.png`
  - ❌ `image.jpg` (нет протокола)

#### `upscale_factor`
- **Тип**: string
- **Обязательный**: да
- **Описание**: Коэффициент увеличения (например, 2.0 удваивает ширину и высоту)
- **Значения**:
  - `"1"` - 1x (без увеличения)
  - `"2"` - 2x (удваивает размер) (default)
  - `"4"` - 4x (увеличивает в 4 раза)
  - `"8"` - 8x (увеличивает в 8 раз)
- **Default**: `"2"`
- **Важно**: Обязательный параметр! Только 4 значения!
- **Поддерживаемые форматы**:
  - `"1"`, `"1x"`, `"1.0"`, `"1.0x"` → `"1"`
  - `"2"`, `"2x"`, `"2.0"`, `"2.0x"` → `"2"`
  - `"4"`, `"4x"`, `"4.0"`, `"4.0x"` → `"4"`
  - `"8"`, `"8x"`, `"8.0"`, `"8.0x"` → `"8"`

## 🔧 Реализация

### Валидация

Функция `_validate_topaz_image_upscale()` проверяет:

1. **Обязательность `image_url`**: Поле должно быть указано
2. **Формат URL**: Должен начинаться с `http://` или `https://`
3. **Обязательность `upscale_factor`**: Поле должно быть указано
4. **Значение `upscale_factor`**: Должно быть одним из "1", "2", "4", "8"

### Нормализация

Функция `_normalize_upscale_factor_for_topaz()`:

1. Конвертирует значение в строку
2. Удаляет пробелы (trim)
3. Проверяет что значение одно из: "1", "2", "4", "8"
4. Поддерживает алиасы: "1x", "2x", "4x", "8x", "1.0", "2.0", и т.д.
5. Возвращает нормализованную строку или `None` если невалидный

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'image_url' not in normalized_input:
    normalized_input['image_url'] = "https://static.aiquickdraw.com/tools/example/1762752805607_mErUj1KR.png"
if 'upscale_factor' not in normalized_input:
    normalized_input['upscale_factor'] = "2"
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "topaz/image-upscale",
  "input": {
    "image_url": "https://static.aiquickdraw.com/tools/example/1762752805607_mErUj1KR.png",
    "upscale_factor": "2"
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения |
| `Поле 'image_url' должно быть валидным URL...` | URL не начинается с `http://` или `https://` | Используйте полный URL с протоколом |
| `Поле 'upscale_factor' обязательно...` | `upscale_factor` не указан | Укажите коэффициент увеличения: '1', '2', '4' или '8' |
| `Поле 'upscale_factor' должно быть одним из...` | Неверное значение `upscale_factor` | Используйте одно из: "1", "2", "4", "8" |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("topaz/image-upscale")
user_payload = {
    "image_url": "https://static.aiquickdraw.com/tools/example/1762752805607_mErUj1KR.png",
    "upscale_factor": "2"
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлена схема `upscale` с полями `image_url` и `upscale_factor`
- [x] Реализована функция `_normalize_upscale_factor_for_topaz()`
- [x] Реализована функция `_validate_topaz_image_upscale()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=topaz/image-upscale TYPE=upscale INPUT_KEYS=['image_url', 'upscale_factor'] 
INPUT_PREVIEW={'image_url': 'https://...', 'upscale_factor': '2'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **image_url обязательный** - обязателен для увеличения изображения
2. **image_url должен быть валидным URL** - должен начинаться с `http://` или `https://`
3. **upscale_factor обязательный** - обязателен для увеличения изображения
4. **upscale_factor только 4 значения** - только "1", "2", "4", "8"!
5. **upscale_factor default "2"** - если не указан, используется "2"

