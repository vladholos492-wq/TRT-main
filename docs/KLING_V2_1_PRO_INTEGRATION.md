# Интеграция kling/v2-1-pro

## 📋 Обзор

Документация по интеграции модели `kling/v2-1-pro` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `kling/v2-1-pro`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"kling/v2-1-pro"` |
| `input.prompt` | string | Текстовый промпт, описывающий видео для генерации | Обязательный, максимум 5000 символов |
| `input.image_url` | string | URL изображения для использования в видео | Обязательный, максимум 10MB, форматы: jpeg/png/webp |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.duration` | string | Длительность сгенерированного видео в секундах | `"5"` |
| `input.negative_prompt` | string | Термины, которых следует избегать в сгенерированном видео | `"blur, distort, and low quality"` |
| `input.cfg_scale` | number | CFG (Classifier Free Guidance) scale - мера того, насколько близко модель должна следовать вашему промпту | `0.5` |
| `input.tail_image_url` | string | URL изображения для использования в конце видео | `""` (пустая строка) |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp!

#### `duration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"5"`, `"10"`
- Default: `"5"`
- **Важно**: Опциональный параметр! Только 2 указанных значения (string enum)! Default: `"5"`! Длительность видео в секундах!

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 500 символов
- Default: `"blur, distort, and low quality"`
- **Важно**: Опциональный параметр! Максимум 500 символов! Default: `"blur, distort, and low quality"`! Термины, которых следует избегать в сгенерированном видео!

#### `cfg_scale`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0-1 (шаг: 0.1)
- Default: `0.5`
- **Важно**: Опциональный параметр! Число от 0 до 1 (шаг 0.1)! Default: `0.5`! CFG (Classifier Free Guidance) scale - мера того, насколько близко модель должна следовать вашему промпту!

#### `tail_image_url`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимальный размер файла: 10MB (если указан)
- Форматы: `image/jpeg`, `image/png`, `image/webp` (если указан)
- Default: `""` (пустая строка!)
- **Важно**: Опциональный параметр! URL изображения для использования в конце видео! Default: `""` (пустая строка!)! Максимум 10MB! Форматы: jpeg/png/webp!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | kling/v2-1-pro | kling/v2-1-standard | kling/v2-1-master-image-to-video |
|----------|----------------|---------------------|-----------------------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный (max 5000) | ✅ Обязательный (max 5000) |
| `image_url` | ✅ Обязательный (max 10MB) | ✅ Обязательный (max 10MB) | ✅ Обязательный (max 10MB) |
| `duration` | ✅ Есть (enum: "5"/"10") | ✅ Есть (enum: "5"/"10") | ✅ Есть (enum: "5"/"10") |
| `negative_prompt` | ✅ Есть (max 500, default "blur, distort, and low quality") | ✅ Есть (max 500, default "blur, distort, and low quality") | ✅ Есть (max 500, default "blur, distort, and low quality") |
| `cfg_scale` | ✅ Есть (0-1, шаг 0.1, default 0.5) | ✅ Есть (0-1, шаг 0.1, default 0.5) | ✅ Есть (0-1, шаг 0.1, default 0.5) |
| `tail_image_url` | ✅ Есть (default "") | ❌ Нет | ❌ Нет |
| `resolution` | ❌ Нет | ❌ Нет | ❌ Нет |
| `camera_fixed` | ❌ Нет | ❌ Нет | ❌ Нет |
| `seed` | ❌ Нет | ❌ Нет | ❌ Нет |
| `enable_safety_checker` | ❌ Нет | ❌ Нет | ❌ Нет |
| `fps` | ❌ Нет | ❌ Нет | ❌ Нет |
| `with_audio` | ❌ Нет | ❌ Нет | ❌ Нет |
| `width` | ❌ Нет | ❌ Нет | ❌ Нет |
| `height` | ❌ Нет | ❌ Нет | ❌ Нет |
| `guidance` | ❌ Нет | ❌ Нет | ❌ Нет |
| `steps` | ❌ Нет | ❌ Нет | ❌ Нет |
| `motion` | ❌ Нет | ❌ Нет | ❌ Нет |
| `style` | ❌ Нет | ❌ Нет | ❌ Нет |
| `strength` | ❌ Нет | ❌ Нет | ❌ Нет |

**ВАЖНО**: 
- Это модель для генерации видео из изображения (image-to-video) - Pro версия!
- Использует `duration` как string enum ("5" или "10")!
- Использует `cfg_scale` для управления следованием промпту (диапазон 0-1, шаг 0.1, default 0.5)!
- `negative_prompt` имеет default "blur, distort, and low quality" (не пустая строка!)!
- `tail_image_url` имеет default "" (пустая строка!) - уникальный параметр для Pro версии!
- `prompt` имеет максимум 5000 символов!
- НЕТ параметров `resolution`, `camera_fixed`, `seed`, `enable_safety_checker`, `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `motion`, `style`, `strength` (в отличие от других i2v моделей)!
- **Отличается от kling/v2-1-standard и kling/v2-1-master-image-to-video наличием параметра `tail_image_url`!**

## 🔧 Реализация

### Валидация

Функция `_validate_kling_v2_1_pro()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Обязательность `image_url`**: Поле должно быть указано, не пустое, string
3. **Значение `duration`**: Должно быть одним из 2 допустимых значений (опциональный, string enum)
4. **Длина `negative_prompt`**: Максимум 500 символов (опциональный)
5. **Значение `cfg_scale`**: Должно быть числом в диапазоне от 0 до 1, округляется до 1 знака после запятой (опциональный)
6. **Значение `tail_image_url`**: Опциональный, если указан - должен быть строкой (опциональный, default "")

### Нормализация

1. **`duration`**: Нормализуется в строку ("5" или "10") если передан как число
2. **`cfg_scale`**: Нормализуется в число с округлением до 1 знака после запятой (шаг 0.1)
3. **`tail_image_url`**: Нормализуется в строку, если пустой - устанавливается в "" (default)

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'duration' not in normalized_input:
    normalized_input['duration'] = "5"  # Default согласно документации
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = "blur, distort, and low quality"  # Default согласно документации
if 'cfg_scale' not in normalized_input:
    normalized_input['cfg_scale'] = 0.5  # Default согласно документации
if 'tail_image_url' not in normalized_input:
    normalized_input['tail_image_url'] = ""  # Default согласно документации (пустая строка!)
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "kling/v2-1-pro",
  "input": {
    "prompt": "POV shot of a gravity surfer diving between ancient ruins suspended midair, glowing moss lights the path, the board hisses as it carves through thin mist, echoes rise with speed ",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1754892534386c8wt0qfs.webp",
    "duration": "5",
    "negative_prompt": "blur, distort, and low quality",
    "cfg_scale": 0.5,
    "tail_image_url": ""
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации видео |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения для генерации видео |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите непустой URL изображения |
| `Поле 'duration' должно быть одним из...` | Неверное значение `duration` | Используйте одно из: 5, 10 |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 500 символов | Сократите до 500 символов |
| `Поле 'cfg_scale' должно быть в диапазоне...` | Неверное значение `cfg_scale` | Используйте число от 0 до 1 |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("kling/v2-1-pro")
user_payload = {
    "prompt": "POV shot of a gravity surfer diving between ancient ruins...",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1754892534386c8wt0qfs.webp",
    "duration": "5",
    "negative_prompt": "blur, distort, and low quality",
    "cfg_scale": 0.5,
    "tail_image_url": ""
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Параметры уже есть в схеме `i2v` (cfg_scale, negative_prompt, tail_image_url)
- [x] Реализована функция `_validate_kling_v2_1_pro()`
- [x] Переиспользованы функции нормализации из других моделей
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=kling/v2-1-pro TYPE=i2v INPUT_KEYS=['prompt', 'image_url', 'duration', 'negative_prompt', 'cfg_scale', 'tail_image_url'] 
INPUT_PREVIEW={'prompt': 'POV shot of a gravity surfer...', 'image_url': 'https://...', 'duration': '5', 'negative_prompt': 'blur, distort, and low quality', 'cfg_scale': 0.5, 'tail_image_url': ''}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео (максимум 5000 символов)
2. **image_url обязательный** - обязателен для генерации видео! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp!
3. **duration опциональный** - не обязательный! Только 2 значения (string enum: "5", "10"), default "5"! Длительность видео в секундах!
4. **negative_prompt опциональный** - не обязательный! Максимум 500 символов, default "blur, distort, and low quality"! Термины, которых следует избегать в сгенерированном видео!
5. **cfg_scale опциональный** - не обязательный! Число от 0 до 1 (шаг 0.1), default 0.5! CFG (Classifier Free Guidance) scale - мера того, насколько близко модель должна следовать вашему промпту!
6. **tail_image_url опциональный** - не обязательный! URL изображения для использования в конце видео! Default: `""` (пустая строка!)! Максимум 10MB! Форматы: jpeg/png/webp! **Уникальный параметр для Pro версии!**
7. **Уникальные параметры** - модель использует `cfg_scale`, `negative_prompt` и `tail_image_url` (уникальный!) вместо стандартных `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `motion`, `style`, `strength`!
8. **НЕТ стандартных параметров** - модель НЕ поддерживает `resolution`, `camera_fixed`, `seed`, `enable_safety_checker`, `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `motion`, `style`, `strength` (в отличие от других i2v моделей)!
9. **prompt max 5000** - `prompt` имеет максимум 5000 символов!
10. **Отличается от других v2-1 моделей** - имеет параметр `tail_image_url` (default ""), которого нет в `kling/v2-1-standard` и `kling/v2-1-master-image-to-video`!













