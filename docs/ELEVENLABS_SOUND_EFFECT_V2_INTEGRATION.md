# Интеграция elevenlabs/sound-effect-v2

## 📋 Обзор

Документация по интеграции модели `elevenlabs/sound-effect-v2` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `elevenlabs/sound-effect-v2`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"elevenlabs/sound-effect-v2"` |
| `input.text` | string | Текстовое описание звукового эффекта для генерации | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.loop` | boolean | Создавать ли звуковой эффект, который плавно зацикливается | `false` |
| `input.duration_seconds` | number | Длительность в секундах (0.5-22). Если не указано, оптимальная длительность определяется из промпта | Не указан |
| `input.prompt_influence` | number | Насколько точно следовать промпту (0-1). Более высокие значения означают меньше вариаций | `0.3` |
| `input.output_format` | string | Формат выходного аудио. Форматируется как codec_sample_rate_bitrate | `"mp3_44100_128"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `text`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов! Параметр называется `text` (не `prompt`), но `prompt` автоматически нормализуется в `text`!

#### `loop`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `false`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `false`!

#### `duration_seconds`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0.5-22 (шаг: 0.1)
- **Важно**: Опциональный параметр! Только числа от 0.5 до 22! Если не указано, оптимальная длительность определяется из промпта!

#### `prompt_influence`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0-1 (шаг: 0.01)
- Default: `0.3`
- **Важно**: Опциональный параметр! Только числа от 0 до 1! Default: `0.3`! Более высокие значения означают меньше вариаций!

#### `output_format`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"mp3_22050_32"` - MP3 22050Hz 32kbps
  - `"mp3_44100_32"` - MP3 44100Hz 32kbps
  - `"mp3_44100_64"` - MP3 44100Hz 64kbps
  - `"mp3_44100_96"` - MP3 44100Hz 96kbps
  - `"mp3_44100_128"` - MP3 44100Hz 128kbps (default)
  - `"mp3_44100_192"` - MP3 44100Hz 192kbps
  - `"pcm_8000"` - PCM 8000Hz
  - `"pcm_16000"` - PCM 16000Hz
  - `"pcm_22050"` - PCM 22050Hz
  - `"pcm_24000"` - PCM 24000Hz
  - `"pcm_44100"` - PCM 44100Hz
  - `"pcm_48000"` - PCM 48000Hz
  - `"ulaw_8000"` - μ-law 8000Hz
  - `"alaw_8000"` - A-law 8000Hz
  - `"opus_48000_32"` - Opus 48000Hz 32kbps
  - `"opus_48000_64"` - Opus 48000Hz 64kbps
  - `"opus_48000_96"` - Opus 48000Hz 96kbps
  - `"opus_48000_128"` - Opus 48000Hz 128kbps
  - `"opus_48000_192"` - Opus 48000Hz 192kbps
- **Важно**: Опциональный параметр! Только указанные значения! Default: `"mp3_44100_128"`!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | elevenlabs/sound-effect-v2 | Другие sfx модели |
|----------|----------------------------|-------------------|
| `text` | ✅ Обязательный (max 5000) | ✅ Обязательный (обычно `prompt`) |
| `prompt` | ❌ Нет (нормализуется в `text`) | ✅ Может быть обязательным |
| `loop` | ✅ Есть (boolean) | ❌ Нет |
| `duration_seconds` | ✅ Есть (0.5-22) | ❌ Нет (используют `duration`) |
| `prompt_influence` | ✅ Есть (0-1) | ❌ Нет |
| `output_format` | ✅ Есть (enum, 18 значений) | ❌ Нет |
| `duration` | ❌ Нет | ✅ Может быть |
| `style` | ❌ Нет | ✅ Может быть |
| `seed` | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для генерации звуковых эффектов!
- Использует `text` (не `prompt`) для описания звукового эффекта!
- Использует `duration_seconds` (0.5-22) вместо стандартного `duration`!
- Имеет уникальные параметры: `loop`, `prompt_influence`, `output_format`!
- НЕТ параметров `style` и `seed` (в отличие от других sfx моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_elevenlabs_sound_effect_v2()` проверяет:

1. **Обязательность `text`**: Поле должно быть указано (может быть передано как `text` или `prompt`)
2. **Длина `text`**: Максимум 5000 символов
3. **Значение `loop`**: Должно быть boolean (опциональный)
4. **Значение `duration_seconds`**: Должно быть числом от 0.5 до 22 (опциональный)
5. **Значение `prompt_influence`**: Должно быть числом от 0 до 1 (опциональный)
6. **Значение `output_format`**: Должно быть одним из 18 допустимых значений (опциональный)

### Нормализация

1. **Параметр `text`**: Если передан `prompt`, нормализуется в `text`
2. **`_normalize_boolean()`**: Нормализует loop (boolean) - переиспользуется из других моделей
3. **`_normalize_output_format_for_elevenlabs_sound_effect_v2()`**: Нормализует output_format (18 значений)

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'loop' not in normalized_input:
    normalized_input['loop'] = False  # Default согласно документации
if 'prompt_influence' not in normalized_input:
    normalized_input['prompt_influence'] = 0.3  # Default согласно документации
if 'output_format' not in normalized_input:
    normalized_input['output_format'] = "mp3_44100_128"  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "elevenlabs/sound-effect-v2",
  "input": {
    "text": "Enter your prompt here...",
    "loop": false,
    "duration_seconds": 42,
    "prompt_influence": 0.3,
    "output_format": "mp3_44100_128"
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'text' обязательно...` | `text` не указан | Введите текстовое описание звукового эффекта |
| `Поле 'text' не может быть пустым` | `text` пустой | Введите непустое текстовое описание |
| `Поле 'text' слишком длинное...` | `text` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'loop' должно быть boolean...` | Неверное значение `loop` | Используйте true/false |
| `Поле 'duration_seconds' должно быть в диапазоне...` | Неверное значение `duration_seconds` | Используйте число от 0.5 до 22 |
| `Поле 'prompt_influence' должно быть в диапазоне...` | Неверное значение `prompt_influence` | Используйте число от 0 до 1 |
| `Поле 'output_format' должно быть одним из...` | Неверное значение `output_format` | Используйте одно из 18 допустимых значений |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("elevenlabs/sound-effect-v2")
user_payload = {
    "text": "Enter your prompt here...",
    "loop": False,
    "duration_seconds": 42,
    "prompt_influence": 0.3,
    "output_format": "mp3_44100_128"
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлены параметры `text`, `loop`, `duration_seconds`, `prompt_influence`, `output_format` в схему `sfx`
- [x] Реализована функция `_validate_elevenlabs_sound_effect_v2()`
- [x] Добавлена функция нормализации `_normalize_output_format_for_elevenlabs_sound_effect_v2()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=elevenlabs/sound-effect-v2 TYPE=sfx INPUT_KEYS=['text', 'loop', 'duration_seconds', 'prompt_influence', 'output_format'] 
INPUT_PREVIEW={'text': 'Enter your prompt here...', 'loop': False, 'duration_seconds': 42, 'prompt_influence': 0.3, 'output_format': 'mp3_44100_128'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **text обязательный** - обязателен для генерации звукового эффекта (максимум 5000 символов)
2. **Параметр называется `text`** - не `prompt`! Но если передан `prompt`, будет автоматически нормализован в `text`!
3. **loop опциональный** - не обязательный! Boolean (true/false), default false!
4. **duration_seconds опциональный** - не обязательный! Только числа от 0.5 до 22! Если не указано, оптимальная длительность определяется из промпта!
5. **prompt_influence опциональный** - не обязательный! Только числа от 0 до 1, default 0.3! Более высокие значения означают меньше вариаций!
6. **output_format опциональный** - не обязательный! Только 18 значений, default "mp3_44100_128"!
7. **Уникальные параметры** - модель использует `text`, `duration_seconds`, `loop`, `prompt_influence`, `output_format` вместо стандартных `prompt`, `duration`, `style`, `seed`!

