# Интеграция z-image

## 📋 Обзор

Документация по интеграции модели `z-image` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `z-image`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"z-image"` |
| `input.prompt` | string | Текстовое описание изображения | Обязательный, максимум 1000 символов |
| `input.aspect_ratio` | string | Соотношение сторон изображения | Обязательный, один из: "1:1", "4:3", "3:4", "16:9", "9:16" |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 1000 символов
- **Важно**: Не может быть пустым! Максимум 1000 символов (не 3000!)

#### `aspect_ratio`
- Тип: string
- Обязательный: да
- Значения:
  - `"1:1"` - Квадрат (default)
  - `"4:3"` - Горизонтальный 4:3
  - `"3:4"` - Вертикальный 3:4
  - `"16:9"` - Горизонтальный 16:9
  - `"9:16"` - Вертикальный 9:16
- **Важно**: Обязательный параметр, не опциональный! Только 5 значений (не 8 как у seedream)!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_z_image(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для z-image согласно документации API.
    """
    if model_id != "z-image":
        return True, None
    
    # Валидация prompt: обязательный, максимум 1000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации изображения"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len > 1000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 1000)"
    
    # Валидация aspect_ratio: обязательный, "1:1" | "4:3" | "3:4" | "16:9" | "9:16"
    aspect_ratio = normalized_input.get('aspect_ratio')
    if not aspect_ratio:
        return False, "Поле 'aspect_ratio' обязательно для генерации изображения"
    
    normalized_aspect_ratio = _normalize_aspect_ratio_for_z_image(aspect_ratio)
    if normalized_aspect_ratio is None:
        valid_values = ["1:1", "4:3", "3:4", "16:9", "9:16"]
        return False, f"Поле 'aspect_ratio' должно быть одним из: {', '.join(valid_values)}"
    normalized_input['aspect_ratio'] = normalized_aspect_ratio
    
    return True, None
```

### Нормализация aspect_ratio

Функция нормализации для z-image поддерживает только 5 значений (в отличие от seedream с 8):

```python
def _normalize_aspect_ratio_for_z_image(value: Any) -> Optional[str]:
    """
    Нормализует aspect_ratio для z-image.
    ВАЖНО: Для z-image поддерживаются только "1:1", "4:3", "3:4", "16:9", "9:16"!
    """
    valid_values = ["1:1", "4:3", "3:4", "16:9", "9:16"]
    if str_value in valid_values:
        return str_value
    
    # Пробуем нормализовать варианты написания
    str_lower = str_value.lower()
    if str_lower in ["1:1", "1/1", "1x1", "square"]:
        return "1:1"
    elif str_lower in ["4:3", "4/3", "4x3"]:
        return "4:3"
    elif str_lower in ["3:4", "3/4", "3x4"]:
        return "3:4"
    elif str_lower in ["16:9", "16/9", "16x9", "landscape", "wide"]:
        return "16:9"
    elif str_lower in ["9:16", "9/16", "9x16", "portrait", "vertical"]:
        return "9:16"
    
    return None
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id == "z-image":
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "1:1"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "z-image",
  "input": {
    "prompt": "A hyper-realistic, close-up portrait of a 30-year-old mixed-heritage French-Italian woman drinking coffee from a cup that says \"Z-Image × Kie AI.\" Natural light. Shot on a Leica M6 with a Kodak Portra 400 film-grain aesthetic.",
    "aspect_ratio": "1:1"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (максимум 1000 символов)
2. **Валидация aspect_ratio**: Проверяется что значение из допустимого списка (только 5 значений!)
3. **Дефолтные значения**: Применяются если параметры не указаны (aspect_ratio="1:1")
4. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=z-image TYPE=t2i INPUT_KEYS=['prompt', 'aspect_ratio'] 
INPUT_PREVIEW={'prompt': 'A hyper-realistic...', 'aspect_ratio': '1:1'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt максимум 1000 символов** - проверяется при валидации (не 3000!)
2. **aspect_ratio обязательный** - не опциональный! Только 5 значений: "1:1", "4:3", "3:4", "16:9", "9:16" (не 8 как у seedream!)
3. **Нет параметра quality** - в отличие от seedream/4.5-text-to-image, z-image не имеет параметра quality
4. **Дефолтные значения**: aspect_ratio="1:1"
5. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими моделями

### Сравнение с seedream/4.5-text-to-image

| Параметр | seedream/4.5-text-to-image | z-image |
|----------|----------------------------|---------|
| `prompt` | Максимум 3000 символов | Максимум 1000 символов |
| `aspect_ratio` | 8 значений | 5 значений |
| `quality` | Обязательный, "basic"\|"high" | Не требуется |
| Тип модели | t2i | t2i |

### Сравнение с kling-2.6/text-to-video

| Параметр | kling-2.6/text-to-video | z-image |
|----------|--------------------------|---------|
| `prompt` | Максимум 1000 символов | Максимум 1000 символов |
| `aspect_ratio` | 3 значения ("1:1", "16:9", "9:16") | 5 значений |
| `sound` | Обязательный boolean | Не требуется |
| `duration` | Обязательный, "5"\|"10" | Не требуется |
| Тип модели | t2v | t2i |

## 🎯 Ключевые отличия

1. **Простая модель**: Только 2 обязательных параметра (prompt и aspect_ratio)
2. **Ограничение prompt**: Максимум 1000 символов (не 3000)
3. **Ограничение aspect_ratio**: Только 5 значений (не 8 как у seedream)
4. **Нет quality**: В отличие от seedream, не имеет параметра quality
5. **Дефолтный aspect_ratio**: "1:1" если не указан

