# Интеграция kling/v2-5-turbo-text-to-video-pro

## 📋 Обзор

Документация по интеграции модели `kling/v2-5-turbo-text-to-video-pro` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `kling/v2-5-turbo-text-to-video-pro`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"kling/v2-5-turbo-text-to-video-pro"` |
| `input.prompt` | string | Текстовое описание видео | Обязательный, максимум 2500 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.duration` | string | Длительность видео в секундах | `"5"` |
| `input.aspect_ratio` | string | Соотношение сторон видео | `"16:9"` |
| `input.negative_prompt` | string | Что избегать в видео | `"blur, distort, and low quality"` |
| `input.cfg_scale` | number | CFG scale (насколько близко следовать промпту) | `0.5` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 2500 символов
- **Важно**: Не может быть пустым! Максимум 2500 символов (меньше чем у других моделей)!

#### `duration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"5"` - 5 секунд (default)
  - `"10"` - 10 секунд
- **Важно**: Опциональный параметр! Только 2 значения!

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"16:9"` - Широкоэкранный формат (default)
  - `"9:16"` - Вертикальный формат
  - `"1:1"` - Квадратный формат
- **Важно**: Опциональный параметр! Только 3 значения (отличается от других моделей Kling)!
- **Поддерживаемые алиасы**:
  - `"16:9"`, `"16/9"`, `"16x9"`, `"widescreen"`, `"landscape"` → `"16:9"`
  - `"9:16"`, `"9/16"`, `"9x16"`, `"portrait"`, `"vertical"` → `"9:16"`
  - `"1:1"`, `"1/1"`, `"1x1"`, `"square"` → `"1:1"`

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 2500 символов
- Default: `"blur, distort, and low quality"`
- **Важно**: Опциональный параметр! Максимум 2500 символов!

#### `cfg_scale`
- Тип: number (float)
- Обязательный: нет (опциональный!)
- Диапазон: 0 - 1
- Шаг: 0.1
- Default: `0.5`
- **Важно**: Опциональный параметр! Число от 0 до 1 (шаг 0.1)!
- **Описание**: CFG (Classifier Free Guidance) scale - мера того, насколько близко модель должна следовать промпту

## 🔧 Реализация

### Валидация

Функция `_validate_kling_v2_5_turbo_text_to_video_pro()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано
2. **Длина `prompt`**: Максимум 2500 символов (меньше чем у других моделей!)
3. **Значение `duration`**: Должно быть "5" или "10" (опциональный)
4. **Значение `aspect_ratio`**: Должно быть "16:9", "9:16" или "1:1" (опциональный)
5. **Длина `negative_prompt`**: Максимум 2500 символов (опциональный)
6. **Значение `cfg_scale`**: Должно быть числом от 0 до 1, шаг 0.1 (опциональный)

### Нормализация

1. **`_normalize_duration_for_kling_v2_5_turbo()`**: Нормализует duration ("5" | "10")
2. **`_normalize_aspect_ratio_for_kling_v2_5_turbo()`**: Нормализует aspect_ratio ("16:9" | "9:16" | "1:1")
3. **`_normalize_cfg_scale()`**: Нормализует cfg_scale (число 0-1, шаг 0.1)

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'duration' not in normalized_input:
    normalized_input['duration'] = "5"  # Default согласно документации
if 'aspect_ratio' not in normalized_input:
    normalized_input['aspect_ratio'] = "16:9"  # Default согласно документации
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = "blur, distort, and low quality"  # Default согласно документации
if 'cfg_scale' not in normalized_input:
    normalized_input['cfg_scale'] = 0.5  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "kling/v2-5-turbo-text-to-video-pro",
  "input": {
    "prompt": "Real-time playback. Wide shot of a ruined city: collapsed towers, fires blazing, storm clouds with lightning.",
    "duration": "5",
    "aspect_ratio": "16:9",
    "negative_prompt": "blur, distort, and low quality",
    "cfg_scale": 0.5
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Укажите текстовое описание видео |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 2500 символов | Сократите описание до 2500 символов |
| `Поле 'duration' должно быть одним из...` | Неверное значение `duration` | Используйте "5" или "10" |
| `Поле 'aspect_ratio' должно быть одним из...` | Неверное значение `aspect_ratio` | Используйте "16:9", "9:16" или "1:1" |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 2500 символов | Сократите до 2500 символов |
| `Поле 'cfg_scale' должно быть числом...` | Неверное значение `cfg_scale` | Используйте число от 0 до 1 (шаг 0.1) |

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Отличия от других моделей Kling

| Параметр | kling/v2-5-turbo-text-to-video-pro | kling-2.6/text-to-video |
|----------|-----------------------------------|------------------------|
| `prompt` max | 2500 символов | 1000 символов |
| `aspect_ratio` | "16:9" \| "9:16" \| "1:1" | "1:1" \| "16:9" \| "9:16" |
| `duration` | "5" \| "10" | "5" \| "10" |
| `negative_prompt` | ✅ Есть (max 2500) | ❌ Нет |
| `cfg_scale` | ✅ Есть (0-1) | ❌ Нет |
| `sound` | ❌ Нет | ✅ Есть (boolean) |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("kling/v2-5-turbo-text-to-video-pro")
user_payload = {
    "prompt": "Real-time playback. Wide shot of a ruined city...",
    "duration": "5",
    "aspect_ratio": "16:9",
    "negative_prompt": "blur, distort, and low quality",
    "cfg_scale": 0.5
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлена схема `t2v` с полем `cfg_scale`
- [x] Реализована функция `_normalize_duration_for_kling_v2_5_turbo()`
- [x] Реализована функция `_normalize_aspect_ratio_for_kling_v2_5_turbo()`
- [x] Реализована функция `_normalize_cfg_scale()`
- [x] Реализована функция `_validate_kling_v2_5_turbo_text_to_video_pro()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=kling/v2-5-turbo-text-to-video-pro TYPE=t2v INPUT_KEYS=['prompt', 'duration', 'aspect_ratio', 'negative_prompt', 'cfg_scale'] 
INPUT_PREVIEW={'prompt': 'Real-time playback...', 'duration': '5', 'aspect_ratio': '16:9', 'negative_prompt': 'blur, distort, and low quality', 'cfg_scale': 0.5}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео
2. **prompt максимум 2500 символов** - меньше чем у других моделей (обычно 10000)!
3. **duration опциональный** - не обязательный! Только 2 значения: "5" или "10"!
4. **aspect_ratio опциональный** - не обязательный! Только 3 значения: "16:9", "9:16", "1:1"!
5. **negative_prompt опциональный** - не обязательный! Максимум 2500 символов!
6. **cfg_scale опциональный** - не обязательный! Число от 0 до 1, шаг 0.1!
7. **Отличия от других моделей Kling** - другие значения aspect_ratio, есть negative_prompt и cfg_scale, нет sound!

