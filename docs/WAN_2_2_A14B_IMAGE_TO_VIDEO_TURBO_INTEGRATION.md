# Интеграция wan/2-2-a14b-image-to-video-turbo

## 📋 Обзор

Документация по интеграции модели `wan/2-2-a14b-image-to-video-turbo` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `wan/2-2-a14b-image-to-video-turbo`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"wan/2-2-a14b-image-to-video-turbo"` |
| `input.image_url` | string | URL входного изображения. Если входное изображение не соответствует выбранному соотношению сторон, оно изменяется и обрезается по центру | Обязательный, максимум 10MB, форматы: jpeg/png/webp |
| `input.prompt` | string | Текстовый промпт для генерации видео | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.resolution` | string | Разрешение сгенерированного видео (480p, 580p или 720p) | `"720p"` |
| `input.aspect_ratio` | string | Соотношение сторон сгенерированного видео. Если 'auto', соотношение сторон будет определено автоматически на основе входного изображения | `"auto"` |
| `input.enable_prompt_expansion` | boolean | Включить ли расширение промпта. Это будет использовать большую языковую модель для расширения промпта с дополнительными деталями, сохраняя исходный смысл | `false` |
| `input.seed` | number | Случайный seed для воспроизводимости. Если None, выбирается случайный seed | `0` |
| `input.acceleration` | string | Уровень ускорения для использования. Чем больше ускорение, тем быстрее генерация, но с более низким качеством. Рекомендуемое значение - 'none' | `"none"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp! URL входного изображения. Если входное изображение не соответствует выбранному соотношению сторон, оно изменяется и обрезается по центру!

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов! Текстовый промпт для генерации видео!

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"480p"`, `"580p"`, `"720p"`
- Default: `"720p"`
- **Важно**: Опциональный параметр! Только 3 указанных значения (enum)! Default: `"720p"`! Разрешение сгенерированного видео (480p, 580p или 720p)!

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"auto"`, `"16:9"`, `"9:16"`, `"1:1"`
- Default: `"auto"`
- **Важно**: Опциональный параметр! Только 4 указанных значения (enum)! Default: `"auto"`! Соотношение сторон сгенерированного видео. Если 'auto', соотношение сторон будет определено автоматически на основе входного изображения! ВАЖНО: Включает значение "auto" (которого нет в wan/2-2-a14b-text-to-video-turbo)!

#### `enable_prompt_expansion`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `false`
- **Важно**: Опциональный параметр! Boolean! Default: `false`! Включить ли расширение промпта. Это будет использовать большую языковую модель для расширения промпта с дополнительными деталями, сохраняя исходный смысл!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0-2147483647 (шаг: 1)
- Default: `0`
- **Важно**: Опциональный параметр! Целое число от 0 до 2147483647! Default: `0`! Случайный seed для воспроизводимости. Если None, выбирается случайный seed!

#### `acceleration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"none"`, `"regular"`
- Default: `"none"`
- **Важно**: Опциональный параметр! Только 2 указанных значения (enum)! Default: `"none"`! Уровень ускорения для использования. Чем больше ускорение, тем быстрее генерация, но с более низким качеством. Рекомендуемое значение - 'none'!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | wan/2-2-a14b-image-to-video-turbo | wan/2-2-a14b-text-to-video-turbo | Другие i2v модели |
|----------|-----------------------------------|-----------------------------------|------------------|
| `image_url` | ✅ Обязательный (max 10MB) | ❌ Нет | ✅ Может быть |
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный (max 5000) | ✅ Может быть |
| `resolution` | ✅ Есть (enum: 480p/580p/720p, default "720p") | ✅ Есть (enum: 480p/580p/720p, default "720p") | ✅ Может быть |
| `aspect_ratio` | ✅ Есть (enum: auto/16:9/9:16/1:1, default "auto") | ✅ Есть (enum: 16:9/9:16/1:1, default "16:9") | ✅ Может быть |
| `enable_prompt_expansion` | ✅ Есть (boolean, default false) | ✅ Есть (boolean, default false) | ✅ Может быть |
| `seed` | ✅ Есть (0-2147483647, default 0) | ✅ Есть (0-2147483647, default 0) | ✅ Может быть |
| `acceleration` | ✅ Есть (enum: none/regular, default "none") | ✅ Есть (enum: none/regular, default "none") | ✅ Может быть |
| `duration` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `fps` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `with_audio` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `width` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `height` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `guidance` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `steps` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `negative_prompt` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `cfg_scale` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `strength` | ❌ Нет | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для генерации видео из изображения (image-to-video) - Turbo версия с A14b!
- Использует `image_url` для указания входного изображения (обязательный параметр!) - если входное изображение не соответствует выбранному соотношению сторон, оно изменяется и обрезается по центру!
- Использует `prompt` для управления генерацией видео (обязательный параметр, максимум 5000 символов)!
- Использует `resolution` для управления разрешением (enum: 480p/580p/720p, default 720p)! ВАЖНО: Включает 580p (уникальное значение, которого нет в других wan моделях)!
- Использует `aspect_ratio` для управления соотношением сторон (enum: auto/16:9/9:16/1:1, default auto)! ВАЖНО: Включает "auto" (которого нет в wan/2-2-a14b-text-to-video-turbo)! Если 'auto', соотношение сторон будет определено автоматически на основе входного изображения!
- Использует `enable_prompt_expansion` для управления расширением промпта (boolean, default false)!
- Использует `seed` для управления воспроизводимостью (number, диапазон 0-2147483647, default 0)!
- Использует `acceleration` для управления уровнем ускорения (enum: none/regular, default none)!
- `prompt` имеет максимум 5000 символов!
- НЕТ параметров `duration`, `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `negative_prompt`, `cfg_scale`, `strength` (в отличие от других i2v моделей)!
- **Отличается от wan/2-2-a14b-text-to-video-turbo тем, что это image-to-video (есть обязательный image_url), и aspect_ratio имеет default "auto" (вместо "16:9")!**

## 🔧 Реализация

### Валидация

Функция `_validate_wan_2_2_a14b_image_to_video_turbo()` проверяет:

1. **Обязательность `image_url`**: Поле должно быть указано, не пустое, string
2. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
3. **Значение `resolution`**: Должно быть одним из 3 допустимых значений (опциональный, enum)
4. **Значение `aspect_ratio`**: Должно быть одним из 4 допустимых значений (опциональный, enum, ВАЖНО: включает "auto"!)
5. **Значение `enable_prompt_expansion`**: Должно быть boolean (опциональный)
6. **Значение `seed`**: Должно быть целым числом в диапазоне от 0 до 2147483647 (опциональный)
7. **Значение `acceleration`**: Должно быть одним из 2 допустимых значений (опциональный, enum)

### Нормализация

1. **`image_url`**: Проверяется что это непустая строка
2. **`resolution`**: Проверяется что это одно из 3 допустимых значений
3. **`aspect_ratio`**: Проверяется что это одно из 4 допустимых значений (включая "auto"!)
4. **`enable_prompt_expansion`**: Нормализуется через `_normalize_boolean()`
5. **`seed`**: Нормализуется в целое число (int), проверяется диапазон 0-2147483647
6. **`acceleration`**: Проверяется что это одно из 2 допустимых значений

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'resolution' not in normalized_input:
    normalized_input['resolution'] = "720p"  # Default согласно документации
if 'aspect_ratio' not in normalized_input:
    normalized_input['aspect_ratio'] = "auto"  # Default согласно документации (ВАЖНО: "auto", а не "16:9"!)
if 'enable_prompt_expansion' not in normalized_input:
    normalized_input['enable_prompt_expansion'] = False  # Default согласно документации
if 'seed' not in normalized_input:
    normalized_input['seed'] = 0  # Default согласно документации
if 'acceleration' not in normalized_input:
    normalized_input['acceleration'] = "none"  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "wan/2-2-a14b-image-to-video-turbo",
  "input": {
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755166042585gtf2mlrk.png",
    "prompt": "Overcast lighting, medium lens, soft lighting, low contrast lighting, edge lighting, low angle shot, desaturated colors, medium close-up shot, clean single shot, cool colors, center composition.The camera captures a low-angle close-up of a Western man outdoors, sharply dressed in a black coat over a gray sweater, white shirt, and black tie. His gaze is fixed on the lens as he advances. In the background, a brown building looms, its windows glowing with warm, yellow light above a dark doorway. As the camera pushes in, a blurred black object on the right side of the frame drifts back and forth, partially obscuring the view against a dark, nighttime background.",
    "resolution": "720p",
    "aspect_ratio": "auto",
    "enable_prompt_expansion": false,
    "seed": 42,
    "acceleration": "none"
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения для генерации видео |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите непустой URL изображения |
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации видео |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'resolution' должно быть одним из...` | Неверное значение `resolution` | Используйте одно из: 480p, 580p, 720p |
| `Поле 'aspect_ratio' должно быть одним из...` | Неверное значение `aspect_ratio` | Используйте одно из: auto, 16:9, 9:16, 1:1 |
| `Поле 'enable_prompt_expansion' должно быть boolean...` | Неверное значение `enable_prompt_expansion` | Используйте true или false |
| `Поле 'seed' должно быть в диапазоне...` | Неверное значение `seed` | Используйте целое число от 0 до 2147483647 |
| `Поле 'acceleration' должно быть одним из...` | Неверное значение `acceleration` | Используйте одно из: none, regular |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("wan/2-2-a14b-image-to-video-turbo")
user_payload = {
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755166042585gtf2mlrk.png",
    "prompt": "Overcast lighting, medium lens, soft lighting...",
    "resolution": "720p",
    "aspect_ratio": "auto",
    "enable_prompt_expansion": False,
    "seed": 42,
    "acceleration": "none"
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Параметры уже есть в схеме `i2v` (image_url, prompt, resolution, aspect_ratio, enable_prompt_expansion, seed, acceleration)
- [x] Реализована функция `_validate_wan_2_2_a14b_image_to_video_turbo()`
- [x] Переиспользованы функции нормализации из других моделей
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=wan/2-2-a14b-image-to-video-turbo TYPE=i2v INPUT_KEYS=['image_url', 'prompt', 'resolution', 'aspect_ratio', 'enable_prompt_expansion', 'seed', 'acceleration'] 
INPUT_PREVIEW={'image_url': 'https://...', 'prompt': 'Overcast lighting...', 'resolution': '720p', 'aspect_ratio': 'auto', 'enable_prompt_expansion': False, 'seed': 42, 'acceleration': 'none'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **image_url обязательный** - обязателен для генерации видео из изображения! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp! URL входного изображения. Если входное изображение не соответствует выбранному соотношению сторон, оно изменяется и обрезается по центру!
2. **prompt обязательный** - обязателен для генерации видео (максимум 5000 символов)! Текстовый промпт для генерации видео!
3. **resolution опциональный** - не обязательный! Только 3 значения (enum: 480p, 580p, 720p), default "720p"! Разрешение сгенерированного видео (480p, 580p или 720p)! ВАЖНО: Включает 580p (уникальное значение, которого нет в других wan моделях)!
4. **aspect_ratio опциональный** - не обязательный! Только 4 значения (enum: auto, 16:9, 9:16, 1:1), default "auto"! Соотношение сторон сгенерированного видео. Если 'auto', соотношение сторон будет определено автоматически на основе входного изображения! ВАЖНО: Включает значение "auto" (которого нет в wan/2-2-a14b-text-to-video-turbo)!
5. **enable_prompt_expansion опциональный** - не обязательный! Boolean, default false! Включить ли расширение промпта. Это будет использовать большую языковую модель для расширения промпта с дополнительными деталями, сохраняя исходный смысл!
6. **seed опциональный** - не обязательный! Целое число от 0 до 2147483647, default 0! Случайный seed для воспроизводимости. Если None, выбирается случайный seed!
7. **acceleration опциональный** - не обязательный! Только 2 значения (enum: none, regular), default "none"! Уровень ускорения для использования. Чем больше ускорение, тем быстрее генерация, но с более низким качеством. Рекомендуемое значение - 'none'!
8. **Уникальные параметры** - модель использует `image_url` (обязательный!), `resolution` (включает 580p!), `aspect_ratio` (включает "auto"!), `enable_prompt_expansion`, `acceleration` вместо стандартных `duration`, `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `negative_prompt`, `cfg_scale`, `strength`!
9. **НЕТ стандартных параметров** - модель НЕ поддерживает `duration`, `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `negative_prompt`, `cfg_scale`, `strength` (в отличие от других i2v моделей)!
10. **prompt max 5000** - `prompt` имеет максимум 5000 символов!
11. **Отличается от wan/2-2-a14b-text-to-video-turbo** - это image-to-video (есть обязательный `image_url`), и `aspect_ratio` имеет default "auto" (вместо "16:9")!

