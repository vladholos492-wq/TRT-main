# Интеграция elevenlabs/speech-to-text

## 📋 Обзор

Документация по интеграции модели `elevenlabs/speech-to-text` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `elevenlabs/speech-to-text`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"elevenlabs/speech-to-text"` |
| `input.audio_url` | string | URL аудио файла для транскрипции | Обязательный, макс 200MB, mpeg/wav/aac/mp4/ogg |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.language_code` | string | Код языка аудио | `""` (пустая строка) |
| `input.tag_audio_events` | boolean | Тегировать ли аудио события (смех, аплодисменты и т.д.) | `true` |
| `input.diarize` | boolean | Аннотировать ли, кто говорит | `true` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `audio_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 200MB
- Поддерживаемые форматы: `audio/mpeg`, `audio/wav`, `audio/x-wav`, `audio/aac`, `audio/mp4`, `audio/ogg`
- **Важно**: Обязательный параметр! Должен быть валидным URL аудио файла! Максимальный размер файла: 200MB!

#### `language_code`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 500 символов
- Default: `""` (пустая строка)
- **Важно**: Опциональный параметр! Максимум 500 символов! Default: пустая строка!

#### `tag_audio_events`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `true`! Тегирует аудио события (смех, аплодисменты и т.д.)!

#### `diarize`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `true`! Аннотирует, кто говорит!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | elevenlabs/speech-to-text | Другие stt модели |
|----------|---------------------------|-------------------|
| `audio_url` | ✅ Обязательный (макс 200MB) | ✅ Обязательный (обычно меньше) |
| `audio` | ❌ Нет (нормализуется в `audio_url`) | ✅ Может быть обязательным |
| `language_code` | ✅ Есть (string, макс 500) | ❌ Нет (используют `language`) |
| `tag_audio_events` | ✅ Есть (boolean) | ❌ Нет |
| `diarize` | ✅ Есть (boolean) | ❌ Нет |
| `language` | ❌ Нет | ✅ Может быть |
| `model` | ❌ Нет | ✅ Может быть |
| `format` | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для транскрипции аудио в текст!
- Использует `audio_url` (макс 200MB) для аудио файла!
- Использует `language_code` (string, макс 500) вместо стандартного `language`!
- Имеет уникальные параметры: `tag_audio_events`, `diarize`!
- НЕТ параметров `model` и `format` (в отличие от других stt моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_elevenlabs_speech_to_text()` проверяет:

1. **Обязательность `audio_url`**: Поле должно быть указано (может быть передано как `audio_url` или `audio`)
2. **Длина `language_code`**: Максимум 500 символов (опциональный)
3. **Значение `tag_audio_events`**: Должно быть boolean (опциональный)
4. **Значение `diarize`**: Должно быть boolean (опциональный)

### Нормализация

1. **Параметр `audio_url`**: Если передан `audio`, нормализуется в `audio_url`
2. **`_normalize_boolean()`**: Нормализует tag_audio_events и diarize (boolean) - переиспользуется из других моделей

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'language_code' not in normalized_input:
    normalized_input['language_code'] = ""  # Default согласно документации
if 'tag_audio_events' not in normalized_input:
    normalized_input['tag_audio_events'] = True  # Default согласно документации
if 'diarize' not in normalized_input:
    normalized_input['diarize'] = True  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "elevenlabs/speech-to-text",
  "input": {
    "audio_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1757157053357tn37vxc8.mp3",
    "language_code": "",
    "tag_audio_events": true,
    "diarize": true
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

### Пример результата (resultJson)

```json
{
  "resultObject": {
    "language_code": "eng",
    "language_probability": 0.9915943741798401,
    "words": [
      {
        "speaker_id": "speaker_0",
        "start": 0.14,
        "end": 0.339,
        "text": "Ever",
        "type": "word"
      },
      ...
    ],
    "text": "Ever tried Ki AI? They've got a bunch of solid AI APIs, and the prices are more than 50% lower than the usual big name platforms."
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'audio_url' обязательно...` | `audio_url` не указан | Загрузите аудио файл для транскрипции |
| `Поле 'audio_url' не может быть пустым` | `audio_url` пустой | Укажите валидный URL аудио файла |
| `Поле 'language_code' слишком длинное...` | `language_code` больше 500 символов | Сократите код языка до 500 символов |
| `Поле 'tag_audio_events' должно быть boolean...` | Неверное значение `tag_audio_events` | Используйте true/false |
| `Поле 'diarize' должно быть boolean...` | Неверное значение `diarize` | Используйте true/false |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("elevenlabs/speech-to-text")
user_payload = {
    "audio_url": "https://example.com/audio.mp3",
    "language_code": "",
    "tag_audio_events": True,
    "diarize": True
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлены параметры `language_code`, `tag_audio_events`, `diarize` в схему `stt`
- [x] Реализована функция `_validate_elevenlabs_speech_to_text()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=elevenlabs/speech-to-text TYPE=stt INPUT_KEYS=['audio_url', 'language_code', 'tag_audio_events', 'diarize'] 
INPUT_PREVIEW={'audio_url': 'https://...', 'language_code': '', 'tag_audio_events': True, 'diarize': True}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **audio_url обязательный** - обязателен для транскрипции аудио (макс 200MB, mpeg/wav/aac/mp4/ogg)
2. **language_code опциональный** - не обязательный! Максимум 500 символов, default пустая строка!
3. **tag_audio_events опциональный** - не обязательный! Boolean (true/false), default true! Тегирует аудио события (смех, аплодисменты и т.д.)!
4. **diarize опциональный** - не обязательный! Boolean (true/false), default true! Аннотирует, кто говорит!
5. **Нормализация** - если передан `audio`, будет автоматически нормализован в `audio_url`!
6. **Форматы файлов** - поддерживаются только указанные форматы (валидация формата на стороне KIE API)
7. **Размер файла** - максимальный размер файла: 200MB (валидация размера на стороне KIE API)
8. **Уникальные параметры** - модель использует `language_code`, `tag_audio_events`, `diarize` вместо стандартных `language`, `model`, `format`!













