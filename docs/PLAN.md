# ПЛАН ИДЕАЛИЗАЦИИ

**Дата:** 2025-12-21  
**Версия:** 1.0

---

## 🎯 P0 - СТАБИЛИЗАЦИЯ RENDER (1-2 дня)

### P0-1: Убрать блокирующие операции
**Scope:**
- Заменить все `open()` на `aiofiles.open()` или `asyncio.to_thread()`
- Обернуть PIL/OCR в `asyncio.to_thread()`

**Файлы:**
- `bot_kie.py:679,697,756,794,925,1473,1553,1771` - `open()`
- `bot_kie.py:2030,2034,2039,2041` - PIL/OCR

**Риск:** Низкий (изолированные изменения)

**Verify:**
- `python scripts/scan_blocking.py` - должно быть 0 блокирующих операций
- `python scripts/verify_project.py` - все тесты проходят
- Проверка на Render: нет зависаний

---

### P0-2: Убрать упоминания "KIE" из UI
**Scope:**
- Найти все user-facing тексты с "KIE"
- Заменить на нейтральные термины ("AI", "генерация", "модель")

**Файлы:**
- `bot_kie.py` - сообщения пользователю
- `translations.py` - переводы (если есть)

**Риск:** Низкий (только тексты)

**Verify:**
- `grep -r "KIE" bot_kie.py | grep -v "#" | grep -v "TODO"` - не должно быть в user-facing текстах
- Проверка в боте: пользователь не видит "KIE"

---

### P0-3: Добавить query.answer() везде
**Scope:**
- Проверить все callback handlers
- Добавить `await query.answer()` где отсутствует

**Файлы:**
- `bot_kie.py` - все callback handlers

**Риск:** Низкий (добавление вызовов)

**Verify:**
- `python scripts/scan_callbacks.py` - все handlers должны иметь `query.answer()`
- Тестирование: все кнопки отвечают сразу

---

### P0-4: Защитить глобальные словари locks
**Scope:**
- Добавить `asyncio.Lock()` для `user_sessions`, `saved_generations`, `_data_cache`
- Все операции в `async with lock:`

**Файлы:**
- `bot_kie.py:312,320,570` - глобальные словари

**Риск:** Средний (нужно аккуратно добавить locks)

**Verify:**
- Проверка что все операции с словарями защищены
- Тестирование: нет race conditions

---

## 🟡 P1 - КАЧЕСТВО/UX/НАДЁЖНОСТЬ (3-7 дней)

### P1-1: Исправить циклические импорты
**Scope:**
- Найти все циклы через `scan_imports.py`
- Рефакторинг структуры импортов

**Риск:** Средний (может потребовать рефакторинга)

**Verify:**
- `python scripts/scan_imports.py` - нет циклов
- Время импорта < 1 секунды

---

### P1-2: Ленивая инициализация тяжелых модулей
**Scope:**
- Перенести тяжелые импорты в функции
- Инициализация только при использовании

**Риск:** Низкий (улучшение производительности)

**Verify:**
- Время импорта `bot_kie.py` < 0.5 секунды
- Старт бота < 5 секунд

---

### P1-3: Добавить error handling везде
**Scope:**
- Добавить try/except во все handlers
- Улучшить сообщения об ошибках

**Риск:** Низкий (улучшение стабильности)

**Verify:**
- Все handlers имеют error handling
- Пользователь всегда получает ответ

---

### P1-4: Очистка сессий
**Scope:**
- Добавить TTL для сессий (30 минут неактивности)
- Периодическая очистка (каждые 10 минут)
- Очистка при `ConversationHandler.END`

**Риск:** Низкий (улучшение памяти)

**Verify:**
- Сессии очищаются автоматически
- Нет утечки памяти

---

### P1-5: Валидация параметров
**Scope:**
- Использовать `app/domain/models_registry.py` для валидации
- Валидировать перед показом кнопки подтверждения

**Риск:** Низкий (улучшение качества)

**Verify:**
- Все параметры валидируются
- Нет ошибок в KIE API из-за невалидных параметров

---

## 🟢 P2 - АРХИТЕКТУРА/МОДУЛЬНОСТЬ (дальше)

### P2-1: Рефакторинг bot_kie.py
**Scope:**
- Вынести handlers в `app/handlers/`
- Вынести бизнес-логику в services
- Уменьшить размер до <10,000 строк

**Риск:** Высокий (большие изменения)

**Verify:**
- Размер `bot_kie.py` < 10,000 строк
- Все тесты проходят
- Функциональность не сломана

---

### P2-2: Убрать дублирование кода
**Scope:**
- Найти дублирование через анализ кода
- Вынести общую логику в helpers/services

**Риск:** Средний (рефакторинг)

**Verify:**
- Нет дублирования кода
- Общая логика в одном месте

---

### P2-3: Добавить тесты
**Scope:**
- Unit тесты для критических функций
- Integration тесты для flows
- Расширить `verify_project.py`

**Риск:** Низкий (добавление тестов)

**Verify:**
- Покрытие тестами >50%
- Все тесты проходят

---

### P2-4: Мониторинг и метрики
**Scope:**
- Добавить метрики в healthcheck
- Логирование метрик (uptime, errors, generations)

**Риск:** Низкий (добавление функциональности)

**Verify:**
- Healthcheck возвращает метрики
- Метрики логируются

---

## 📋 ПРИОРИТЕТЫ

1. **P0** - Критические для работы на Render (1-2 дня)
2. **P1** - Важные для стабильности/UX (3-7 дней)
3. **P2** - Желательные для качества кода (дальше)

---

## ✅ КРИТЕРИИ УСПЕХА

### После P0:
- ✅ Нет блокирующих операций в async
- ✅ Нет упоминаний "KIE" в UI
- ✅ Все callbacks имеют `query.answer()`
- ✅ Глобальные словари защищены locks
- ✅ `python scripts/verify_project.py` проходит
- ✅ Бот стабильно работает на Render

### После P1:
- ✅ Нет циклических импортов
- ✅ Быстрый старт (< 5 секунд)
- ✅ Все handlers имеют error handling
- ✅ Сессии очищаются автоматически
- ✅ Параметры валидируются

### После P2:
- ✅ `bot_kie.py` < 10,000 строк
- ✅ Нет дублирования кода
- ✅ Покрытие тестами >50%
- ✅ Есть мониторинг и метрики

---

## 🚀 ПЛАН ВЫПОЛНЕНИЯ

### Неделя 1: P0 стабилизация
- День 1-2: P0-1, P0-2, P0-3
- День 3-4: P0-4, тестирование
- День 5: Проверка на Render, финальные правки

### Неделя 2: P1 качество
- День 1-2: P1-1, P1-2
- День 3-4: P1-3, P1-4
- День 5-7: P1-5, тестирование

### Неделя 3+: P2 архитектура
- Постепенный рефакторинг
- Добавление тестов
- Мониторинг

---

## ⚠️ РИСКИ

### Высокие:
- P2-1 (рефакторинг bot_kie.py) - может сломать функциональность

### Средние:
- P0-4 (locks) - нужно аккуратно добавить
- P1-1 (циклические импорты) - может потребовать рефакторинга

### Низкие:
- Остальные задачи - изолированные изменения

---

## 📝 ЗАМЕТКИ

- Все изменения должны проходить `python scripts/verify_project.py`
- После каждого этапа - проверка на Render
- Не ломать текущую структуру главного меню/оплаты
- Пользователь не должен знать название интегратора

