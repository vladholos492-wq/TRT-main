# Интеграция hailuo/02-image-to-video-pro

## 📋 Обзор

Документация по интеграции модели `hailuo/02-image-to-video-pro` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `hailuo/02-image-to-video-pro`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"hailuo/02-image-to-video-pro"` |
| `input.prompt` | string | Текстовый промпт, описывающий желаемую анимацию видео | Обязательный, максимум 1500 символов |
| `input.image_url` | string | Входное изображение для анимации | Обязательный string (не массив!), максимум 10MB, форматы: JPEG, PNG, WebP |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.end_image_url` | string | Опциональный URL изображения для использования в качестве последнего кадра видео | `""` (пустая строка) |
| `input.prompt_optimizer` | boolean | Использовать ли оптимизатор промпта модели | `true` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 1500 символов
- **Важно**: Не может быть пустым! Максимум 1500 символов (меньше чем у других моделей hailuo)!

#### `image_url`
- **Тип**: string (не массив!)
- **Обязательный**: да
- **Описание**: Входное изображение для анимации
- **Ограничения**:
  - Максимум 10MB на файл
  - Форматы: JPEG, PNG, WebP
  - Должен быть валидным URL (начинается с `http://` или `https://`)
- **Default**: `"https://file.aiquickdraw.com/custom-page/akr/section-images/17585210783150ispzfo7.png"`
- **ВАЖНО**: Это string, а не массив! (в отличие от других i2v моделей, где используется `image_urls`)

#### `end_image_url`
- **Тип**: string
- **Обязательный**: нет (опциональный!)
- **Описание**: Опциональный URL изображения для использования в качестве последнего кадра видео
- **Ограничения**:
  - Максимум 10MB на файл
  - Форматы: JPEG, PNG, WebP
  - Должен быть валидным URL (начинается с `http://` или `https://`)
- **Default**: `""` (пустая строка, если не указан)
- **Важно**: Опциональный параметр! Если пустая строка, параметр не передаётся в API

#### `prompt_optimizer`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)!
- **Описание**: Использовать ли оптимизатор промпта модели
- **Поддерживаемые значения**:
  - `true`, `1`, `"true"`, `"yes"`, `"y"`, `"on"` → `true`
  - `false`, `0`, `"false"`, `"no"`, `"n"`, `"off"` → `false`

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Отличия от других моделей Hailuo

| Параметр | hailuo/02-image-to-video-pro | hailuo/2-3-image-to-video-pro |
|----------|----------------------------|-------------------------------|
| `prompt` max | 1500 символов | 5000 символов |
| `image_url` | ✅ Обязательный string | ✅ Обязательный string |
| `end_image_url` | ✅ Есть (опциональный) | ❌ Нет |
| `prompt_optimizer` | ✅ Есть (boolean) | ❌ Нет |
| `duration` | ❌ Нет | ✅ Опциональный ("6" \| "10") |
| `resolution` | ❌ Нет | ✅ Опциональный ("768P" \| "1080P") |

**ВАЖНО**: 
- `prompt` максимум 1500 символов (меньше чем у других моделей hailuo)!
- `image_url` - это string, а не массив! Если передан `image_urls`, он будет удалён с предупреждением
- Есть параметр `end_image_url` (опциональный string для последнего кадра)!
- Есть параметр `prompt_optimizer` (boolean)!
- НЕТ параметров `duration`, `resolution` и других (в отличие от других hailuo моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_hailuo_02_image_to_video_pro()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано
2. **Длина `prompt`**: Максимум 1500 символов
3. **Обязательность `image_url`**: Поле должно быть указано (string, не массив!)
4. **Формат `image_url`**: Должен быть валидным URL
5. **Формат `end_image_url`**: Если указан, должен быть валидным URL (опциональный)
6. **Значение `prompt_optimizer`**: Должно быть boolean (опциональный)

### Нормализация

1. **`_normalize_boolean()`**: Нормализует prompt_optimizer (boolean) - переиспользуется из других моделей

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'image_url' not in normalized_input:
    normalized_input['image_url'] = "https://file.aiquickdraw.com/custom-page/akr/section-images/17585210783150ispzfo7.png"
if 'prompt_optimizer' not in normalized_input:
    normalized_input['prompt_optimizer'] = True  # Default согласно документации
# end_image_url опциональный, default "" (пустая строка) - не добавляем если не указан
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "hailuo/02-image-to-video-pro",
  "input": {
    "prompt": "Cinematic wide shot: A colossal starship drifts silently above the rings of Saturn.",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17585210783150ispzfo7.png",
    "end_image_url": "",
    "prompt_optimizer": true
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Укажите текстовое описание видео |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 1500 символов | Сократите описание до 1500 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения |
| `Поле 'image_url' должно быть валидным URL...` | URL не начинается с `http://` или `https://` | Используйте полный URL с протоколом |
| `Поле 'end_image_url' должно быть валидным URL...` | URL не начинается с `http://` или `https://` | Используйте полный URL с протоколом |
| `Поле 'prompt_optimizer' должно быть boolean...` | Неверное значение `prompt_optimizer` | Используйте true/false |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("hailuo/02-image-to-video-pro")
user_payload = {
    "prompt": "Cinematic wide shot: A colossal starship drifts silently above the rings of Saturn.",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17585210783150ispzfo7.png",
    "end_image_url": "",
    "prompt_optimizer": True
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлена схема `i2v` с полями `end_image_url` и `prompt_optimizer`
- [x] Реализована функция `_validate_hailuo_02_image_to_video_pro()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Реализовано автоматическое удаление `image_urls` (если передан)
- [x] Переиспользована функция нормализации boolean из других моделей
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=hailuo/02-image-to-video-pro TYPE=i2v INPUT_KEYS=['prompt', 'image_url', 'end_image_url', 'prompt_optimizer'] 
INPUT_PREVIEW={'prompt': 'Cinematic wide shot...', 'image_url': 'https://...', 'prompt_optimizer': True}
```

Если передан параметр `image_urls`, будет предупреждение:
```
Parameter 'image_urls' is not supported for hailuo/02-image-to-video-pro (use 'image_url' as string), removing it
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео
2. **prompt максимум 1500 символов** - меньше чем у других моделей hailuo (обычно 5000)!
3. **image_url обязательный** - обязателен для генерации видео! Это string, а не массив!
4. **end_image_url опциональный** - не обязательный! Если пустая строка, параметр не передаётся
5. **prompt_optimizer опциональный** - не обязательный! Boolean (true/false), default true!
6. **НЕТ параметра duration** - в отличие от других hailuo моделей, параметр `duration` не поддерживается!
7. **НЕТ параметра resolution** - в отличие от других hailuo моделей, параметр `resolution` не поддерживается!

