# Интеграция wan/2-5-image-to-video

## 📋 Обзор

Документация по интеграции модели `wan/2-5-image-to-video` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `wan/2-5-image-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"wan/2-5-image-to-video"` |
| `input.prompt` | string | Текстовое описание желаемого движения видео | Обязательный, максимум 800 символов |
| `input.image_url` | string | URL изображения для использования в качестве первого кадра | Обязательный string (не массив!), максимум 10MB, форматы: JPEG, PNG, WebP |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.duration` | string | Длительность видео в секундах | `"5"` |
| `input.resolution` | string | Разрешение видео | `"1080p"` |
| `input.negative_prompt` | string | Негативный промпт для описания контента, которого следует избегать | `""` (пустая строка) |
| `input.enable_prompt_expansion` | boolean | Включить ли переписывание промпта с помощью LLM | `true` |
| `input.seed` | number | Случайное зерно для воспроизводимости | Не указан (случайное) |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 800 символов
- **Важно**: Не может быть пустым! Максимум 800 символов (меньше чем у wan/2-6-image-to-video, где 5000)!

#### `image_url`
- **Тип**: string (не массив!)
- **Обязательный**: да
- **Описание**: URL изображения для использования в качестве первого кадра
- **Ограничения**:
  - Максимум 10MB на файл
  - Форматы: JPEG, PNG, WebP
  - Должен быть валидным URL (начинается с `http://` или `https://`)
  - Должен быть публично доступным
- **Default**: `"https://file.aiquickdraw.com/custom-page/akr/section-images/1758796480945qb63zxq8.webp"`
- **ВАЖНО**: Это string, а не массив! (в отличие от wan/2-6-image-to-video, где используется `image_urls`)

#### `duration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"5"` - 5 секунд (default)
  - `"10"` - 10 секунд
- **Важно**: Опциональный параметр! Только 2 значения (в отличие от wan/2-6-image-to-video, где есть "15")!

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"720p"` - 720p
  - `"1080p"` - 1080p (default)
- **Важно**: Опциональный параметр! Только 2 значения!
- **Поддерживаемые алиасы**:
  - `"720"`, `"720p"`, `"hd"` → `"720p"`
  - `"1080"`, `"1080p"`, `"fullhd"`, `"fhd"` → `"1080p"`

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 500 символов
- Default: `""` (пустая строка)
- **Важно**: Опциональный параметр! Максимум 500 символов (меньше чем у других моделей)!
- **Если пустая строка**: Параметр не передаётся в API

#### `enable_prompt_expansion`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)!
- **Описание**: Включить ли переписывание промпта с помощью LLM
- **Поддерживаемые значения**:
  - `true`, `1`, `"true"`, `"yes"`, `"y"`, `"on"` → `true`
  - `false`, `0`, `"false"`, `"no"`, `"n"`, `"off"` → `false`

#### `seed`
- Тип: number (integer)
- Обязательный: нет (опциональный!)
- Default: Не указан (случайное зерно)
- **Важно**: Опциональный параметр! Число для воспроизводимости результатов!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Отличия от wan/2-6-image-to-video

| Параметр | wan/2-5-image-to-video | wan/2-6-image-to-video |
|----------|------------------------|------------------------|
| `prompt` max | 800 символов | 5000 символов |
| `image_url` | ✅ Обязательный string | ✅ Обязательный массив (`image_urls`) |
| `duration` | "5" \| "10" | "5" \| "10" \| "15" |
| `resolution` | "720p" \| "1080p" | "720p" \| "1080p" |
| `negative_prompt` | ✅ Есть (max 500) | ❌ Нет |
| `enable_prompt_expansion` | ✅ Есть (boolean) | ❌ Нет |
| `seed` | ✅ Есть (number) | ❌ Нет |

**ВАЖНО**: 
- `image_url` - это string, а не массив! Если передан `image_urls`, он будет удалён с предупреждением
- `prompt` максимум 800 символов (в 2-6 было 5000)!
- `negative_prompt` максимум 500 символов!
- `duration` только "5" или "10" (в 2-6 есть "15")!

## 🔧 Реализация

### Валидация

Функция `_validate_wan_2_5_image_to_video()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано
2. **Длина `prompt`**: Максимум 800 символов
3. **Обязательность `image_url`**: Поле должно быть указано (string, не массив!)
4. **Формат `image_url`**: Должен быть валидным URL
5. **Значение `duration`**: Должно быть "5" или "10" (опциональный)
6. **Значение `resolution`**: Должно быть "720p" или "1080p" (опциональный)
7. **Длина `negative_prompt`**: Максимум 500 символов (опциональный)
8. **Значение `enable_prompt_expansion`**: Должно быть boolean (опциональный)
9. **Значение `seed`**: Должно быть числом (опциональный)

### Нормализация

1. **`_normalize_duration_for_wan_2_6()`**: Нормализует duration ("5" | "10") - переиспользуется из wan/2-6
2. **`_normalize_resolution_for_wan_2_5()`**: Нормализует resolution ("720p" | "1080p")
3. **`_normalize_boolean()`**: Нормализует enable_prompt_expansion (boolean)

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'image_url' not in normalized_input:
    normalized_input['image_url'] = "https://file.aiquickdraw.com/custom-page/akr/section-images/1758796480945qb63zxq8.webp"
if 'duration' not in normalized_input:
    normalized_input['duration'] = "5"  # Default согласно документации
if 'resolution' not in normalized_input:
    normalized_input['resolution'] = "1080p"  # Default согласно документации
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = ""  # Default согласно документации (пустая строка)
if 'enable_prompt_expansion' not in normalized_input:
    normalized_input['enable_prompt_expansion'] = True  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "wan/2-5-image-to-video",
  "input": {
    "prompt": "The same woman from the reference image looks directly into the camera, takes a breath, then smiles brightly and speaks with enthusiasm.",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1758796480945qb63zxq8.webp",
    "duration": "5",
    "resolution": "1080p",
    "negative_prompt": "",
    "enable_prompt_expansion": true,
    "seed": 42
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Укажите текстовое описание видео |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 800 символов | Сократите описание до 800 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения |
| `Поле 'image_url' должно быть валидным URL...` | URL не начинается с `http://` или `https://` | Используйте полный URL с протоколом |
| `Поле 'duration' должно быть одним из...` | Неверное значение `duration` | Используйте "5" или "10" |
| `Поле 'resolution' должно быть одним из...` | Неверное значение `resolution` | Используйте "720p" или "1080p" |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 500 символов | Сократите до 500 символов |
| `Поле 'enable_prompt_expansion' должно быть boolean...` | Неверное значение `enable_prompt_expansion` | Используйте true/false |
| `Поле 'seed' должно быть числом...` | Неверное значение `seed` | Используйте число |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("wan/2-5-image-to-video")
user_payload = {
    "prompt": "The same woman from the reference image looks directly into the camera...",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1758796480945qb63zxq8.webp",
    "duration": "5",
    "resolution": "1080p",
    "negative_prompt": "",
    "enable_prompt_expansion": True,
    "seed": 42
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлена схема `i2v` с полем `enable_prompt_expansion`
- [x] Реализована функция `_normalize_resolution_for_wan_2_5()`
- [x] Реализована функция `_normalize_boolean()`
- [x] Реализована функция `_validate_wan_2_5_image_to_video()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Реализовано автоматическое удаление `image_urls` (если передан)
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=wan/2-5-image-to-video TYPE=i2v INPUT_KEYS=['prompt', 'image_url', 'duration', 'resolution', 'negative_prompt', 'enable_prompt_expansion', 'seed'] 
INPUT_PREVIEW={'prompt': 'The same woman from the reference image...', 'image_url': 'https://...', 'duration': '5', 'resolution': '1080p', 'enable_prompt_expansion': True, 'seed': 42}
```

Если передан параметр `image_urls`, будет предупреждение:
```
Parameter 'image_urls' is not supported for wan/2-5-image-to-video (use 'image_url' as string), removing it
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео
2. **prompt максимум 800 символов** - меньше чем у wan/2-6-image-to-video (5000)!
3. **image_url обязательный** - обязателен для генерации видео! Это string, а не массив!
4. **duration опциональный** - не обязательный! Только 2 значения: "5" или "10" (в 2-6 есть "15")!
5. **resolution опциональный** - не обязательный! Только 2 значения: "720p" или "1080p"!
6. **negative_prompt опциональный** - не обязательный! Максимум 500 символов!
7. **enable_prompt_expansion опциональный** - не обязательный! Boolean (true/false), default true!
8. **seed опциональный** - не обязательный! Число для воспроизводимости!
9. **Отличия от wan/2-6-image-to-video** - другие ограничения prompt, image_url как string, есть negative_prompt и enable_prompt_expansion!

