# Интеграция grok-imagine/text-to-image

## 📋 Обзор

Документация по интеграции модели `grok-imagine/text-to-image` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `grok-imagine/text-to-image`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"grok-imagine/text-to-image"` |
| `input.prompt` | string | Текстовое описание изображения | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.aspect_ratio` | string | Соотношение сторон изображения | `"3:2"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Не может быть пустым! Максимум 5000 символов!

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"2:3"` - Вертикальный 2:3
  - `"3:2"` - Горизонтальный 3:2 (default)
  - `"1:1"` - Квадрат
- **Важно**: Опциональный параметр! Только 3 значения (не 5, 8 или 11)! Default "3:2" (отличается от text-to-video, где default "2:3")!

#### `mode`
- **ВАЖНО**: Параметр `mode` НЕ поддерживается для text-to-image (в отличие от text-to-video и image-to-video)!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_grok_imagine_text_to_image(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для grok-imagine/text-to-image согласно документации API.
    """
    # Проверяем оба возможных ID модели
    if model_id not in ["grok-imagine/text-to-image", "grok/imagine-text-to-image"]:
        return True, None
    
    # Валидация prompt: обязательный, максимум 5000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации изображения"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len > 5000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 5000)"
    
    # Валидация aspect_ratio: опциональный, enum
    aspect_ratio = normalized_input.get('aspect_ratio')
    if aspect_ratio is not None:
        # Переиспользуем функцию нормализации из text-to-video (те же 3 значения)
        normalized_aspect_ratio = _normalize_aspect_ratio_for_grok_imagine_text_to_video(aspect_ratio)
        if normalized_aspect_ratio is None:
            valid_values = ["2:3", "3:2", "1:1"]
            return False, f"Поле 'aspect_ratio' должно быть одним из: {', '.join(valid_values)}"
        normalized_input['aspect_ratio'] = normalized_aspect_ratio
    
    # ВАЖНО: Нет параметра mode для text-to-image (в отличие от text-to-video и image-to-video)
    
    return True, None
```

### Нормализация aspect_ratio

Переиспользуется функция нормализации из `grok-imagine/text-to-video` (те же 3 значения):

```python
def _normalize_aspect_ratio_for_grok_imagine_text_to_video(value: Any) -> Optional[str]:
    """
    Нормализует aspect_ratio для grok-imagine/text-to-image.
    ВАЖНО: Для grok-imagine/text-to-image поддерживаются только "2:3", "3:2", "1:1"!
    """
    valid_values = ["2:3", "3:2", "1:1"]
    # Поддерживает алиасы (2/3, 2x3, 3/2, 3x2, 1/1, 1x1, square)
    ...
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id in ["grok-imagine/text-to-image", "grok/imagine-text-to-image"]:
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "3:2"  # Default согласно документации (отличается от text-to-video!)
    # ВАЖНО: Нет параметра mode для text-to-image (в отличие от text-to-video и image-to-video)
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "grok-imagine/text-to-image",
  "input": {
    "prompt": "Cinematic portrait of a woman sitting by a vinyl record player, retro living room background, soft ambient lighting, warm earthy tones, nostalgic 1970s wardrobe, reflective mood, gentle film grain texture, shallow depth of field, vintage editorial photography style.",
    "aspect_ratio": "3:2"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (максимум 5000 символов) - обязательный!
2. **Валидация aspect_ratio**: Проверяется что значение из допустимого списка (3 значения) (опциональный!)
3. **Дефолтные значения**: Применяются если параметры не указаны (aspect_ratio="3:2")
4. **Нет параметра mode**: Параметр mode не поддерживается (в отличие от text-to-video и image-to-video)
5. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=grok-imagine/text-to-image TYPE=t2i INPUT_KEYS=['prompt', 'aspect_ratio'] 
INPUT_PREVIEW={'prompt': 'Cinematic portrait...', 'aspect_ratio': '3:2'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации изображения
2. **prompt максимум 5000 символов** - проверяется при валидации
3. **aspect_ratio опциональный** - не обязательный! Только 3 значения: "2:3", "3:2", "1:1" (не 5, 8 или 11)!
4. **aspect_ratio default "3:2"** - отличается от text-to-video, где default "2:3"!
5. **Нет параметра mode** - в отличие от text-to-video и image-to-video, здесь нет параметра mode!
6. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими моделями

### Сравнение с grok-imagine/text-to-video

| Параметр | grok-imagine/text-to-video | grok-imagine/text-to-image |
|----------|----------------------------|----------------------------|
| `prompt` | Обязательный, max 5000 | Обязательный, max 5000 |
| `aspect_ratio` | Опциональный, 3 значения | Опциональный, 3 значения |
| `aspect_ratio` default | `"2:3"` | `"3:2"` (отличается!) |
| `mode` | Опциональный ("fun", "normal", "spicy") | **НЕ поддерживается** |
| Тип модели | t2v | t2i |

### Сравнение с grok-imagine/image-to-video

| Параметр | grok-imagine/image-to-video | grok-imagine/text-to-image |
|----------|-----------------------------|----------------------------|
| `prompt` | Опциональный, max 5000 | Обязательный, max 5000 |
| `image_urls` | Опциональный массив (только одно!) | Не требуется |
| `task_id` | Опциональный | Не требуется |
| `index` | Опциональный (только с task_id) | Не требуется |
| `aspect_ratio` | Не требуется | Опциональный, 3 значения |
| `mode` | Опциональный ("fun", "normal", "spicy") | **НЕ поддерживается** |
| Тип модели | i2v | t2i |

### Сравнение с другими text-to-image моделями

| Параметр | seedream/4.5-text-to-image | grok-imagine/text-to-image |
|----------|----------------------------|----------------------------|
| `prompt` | Обязательный, max 3000 | Обязательный, max 5000 |
| `aspect_ratio` | Обязательный, 5 значений | Опциональный, 3 значения |
| `quality` | Обязательный enum | Не требуется |
| `mode` | Не требуется | Не требуется |
| Тип модели | t2i | t2i |

| Параметр | flux-2/pro-text-to-image | grok-imagine/text-to-image |
|----------|--------------------------|----------------------------|
| `prompt` | Обязательный, 3-5000 | Обязательный, max 5000 |
| `aspect_ratio` | Обязательный, 8 значений (с "auto") | Опциональный, 3 значения |
| `resolution` | Обязательный, "1K"\|"2K" | Не требуется |
| `mode` | Не требуется | Не требуется |
| Тип модели | t2i | t2i |

## 🎯 Ключевые отличия

1. **prompt обязательный**: Обязателен для генерации изображения
2. **Нет изображений**: В отличие от image-to-video, не требует входных изображений
3. **aspect_ratio опциональный**: Только 3 значения (не 5, 8 или 11)
4. **aspect_ratio default "3:2"**: Отличается от text-to-video, где default "2:3"
5. **Нет параметра mode**: В отличие от text-to-video и image-to-video, здесь нет параметра mode
6. **Переиспользование кода**: Использует ту же функцию нормализации aspect_ratio, что и text-to-video
7. **Нормализация типов**: aspect_ratio конвертируется с поддержкой алиасов
8. **Дефолтные значения**: Применяются автоматически
9. **Сообщения об ошибках**: Понятные сообщения для пользователя

