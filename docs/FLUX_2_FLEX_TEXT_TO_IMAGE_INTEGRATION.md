# Интеграция flux-2/flex-text-to-image

## 📋 Обзор

Документация по интеграции модели `flux-2/flex-text-to-image` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `flux-2/flex-text-to-image`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"flux-2/flex-text-to-image"` |
| `input.prompt` | string | Текстовое описание изображения | Обязательный, от 3 до 5000 символов |
| `input.aspect_ratio` | string | Соотношение сторон изображения | Обязательный, один из допустимых значений |
| `input.resolution` | string | Разрешение выходного изображения | Обязательный, "1K" или "2K" |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Минимум: 3 символа
- Максимум: 5000 символов
- **Важно**: Не может быть пустым! Должен быть от 3 до 5000 символов!

#### `aspect_ratio`
- Тип: string
- Обязательный: да
- Значения:
  - `"1:1"` - Квадрат (default)
  - `"4:3"` - Горизонтальный 4:3
  - `"3:4"` - Вертикальный 3:4
  - `"16:9"` - Горизонтальный 16:9
  - `"9:16"` - Вертикальный 9:16
  - `"3:2"` - Классический горизонтальный
  - `"2:3"` - Классический вертикальный
  - `"auto"` - Автоматически на основе первого входного изображения
- **Важно**: Обязательный параметр, не опциональный! 8 значений (включая "auto")!

#### `resolution`
- Тип: string
- Обязательный: да
- Значения:
  - `"1K"` - 1K разрешение (default)
  - `"2K"` - 2K разрешение
- **Важно**: Обязательный параметр, не опциональный! Только "1K" или "2K"!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_flux_2_flex_text_to_image(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для flux-2/flex-text-to-image согласно документации API.
    """
    if model_id != "flux-2/flex-text-to-image":
        return True, None
    
    # Валидация prompt: обязательный, от 3 до 5000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации изображения"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len < 3:
        return False, f"Поле 'prompt' слишком короткое: {prompt_len} символов (минимум 3)"
    if prompt_len > 5000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 5000)"
    
    # Валидация aspect_ratio: обязательный, enum
    aspect_ratio = normalized_input.get('aspect_ratio')
    if not aspect_ratio:
        return False, "Поле 'aspect_ratio' обязательно для генерации изображения"
    
    normalized_aspect_ratio = _normalize_aspect_ratio_for_flux_2_pro(aspect_ratio)
    if normalized_aspect_ratio is None:
        valid_values = ["1:1", "4:3", "3:4", "16:9", "9:16", "3:2", "2:3", "auto"]
        return False, f"Поле 'aspect_ratio' должно быть одним из: {', '.join(valid_values)}"
    normalized_input['aspect_ratio'] = normalized_aspect_ratio
    
    # Валидация resolution: обязательный, "1K" | "2K"
    resolution = normalized_input.get('resolution')
    if not resolution:
        return False, "Поле 'resolution' обязательно для генерации изображения"
    
    normalized_resolution = _normalize_resolution_for_flux_2_pro(resolution)
    if normalized_resolution is None:
        return False, f"Поле 'resolution' должно быть '1K' или '2K'"
    normalized_input['resolution'] = normalized_resolution
    
    return True, None
```

### Нормализация параметров

Все функции нормализации переиспользуются из `flux-2/pro-text-to-image`:

- `_normalize_aspect_ratio_for_flux_2_pro()` - нормализует aspect_ratio
- `_normalize_resolution_for_flux_2_pro()` - нормализует resolution

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id == "flux-2/flex-text-to-image":
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "1:1"  # Default согласно документации
    if 'resolution' not in normalized_input:
        normalized_input['resolution'] = "1K"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "flux-2/flex-text-to-image",
  "input": {
    "prompt": "A humanoid figure with a vintage television set for a head, featuring a green-tinted screen displaying a `Hello FLUX.2` writing in ASCII font. The figure is wearing a yellow raincoat, and there are various wires and components attached to the television.",
    "aspect_ratio": "1:1",
    "resolution": "1K"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (от 3 до 5000 символов)
2. **Валидация aspect_ratio**: Проверяется что значение из допустимого списка (8 значений, включая "auto")
3. **Валидация resolution**: Проверяется что значение "1K" или "2K" (конвертируется в верхний регистр)
4. **Дефолтные значения**: Применяются если параметры не указаны (aspect_ratio="1:1", resolution="1K")
5. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=flux-2/flex-text-to-image TYPE=t2i INPUT_KEYS=['prompt', 'aspect_ratio', 'resolution'] 
INPUT_PREVIEW={'prompt': 'A humanoid figure...', 'aspect_ratio': '1:1', 'resolution': '1K'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt от 3 до 5000 символов** - проверяется минимум и максимум!
2. **aspect_ratio обязательный** - не опциональный! 8 значений (включая "auto")!
3. **resolution обязательный** - не опциональный! Только "1K" или "2K"!
4. **Дефолтные значения**: aspect_ratio="1:1", resolution="1K"
5. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими моделями

### Сравнение с flux-2/pro-text-to-image

| Параметр | flux-2/pro-text-to-image | flux-2/flex-text-to-image |
|----------|---------------------------|----------------------------|
| `prompt` | От 3 до 5000 символов | От 3 до 5000 символов |
| `aspect_ratio` | 8 значений (с "auto") | 8 значений (с "auto") |
| `resolution` | Обязательный, "1K"\|"2K" | Обязательный, "1K"\|"2K" |
| Тип модели | t2i | t2i |
| **Разница** | Pro версия (более качественная) | Flex версия (более быстрая) |

### Сравнение с flux-2/flex-image-to-image

| Параметр | flux-2/flex-image-to-image | flux-2/flex-text-to-image |
|----------|----------------------------|----------------------------|
| `prompt` | От 3 до 5000 символов | От 3 до 5000 символов |
| `input_urls` | Обязательный массив (1-8) | Не требуется |
| `aspect_ratio` | 8 значений (с "auto") | 8 значений (с "auto") |
| `resolution` | Обязательный, "1K"\|"2K" | Обязательный, "1K"\|"2K" |
| Тип модели | i2i | t2i |

### Сравнение с seedream/4.5-text-to-image

| Параметр | seedream/4.5-text-to-image | flux-2/flex-text-to-image |
|----------|----------------------------|---------------------------|
| `prompt` | Максимум 3000 символов | От 3 до 5000 символов |
| `aspect_ratio` | 8 значений (без "auto") | 8 значений (с "auto") |
| `quality` | Обязательный, "basic"\|"high" | Не требуется |
| `resolution` | Не требуется | Обязательный, "1K"\|"2K" |
| Тип модели | t2i | t2i |

## 🎯 Ключевые отличия

1. **Диапазон prompt**: От 3 до 5000 символов (не просто максимум!)
2. **Нет input_urls**: В отличие от image-to-image, text-to-image не требует входных изображений
3. **aspect_ratio с "auto"**: Включает опцию "auto" для автоматического определения соотношения сторон
4. **resolution обязательный**: В отличие от других t2i моделей, требует явного указания разрешения
5. **Дефолтные значения**: aspect_ratio="1:1", resolution="1K"
6. **Переиспользование кода**: Использует те же функции нормализации, что и `flux-2/pro-text-to-image`

