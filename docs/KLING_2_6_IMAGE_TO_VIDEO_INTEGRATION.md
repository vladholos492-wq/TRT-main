# Интеграция kling-2.6/image-to-video

## 📋 Обзор

Документация по интеграции модели `kling-2.6/image-to-video` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `kling-2.6/image-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"kling-2.6/image-to-video"` |
| `input.prompt` | string | Текстовое описание видео | Обязательный, максимум 1000 символов |
| `input.image_urls` | array | Массив URL изображений | Обязательный, минимум 1 URL |
| `input.sound` | boolean | Включить звук в видео | Обязательный boolean |
| `input.duration` | string | Длительность видео в секундах | Обязательный, "5" или "10" |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 1000 символов
- **Важно**: Не может быть пустым! Максимум 1000 символов (не 3000!)

#### `image_urls`
- Тип: массив строк (array of strings)
- Обязательный: да
- Формат: `["https://example.com/image.jpg"]`
- Поддерживает множественные изображения
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: JPEG, PNG, WebP
- **Важно**: Должен быть массивом, не строкой!

#### `sound`
- Тип: boolean
- Обязательный: да
- Значения: `true` или `false`
- Default: `false`
- **Важно**: Обязательный параметр, не опциональный! Принимает boolean, строки конвертируются автоматически.

#### `duration`
- Тип: string
- Обязательный: да
- Значения:
  - `"5"` - 5 секунд (default)
  - `"10"` - 10 секунд
- **Важно**: Обязательный параметр, не опциональный! Только "5" и "10" (не "15"!)
- **Важно**: Должно быть строкой, не числом!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_kling_2_6_image_to_video(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для kling-2.6/image-to-video согласно документации API.
    """
    if model_id != "kling-2.6/image-to-video":
        return True, None
    
    # Валидация prompt: обязательный, максимум 1000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации видео"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len > 1000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 1000)"
    
    # Валидация image_urls: обязательный массив
    image_urls = normalized_input.get('image_urls')
    if not image_urls:
        return False, "Поле 'image_urls' обязательно для генерации видео из изображения"
    
    # Нормализуем image_urls (конвертируем строку в массив если нужно)
    normalized_image_urls = _normalize_image_urls_for_wan_2_6(image_urls)
    if not normalized_image_urls:
        return False, "Поле 'image_urls' должно содержать хотя бы один валидный URL изображения"
    
    # Проверяем что все URL начинаются с http:// или https://
    for idx, url in enumerate(normalized_image_urls):
        if not (url.startswith('http://') or url.startswith('https://')):
            return False, f"URL изображения #{idx + 1} должен начинаться с http:// или https://"
    
    # Сохраняем нормализованное значение
    normalized_input['image_urls'] = normalized_image_urls
    
    # Валидация sound: обязательный boolean
    sound = normalized_input.get('sound')
    if sound is None:
        return False, "Поле 'sound' обязательно для генерации видео"
    
    normalized_sound = _normalize_sound_for_kling_2_6(sound)
    if normalized_sound is None:
        return False, f"Поле 'sound' должно быть boolean (true/false)"
    normalized_input['sound'] = normalized_sound
    
    # Валидация duration: обязательный, "5" | "10", default "5"
    duration = normalized_input.get('duration')
    if not duration:
        return False, "Поле 'duration' обязательно для генерации видео"
    
    normalized_duration = _normalize_duration_for_kling_2_6(duration)
    if normalized_duration is None:
        return False, f"Поле 'duration' должно быть '5' или '10'"
    normalized_input['duration'] = normalized_duration
    
    return True, None
```

### Нормализация sound

Функция нормализации конвертирует строки и числа в boolean:

```python
def _normalize_sound_for_kling_2_6(value: Any) -> Optional[bool]:
    """
    Нормализует sound для kling-2.6/image-to-video.
    Принимает boolean, строку или число и возвращает boolean.
    """
    # Если это уже boolean, возвращаем как есть
    if isinstance(value, bool):
        return value
    
    # Если это строка, конвертируем в boolean
    if isinstance(value, str):
        str_value = str(value).strip().lower()
        if str_value in ['true', '1', 'yes', 'on']:
            return True
        elif str_value in ['false', '0', 'no', 'off']:
            return False
    
    # Если это число, конвертируем в boolean
    if isinstance(value, (int, float)):
        return bool(value)
    
    return None
```

### Нормализация duration

Функция нормализации конвертирует числа в строки:

```python
def _normalize_duration_for_kling_2_6(value: Any) -> Optional[str]:
    """
    Нормализует duration для kling-2.6/image-to-video.
    ВАЖНО: Для kling-2.6 поддерживаются только "5" и "10"!
    """
    # Проверяем что это валидное значение (только 5 и 10!)
    valid_values = ["5", "10"]
    if str_value in valid_values:
        return str_value
    ...
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id == "kling-2.6/image-to-video":
    if 'sound' not in normalized_input:
        normalized_input['sound'] = False  # Default согласно документации
    if 'duration' not in normalized_input:
        normalized_input['duration'] = "5"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "kling-2.6/image-to-video",
  "input": {
    "prompt": "In a bright rehearsal room, sunlight streams through the window, and a standing microphone is placed in the center of the room. [Campus band female lead singer] stands in front of the microphone with her eyes closed, while the other members stand around her.",
    "image_urls": ["https://static.aiquickdraw.com/tools/example/1764851002741_i0lEiI8I.png"],
    "sound": false,
    "duration": "5"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (максимум 1000 символов)
2. **Валидация image_urls**: Проверяется что это массив с хотя бы одним валидным URL
3. **Нормализация image_urls**: Автоматически конвертирует строку в массив
4. **Валидация sound**: Проверяется что это boolean (конвертируется из строки/числа если нужно)
5. **Валидация duration**: Проверяется что значение "5" или "10" (конвертируется в строку)
6. **Дефолтные значения**: Применяются если параметры не указаны (sound=false, duration="5")
7. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=kling-2.6/image-to-video TYPE=i2v INPUT_KEYS=['prompt', 'image_urls', 'sound', 'duration'] 
INPUT_PREVIEW={'prompt': 'In a bright rehearsal...', 'image_urls': ['https://...'], 'sound': False, 'duration': '5'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt максимум 1000 символов** - проверяется при валидации (не 3000!)
2. **image_urls обязательный массив** - автоматически конвертируется из строки если нужно
3. **sound обязательный boolean** - не опциональный! Конвертируется из строки/числа автоматически
4. **duration обязательный** - не опциональный! Только "5" или "10" (не "15"!)
5. **duration должно быть строкой**, не числом
6. **Дефолтные значения**: sound=false, duration="5"
7. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими i2v моделями

| Параметр | wan/2-6-image-to-video | kling-2.6/image-to-video |
|----------|------------------------|---------------------------|
| `prompt` | 2-5000 символов | Максимум 1000 символов |
| `image_urls` | Обязательный массив | Обязательный массив |
| `sound` | Не требуется | Обязательный boolean |
| `duration` | "5"\|"10"\|"15" | "5"\|"10" (обязательный!) |
| `resolution` | "720p"\|"1080p" | Не требуется |

## 🎯 Ключевые отличия

1. **sound обязательный**: В отличие от других моделей, kling-2.6 требует явного указания sound
2. **duration обязательный**: Не опциональный, должен быть указан явно
3. **Ограничение prompt**: Максимум 1000 символов (не 3000 или 5000)
4. **Нет resolution**: В отличие от wan/2-6, kling-2.6 не использует resolution параметр

