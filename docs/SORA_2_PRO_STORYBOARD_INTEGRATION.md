# Интеграция sora-2-pro-storyboard

## 📋 Обзор

Документация по интеграции модели `sora-2-pro-storyboard` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `sora-2-pro-storyboard`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"sora-2-pro-storyboard"` |
| `input.n_frames` | string | Общая длительность видео | Обязательный, только "10", "15", или "25" |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.image_urls` | array | Массив URL изображений для использования в качестве входных данных | Не указан (опциональный) |
| `input.aspect_ratio` | string | Соотношение сторон изображения | `"landscape"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `n_frames`
- Тип: string
- Обязательный: да
- Значения:
  - `"10"` - 10 секунд
  - `"15"` - 15 секунд (default)
  - `"25"` - 25 секунд
- **Важно**: Обязательный параметр! Только 3 значения!

#### `image_urls`
- Тип: array
- Обязательный: нет (опциональный!)
- **Важно**: Массив URL изображений (может быть несколько файлов!)
- **Важно**: Максимум 10MB на файл, форматы: JPEG, PNG, WebP
- **Важно**: Каждый URL должен начинаться с `http://` или `https://`

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"portrait"` - Портретная ориентация
  - `"landscape"` - Альбомная ориентация (default)
- **Важно**: Опциональный параметр! Только 2 значения!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_sora_2_pro_storyboard(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для sora-2-pro-storyboard согласно документации API.
    """
    # Проверяем оба возможных ID модели
    if model_id not in ["sora-2-pro-storyboard", "sora-2-pro/storyboard", "openai/sora-2-pro-storyboard"]:
        return True, None
    
    # Валидация n_frames: обязательный, enum ("10", "15", "25")
    n_frames = normalized_input.get('n_frames')
    if not n_frames:
        return False, "Поле 'n_frames' обязательно для генерации storyboard. Укажите длительность видео: '10', '15' или '25' секунд"
    
    normalized_n_frames = _normalize_n_frames_for_sora_2_pro_storyboard(n_frames)
    if normalized_n_frames is None:
        valid_values = ["10", "15", "25"]
        return False, f"Поле 'n_frames' должно быть одним из: {', '.join(valid_values)}"
    normalized_input['n_frames'] = normalized_n_frames
    
    # Валидация image_urls: опциональный массив
    image_urls = normalized_input.get('image_urls')
    if image_urls is not None:
        # Переиспользуем функцию нормализации из wan/2-6-image-to-video
        normalized_image_urls = _normalize_image_urls_for_wan_2_6(image_urls)
        if normalized_image_urls is None:
            return False, "Поле 'image_urls' должно быть массивом валидных URL изображений"
        normalized_input['image_urls'] = normalized_image_urls
    
    # Валидация aspect_ratio: опциональный, enum ("portrait" или "landscape")
    aspect_ratio = normalized_input.get('aspect_ratio')
    if aspect_ratio is not None:
        normalized_aspect_ratio = _normalize_aspect_ratio_for_sora_2_pro_storyboard(aspect_ratio)
        if normalized_aspect_ratio is None:
            valid_values = ["portrait", "landscape"]
            return False, f"Поле 'aspect_ratio' должно быть одним из: {', '.join(valid_values)}"
        normalized_input['aspect_ratio'] = normalized_aspect_ratio
    
    return True, None
```

### Нормализация n_frames

Функция нормализации поддерживает только 3 значения:

```python
def _normalize_n_frames_for_sora_2_pro_storyboard(value: Any) -> Optional[str]:
    """
    Нормализует n_frames для sora-2-pro-storyboard.
    ВАЖНО: Для sora-2-pro-storyboard поддерживаются только "10", "15", "25"!
    """
    valid_values = ["10", "15", "25"]
    # Поддерживает алиасы (10s, 10sec, 10 seconds, 15s, 15sec, 15 seconds, 25s, 25sec, 25 seconds)
    # Поддерживает числа (10, 15, 25)
    ...
```

### Нормализация aspect_ratio

Функция нормализации поддерживает только 2 значения:

```python
def _normalize_aspect_ratio_for_sora_2_pro_storyboard(value: Any) -> Optional[str]:
    """
    Нормализует aspect_ratio для sora-2-pro-storyboard.
    ВАЖНО: Для sora-2-pro-storyboard поддерживаются только "portrait" и "landscape"!
    """
    valid_values = ["portrait", "landscape"]
    # Поддерживает алиасы (port, vertical, vert, land, horizontal, horiz)
    ...
```

### Нормализация image_urls

Переиспользуется функция нормализации из `wan/2-6-image-to-video`:

```python
# Переиспользуем функцию нормализации из wan/2-6-image-to-video
_normalize_image_urls_for_wan_2_6(image_urls)
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id in ["sora-2-pro-storyboard", "sora-2-pro/storyboard", "openai/sora-2-pro-storyboard"]:
    if 'n_frames' not in normalized_input:
        normalized_input['n_frames'] = "15"  # Default согласно документации
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "landscape"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "sora-2-pro-storyboard",
  "input": {
    "n_frames": "15",
    "image_urls": ["https://file.aiquickdraw.com/custom-page/akr/section-images/1760776438785hyue5ogz.png"],
    "aspect_ratio": "landscape"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация n_frames**: Проверяется что значение "10", "15" или "25" - обязательный!
2. **Валидация image_urls**: Проверяется что это массив валидных URL (опциональный!)
3. **Валидация aspect_ratio**: Проверяется что значение "portrait" или "landscape" (опциональный!)
4. **Дефолтные значения**: Применяются если параметры не указаны (n_frames="15", aspect_ratio="landscape")
5. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=sora-2-pro-storyboard TYPE=i2v INPUT_KEYS=['n_frames', 'image_urls', 'aspect_ratio'] 
INPUT_PREVIEW={'n_frames': '15', 'image_urls': ['https://...'], 'aspect_ratio': 'landscape'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **n_frames обязательный** - обязателен для генерации storyboard
2. **n_frames только 3 значения** - только "10", "15", или "25" (не другие значения!)
3. **image_urls опциональный** - не обязательный! Может быть несколько изображений!
4. **image_urls массив** - должен быть массивом URL (не строкой!)
5. **aspect_ratio опциональный** - не обязательный! Только 2 значения: "portrait" или "landscape"!
6. **Дефолтные значения**: n_frames="15", aspect_ratio="landscape"
7. **Переиспользование кода**: Использует функцию нормализации image_urls из wan/2-6-image-to-video
8. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими моделями

### Сравнение с sora-2-text-to-video

| Параметр | sora-2-text-to-video | sora-2-pro-storyboard |
|----------|----------------------|----------------------|
| `prompt` | Обязательный, max 10000 | Не требуется |
| `n_frames` | Опциональный, "10"\|"15" | Обязательный, "10"\|"15"\|"25" |
| `aspect_ratio` | Опциональный, "portrait"\|"landscape" | Опциональный, "portrait"\|"landscape" |
| `image_urls` | Не требуется | Опциональный массив |
| Тип модели | t2v | i2v (storyboard) |

### Сравнение с sora-2-image-to-video

| Параметр | sora-2-image-to-video | sora-2-pro-storyboard |
|----------|----------------------|----------------------|
| `prompt` | Обязательный, max 10000 | Не требуется |
| `image_url` | Обязательный строка | Не используется |
| `image_urls` | Не используется | Опциональный массив |
| `n_frames` | Опциональный, "10"\|"15" | Обязательный, "10"\|"15"\|"25" |
| `aspect_ratio` | Опциональный, "portrait"\|"landscape" | Опциональный, "portrait"\|"landscape" |
| Тип модели | i2v | i2v (storyboard) |

### Сравнение с wan/2-6-image-to-video

| Параметр | wan/2-6-image-to-video | sora-2-pro-storyboard |
|----------|------------------------|----------------------|
| `prompt` | Обязательный, 1-5000 символов | Не требуется |
| `image_urls` | Обязательный массив | Опциональный массив |
| `duration` | Обязательный, "5"\|"10"\|"15" | Не используется |
| `resolution` | Обязательный, "720p"\|"1080p" | Не используется |
| `n_frames` | Не используется | Обязательный, "10"\|"15"\|"25" |
| `aspect_ratio` | Не требуется | Опциональный, "portrait"\|"landscape" |
| Тип модели | i2v | i2v (storyboard) |

## 🎯 Ключевые отличия

1. **n_frames обязательный**: Обязателен для генерации storyboard, только 3 значения ("10", "15", "25")
2. **Нет prompt**: В отличие от других моделей, здесь нет параметра prompt!
3. **image_urls опциональный**: Может быть несколько изображений, но не обязателен
4. **aspect_ratio опциональный**: Только 2 значения ("portrait" или "landscape")
5. **Переиспользование кода**: Использует функцию нормализации image_urls из wan/2-6-image-to-video
6. **Нормализация типов**: n_frames и aspect_ratio конвертируются с поддержкой алиасов
7. **Дефолтные значения**: Применяются автоматически (n_frames="15", aspect_ratio="landscape")
8. **Сообщения об ошибках**: Понятные сообщения для пользователя

## 💡 Особенности Storyboard

Модель `sora-2-pro-storyboard` предназначена для создания storyboard (раскадровки) из изображений:

- **Входные данные**: Одно или несколько изображений (опционально)
- **Выходные данные**: Видео storyboard заданной длительности
- **Длительность**: 10, 15 или 25 секунд
- **Ориентация**: Портретная или альбомная

Это отличается от обычных image-to-video моделей тем, что:
- Не требует prompt (описание текстом)
- Работает с изображениями как с раскадровкой
- Создаёт видео на основе последовательности изображений

