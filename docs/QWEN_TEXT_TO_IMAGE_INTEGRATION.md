# Интеграция qwen/text-to-image

## 📋 Обзор

Документация по интеграции модели `qwen/text-to-image` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `qwen/text-to-image`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"qwen/text-to-image"` |
| `input.prompt` | string | Промпт для генерации изображения | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.image_size` | string | Размер сгенерированного изображения | `"square_hd"` |
| `input.num_inference_steps` | number | Количество шагов вывода для выполнения | `30` |
| `input.seed` | number | Одинаковое зерно и одинаковый промпт для одной версии модели будут выдавать одно и то же изображение каждый раз | Не указан |
| `input.guidance_scale` | number | Масштаб CFG (Classifier Free Guidance) - мера того, насколько близко модель должна следовать вашему промпту | `2.5` |
| `input.enable_safety_checker` | boolean | Проверщик безопасности всегда включен в Playground. Может быть отключен только установкой false через API | `true` |
| `input.output_format` | string | Формат сгенерированного изображения | `"png"` |
| `input.negative_prompt` | string | Негативный промпт для генерации | `" "` (пробел!) |
| `input.acceleration` | string | Уровень ускорения для генерации изображения. 'regular' балансирует скорость и качество. 'high' рекомендуется для изображений без текста | `"none"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `image_size`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"square"` - Квадрат (512*512)
  - `"square_hd"` - Квадрат HD (1024*1024) (default)
  - `"portrait_4_3"` - Портрет 3:4 (768*1024)
  - `"portrait_16_9"` - Портрет 9:16 (576*1024)
  - `"landscape_4_3"` - Пейзаж 4:3 (1024*768)
  - `"landscape_16_9"` - Пейзаж 16:9 (1024*576)
- **Важно**: Опциональный параметр! Только 6 указанных значений! Default: `"square_hd"`!

#### `num_inference_steps`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 2-250 (шаг: 1)
- Default: `30`
- **Важно**: Опциональный параметр! Только числа от 2 до 250! Default: `30`!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- **Важно**: Опциональный параметр! Одинаковое зерно и одинаковый промпт для одной версии модели будут выдавать одно и то же изображение каждый раз!

#### `guidance_scale`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0-20 (шаг: 0.1)
- Default: `2.5`
- **Важно**: Опциональный параметр! Только числа от 0 до 20! Default: `2.5`! Мера того, насколько близко модель должна следовать вашему промпту!

#### `enable_safety_checker`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `true`! Проверщик безопасности всегда включен в Playground. Может быть отключен только установкой false через API!

#### `output_format`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"png"`, `"jpeg"`
- Default: `"png"`
- **Важно**: Опциональный параметр! Только указанные значения! Default: `"png"`!

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 500 символов
- Default: `" "` (пробел!)
- **Важно**: Опциональный параметр! Максимум 500 символов! Default: пробел `" "` (не пустая строка!)!

#### `acceleration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"none"` - Нет (default)
  - `"regular"` - Обычное (балансирует скорость и качество)
  - `"high"` - Высокое (рекомендуется для изображений без текста)
- Default: `"none"`
- **Важно**: Опциональный параметр! Только 3 указанных значения! Default: `"none"`! Более высокое ускорение увеличивает скорость!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | qwen/text-to-image | Другие t2i модели |
|----------|-------------------|-------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный |
| `image_size` | ✅ Есть (enum, 6 значений) | ✅ Может быть |
| `num_inference_steps` | ✅ Есть (2-250) | ❌ Нет (используют `steps`) |
| `seed` | ✅ Есть | ✅ Может быть |
| `guidance_scale` | ✅ Есть (0-20) | ❌ Нет (используют `guidance`) |
| `enable_safety_checker` | ✅ Есть (boolean) | ❌ Нет |
| `output_format` | ✅ Есть (enum: png/jpeg) | ❌ Нет |
| `negative_prompt` | ✅ Есть (max 500, default " ") | ✅ Может быть |
| `acceleration` | ✅ Есть (enum: none/regular/high) | ❌ Нет |
| `width` | ❌ Нет | ✅ Может быть |
| `height` | ❌ Нет | ✅ Может быть |
| `steps` | ❌ Нет | ✅ Может быть |
| `guidance` | ❌ Нет | ✅ Может быть |
| `style` | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для генерации изображений из текста (text-to-image)!
- Использует `num_inference_steps` (2-250) вместо стандартного `steps`!
- Использует `guidance_scale` (0-20) вместо стандартного `guidance`!
- Использует `image_size` (enum, 6 значений) вместо стандартных `width`/`height`!
- Имеет уникальные параметры: `output_format`, `acceleration`, `enable_safety_checker`!
- `negative_prompt` имеет default `" "` (пробел!), а не пустую строку!
- НЕТ параметров `width`, `height`, `steps`, `guidance`, `style` (в отличие от других t2i моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_qwen_text_to_image()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Значение `image_size`**: Должно быть одним из 6 допустимых значений (опциональный)
3. **Значение `num_inference_steps`**: Должно быть числом от 2 до 250 (опциональный)
4. **Значение `seed`**: Должно быть числом (опциональный)
5. **Значение `guidance_scale`**: Должно быть числом от 0 до 20 (опциональный)
6. **Значение `enable_safety_checker`**: Должно быть boolean (опциональный)
7. **Значение `output_format`**: Должно быть одним из 2 допустимых значений (опциональный)
8. **Длина `negative_prompt`**: Максимум 500 символов (опциональный)
9. **Значение `acceleration`**: Должно быть одним из 3 допустимых значений (опциональный)

### Нормализация

1. **`_normalize_image_size_for_qwen_t2i()`**: Нормализует image_size (6 значений, case-insensitive)
2. **`_normalize_output_format_for_qwen_i2i()`**: Нормализует output_format (2 значения) - переиспользуется из i2i
3. **`_normalize_acceleration_for_qwen_i2i()`**: Нормализует acceleration (3 значения) - переиспользуется из i2i
4. **`_normalize_boolean()`**: Нормализует enable_safety_checker (boolean) - переиспользуется из других моделей

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'image_size' not in normalized_input:
    normalized_input['image_size'] = "square_hd"  # Default согласно документации
if 'num_inference_steps' not in normalized_input:
    normalized_input['num_inference_steps'] = 30  # Default согласно документации
if 'guidance_scale' not in normalized_input:
    normalized_input['guidance_scale'] = 2.5  # Default согласно документации
if 'enable_safety_checker' not in normalized_input:
    normalized_input['enable_safety_checker'] = True  # Default согласно документации
if 'output_format' not in normalized_input:
    normalized_input['output_format'] = "png"  # Default согласно документации
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = " "  # Default согласно документации (пробел!)
if 'acceleration' not in normalized_input:
    normalized_input['acceleration'] = "none"  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "qwen/text-to-image",
  "input": {
    "prompt": "Enter your prompt here...",
    "image_size": "square_hd",
    "num_inference_steps": 30,
    "seed": 42,
    "guidance_scale": 2.5,
    "enable_safety_checker": true,
    "output_format": "png",
    "negative_prompt": " ",
    "acceleration": "none"
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации изображения |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'image_size' должно быть одним из...` | Неверное значение `image_size` | Используйте одно из 6 допустимых значений |
| `Поле 'num_inference_steps' должно быть в диапазоне...` | Неверное значение `num_inference_steps` | Используйте число от 2 до 250 |
| `Поле 'guidance_scale' должно быть в диапазоне...` | Неверное значение `guidance_scale` | Используйте число от 0 до 20 |
| `Поле 'enable_safety_checker' должно быть boolean...` | Неверное значение `enable_safety_checker` | Используйте true/false |
| `Поле 'output_format' должно быть одним из...` | Неверное значение `output_format` | Используйте одно из: png, jpeg |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 500 символов | Сократите до 500 символов |
| `Поле 'acceleration' должно быть одним из...` | Неверное значение `acceleration` | Используйте одно из: none, regular, high |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("qwen/text-to-image")
user_payload = {
    "prompt": "Enter your prompt here...",
    "image_size": "square_hd",
    "num_inference_steps": 30,
    "seed": 42,
    "guidance_scale": 2.5,
    "enable_safety_checker": True,
    "output_format": "png",
    "negative_prompt": " ",
    "acceleration": "none"
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлены параметры `output_format`, `acceleration`, `num_inference_steps`, `enable_safety_checker` в схему `t2i`
- [x] Реализована функция `_validate_qwen_text_to_image()`
- [x] Добавлена функция нормализации `_normalize_image_size_for_qwen_t2i()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения (включая `negative_prompt=" "` (пробел!))
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=qwen/text-to-image TYPE=t2i INPUT_KEYS=['prompt', 'image_size', 'num_inference_steps', 'seed', 'guidance_scale', 'enable_safety_checker', 'output_format', 'negative_prompt', 'acceleration'] 
INPUT_PREVIEW={'prompt': 'Enter your prompt here...', 'image_size': 'square_hd', 'num_inference_steps': 30, 'seed': 42, 'guidance_scale': 2.5, 'enable_safety_checker': True, 'output_format': 'png', 'negative_prompt': ' ', 'acceleration': 'none'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации изображения (максимум 5000 символов)
2. **image_size опциональный** - не обязательный! Только 6 значений, default "square_hd"!
3. **num_inference_steps опциональный** - не обязательный! Только числа от 2 до 250, default 30! Используется вместо стандартного `steps`!
4. **guidance_scale опциональный** - не обязательный! Только числа от 0 до 20, default 2.5! Используется вместо стандартного `guidance`!
5. **enable_safety_checker опциональный** - не обязательный! Boolean (true/false), default true! Проверщик безопасности всегда включен в Playground. Может быть отключен только установкой false через API!
6. **output_format опциональный** - не обязательный! Только 2 значения, default "png"!
7. **negative_prompt опциональный** - не обязательный! Максимум 500 символов, default `" "` (пробел!), а не пустая строка!
8. **acceleration опциональный** - не обязательный! Только 3 значения, default "none"! 'regular' балансирует скорость и качество. 'high' рекомендуется для изображений без текста!
9. **Уникальные параметры** - модель использует `num_inference_steps`, `guidance_scale`, `output_format`, `acceleration`, `enable_safety_checker` вместо стандартных `width`, `height`, `steps`, `guidance`, `style`!

