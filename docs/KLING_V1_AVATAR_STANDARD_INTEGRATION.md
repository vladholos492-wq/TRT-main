# Интеграция kling/v1-avatar-standard

## 📋 Обзор

Документация по интеграции модели `kling/v1-avatar-standard` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `kling/v1-avatar-standard`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"kling/v1-avatar-standard"` |
| `input.image_url` | string | URL изображения для аватара | Обязательный, макс 10MB, jpeg/png/webp |
| `input.audio_url` | string | URL аудио файла | Обязательный, макс 10MB, mpeg/wav/aac/mp4/ogg |
| `input.prompt` | string | Промпт для генерации видео | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Должен быть валидным URL изображения!

#### `audio_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: `audio/mpeg`, `audio/wav`, `audio/x-wav`, `audio/aac`, `audio/mp4`, `audio/ogg`
- **Важно**: Обязательный параметр! Должен быть валидным URL аудио файла!

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | kling/v1-avatar-standard | Другие lip_sync модели |
|----------|-------------------------|------------------------|
| `image_url` | ✅ Обязательный | ✅ Обязательный |
| `audio_url` | ✅ Обязательный | ✅ Обязательный |
| `prompt` | ✅ Обязательный (max 5000) | ❌ Опциональный |
| `video_url` | ❌ Не поддерживается | ✅ Может быть обязательным |

**ВАЖНО**: 
- Это модель lip sync для генерации аватара с синхронизацией губ!
- Требует **все три параметра**: `image_url`, `audio_url`, `prompt`!
- `prompt` обязательный (в отличие от других lip sync моделей)!
- Максимальный размер файлов: 10MB для изображения и аудио!

## 🔧 Реализация

### Валидация

Функция `_validate_kling_v1_avatar_standard()` проверяет:

1. **Обязательность `image_url`**: Поле должно быть указано и не пустое
2. **Обязательность `audio_url`**: Поле должно быть указано и не пустое
3. **Обязательность `prompt`**: Поле должно быть указано и не пустое
4. **Длина `prompt`**: Максимум 5000 символов

### Нормализация

1. **`image_url`**: Нормализуется как строка (trim)
2. **`audio_url`**: Нормализуется как строка (trim)
3. **`prompt`**: Нормализуется как строка (trim)

### Дефолтные значения

Дефолтные значения не применяются, так как все параметры обязательные.

## 📊 Пример использования

### Запрос

```json
{
  "model": "kling/v1-avatar-standard",
  "input": {
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17579268936223zs9l3dt.png",
    "audio_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17579258340109gghun47.mp3",
    "prompt": "Enter your prompt here..."
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'image_url' обязательно...` | `image_url` не указан | Загрузите изображение для аватара |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите валидный URL изображения |
| `Поле 'audio_url' обязательно...` | `audio_url` не указан | Загрузите аудио файл |
| `Поле 'audio_url' не может быть пустым` | `audio_url` пустой | Укажите валидный URL аудио файла |
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("kling/v1-avatar-standard")
user_payload = {
    "image_url": "https://example.com/image.png",
    "audio_url": "https://example.com/audio.mp3",
    "prompt": "Enter your prompt here..."
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлен `prompt` в схему `lip_sync` в `input_schemas.py`
- [x] Реализована функция `_validate_kling_v1_avatar_standard()`
- [x] Добавлена валидация в `build_input()`
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=kling/v1-avatar-standard TYPE=lip_sync INPUT_KEYS=['image_url', 'audio_url', 'prompt'] 
INPUT_PREVIEW={'image_url': 'https://...', 'audio_url': 'https://...', 'prompt': 'Enter your prompt here...'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **image_url обязательный** - обязателен для генерации аватара (макс 10MB, jpeg/png/webp)
2. **audio_url обязательный** - обязателен для генерации аватара (макс 10MB, mpeg/wav/aac/mp4/ogg)
3. **prompt обязательный** - обязателен для генерации видео (максимум 5000 символов)!
4. **Все три параметра обязательны** - модель требует все три параметра одновременно!
5. **Размер файлов** - максимальный размер файлов: 10MB для изображения и аудио (валидация размера на стороне KIE API)
6. **Форматы файлов** - поддерживаются только указанные форматы (валидация формата на стороне KIE API)













