# Интеграция grok-imagine/text-to-video

## 📋 Обзор

Документация по интеграции модели `grok-imagine/text-to-video` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `grok-imagine/text-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"grok-imagine/text-to-video"` |
| `input.prompt` | string | Текстовое описание движения видео | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.aspect_ratio` | string | Соотношение сторон видео | `"2:3"` |
| `input.mode` | string | Режим генерации | `"normal"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Не может быть пустым! Максимум 5000 символов!

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"2:3"` - Вертикальный 2:3 (default)
  - `"3:2"` - Горизонтальный 3:2
  - `"1:1"` - Квадрат
- **Важно**: Опциональный параметр! Только 3 значения (не 5, 8 или 11)!

#### `mode`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"fun"` - Fun режим
  - `"normal"` - Normal режим (default)
  - `"spicy"` - Spicy режим
- **Важно**: Опциональный параметр! Те же значения, что и для image-to-video!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_grok_imagine_text_to_video(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для grok-imagine/text-to-video согласно документации API.
    """
    # Проверяем оба возможных ID модели
    if model_id not in ["grok-imagine/text-to-video", "grok/imagine-text-to-video"]:
        return True, None
    
    # Валидация prompt: обязательный, максимум 5000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации видео"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len > 5000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 5000)"
    
    # Валидация aspect_ratio: опциональный, enum
    aspect_ratio = normalized_input.get('aspect_ratio')
    if aspect_ratio is not None:
        normalized_aspect_ratio = _normalize_aspect_ratio_for_grok_imagine_text_to_video(aspect_ratio)
        if normalized_aspect_ratio is None:
            valid_values = ["2:3", "3:2", "1:1"]
            return False, f"Поле 'aspect_ratio' должно быть одним из: {', '.join(valid_values)}"
        normalized_input['aspect_ratio'] = normalized_aspect_ratio
    
    # Валидация mode: опциональный, enum
    mode = normalized_input.get('mode')
    if mode is not None:
        normalized_mode = _normalize_mode_for_grok_imagine(mode)
        if normalized_mode is None:
            return False, f"Поле 'mode' должно быть 'fun', 'normal' или 'spicy'"
        normalized_input['mode'] = normalized_mode
    
    return True, None
```

### Нормализация aspect_ratio

Функция нормализации поддерживает только 3 значения:

```python
def _normalize_aspect_ratio_for_grok_imagine_text_to_video(value: Any) -> Optional[str]:
    """
    Нормализует aspect_ratio для grok-imagine/text-to-video.
    ВАЖНО: Для grok-imagine/text-to-video поддерживаются только "2:3", "3:2", "1:1"!
    """
    valid_values = ["2:3", "3:2", "1:1"]
    # Поддерживает алиасы (2/3, 2x3, 3/2, 3x2, 1/1, 1x1, square)
    ...
```

### Нормализация mode

Используется та же функция нормализации, что и для `grok-imagine/image-to-video`:

```python
def _normalize_mode_for_grok_imagine(value: Any) -> Optional[str]:
    """
    Нормализует mode для grok-imagine/text-to-video.
    ВАЖНО: Для grok-imagine поддерживаются только "fun", "normal", "spicy"!
    """
    # Конвертирует в нижний регистр и проверяет валидность
    ...
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id in ["grok-imagine/text-to-video", "grok/imagine-text-to-video"]:
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "2:3"  # Default согласно документации
    if 'mode' not in normalized_input:
        normalized_input['mode'] = "normal"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "grok-imagine/text-to-video",
  "input": {
    "prompt": "A couple of doors open to the right one by one randomly and stay open, to show the inside, each is either an living room, or a kitchen, or a bedroom or an office, with little people living inside.",
    "aspect_ratio": "2:3",
    "mode": "normal"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (максимум 5000 символов) - обязательный!
2. **Валидация aspect_ratio**: Проверяется что значение из допустимого списка (3 значения) (опциональный!)
3. **Валидация mode**: Проверяется что значение "fun", "normal" или "spicy" (опциональный!)
4. **Дефолтные значения**: Применяются если параметры не указаны (aspect_ratio="2:3", mode="normal")
5. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=grok-imagine/text-to-video TYPE=t2v INPUT_KEYS=['prompt', 'aspect_ratio', 'mode'] 
INPUT_PREVIEW={'prompt': 'A couple of doors...', 'aspect_ratio': '2:3', 'mode': 'normal'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - в отличие от image-to-video, здесь prompt обязателен!
2. **prompt максимум 5000 символов** - проверяется при валидации
3. **aspect_ratio опциональный** - не обязательный! Только 3 значения: "2:3", "3:2", "1:1" (не 5, 8 или 11)!
4. **mode опциональный** - не обязательный! Те же значения, что и для image-to-video
5. **Дефолтные значения**: aspect_ratio="2:3", mode="normal"
6. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими моделями

### Сравнение с grok-imagine/image-to-video

| Параметр | grok-imagine/image-to-video | grok-imagine/text-to-video |
|----------|-----------------------------|----------------------------|
| `prompt` | Опциональный, max 5000 | Обязательный, max 5000 |
| `image_urls` | Опциональный массив (только одно!) | Не требуется |
| `task_id` | Опциональный | Не требуется |
| `index` | Опциональный (только с task_id) | Не требуется |
| `aspect_ratio` | Не требуется | Опциональный, 3 значения |
| `mode` | Опциональный ("fun", "normal", "spicy") | Опциональный ("fun", "normal", "spicy") |
| Тип модели | i2v | t2v |

### Сравнение с kling-2.6/text-to-video

| Параметр | kling-2.6/text-to-video | grok-imagine/text-to-video |
|----------|--------------------------|----------------------------|
| `prompt` | Обязательный, max 1000 | Обязательный, max 5000 |
| `sound` | Обязательный boolean | Не требуется |
| `aspect_ratio` | Обязательный, 3 значения | Опциональный, 3 значения |
| `duration` | Обязательный, "5"\|"10" | Не требуется |
| `mode` | Не требуется | Опциональный ("fun", "normal", "spicy") |
| Тип модели | t2v | t2v |

### Сравнение с wan/2-6-text-to-video

| Параметр | wan/2-6-text-to-video | grok-imagine/text-to-video |
|----------|-----------------------|----------------------------|
| `prompt` | Обязательный, 1-5000 символов | Обязательный, max 5000 |
| `duration` | Обязательный, "5"\|"10"\|"15" | Не требуется |
| `resolution` | Обязательный, "720p"\|"1080p" | Не требуется |
| `aspect_ratio` | Не требуется | Опциональный, 3 значения |
| `mode` | Не требуется | Опциональный ("fun", "normal", "spicy") |
| Тип модели | t2v | t2v |

## 🎯 Ключевые отличия

1. **prompt обязательный**: В отличие от image-to-video, здесь prompt обязателен
2. **Нет изображений**: В отличие от image-to-video, не требует входных изображений
3. **aspect_ratio опциональный**: Только 3 значения (не 5, 8 или 11)
4. **mode опциональный**: Те же значения, что и для image-to-video
5. **Дефолтные значения**: aspect_ratio="2:3", mode="normal"
6. **Переиспользование кода**: Использует ту же функцию нормализации mode, что и image-to-video

