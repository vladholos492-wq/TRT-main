# Интеграция recraft/remove-background

## 📋 Обзор

Документация по интеграции модели `recraft/remove-background` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `recraft/remove-background`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"recraft/remove-background"` |
| `input.image` | string | URL изображения для удаления фона | Обязательный, макс 5MB, PNG/JPG/WEBP, макс 16MP, макс размер 4096px, мин размер 256px |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `image`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 5MB
- Поддерживаемые форматы: `image/jpeg`, `image/png`, `image/webp`
- Максимальное разрешение: 16MP
- Максимальный размер: 4096px (по любой стороне)
- Минимальный размер: 256px (по любой стороне)
- **Важно**: Обязательный параметр! Должен быть валидным URL изображения! Параметр называется `image` (не `image_url`)!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | recraft/remove-background | Другие bg_remove модели |
|----------|--------------------------|------------------------|
| `image` | ✅ Обязательный (называется `image`) | ✅ Обязательный (обычно `image_url`) |
| `image_url` | ❌ Не используется (нормализуется в `image`) | ✅ Может быть обязательным |
| `image_base64` | ❌ Не используется (нормализуется в `image`) | ✅ Может быть обязательным |
| Максимальный размер файла | 5MB | Обычно 10MB |
| Максимальное разрешение | 16MP | Обычно без ограничений |
| Максимальный размер | 4096px | Обычно без ограничений |
| Минимальный размер | 256px | Обычно без ограничений |

**ВАЖНО**: 
- Это модель для удаления фона с изображения!
- Параметр называется `image` (не `image_url` или `image_base64`)!
- Более строгие ограничения по размеру файла (5MB вместо 10MB)!
- Ограничения по разрешению и размерам (16MP, 4096px макс, 256px мин)!

## 🔧 Реализация

### Валидация

Функция `_validate_recraft_remove_background()` проверяет:

1. **Обязательность `image`**: Поле должно быть указано (может быть передано как `image`, `image_url`, или `image_base64`)
2. **Нормализация параметра**: Если передан `image_url` или `image_base64`, нормализуется в `image`
3. **Удаление лишних параметров**: Удаляет `image_url` и `image_base64`, если они были переданы

### Нормализация

1. **Параметр `image`**: Если передан `image_url` или `image_base64`, нормализуется в `image`
2. **Удаление дубликатов**: Удаляет `image_url` и `image_base64`, если `image` уже установлен

### Дефолтные значения

Дефолтные значения не применяются, так как параметр обязательный.

## 📊 Пример использования

### Запрос

```json
{
  "model": "recraft/remove-background",
  "input": {
    "image": "https://file.aiquickdraw.com/custom-page/akr/section-images/1757057285447k9qcbki1.webp"
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'image' обязательно...` | `image` не указан | Загрузите изображение для удаления фона |
| `Поле 'image' не может быть пустым` | `image` пустой | Укажите валидный URL изображения |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("recraft/remove-background")
user_payload = {
    "image": "https://example.com/image.webp"
    # Или можно использовать:
    # "image_url": "https://example.com/image.webp"
    # Будет автоматически нормализовано в "image"
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Реализована функция `_validate_recraft_remove_background()`
- [x] Добавлена валидация в `build_input()`
- [x] Реализована нормализация параметра `image` из `image_url` или `image_base64`
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=recraft/remove-background TYPE=bg_remove INPUT_KEYS=['image'] 
INPUT_PREVIEW={'image': 'https://...'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **image обязательный** - обязателен для удаления фона (макс 5MB, PNG/JPG/WEBP)
2. **Параметр называется `image`** - не `image_url` или `image_base64`! Но если передан `image_url` или `image_base64`, будет автоматически нормализован в `image`!
3. **Ограничения по размеру** - более строгие, чем у других моделей: макс 5MB (вместо 10MB), макс 16MP, макс 4096px, мин 256px!
4. **Форматы файлов** - поддерживаются только PNG, JPG, WEBP (валидация формата на стороне KIE API)
5. **Размеры файлов** - ограничения по разрешению и размерам проверяются на стороне KIE API

