# Интеграция elevenlabs/text-to-speech-multilingual-v2

## 📋 Обзор

Документация по интеграции модели `elevenlabs/text-to-speech-multilingual-v2` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `elevenlabs/text-to-speech-multilingual-v2`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"elevenlabs/text-to-speech-multilingual-v2"` |
| `input.text` | string | Текст для преобразования в речь | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.voice` | string | Голос для генерации речи | `"Rachel"` |
| `input.stability` | number | Стабильность голоса (0-1) | `0.5` |
| `input.similarity_boost` | number | Усиление схожести (0-1) | `0.75` |
| `input.style` | number | Преувеличение стиля (0-1) | `0` |
| `input.speed` | number | Скорость речи (0.7-1.2) | `1` |
| `input.timestamps` | boolean | Возвращать ли временные метки для каждого слова | `false` |
| `input.previous_text` | string | Текст, который был до текущего запроса (для непрерывности) | `""` |
| `input.next_text` | string | Текст, который будет после текущего запроса (для непрерывности) | `""` |
| `input.language_code` | string | Код языка (ISO 639-1) для принудительного использования языка | `""` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `text`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `voice`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"Rachel"` (default)
  - `"Aria"`, `"Roger"`, `"Sarah"`, `"Laura"`, `"Charlie"`, `"George"`, `"Callum"`
  - `"River"`, `"Liam"`, `"Charlotte"`, `"Alice"`, `"Matilda"`, `"Will"`, `"Jessica"`, `"Eric"`
  - `"Chris"`, `"Brian"`, `"Daniel"`, `"Lily"`, `"Bill"`
- **Важно**: Опциональный параметр! Только 21 указанное значение! Default: `"Rachel"`!

#### `stability`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0-1 (шаг: 0.01)
- Default: `0.5`
- **Важно**: Опциональный параметр! Только числа от 0 до 1! Default: `0.5`!

#### `similarity_boost`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0-1 (шаг: 0.01)
- Default: `0.75`
- **Важно**: Опциональный параметр! Только числа от 0 до 1! Default: `0.75`!

#### `style`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0-1 (шаг: 0.01)
- Default: `0`
- **Важно**: Опциональный параметр! Только числа от 0 до 1! Default: `0`!

#### `speed`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0.7-1.2 (шаг: 0.01)
- Default: `1`
- **Важно**: Опциональный параметр! Только числа от 0.7 до 1.2! Default: `1`! Значения ниже 1.0 замедляют речь, выше 1.0 ускоряют. Экстремальные значения могут повлиять на качество!

#### `timestamps`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `false`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `false`! Возвращает временные метки для каждого слова!

#### `previous_text`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 5000 символов
- Default: `""`
- **Важно**: Опциональный параметр! Максимум 5000 символов! Default: пустая строка! Используется для улучшения непрерывности речи при объединении нескольких генераций!

#### `next_text`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 5000 символов
- Default: `""`
- **Важно**: Опциональный параметр! Максимум 5000 символов! Default: пустая строка! Используется для улучшения непрерывности речи при объединении нескольких генераций!

#### `language_code`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 500 символов
- Default: `""`
- **Важно**: Опциональный параметр! Максимум 500 символов! Default: пустая строка! Код языка (ISO 639-1) для принудительного использования языка. В настоящее время поддерживается только для Turbo v2.5 и Flash v2.5. Для других моделей будет возвращена ошибка, если указан language_code!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | elevenlabs/text-to-speech-multilingual-v2 | Другие tts модели |
|----------|-------------------------------------------|-------------------|
| `text` | ✅ Обязательный (max 5000) | ✅ Обязательный |
| `voice` | ✅ Есть (enum, 21 значение) | ✅ Может быть |
| `stability` | ✅ Есть (0-1) | ❌ Нет |
| `similarity_boost` | ✅ Есть (0-1) | ❌ Нет |
| `style` | ✅ Есть (0-1) | ✅ Может быть |
| `speed` | ✅ Есть (0.7-1.2) | ✅ Может быть |
| `timestamps` | ✅ Есть (boolean) | ❌ Нет |
| `previous_text` | ✅ Есть (max 5000) | ❌ Нет |
| `next_text` | ✅ Есть (max 5000) | ❌ Нет |
| `language_code` | ✅ Есть (max 500) | ❌ Нет (используют `language`) |
| `language` | ❌ Нет | ✅ Может быть |
| `model` | ❌ Нет | ✅ Может быть |
| `emotion` | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для преобразования текста в речь (мультиязычная версия 2)!
- Использует `text` (макс 5000 символов) для текста!
- Использует `language_code` (string, макс 500) вместо стандартного `language`!
- Имеет уникальные параметры: `stability`, `similarity_boost`, `timestamps`, `previous_text`, `next_text`!
- НЕТ параметров `model` и `emotion` (в отличие от других tts моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_elevenlabs_text_to_speech_multilingual_v2()` проверяет:

1. **Обязательность `text`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Значение `voice`**: Должно быть одним из 21 допустимых значений (опциональный)
3. **Значение `stability`**: Должно быть числом от 0 до 1 (опциональный)
4. **Значение `similarity_boost`**: Должно быть числом от 0 до 1 (опциональный)
5. **Значение `style`**: Должно быть числом от 0 до 1 (опциональный)
6. **Значение `speed`**: Должно быть числом от 0.7 до 1.2 (опциональный)
7. **Значение `timestamps`**: Должно быть boolean (опциональный)
8. **Длина `previous_text`**: Максимум 5000 символов (опциональный)
9. **Длина `next_text`**: Максимум 5000 символов (опциональный)
10. **Длина `language_code`**: Максимум 500 символов (опциональный)

### Нормализация

1. **`_normalize_voice_for_elevenlabs_tts_multilingual_v2()`**: Нормализует voice (21 значение, case-insensitive)
2. **`_normalize_boolean()`**: Нормализует timestamps (boolean) - переиспользуется из других моделей

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'voice' not in normalized_input:
    normalized_input['voice'] = "Rachel"  # Default согласно документации
if 'stability' not in normalized_input:
    normalized_input['stability'] = 0.5  # Default согласно документации
if 'similarity_boost' not in normalized_input:
    normalized_input['similarity_boost'] = 0.75  # Default согласно документации
if 'style' not in normalized_input:
    normalized_input['style'] = 0  # Default согласно документации
if 'speed' not in normalized_input:
    normalized_input['speed'] = 1  # Default согласно документации
if 'timestamps' not in normalized_input:
    normalized_input['timestamps'] = False  # Default согласно документации
if 'previous_text' not in normalized_input:
    normalized_input['previous_text'] = ""  # Default согласно документации
if 'next_text' not in normalized_input:
    normalized_input['next_text'] = ""  # Default согласно документации
if 'language_code' not in normalized_input:
    normalized_input['language_code'] = ""  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "elevenlabs/text-to-speech-multilingual-v2",
  "input": {
    "text": "Unlock powerful API with Kie.ai! Affordable, scalable APl integration, free trial playground, and secure, reliable performance.",
    "voice": "Rachel",
    "stability": 0.5,
    "similarity_boost": 0.75,
    "style": 0,
    "speed": 1,
    "timestamps": false,
    "previous_text": "",
    "next_text": "",
    "language_code": ""
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'text' обязательно...` | `text` не указан | Введите текст для преобразования в речь |
| `Поле 'text' не может быть пустым` | `text` пустой | Введите непустой текст |
| `Поле 'text' слишком длинное...` | `text` больше 5000 символов | Сократите текст до 5000 символов |
| `Поле 'voice' должно быть одним из...` | Неверное значение `voice` | Используйте одно из 21 допустимых значений |
| `Поле 'stability' должно быть в диапазоне...` | Неверное значение `stability` | Используйте число от 0 до 1 |
| `Поле 'similarity_boost' должно быть в диапазоне...` | Неверное значение `similarity_boost` | Используйте число от 0 до 1 |
| `Поле 'style' должно быть в диапазоне...` | Неверное значение `style` | Используйте число от 0 до 1 |
| `Поле 'speed' должно быть в диапазоне...` | Неверное значение `speed` | Используйте число от 0.7 до 1.2 |
| `Поле 'timestamps' должно быть boolean...` | Неверное значение `timestamps` | Используйте true/false |
| `Поле 'previous_text' слишком длинное...` | `previous_text` больше 5000 символов | Сократите до 5000 символов |
| `Поле 'next_text' слишком длинное...` | `next_text` больше 5000 символов | Сократите до 5000 символов |
| `Поле 'language_code' слишком длинное...` | `language_code` больше 500 символов | Сократите до 500 символов |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("elevenlabs/text-to-speech-multilingual-v2")
user_payload = {
    "text": "Unlock powerful API with Kie.ai!",
    "voice": "Rachel",
    "stability": 0.5,
    "similarity_boost": 0.75,
    "style": 0,
    "speed": 1,
    "timestamps": False,
    "previous_text": "",
    "next_text": "",
    "language_code": ""
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлены параметры `language_code`, `stability`, `similarity_boost`, `timestamps`, `previous_text`, `next_text` в схему `tts`
- [x] Реализована функция `_validate_elevenlabs_text_to_speech_multilingual_v2()`
- [x] Добавлена функция нормализации `_normalize_voice_for_elevenlabs_tts_multilingual_v2()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=elevenlabs/text-to-speech-multilingual-v2 TYPE=tts INPUT_KEYS=['text', 'voice', 'stability', 'similarity_boost', 'style', 'speed', 'timestamps', 'previous_text', 'next_text', 'language_code'] 
INPUT_PREVIEW={'text': 'Unlock powerful API...', 'voice': 'Rachel', 'stability': 0.5, 'similarity_boost': 0.75, 'style': 0, 'speed': 1, 'timestamps': False, 'previous_text': '', 'next_text': '', 'language_code': ''}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **text обязательный** - обязателен для преобразования текста в речь (максимум 5000 символов)
2. **voice опциональный** - не обязательный! Только 21 значение, default "Rachel"!
3. **stability опциональный** - не обязательный! Только числа от 0 до 1, default 0.5!
4. **similarity_boost опциональный** - не обязательный! Только числа от 0 до 1, default 0.75!
5. **style опциональный** - не обязательный! Только числа от 0 до 1, default 0!
6. **speed опциональный** - не обязательный! Только числа от 0.7 до 1.2, default 1! Значения ниже 1.0 замедляют речь, выше 1.0 ускоряют!
7. **timestamps опциональный** - не обязательный! Boolean (true/false), default false! Возвращает временные метки для каждого слова!
8. **previous_text опциональный** - не обязательный! Максимум 5000 символов, default пустая строка! Используется для улучшения непрерывности речи!
9. **next_text опциональный** - не обязательный! Максимум 5000 символов, default пустая строка! Используется для улучшения непрерывности речи!
10. **language_code опциональный** - не обязательный! Максимум 500 символов, default пустая строка! Поддерживается только для Turbo v2.5 и Flash v2.5!
11. **Уникальные параметры** - модель использует `stability`, `similarity_boost`, `timestamps`, `previous_text`, `next_text`, `language_code` вместо стандартных `language`, `model`, `emotion`!

