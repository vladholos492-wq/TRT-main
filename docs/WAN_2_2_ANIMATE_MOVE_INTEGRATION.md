# Интеграция wan/2-2-animate-move

## 📋 Обзор

Документация по интеграции модели `wan/2-2-animate-move` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `wan/2-2-animate-move`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"wan/2-2-animate-move"` |
| `input.video_url` | string | URL входного видео | Обязательный string (не массив!), максимум 10MB, форматы: MP4, QuickTime, Matroska |
| `input.image_url` | string | URL входного изображения | Обязательный string, максимум 10MB, форматы: JPEG, PNG, WebP |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.resolution` | string | Разрешение сгенерированного видео | `"480p"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `video_url`
- **Тип**: string (не массив!)
- **Обязательный**: да
- **Описание**: URL входного видео
- **Ограничения**:
  - Максимум 10MB на файл
  - Форматы: MP4, QuickTime, Matroska
  - Должен быть валидным URL (начинается с `http://` или `https://`)
- **Default**: `"https://file.aiquickdraw.com/custom-page/akr/section-images/17586254974931y2hottk.mp4"`
- **ВАЖНО**: Это string, а не массив! (в отличие от wan/2-6-video-to-video, где используется `video_urls`)

#### `image_url`
- **Тип**: string
- **Обязательный**: да
- **Описание**: URL входного изображения. Если входное изображение не соответствует выбранному соотношению сторон, оно изменяется и обрезается по центру
- **Ограничения**:
  - Максимум 10MB на файл
  - Форматы: JPEG, PNG, WebP
  - Должен быть валидным URL (начинается с `http://` или `https://`)
- **Default**: `"https://file.aiquickdraw.com/custom-page/akr/section-images/1758625466310wpehpbnf.png"`
- **ВАЖНО**: Обязательный параметр! Изображение будет изменено и обрезано по центру, если не соответствует соотношению сторон!

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"480p"` - 480p (default)
  - `"580p"` - 580p
  - `"720p"` - 720p
- **Важно**: Опциональный параметр! Только 3 значения!
- **Поддерживаемые алиасы**:
  - `"480"`, `"480p"` → `"480p"`
  - `"580"`, `"580p"` → `"580p"`
  - `"720"`, `"720p"`, `"hd"` → `"720p"`

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | wan/2-2-animate-move | wan/2-6-video-to-video |
|----------|---------------------|------------------------|
| `video_url` | ✅ Обязательный string | ✅ Обязательный массив (`video_urls`) |
| `image_url` | ✅ Обязательный string | ❌ Нет |
| `prompt` | ❌ Нет | ✅ Обязательный |
| `resolution` | "480p" \| "580p" \| "720p" | "720p" \| "1080p" |
| `duration` | ❌ Нет | ✅ Опциональный |

**ВАЖНО**: 
- Эта модель требует **оба** параметра: `video_url` И `image_url` (оба обязательны)!
- Модель анимирует изображение на основе движения из видео
- `video_url` - это string, а не массив! Если передан `video_urls`, он будет удалён с предупреждением
- НЕТ параметра `prompt` (в отличие от других v2v моделей)!
- НЕТ параметра `duration` (в отличие от других v2v моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_wan_2_2_animate_move()` проверяет:

1. **Обязательность `video_url`**: Поле должно быть указано (string, не массив!)
2. **Формат `video_url`**: Должен быть валидным URL
3. **Обязательность `image_url`**: Поле должно быть указано
4. **Формат `image_url`**: Должен быть валидным URL
5. **Значение `resolution`**: Должно быть "480p", "580p" или "720p" (опциональный)

### Нормализация

1. **`_normalize_resolution_for_wan_2_2_animate_move()`**: Нормализует resolution ("480p" | "580p" | "720p")

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'video_url' not in normalized_input:
    normalized_input['video_url'] = "https://file.aiquickdraw.com/custom-page/akr/section-images/17586254974931y2hottk.mp4"
if 'image_url' not in normalized_input:
    normalized_input['image_url'] = "https://file.aiquickdraw.com/custom-page/akr/section-images/1758625466310wpehpbnf.png"
if 'resolution' not in normalized_input:
    normalized_input['resolution'] = "480p"  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "wan/2-2-animate-move",
  "input": {
    "video_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17586254974931y2hottk.mp4",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1758625466310wpehpbnf.png",
    "resolution": "480p"
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'video_url' обязательно...` | `video_url` не указан | Укажите URL входного видео |
| `Поле 'video_url' должно быть валидным URL...` | URL не начинается с `http://` или `https://` | Используйте полный URL с протоколом |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL входного изображения |
| `Поле 'image_url' должно быть валидным URL...` | URL не начинается с `http://` или `https://` | Используйте полный URL с протоколом |
| `Поле 'resolution' должно быть одним из...` | Неверное значение `resolution` | Используйте "480p", "580p" или "720p" |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("wan/2-2-animate-move")
user_payload = {
    "video_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17586254974931y2hottk.mp4",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1758625466310wpehpbnf.png",
    "resolution": "480p"
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлена схема `v2v` с полем `image_url`
- [x] Реализована функция `_normalize_resolution_for_wan_2_2_animate_move()`
- [x] Реализована функция `_validate_wan_2_2_animate_move()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Реализовано автоматическое удаление `video_urls` (если передан)
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=wan/2-2-animate-move TYPE=v2v INPUT_KEYS=['video_url', 'image_url', 'resolution'] 
INPUT_PREVIEW={'video_url': 'https://...', 'image_url': 'https://...', 'resolution': '480p'}
```

Если передан параметр `video_urls`, будет предупреждение:
```
Parameter 'video_urls' is not supported for wan/2-2-animate-move (use 'video_url' as string), removing it
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **video_url обязательный** - обязателен для генерации видео! Это string, а не массив!
2. **image_url обязательный** - обязателен для генерации видео! Изображение будет изменено и обрезано по центру, если не соответствует соотношению сторон!
3. **resolution опциональный** - не обязательный! Только 3 значения: "480p", "580p" или "720p"!
4. **НЕТ параметра prompt** - в отличие от других v2v моделей, параметр `prompt` не поддерживается!
5. **НЕТ параметра duration** - в отличие от других v2v моделей, параметр `duration` не поддерживается!
6. **Уникальная модель** - эта модель анимирует изображение на основе движения из видео, требуя оба входных параметра!

