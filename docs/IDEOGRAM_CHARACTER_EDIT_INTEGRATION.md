# Интеграция ideogram/character-edit

## 📋 Обзор

Документация по интеграции модели `ideogram/character-edit` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `ideogram/character-edit`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"ideogram/character-edit"` |
| `input.prompt` | string | Промпт для заполнения замаскированной части изображения | Обязательный, максимум 5000 символов |
| `input.image_url` | string | URL изображения для генерации | Обязательный, максимум 10MB, форматы: jpeg/png/webp |
| `input.mask_url` | string | URL маски для inpainting | Обязательный, максимум 10MB, форматы: jpeg/png/webp |
| `input.reference_image_urls` | array | Набор изображений для использования в качестве референсов персонажа | Обязательный, массив, максимум 10MB общий размер, форматы: jpeg/png/webp, только 1 изображение поддерживается |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.rendering_speed` | string | Скорость рендеринга | `"BALANCED"` |
| `input.style` | string | Тип стиля для генерации | `"AUTO"` |
| `input.expand_prompt` | boolean | Определяет, должен ли использоваться MagicPrompt | `true` |
| `input.num_images` | string | Количество изображений для генерации | `"1"` |
| `input.seed` | number | Зерно для генератора случайных чисел | Не указан |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp! Должен соответствовать размерам маски!

#### `mask_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp! Должен соответствовать размерам входного изображения!

#### `reference_image_urls`
- Тип: array
- Обязательный: да
- Максимальный размер файла: 10MB общий размер
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- Количество: только 1 изображение поддерживается, остальные игнорируются
- **Важно**: Обязательный параметр! Должен быть массивом URL изображений! Минимум 1 изображение! Максимум 10MB общий размер! Форматы: jpeg/png/webp! Только 1 изображение поддерживается, остальные игнорируются!

#### `rendering_speed`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"TURBO"`, `"BALANCED"`, `"QUALITY"`
- Default: `"BALANCED"`
- **Важно**: Опциональный параметр! Только 3 указанных значения! Default: `"BALANCED"`!

#### `style`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"AUTO"`, `"REALISTIC"`, `"FICTION"`
- Default: `"AUTO"`
- **Важно**: Опциональный параметр! Только 3 указанных значения! Default: `"AUTO"`! Не может использоваться с style_codes!

#### `expand_prompt`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `true`! Определяет, должен ли использоваться MagicPrompt в генерации запроса!

#### `num_images`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"1"`, `"2"`, `"3"`, `"4"`
- Default: `"1"`
- **Важно**: Опциональный параметр! Только 4 указанных значения! Default: `"1"`! String enum (не number)!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- **Важно**: Опциональный параметр! Зерно для генератора случайных чисел!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | ideogram/character-edit | Другие i2i модели |
|----------|------------------------|-------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный |
| `image_url` | ✅ Обязательный | ✅ Обязательный |
| `mask_url` | ✅ Обязательный (уникальный!) | ❌ Нет |
| `reference_image_urls` | ✅ Обязательный (уникальный!) | ❌ Нет |
| `rendering_speed` | ✅ Есть (enum: TURBO/BALANCED/QUALITY) | ✅ Может быть (ideogram/v3-reframe) |
| `style` | ✅ Есть (enum: AUTO/REALISTIC/FICTION) | ✅ Может быть |
| `expand_prompt` | ✅ Есть (boolean) | ❌ Нет |
| `num_images` | ✅ Есть (string enum: 1/2/3/4) | ✅ Может быть |
| `seed` | ✅ Есть | ✅ Может быть |
| `strength` | ❌ Нет | ✅ Может быть |
| `negative_prompt` | ❌ Нет | ✅ Может быть |
| `width` | ❌ Нет | ✅ Может быть |
| `height` | ❌ Нет | ✅ Может быть |
| `steps` | ❌ Нет | ✅ Может быть |
| `guidance` | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для редактирования персонажей с inpainting (image-to-image с маской)!
- Использует `mask_url` для указания области для inpainting!
- Использует `reference_image_urls` для указания референсов персонажа (только 1 изображение поддерживается, остальные игнорируются)!
- Имеет уникальные параметры: `mask_url`, `reference_image_urls`, `expand_prompt`!
- НЕТ параметров `strength`, `negative_prompt`, `width`, `height`, `steps`, `guidance` (в отличие от других i2i моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_ideogram_character_edit()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Обязательность `image_url`**: Поле должно быть указано, не пустое, string
3. **Обязательность `mask_url`**: Поле должно быть указано, не пустое, string
4. **Обязательность `reference_image_urls`**: Поле должно быть массивом URL изображений, минимум 1 изображение (только 1 поддерживается, остальные игнорируются)
5. **Значение `rendering_speed`**: Должно быть одним из 3 допустимых значений (опциональный)
6. **Значение `style`**: Должно быть одним из 3 допустимых значений (опциональный)
7. **Значение `expand_prompt`**: Должно быть boolean (опциональный)
8. **Значение `num_images`**: Должно быть одним из 4 допустимых значений (опциональный)
9. **Значение `seed`**: Должно быть числом (опциональный)

### Нормализация

1. **`_normalize_rendering_speed_for_ideogram_character_edit()`**: Нормализует rendering_speed (3 значения: TURBO, BALANCED, QUALITY, case-insensitive, конвертирует в верхний регистр)
2. **`_normalize_style_for_ideogram_character_edit()`**: Нормализует style (3 значения: AUTO, REALISTIC, FICTION, case-insensitive, конвертирует в верхний регистр)
3. **`_normalize_num_images_for_qwen_image_edit()`**: Нормализует num_images (4 значения: "1", "2", "3", "4") - переиспользуется из qwen/image-edit
4. **`_normalize_boolean()`**: Нормализует expand_prompt (boolean) - переиспользуется из других моделей

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'rendering_speed' not in normalized_input:
    normalized_input['rendering_speed'] = "BALANCED"  # Default согласно документации
if 'style' not in normalized_input:
    normalized_input['style'] = "AUTO"  # Default согласно документации
if 'expand_prompt' not in normalized_input:
    normalized_input['expand_prompt'] = True  # Default согласно документации
if 'num_images' not in normalized_input:
    normalized_input['num_images'] = "1"  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "ideogram/character-edit",
  "input": {
    "prompt": "A fabulous look head tilted down, looking forward with a smile\n",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17557680349256sa0lk53.webp",
    "mask_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755768046014ftgvma28.webp",
    "reference_image_urls": ["https://file.aiquickdraw.com/custom-page/akr/section-images/1755768064644jodsmfhq.webp"],
    "rendering_speed": "BALANCED",
    "style": "AUTO",
    "expand_prompt": true,
    "num_images": "1",
    "seed": 42
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для редактирования персонажа |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения для редактирования |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите непустой URL изображения |
| `Поле 'mask_url' обязательно...` | `mask_url` не указан | Укажите URL маски для inpainting |
| `Поле 'mask_url' не может быть пустым` | `mask_url` пустой | Укажите непустой URL маски |
| `Поле 'reference_image_urls' обязательно...` | `reference_image_urls` не указан | Укажите массив URL изображений-референсов |
| `Поле 'reference_image_urls' должно быть массивом...` | `reference_image_urls` не массив | Укажите массив URL изображений |
| `Поле 'reference_image_urls' не может быть пустым...` | `reference_image_urls` пустой массив | Укажите хотя бы одно изображение-референс |
| `Элемент X в 'reference_image_urls' должен быть строкой...` | Неверный тип элемента | Укажите строку (URL) для каждого элемента |
| `Элемент X в 'reference_image_urls' не может быть пустым` | Пустой URL | Укажите непустой URL для каждого элемента |
| `Поле 'rendering_speed' должно быть одним из...` | Неверное значение `rendering_speed` | Используйте одно из: TURBO, BALANCED, QUALITY |
| `Поле 'style' должно быть одним из...` | Неверное значение `style` | Используйте одно из: AUTO, REALISTIC, FICTION |
| `Поле 'expand_prompt' должно быть boolean...` | Неверное значение `expand_prompt` | Используйте true/false |
| `Поле 'num_images' должно быть одним из...` | Неверное значение `num_images` | Используйте одно из: 1, 2, 3, 4 |
| `Поле 'seed' должно быть числом...` | Неверное значение `seed` | Используйте число |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("ideogram/character-edit")
user_payload = {
    "prompt": "A fabulous look head tilted down, looking forward with a smile\n",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17557680349256sa0lk53.webp",
    "mask_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755768046014ftgvma28.webp",
    "reference_image_urls": ["https://file.aiquickdraw.com/custom-page/akr/section-images/1755768064644jodsmfhq.webp"],
    "rendering_speed": "BALANCED",
    "style": "AUTO",
    "expand_prompt": True,
    "num_images": "1",
    "seed": 42
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлены параметры `mask_url`, `reference_image_urls`, `expand_prompt` в схему `i2i`
- [x] Обновлены комментарии для `rendering_speed` и `num_images` в схеме `i2i`
- [x] Реализована функция `_validate_ideogram_character_edit()`
- [x] Добавлены функции нормализации `_normalize_rendering_speed_for_ideogram_character_edit()` и `_normalize_style_for_ideogram_character_edit()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=ideogram/character-edit TYPE=i2i INPUT_KEYS=['prompt', 'image_url', 'mask_url', 'reference_image_urls', 'rendering_speed', 'style', 'expand_prompt', 'num_images', 'seed'] 
INPUT_PREVIEW={'prompt': 'A fabulous look...', 'image_url': 'https://...', 'mask_url': 'https://...', 'reference_image_urls': ['https://...'], 'rendering_speed': 'BALANCED', 'style': 'AUTO', 'expand_prompt': True, 'num_images': '1', 'seed': 42}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для редактирования персонажа (максимум 5000 символов)
2. **image_url обязательный** - обязателен для редактирования персонажа! Максимум 10MB! Форматы: jpeg/png/webp! Должен соответствовать размерам маски!
3. **mask_url обязательный** - обязателен для редактирования персонажа! Максимум 10MB! Форматы: jpeg/png/webp! Должен соответствовать размерам входного изображения!
4. **reference_image_urls обязательный** - обязателен для редактирования персонажа! Должен быть массивом URL изображений! Минимум 1 изображение! Максимум 10MB общий размер! Форматы: jpeg/png/webp! Только 1 изображение поддерживается, остальные игнорируются!
5. **rendering_speed опциональный** - не обязательный! Только 3 значения, default "BALANCED"!
6. **style опциональный** - не обязательный! Только 3 значения, default "AUTO"! Не может использоваться с style_codes!
7. **expand_prompt опциональный** - не обязательный! Boolean (true/false), default true! Определяет, должен ли использоваться MagicPrompt в генерации запроса!
8. **num_images опциональный** - не обязательный! Только 4 значения (string enum: "1", "2", "3", "4"), default "1"!
9. **seed опциональный** - не обязательный! Зерно для генератора случайных чисел!
10. **Уникальные параметры** - модель использует `mask_url`, `reference_image_urls`, `expand_prompt` вместо стандартных `strength`, `negative_prompt`, `width`, `height`, `steps`, `guidance`!
11. **НЕТ стандартных параметров** - модель НЕ поддерживает `strength`, `negative_prompt`, `width`, `height`, `steps`, `guidance` (в отличие от других i2i моделей)!

