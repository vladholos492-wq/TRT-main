# Интеграция kling/v2-1-master-text-to-video

## 📋 Обзор

Документация по интеграции модели `kling/v2-1-master-text-to-video` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `kling/v2-1-master-text-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"kling/v2-1-master-text-to-video"` |
| `input.prompt` | string | Текстовый промпт, описывающий видео для генерации | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.duration` | string | Длительность сгенерированного видео в секундах | `"5"` |
| `input.aspect_ratio` | string | Соотношение сторон сгенерированного видео | `"16:9"` |
| `input.negative_prompt` | string | Элементы, которых следует избегать в сгенерированном видео | `"blur, distort, and low quality"` |
| `input.cfg_scale` | number | CFG (Classifier Free Guidance) scale - мера того, насколько близко модель должна следовать вашему промпту | `0.5` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `duration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"5"`, `"10"`
- Default: `"5"`
- **Важно**: Опциональный параметр! Только 2 указанных значения (string enum)! Default: `"5"`! Длительность видео в секундах!

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"16:9"`, `"9:16"`, `"1:1"`
- Default: `"16:9"`
- **Важно**: Опциональный параметр! Только 3 указанных значения (enum)! Default: `"16:9"`! Соотношение сторон сгенерированного видео!

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 500 символов
- Default: `"blur, distort, and low quality"`
- **Важно**: Опциональный параметр! Максимум 500 символов! Default: `"blur, distort, and low quality"`! Элементы, которых следует избегать в сгенерированном видео!

#### `cfg_scale`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 0-1 (шаг: 0.1)
- Default: `0.5`
- **Важно**: Опциональный параметр! Число от 0 до 1 (шаг 0.1)! Default: `0.5`! CFG (Classifier Free Guidance) scale - мера того, насколько близко модель должна следовать вашему промпту!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | kling/v2-1-master-text-to-video | kling/v2-1-master-image-to-video | Другие t2v модели |
|----------|----------------------------------|-----------------------------------|-------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный (max 5000) | ✅ Может быть |
| `image_url` | ❌ Нет | ✅ Обязательный (max 10MB) | ❌ Нет (t2v) |
| `duration` | ✅ Есть (enum: "5"/"10") | ✅ Есть (enum: "5"/"10") | ✅ Может быть |
| `aspect_ratio` | ✅ Есть (enum: "16:9"/"9:16"/"1:1") | ❌ Нет | ✅ Может быть |
| `negative_prompt` | ✅ Есть (max 500, default "blur, distort, and low quality") | ✅ Есть (max 500, default "blur, distort, and low quality") | ✅ Может быть |
| `cfg_scale` | ✅ Есть (0-1, шаг 0.1, default 0.5) | ✅ Есть (0-1, шаг 0.1, default 0.5) | ✅ Может быть (cfg_scale) |
| `tail_image_url` | ❌ Нет | ❌ Нет | ❌ Нет |
| `resolution` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `camera_fixed` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `seed` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `enable_safety_checker` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `fps` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `with_audio` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `width` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `height` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `guidance` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `steps` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `motion` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `style` | ❌ Нет | ❌ Нет | ✅ Может быть |
| `strength` | ❌ Нет | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для генерации видео из текста (text-to-video) - Master версия!
- Использует `duration` как string enum ("5" или "10")!
- Использует `aspect_ratio` для управления соотношением сторон (enum: "16:9", "9:16", "1:1", default "16:9")!
- Использует `cfg_scale` для управления следованием промпту (диапазон 0-1, шаг 0.1, default 0.5)!
- `negative_prompt` имеет default "blur, distort, and low quality" (не пустая строка!)!
- `prompt` имеет максимум 5000 символов!
- НЕТ параметров `image_url`, `tail_image_url`, `resolution`, `camera_fixed`, `seed`, `enable_safety_checker`, `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `motion`, `style`, `strength` (в отличие от других t2v моделей)!
- **Отличается от kling/v2-1-master-image-to-video тем, что это text-to-video (нет image_url), но есть aspect_ratio!**

## 🔧 Реализация

### Валидация

Функция `_validate_kling_v2_1_master_text_to_video()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Значение `duration`**: Должно быть одним из 2 допустимых значений (опциональный, string enum)
3. **Значение `aspect_ratio`**: Должно быть одним из 3 допустимых значений (опциональный, enum)
4. **Длина `negative_prompt`**: Максимум 500 символов (опциональный)
5. **Значение `cfg_scale`**: Должно быть числом в диапазоне от 0 до 1, округляется до 1 знака после запятой (опциональный)

### Нормализация

1. **`duration`**: Нормализуется в строку ("5" или "10") если передан как число
2. **`aspect_ratio`**: Проверяется что это одно из 3 допустимых значений
3. **`cfg_scale`**: Нормализуется в число с округлением до 1 знака после запятой (шаг 0.1)

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'duration' not in normalized_input:
    normalized_input['duration'] = "5"  # Default согласно документации
if 'aspect_ratio' not in normalized_input:
    normalized_input['aspect_ratio'] = "16:9"  # Default согласно документации
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = "blur, distort, and low quality"  # Default согласно документации
if 'cfg_scale' not in normalized_input:
    normalized_input['cfg_scale'] = 0.5  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "kling/v2-1-master-text-to-video",
  "input": {
    "prompt": "First-person view from a soldier jumping from a transport plane — the camera shakes with turbulence, oxygen mask reflections flicker — as the clouds part, the battlefield below pulses with anti-air fire and missile trails.",
    "duration": "5",
    "aspect_ratio": "16:9",
    "negative_prompt": "blur, distort, and low quality",
    "cfg_scale": 0.5
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации видео |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'duration' должно быть одним из...` | Неверное значение `duration` | Используйте одно из: 5, 10 |
| `Поле 'aspect_ratio' должно быть одним из...` | Неверное значение `aspect_ratio` | Используйте одно из: 16:9, 9:16, 1:1 |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 500 символов | Сократите до 500 символов |
| `Поле 'cfg_scale' должно быть в диапазоне...` | Неверное значение `cfg_scale` | Используйте число от 0 до 1 |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("kling/v2-1-master-text-to-video")
user_payload = {
    "prompt": "First-person view from a soldier jumping from a transport plane...",
    "duration": "5",
    "aspect_ratio": "16:9",
    "negative_prompt": "blur, distort, and low quality",
    "cfg_scale": 0.5
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Параметры уже есть в схеме `t2v` (cfg_scale, negative_prompt, aspect_ratio)
- [x] Реализована функция `_validate_kling_v2_1_master_text_to_video()`
- [x] Переиспользованы функции нормализации из других моделей
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=kling/v2-1-master-text-to-video TYPE=t2v INPUT_KEYS=['prompt', 'duration', 'aspect_ratio', 'negative_prompt', 'cfg_scale'] 
INPUT_PREVIEW={'prompt': 'First-person view from a soldier...', 'duration': '5', 'aspect_ratio': '16:9', 'negative_prompt': 'blur, distort, and low quality', 'cfg_scale': 0.5}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео (максимум 5000 символов)
2. **duration опциональный** - не обязательный! Только 2 значения (string enum: "5", "10"), default "5"! Длительность видео в секундах!
3. **aspect_ratio опциональный** - не обязательный! Только 3 значения (enum: "16:9", "9:16", "1:1"), default "16:9"! Соотношение сторон сгенерированного видео!
4. **negative_prompt опциональный** - не обязательный! Максимум 500 символов, default "blur, distort, and low quality"! Элементы, которых следует избегать в сгенерированном видео!
5. **cfg_scale опциональный** - не обязательный! Число от 0 до 1 (шаг 0.1), default 0.5! CFG (Classifier Free Guidance) scale - мера того, насколько близко модель должна следовать вашему промпту!
6. **Уникальные параметры** - модель использует `cfg_scale`, `negative_prompt` и `aspect_ratio` вместо стандартных `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `motion`, `style`, `strength`!
7. **НЕТ стандартных параметров** - модель НЕ поддерживает `image_url`, `tail_image_url`, `resolution`, `camera_fixed`, `seed`, `enable_safety_checker`, `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `motion`, `style`, `strength` (в отличие от других t2v моделей)!
8. **prompt max 5000** - `prompt` имеет максимум 5000 символов!
9. **Отличается от kling/v2-1-master-image-to-video** - это text-to-video (нет `image_url`), но есть `aspect_ratio` (которого нет в image-to-video версии)!

