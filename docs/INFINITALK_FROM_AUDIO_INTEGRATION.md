# Интеграция infinitalk/from-audio

## 📋 Обзор

Документация по интеграции модели `infinitalk/from-audio` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `infinitalk/from-audio`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"infinitalk/from-audio"` |
| `input.image_url` | string | URL входного изображения | Обязательный, макс 10MB, jpeg/png/webp |
| `input.audio_url` | string | URL аудио файла | Обязательный, макс 10MB, mpeg/wav/aac/mp4/ogg |
| `input.prompt` | string | Текстовый промпт для генерации видео | Обязательный, максимум 5000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.resolution` | string | Разрешение видео | `"480p"` |
| `input.seed` | number | Случайное seed для воспроизводимости | Не указан |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Должен быть валидным URL изображения! Если изображение не соответствует выбранному соотношению сторон, оно будет изменено и обрезано по центру!

#### `audio_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: `audio/mpeg`, `audio/wav`, `audio/x-wav`, `audio/aac`, `audio/mp4`, `audio/ogg`
- **Важно**: Обязательный параметр! Должен быть валидным URL аудио файла!

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"480p"` - 480p (default)
  - `"720p"` - 720p
- **Важно**: Опциональный параметр! Только 2 значения! Default: `"480p"`!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 10000-1000000
- **Важно**: Опциональный параметр! Только числа от 10000 до 1000000! Используется для воспроизводимости результатов!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | infinitalk/from-audio | kling/v1-avatar-standard | Другие lip_sync модели |
|----------|----------------------|-------------------------|------------------------|
| `image_url` | ✅ Обязательный | ✅ Обязательный | ✅ Обязательный |
| `audio_url` | ✅ Обязательный | ✅ Обязательный | ✅ Обязательный |
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный (max 5000) | ❌ Опциональный |
| `resolution` | ✅ Есть ("480p" \| "720p") | ❌ Нет | ✅ Может быть |
| `seed` | ✅ Есть (10000-1000000) | ❌ Нет | ✅ Может быть (без ограничений) |

**ВАЖНО**: 
- Это модель lip sync для генерации видео из аудио и изображения!
- Требует **все три обязательных параметра**: `image_url`, `audio_url`, `prompt`!
- `prompt` обязательный (в отличие от других lip sync моделей)!
- `resolution` опциональный, только "480p" или "720p", default "480p"!
- `seed` опциональный, но с ограничением: только 10000-1000000 (в отличие от других моделей, где нет ограничений)!
- Максимальный размер файлов: 10MB для изображения и аудио!

## 🔧 Реализация

### Валидация

Функция `_validate_infinitalk_from_audio()` проверяет:

1. **Обязательность `image_url`**: Поле должно быть указано и не пустое
2. **Обязательность `audio_url`**: Поле должно быть указано и не пустое
3. **Обязательность `prompt`**: Поле должно быть указано и не пустое
4. **Длина `prompt`**: Максимум 5000 символов
5. **Значение `resolution`**: Должно быть "480p" или "720p" (опциональный)
6. **Значение `seed`**: Должно быть числом от 10000 до 1000000 (опциональный)

### Нормализация

1. **`_normalize_resolution_for_infinitalk_from_audio()`**: Нормализует resolution ("480p" | "720p")

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'resolution' not in normalized_input:
    normalized_input['resolution'] = "480p"  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "infinitalk/from-audio",
  "input": {
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1757329269873ggqj2hz3.png",
    "audio_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1757329255705mmqwrnri.mp3",
    "prompt": "A young woman with long dark hair talking on a podcast.",
    "resolution": "480p",
    "seed": 42
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'image_url' обязательно...` | `image_url` не указан | Загрузите изображение для генерации видео |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите валидный URL изображения |
| `Поле 'audio_url' обязательно...` | `audio_url` не указан | Загрузите аудио файл |
| `Поле 'audio_url' не может быть пустым` | `audio_url` пустой | Укажите валидный URL аудио файла |
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'resolution' должно быть одним из...` | Неверное значение `resolution` | Используйте "480p" или "720p" |
| `Поле 'seed' должно быть в диапазоне...` | Неверное значение `seed` | Используйте число от 10000 до 1000000 |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("infinitalk/from-audio")
user_payload = {
    "image_url": "https://example.com/image.png",
    "audio_url": "https://example.com/audio.mp3",
    "prompt": "A young woman with long dark hair talking on a podcast.",
    "resolution": "480p",
    "seed": 42
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлены параметры `prompt` и `seed` в схему `lip_sync`
- [x] Реализована функция `_validate_infinitalk_from_audio()`
- [x] Добавлена функция нормализации `_normalize_resolution_for_infinitalk_from_audio()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=infinitalk/from-audio TYPE=lip_sync INPUT_KEYS=['image_url', 'audio_url', 'prompt', 'resolution', 'seed'] 
INPUT_PREVIEW={'image_url': 'https://...', 'audio_url': 'https://...', 'prompt': 'A young woman...', 'resolution': '480p', 'seed': 42}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **image_url обязательный** - обязателен для генерации видео (макс 10MB, jpeg/png/webp)
2. **audio_url обязательный** - обязателен для генерации видео (макс 10MB, mpeg/wav/aac/mp4/ogg)
3. **prompt обязательный** - обязателен для генерации видео (максимум 5000 символов)!
4. **Все три параметра обязательны** - модель требует все три параметра одновременно!
5. **resolution опциональный** - не обязательный! Только 2 значения ("480p" | "720p"), default "480p"!
6. **seed опциональный** - не обязательный! Только числа от 10000 до 1000000 (в отличие от других моделей, где нет ограничений)!
7. **Размер файлов** - максимальный размер файлов: 10MB для изображения и аудио (валидация размера на стороне KIE API)
8. **Форматы файлов** - поддерживаются только указанные форматы (валидация формата на стороне KIE API)
9. **Изменение изображения** - если входное изображение не соответствует выбранному соотношению сторон, оно будет изменено и обрезано по центру!

