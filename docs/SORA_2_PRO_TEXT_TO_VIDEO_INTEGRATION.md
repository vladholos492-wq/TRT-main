# Интеграция sora-2-pro-text-to-video

## 📋 Обзор

Документация по интеграции модели `sora-2-pro-text-to-video` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `sora-2-pro-text-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"sora-2-pro-text-to-video"` |
| `input.prompt` | string | Текстовое описание движения видео | Обязательный, максимум 10000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.aspect_ratio` | string | Соотношение сторон видео | `"landscape"` |
| `input.n_frames` | string | Количество кадров для генерации | `"10"` |
| `input.size` | string | Качество или размер сгенерированного видео | `"high"` |
| `input.remove_watermark` | boolean | Удаление водяных знаков из сгенерированного видео | `true` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 10000 символов
- **Важно**: Не может быть пустым! Максимум 10000 символов!

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"portrait"` - Портретная ориентация
  - `"landscape"` - Альбомная ориентация (default)
- **Важно**: Опциональный параметр! Только 2 значения!

#### `n_frames`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"10"` - 10 секунд (default)
  - `"15"` - 15 секунд
- **Важно**: Опциональный параметр! Только 2 значения (не 3, как в storyboard)!

#### `size`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"standard"` - Стандартное качество
  - `"high"` - Высокое качество (default)
- **Важно**: Опциональный параметр! Только 2 значения!

#### `remove_watermark`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Значения:
  - `true` - Удалять водяные знаки (default)
  - `false` - Не удалять водяные знаки
- **Важно**: Опциональный параметр! Boolean значение!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_sora_2_pro_text_to_video(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для sora-2-pro-text-to-video согласно документации API.
    """
    # Проверяем оба возможных ID модели
    if model_id not in ["sora-2-pro-text-to-video", "sora-2-pro/t2v", "openai/sora-2-pro-text-to-video"]:
        return True, None
    
    # Валидация prompt: обязательный, максимум 10000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации видео"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len > 10000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 10000)"
    
    # Валидация aspect_ratio: опциональный, enum ("portrait" или "landscape")
    # Переиспользуем функцию нормализации из sora-2-pro-storyboard
    aspect_ratio = normalized_input.get('aspect_ratio')
    if aspect_ratio is not None:
        normalized_aspect_ratio = _normalize_aspect_ratio_for_sora_2_pro_storyboard(aspect_ratio)
        if normalized_aspect_ratio is None:
            valid_values = ["portrait", "landscape"]
            return False, f"Поле 'aspect_ratio' должно быть одним из: {', '.join(valid_values)}"
        normalized_input['aspect_ratio'] = normalized_aspect_ratio
    
    # Валидация n_frames: опциональный, enum ("10" или "15")
    n_frames = normalized_input.get('n_frames')
    if n_frames is not None:
        normalized_n_frames = _normalize_n_frames_for_sora_2_pro_text_to_video(n_frames)
        if normalized_n_frames is None:
            valid_values = ["10", "15"]
            return False, f"Поле 'n_frames' должно быть одним из: {', '.join(valid_values)}"
        normalized_input['n_frames'] = normalized_n_frames
    
    # Валидация size: опциональный, enum ("standard" или "high")
    size = normalized_input.get('size')
    if size is not None:
        normalized_size = _normalize_size_for_sora_2_pro_text_to_video(size)
        if normalized_size is None:
            valid_values = ["standard", "high"]
            return False, f"Поле 'size' должно быть одним из: {', '.join(valid_values)}"
        normalized_input['size'] = normalized_size
    
    # Валидация remove_watermark: опциональный boolean
    remove_watermark = normalized_input.get('remove_watermark')
    if remove_watermark is not None:
        normalized_remove_watermark = _normalize_boolean(remove_watermark)
        if normalized_remove_watermark is None:
            return False, f"Поле 'remove_watermark' должно быть boolean (true/false)"
        normalized_input['remove_watermark'] = normalized_remove_watermark
    
    return True, None
```

### Нормализация параметров

#### aspect_ratio

Переиспользуется функция нормализации из `sora-2-pro-storyboard`:

```python
# Переиспользуем функцию нормализации из sora-2-pro-storyboard
_normalize_aspect_ratio_for_sora_2_pro_storyboard(aspect_ratio)
```

#### n_frames

Создана отдельная функция нормализации (только "10" и "15", в отличие от storyboard, где также есть "25"):

```python
def _normalize_n_frames_for_sora_2_pro_text_to_video(value: Any) -> Optional[str]:
    """
    Нормализует n_frames для sora-2-pro-text-to-video.
    ВАЖНО: Для sora-2-pro-text-to-video поддерживаются только "10" и "15"!
    (В отличие от storyboard, где также есть "25")
    """
    valid_values = ["10", "15"]
    # Поддерживает алиасы (10s, 10sec, 10 seconds, 15s, 15sec, 15 seconds)
    # Поддерживает числа (10, 15)
    ...
```

#### size

Функция нормализации поддерживает только 2 значения:

```python
def _normalize_size_for_sora_2_pro_text_to_video(value: Any) -> Optional[str]:
    """
    Нормализует size для sora-2-pro-text-to-video.
    ВАЖНО: Для sora-2-pro-text-to-video поддерживаются только "standard" и "high"!
    """
    valid_values = ["standard", "high"]
    # Поддерживает алиасы (std, normal, medium, hq, quality, best)
    ...
```

#### remove_watermark

Универсальная функция нормализации boolean:

```python
def _normalize_boolean(value: Any) -> Optional[bool]:
    """
    Нормализует boolean значение.
    Поддерживает: bool, int, float, str (true/false, 1/0, yes/no, on/off, enabled/disabled)
    """
    ...
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id in ["sora-2-pro-text-to-video", "sora-2-pro/t2v", "openai/sora-2-pro-text-to-video"]:
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "landscape"  # Default согласно документации
    if 'n_frames' not in normalized_input:
        normalized_input['n_frames'] = "10"  # Default согласно документации
    if 'size' not in normalized_input:
        normalized_input['size'] = "high"  # Default согласно документации
    if 'remove_watermark' not in normalized_input:
        normalized_input['remove_watermark'] = True  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "sora-2-pro-text-to-video",
  "input": {
    "prompt": "a happy dog running in the garden",
    "aspect_ratio": "landscape",
    "n_frames": "10",
    "size": "high",
    "remove_watermark": true
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (максимум 10000 символов) - обязательный!
2. **Валидация aspect_ratio**: Проверяется что значение "portrait" или "landscape" (опциональный!)
3. **Валидация n_frames**: Проверяется что значение "10" или "15" (опциональный!)
4. **Валидация size**: Проверяется что значение "standard" или "high" (опциональный!)
5. **Валидация remove_watermark**: Проверяется что значение boolean (опциональный!)
6. **Дефолтные значения**: Применяются если параметры не указаны (aspect_ratio="landscape", n_frames="10", size="high", remove_watermark=true)
7. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=sora-2-pro-text-to-video TYPE=t2v INPUT_KEYS=['prompt', 'aspect_ratio', 'n_frames', 'size', 'remove_watermark'] 
INPUT_PREVIEW={'prompt': 'a happy dog...', 'aspect_ratio': 'landscape', 'n_frames': '10', 'size': 'high', 'remove_watermark': True}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео
2. **prompt максимум 10000 символов** - проверяется при валидации
3. **aspect_ratio опциональный** - не обязательный! Только 2 значения: "portrait" или "landscape"!
4. **n_frames опциональный** - не обязательный! Только 2 значения: "10" или "15" (не 3, как в storyboard)!
5. **size опциональный** - не обязательный! Только 2 значения: "standard" или "high"!
6. **remove_watermark опциональный** - не обязательный! Boolean значение!
7. **Дефолтные значения**: aspect_ratio="landscape", n_frames="10", size="high", remove_watermark=true
8. **Переиспользование кода**: Использует функцию нормализации aspect_ratio из sora-2-pro-storyboard
9. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими моделями

### Сравнение с sora-2-pro-storyboard

| Параметр | sora-2-pro-storyboard | sora-2-pro-text-to-video |
|----------|----------------------|-------------------------|
| `prompt` | Не требуется | Обязательный, max 10000 |
| `n_frames` | Обязательный, "10"\|"15"\|"25" | Опциональный, "10"\|"15" (только 2 значения!) |
| `aspect_ratio` | Опциональный, "portrait"\|"landscape" | Опциональный, "portrait"\|"landscape" |
| `image_urls` | Опциональный массив | Не требуется |
| `size` | Не требуется | Опциональный, "standard"\|"high" |
| `remove_watermark` | Не требуется | Опциональный boolean |
| Тип модели | i2v (storyboard) | t2v |

### Сравнение с sora-2-text-to-video

| Параметр | sora-2-text-to-video | sora-2-pro-text-to-video |
|----------|----------------------|-------------------------|
| `prompt` | Обязательный, max 10000 | Обязательный, max 10000 |
| `aspect_ratio` | Обязательный, "portrait"\|"landscape" | Опциональный, "portrait"\|"landscape" |
| `n_frames` | Опциональный, "10"\|"15" | Опциональный, "10"\|"15" |
| `size` | Не требуется | Опциональный, "standard"\|"high" |
| `remove_watermark` | Опциональный boolean | Опциональный boolean |
| Тип модели | t2v | t2v |

### Сравнение с kling-2.6/text-to-video

| Параметр | kling-2.6/text-to-video | sora-2-pro-text-to-video |
|----------|-------------------------|-------------------------|
| `prompt` | Обязательный, max 1000 | Обязательный, max 10000 |
| `sound` | Обязательный boolean | Не требуется |
| `aspect_ratio` | Обязательный, "1:1"\|"16:9"\|"9:16" | Опциональный, "portrait"\|"landscape" |
| `duration` | Обязательный, "5"\|"10" | Не требуется |
| `n_frames` | Не требуется | Опциональный, "10"\|"15" |
| `size` | Не требуется | Опциональный, "standard"\|"high" |
| `remove_watermark` | Не требуется | Опциональный boolean |
| Тип модели | t2v | t2v |

## 🎯 Ключевые отличия

1. **prompt обязательный**: Обязателен для генерации видео, максимум 10000 символов
2. **aspect_ratio опциональный**: Только 2 значения ("portrait" или "landscape"), default "landscape"
3. **n_frames опциональный**: Только 2 значения ("10" или "15"), default "10" (не 3, как в storyboard!)
4. **size опциональный**: Только 2 значения ("standard" или "high"), default "high"
5. **remove_watermark опциональный**: Boolean значение, default true
6. **Переиспользование кода**: Использует функцию нормализации aspect_ratio из sora-2-pro-storyboard
7. **Нормализация типов**: Все параметры конвертируются с поддержкой алиасов
8. **Дефолтные значения**: Применяются автоматически
9. **Сообщения об ошибках**: Понятные сообщения для пользователя

## 💡 Особенности Pro версии

Модель `sora-2-pro-text-to-video` является Pro версией `sora-2-text-to-video`:

- **Больше параметров**: Добавлены `size` и `remove_watermark`
- **Гибкость**: `aspect_ratio` стал опциональным (в обычной версии обязательный)
- **Качество**: Поддержка выбора качества через параметр `size`
- **Водяные знаки**: Возможность удаления водяных знаков через параметр `remove_watermark`

