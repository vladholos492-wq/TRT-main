# Интеграция wan/2-2-a14b-speech-to-video-turbo

## 📋 Обзор

Документация по интеграции модели `wan/2-2-a14b-speech-to-video-turbo` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `wan/2-2-a14b-speech-to-video-turbo`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"wan/2-2-a14b-speech-to-video-turbo"` |
| `input.prompt` | string | Текстовый промпт для генерации видео | Обязательный, максимум 5000 символов |
| `input.image_url` | string | URL входного изображения | Обязательный, макс 10MB, jpeg/png/webp |
| `input.audio_url` | string | URL аудио файла | Обязательный, макс 10MB, mp3/wav/ogg/m4a/flac/aac/x-ms-wma/mpeg |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.num_frames` | number | Количество кадров для генерации (40-120, кратно 4) | `80` |
| `input.frames_per_second` | number | Кадров в секунду (4-60) | `16` |
| `input.resolution` | string | Разрешение видео (480p, 580p, 720p) | `"480p"` |
| `input.negative_prompt` | string | Негативный промпт для генерации видео | `""` |
| `input.seed` | number | Случайное зерно для воспроизводимости | Не указан |
| `input.num_inference_steps` | number | Количество шагов вывода (2-40) | `27` |
| `input.guidance_scale` | number | Масштаб классификатора без учителя (1-10) | `3.5` |
| `input.shift` | number | Значение сдвига для видео (1-10) | `5` |
| `input.enable_safety_checker` | boolean | Проверять ли входные данные на безопасность | `true` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 5000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 5000 символов!

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Должен быть валидным URL изображения! Если изображение не соответствует выбранному соотношению сторон, оно будет изменено и обрезано по центру!

#### `audio_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: `audio/mp3`, `audio/wav`, `audio/ogg`, `audio/m4a`, `audio/flac`, `audio/aac`, `audio/x-ms-wma`, `audio/mpeg`
- **Важно**: Обязательный параметр! Должен быть валидным URL аудио файла!

#### `num_frames`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 40-120 (шаг: 4, должно быть кратно 4)
- Default: `80`
- **Важно**: Опциональный параметр! Только числа от 40 до 120, кратные 4! Default: `80`!

#### `frames_per_second`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 4-60 (шаг: 1)
- Default: `16`
- **Важно**: Опциональный параметр! Только числа от 4 до 60! Default: `16`! При использовании интерполяции и `adjust_fps_for_interpolation=true` (default true), финальный FPS будет умножен на количество интерполированных кадров плюс один!

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"480p"`, `"580p"`, `"720p"`
- Default: `"480p"`
- **Важно**: Опциональный параметр! Только указанные значения! Default: `"480p"`!

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 500 символов
- Default: `""`
- **Важно**: Опциональный параметр! Максимум 500 символов! Default: пустая строка!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- **Важно**: Опциональный параметр! Если не указан, выбирается случайное зерно!

#### `num_inference_steps`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 2-40 (шаг: 1)
- Default: `27`
- **Важно**: Опциональный параметр! Только числа от 2 до 40! Default: `27`! Более высокие значения дают лучшее качество, но занимают больше времени!

#### `guidance_scale`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 1-10 (шаг: 0.1)
- Default: `3.5`
- **Важно**: Опциональный параметр! Только числа от 1 до 10! Default: `3.5`! Более высокие значения дают лучшее соответствие промпту, но могут снизить качество!

#### `shift`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: 1-10 (шаг: 0.1)
- Default: `5`
- **Важно**: Опциональный параметр! Только числа от 1 до 10! Default: `5`!

#### `enable_safety_checker`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `true`! Если установлено в true, входные данные будут проверены на безопасность перед обработкой!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | wan/2-2-a14b-speech-to-video-turbo | Другие t2v модели |
|----------|-----------------------------------|------------------|
| `prompt` | ✅ Обязательный (max 5000) | ✅ Обязательный |
| `image_url` | ✅ Обязательный (макс 10MB) | ❌ Нет |
| `audio_url` | ✅ Обязательный (макс 10MB) | ❌ Нет |
| `num_frames` | ✅ Есть (40-120, кратно 4) | ❌ Нет |
| `frames_per_second` | ✅ Есть (4-60) | ✅ Может быть |
| `resolution` | ✅ Есть (enum: 480p/580p/720p) | ✅ Может быть |
| `negative_prompt` | ✅ Есть (max 500) | ✅ Может быть |
| `seed` | ✅ Есть | ✅ Может быть |
| `num_inference_steps` | ✅ Есть (2-40) | ❌ Нет |
| `guidance_scale` | ✅ Есть (1-10) | ❌ Нет |
| `shift` | ✅ Есть (1-10) | ❌ Нет |
| `enable_safety_checker` | ✅ Есть (boolean) | ❌ Нет |
| `duration` | ❌ Нет | ✅ Может быть |
| `with_audio` | ❌ Нет | ✅ Может быть |
| `aspect_ratio` | ❌ Нет | ✅ Может быть |

**ВАЖНО**: 
- Это модель для генерации видео из речи (speech-to-video)!
- Требует ТРИ обязательных параметра: `prompt`, `image_url`, `audio_url`!
- Использует `num_frames` (40-120, кратно 4) вместо стандартного `duration`!
- Имеет уникальные параметры: `num_inference_steps`, `guidance_scale`, `shift`, `enable_safety_checker`!
- НЕТ параметров `duration`, `with_audio`, `aspect_ratio` (в отличие от других t2v моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_wan_2_2_a14b_speech_to_video_turbo()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 5000 символов
2. **Обязательность `image_url`**: Поле должно быть указано, не пустое (макс 10MB, jpeg/png/webp)
3. **Обязательность `audio_url`**: Поле должно быть указано, не пустое (макс 10MB, mp3/wav/ogg/m4a/flac/aac/x-ms-wma/mpeg)
4. **Значение `num_frames`**: Должно быть числом от 40 до 120, кратным 4 (опциональный)
5. **Значение `frames_per_second`**: Должно быть числом от 4 до 60 (опциональный)
6. **Значение `resolution`**: Должно быть одним из 3 допустимых значений (опциональный)
7. **Длина `negative_prompt`**: Максимум 500 символов (опциональный)
8. **Значение `seed`**: Должно быть числом (опциональный)
9. **Значение `num_inference_steps`**: Должно быть числом от 2 до 40 (опциональный)
10. **Значение `guidance_scale`**: Должно быть числом от 1 до 10 (опциональный)
11. **Значение `shift`**: Должно быть числом от 1 до 10 (опциональный)
12. **Значение `enable_safety_checker`**: Должно быть boolean (опциональный)

### Нормализация

1. **`_normalize_resolution_for_wan_2_2_a14b_speech_to_video_turbo()`**: Нормализует resolution (3 значения, case-insensitive)
2. **`_normalize_boolean()`**: Нормализует enable_safety_checker (boolean) - переиспользуется из других моделей

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'num_frames' not in normalized_input:
    normalized_input['num_frames'] = 80  # Default согласно документации
if 'frames_per_second' not in normalized_input:
    normalized_input['frames_per_second'] = 16  # Default согласно документации
if 'resolution' not in normalized_input:
    normalized_input['resolution'] = "480p"  # Default согласно документации
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = ""  # Default согласно документации
if 'num_inference_steps' not in normalized_input:
    normalized_input['num_inference_steps'] = 27  # Default согласно документации
if 'guidance_scale' not in normalized_input:
    normalized_input['guidance_scale'] = 3.5  # Default согласно документации
if 'shift' not in normalized_input:
    normalized_input['shift'] = 5  # Default согласно документации
if 'enable_safety_checker' not in normalized_input:
    normalized_input['enable_safety_checker'] = True  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "wan/2-2-a14b-speech-to-video-turbo",
  "input": {
    "prompt": "The lady is talking",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1756797663082u4pjmcrq.png",
    "audio_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17567977044127d1emlmc.mp3",
    "num_frames": 80,
    "frames_per_second": 16,
    "resolution": "480p",
    "negative_prompt": "",
    "seed": 42,
    "num_inference_steps": 27,
    "guidance_scale": 3.5,
    "shift": 5,
    "enable_safety_checker": true
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации видео |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 5000 символов | Сократите описание до 5000 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите валидный URL изображения |
| `Поле 'audio_url' обязательно...` | `audio_url` не указан | Укажите URL аудио файла |
| `Поле 'audio_url' не может быть пустым` | `audio_url` пустой | Укажите валидный URL аудио файла |
| `Поле 'num_frames' должно быть в диапазоне...` | Неверное значение `num_frames` | Используйте число от 40 до 120, кратное 4 |
| `Поле 'num_frames' должно быть кратно 4...` | `num_frames` не кратно 4 | Используйте число, кратное 4 |
| `Поле 'frames_per_second' должно быть в диапазоне...` | Неверное значение `frames_per_second` | Используйте число от 4 до 60 |
| `Поле 'resolution' должно быть одним из...` | Неверное значение `resolution` | Используйте одно из: 480p, 580p, 720p |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 500 символов | Сократите до 500 символов |
| `Поле 'num_inference_steps' должно быть в диапазоне...` | Неверное значение `num_inference_steps` | Используйте число от 2 до 40 |
| `Поле 'guidance_scale' должно быть в диапазоне...` | Неверное значение `guidance_scale` | Используйте число от 1 до 10 |
| `Поле 'shift' должно быть в диапазоне...` | Неверное значение `shift` | Используйте число от 1 до 10 |
| `Поле 'enable_safety_checker' должно быть boolean...` | Неверное значение `enable_safety_checker` | Используйте true/false |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("wan/2-2-a14b-speech-to-video-turbo")
user_payload = {
    "prompt": "The lady is talking",
    "image_url": "https://example.com/image.png",
    "audio_url": "https://example.com/audio.mp3",
    "num_frames": 80,
    "frames_per_second": 16,
    "resolution": "480p",
    "negative_prompt": "",
    "seed": 42,
    "num_inference_steps": 27,
    "guidance_scale": 3.5,
    "shift": 5,
    "enable_safety_checker": True
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлены параметры `image_url`, `audio_url`, `num_frames`, `num_inference_steps`, `guidance_scale`, `shift`, `enable_safety_checker` в схему `t2v`
- [x] Реализована функция `_validate_wan_2_2_a14b_speech_to_video_turbo()`
- [x] Добавлена функция нормализации `_normalize_resolution_for_wan_2_2_a14b_speech_to_video_turbo()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=wan/2-2-a14b-speech-to-video-turbo TYPE=t2v INPUT_KEYS=['prompt', 'image_url', 'audio_url', 'num_frames', 'frames_per_second', 'resolution', 'negative_prompt', 'seed', 'num_inference_steps', 'guidance_scale', 'shift', 'enable_safety_checker'] 
INPUT_PREVIEW={'prompt': 'The lady is talking', 'image_url': 'https://...', 'audio_url': 'https://...', 'num_frames': 80, 'frames_per_second': 16, 'resolution': '480p', 'negative_prompt': '', 'seed': 42, 'num_inference_steps': 27, 'guidance_scale': 3.5, 'shift': 5, 'enable_safety_checker': True}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **ТРИ обязательных параметра** - `prompt`, `image_url`, `audio_url` обязательны для генерации видео!
2. **num_frames опциональный** - не обязательный! Только числа от 40 до 120, кратные 4, default 80!
3. **frames_per_second опциональный** - не обязательный! Только числа от 4 до 60, default 16! При использовании интерполяции финальный FPS может быть умножен!
4. **resolution опциональный** - не обязательный! Только 3 значения, default "480p"!
5. **negative_prompt опциональный** - не обязательный! Максимум 500 символов, default пустая строка!
6. **num_inference_steps опциональный** - не обязательный! Только числа от 2 до 40, default 27! Более высокие значения дают лучшее качество, но занимают больше времени!
7. **guidance_scale опциональный** - не обязательный! Только числа от 1 до 10, default 3.5! Более высокие значения дают лучшее соответствие промпту, но могут снизить качество!
8. **shift опциональный** - не обязательный! Только числа от 1 до 10, default 5!
9. **enable_safety_checker опциональный** - не обязательный! Boolean (true/false), default true! Проверяет входные данные на безопасность!
10. **Уникальные параметры** - модель использует `num_frames`, `num_inference_steps`, `guidance_scale`, `shift`, `enable_safety_checker` вместо стандартных `duration`, `with_audio`, `aspect_ratio`!

