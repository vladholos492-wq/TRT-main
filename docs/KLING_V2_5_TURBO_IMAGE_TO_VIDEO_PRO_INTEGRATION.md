# Интеграция kling/v2-5-turbo-image-to-video-pro

## 📋 Обзор

Документация по интеграции модели `kling/v2-5-turbo-image-to-video-pro` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `kling/v2-5-turbo-image-to-video-pro`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"kling/v2-5-turbo-image-to-video-pro"` |
| `input.prompt` | string | Текстовое описание для генерации видео | Обязательный, максимум 2500 символов |
| `input.image_url` | string | URL изображения для использования в видео | Обязательный string (не массив!), максимум 10MB, форматы: JPEG, PNG, WebP |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.tail_image_url` | string | URL изображения для последнего кадра видео | `""` (пустая строка) |
| `input.duration` | string | Длительность видео в секундах | `"5"` |
| `input.negative_prompt` | string | Элементы, которых следует избегать в видео | `"blur, distort, and low quality"` |
| `input.cfg_scale` | number | CFG scale (насколько близко следовать промпту) | `0.5` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 2500 символов
- **Важно**: Не может быть пустым! Максимум 2500 символов!

#### `image_url`
- **Тип**: string (не массив!)
- **Обязательный**: да
- **Описание**: URL изображения для использования в видео
- **Ограничения**:
  - Максимум 10MB на файл
  - Форматы: JPEG, PNG, WebP
  - Должен быть валидным URL (начинается с `http://` или `https://`)
- **Default**: `"https://file.aiquickdraw.com/custom-page/akr/section-images/1759211376283gfcw5zcy.png"`
- **ВАЖНО**: Это string, а не массив! (в отличие от других i2v моделей, где используется `image_urls`)

#### `tail_image_url`
- **Тип**: string
- **Обязательный**: нет (опциональный!)
- **Описание**: URL изображения для последнего кадра видео
- **Ограничения**:
  - Максимум 10MB на файл
  - Форматы: JPEG, PNG, WebP
  - Должен быть валидным URL (начинается с `http://` или `https://`)
- **Default**: `""` (пустая строка, если не указан)
- **Важно**: Опциональный параметр! Если пустая строка, параметр не передаётся в API

#### `duration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"5"` - 5 секунд (default)
  - `"10"` - 10 секунд
- **Важно**: Опциональный параметр! Только 2 значения!

#### `negative_prompt`
- Тип: string
- Обязательный: нет (опциональный!)
- Максимум: 2496 символов (не 2500!)
- Default: `"blur, distort, and low quality"`
- **Важно**: Опциональный параметр! Максимум 2496 символов (отличается от text-to-video, где 2500)!

#### `cfg_scale`
- Тип: number (float)
- Обязательный: нет (опциональный!)
- Диапазон: 0 - 1
- Шаг: 0.1
- Default: `0.5`
- **Важно**: Опциональный параметр! Число от 0 до 1 (шаг 0.1)!
- **Описание**: CFG (Classifier Free Guidance) scale - мера того, насколько близко модель должна следовать промпту

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Отличия от других моделей

| Параметр | kling/v2-5-turbo-image-to-video-pro | kling-2.6/image-to-video | kling/v2-5-turbo-text-to-video-pro |
|----------|-----------------------------------|--------------------------|-----------------------------------|
| `prompt` max | 2500 символов | 1000 символов | 2500 символов |
| `image_url` | ✅ Обязательный string | ✅ Обязательный массив (`image_urls`) | ❌ Нет |
| `tail_image_url` | ✅ Опциональный string | ❌ Нет | ❌ Нет |
| `duration` | "5" \| "10" | "5" \| "10" | "5" \| "10" |
| `negative_prompt` | ✅ Есть (max 2496) | ❌ Нет | ✅ Есть (max 2500) |
| `cfg_scale` | ✅ Есть (0-1) | ❌ Нет | ✅ Есть (0-1) |
| `aspect_ratio` | ❌ Нет | ❌ Нет | ✅ Есть ("16:9" \| "9:16" \| "1:1") |
| `sound` | ❌ Нет | ✅ Есть (boolean) | ❌ Нет |

**ВАЖНО**: 
- `image_url` - это string, а не массив! Если передан `image_urls`, он будет удалён с предупреждением
- `negative_prompt` максимум 2496 символов (не 2500, как в text-to-video)!
- Нет параметра `aspect_ratio` (в отличие от text-to-video версии)!

## 🔧 Реализация

### Валидация

Функция `_validate_kling_v2_5_turbo_image_to_video_pro()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано
2. **Длина `prompt`**: Максимум 2500 символов
3. **Обязательность `image_url`**: Поле должно быть указано (string, не массив!)
4. **Формат `image_url`**: Должен быть валидным URL
5. **Формат `tail_image_url`**: Если указан, должен быть валидным URL (опциональный)
6. **Значение `duration`**: Должно быть "5" или "10" (опциональный)
7. **Длина `negative_prompt`**: Максимум 2496 символов (опциональный)
8. **Значение `cfg_scale`**: Должно быть числом от 0 до 1, шаг 0.1 (опциональный)

### Нормализация

1. **`_normalize_duration_for_kling_v2_5_turbo()`**: Нормализует duration ("5" | "10")
2. **`_normalize_cfg_scale()`**: Нормализует cfg_scale (число 0-1, шаг 0.1)

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'image_url' not in normalized_input:
    normalized_input['image_url'] = "https://file.aiquickdraw.com/custom-page/akr/section-images/1759211376283gfcw5zcy.png"
if 'duration' not in normalized_input:
    normalized_input['duration'] = "5"  # Default согласно документации
if 'negative_prompt' not in normalized_input:
    normalized_input['negative_prompt'] = "blur, distort, and low quality"  # Default согласно документации
if 'cfg_scale' not in normalized_input:
    normalized_input['cfg_scale'] = 0.5  # Default согласно документации
# tail_image_url опциональный, default "" (пустая строка) - не добавляем если не указан
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "kling/v2-5-turbo-image-to-video-pro",
  "input": {
    "prompt": "Astronaut instantly teleports through a glowing magical wooden door.",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1759211376283gfcw5zcy.png",
    "tail_image_url": "",
    "duration": "5",
    "negative_prompt": "blur, distort, and low quality",
    "cfg_scale": 0.5
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Укажите текстовое описание видео |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 2500 символов | Сократите описание до 2500 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения |
| `Поле 'image_url' должно быть валидным URL...` | URL не начинается с `http://` или `https://` | Используйте полный URL с протоколом |
| `Поле 'tail_image_url' должно быть валидным URL...` | URL не начинается с `http://` или `https://` | Используйте полный URL с протоколом |
| `Поле 'duration' должно быть одним из...` | Неверное значение `duration` | Используйте "5" или "10" |
| `Поле 'negative_prompt' слишком длинное...` | `negative_prompt` больше 2496 символов | Сократите до 2496 символов |
| `Поле 'cfg_scale' должно быть числом...` | Неверное значение `cfg_scale` | Используйте число от 0 до 1 (шаг 0.1) |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("kling/v2-5-turbo-image-to-video-pro")
user_payload = {
    "prompt": "Astronaut instantly teleports through a glowing magical wooden door.",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1759211376283gfcw5zcy.png",
    "tail_image_url": "",
    "duration": "5",
    "negative_prompt": "blur, distort, and low quality",
    "cfg_scale": 0.5
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Добавлена схема `i2v` с полями `tail_image_url` и `cfg_scale`
- [x] Реализована функция `_validate_kling_v2_5_turbo_image_to_video_pro()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Реализовано автоматическое удаление `image_urls` (если передан)
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=kling/v2-5-turbo-image-to-video-pro TYPE=i2v INPUT_KEYS=['prompt', 'image_url', 'tail_image_url', 'duration', 'negative_prompt', 'cfg_scale'] 
INPUT_PREVIEW={'prompt': 'Astronaut instantly teleports...', 'image_url': 'https://...', 'duration': '5', 'negative_prompt': 'blur, distort, and low quality', 'cfg_scale': 0.5}
```

Если передан параметр `image_urls`, будет предупреждение:
```
Parameter 'image_urls' is not supported for kling/v2-5-turbo-image-to-video-pro (use 'image_url' as string), removing it
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео
2. **prompt максимум 2500 символов** - проверяется при валидации
3. **image_url обязательный** - обязателен для генерации видео! Это string, а не массив!
4. **tail_image_url опциональный** - не обязательный! Если пустая строка, параметр не передаётся
5. **duration опциональный** - не обязательный! Только 2 значения: "5" или "10"!
6. **negative_prompt опциональный** - не обязательный! Максимум 2496 символов (не 2500)!
7. **cfg_scale опциональный** - не обязательный! Число от 0 до 1, шаг 0.1!
8. **НЕТ параметра aspect_ratio** - в отличие от text-to-video версии, параметр `aspect_ratio` не поддерживается!
9. **НЕТ параметра sound** - в отличие от других моделей Kling, параметр `sound` не поддерживается!













