# Интеграция bytedance/v1-pro-image-to-video

## 📋 Обзор

Документация по интеграции модели `bytedance/v1-pro-image-to-video` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `bytedance/v1-pro-image-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"bytedance/v1-pro-image-to-video"` |
| `input.prompt` | string | Текстовый промпт для генерации видео | Обязательный, максимум 10000 символов |
| `input.image_url` | string | URL изображения для генерации видео | Обязательный, максимум 10MB, форматы: jpeg/png/webp |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.resolution` | string | Разрешение видео - 480p для быстрой генерации, 720p для баланса, 1080p для более высокого качества | `"720p"` |
| `input.duration` | string | Длительность видео в секундах | `"5"` |
| `input.camera_fixed` | boolean | Фиксировать ли позицию камеры | `false` |
| `input.seed` | number | Случайное зерно для управления генерацией видео. Используйте -1 для случайного. | `-1` |
| `input.enable_safety_checker` | boolean | Safety checker всегда включен в Playground. Может быть отключен только установкой false через API. | `true` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 10000 символов
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10000 символов!

#### `image_url`
- Тип: string
- Обязательный: да
- Максимальный размер файла: 10MB
- Форматы: `image/jpeg`, `image/png`, `image/webp`
- **Важно**: Обязательный параметр! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp!

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"480p"`, `"720p"`, `"1080p"`
- Default: `"720p"`
- **Важно**: Опциональный параметр! Только 3 указанных значения! Default: `"720p"`! 480p для быстрой генерации, 720p для баланса, 1080p для более высокого качества!

#### `duration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения: `"5"`, `"10"`
- Default: `"5"`
- **Важно**: Опциональный параметр! Только 2 указанных значения (string enum)! Default: `"5"`! Длительность видео в секундах!

#### `camera_fixed`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `false`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `false`! Определяет, фиксировать ли позицию камеры!

#### `seed`
- Тип: number
- Обязательный: нет (опциональный!)
- Диапазон: -1 до 2147483647
- Специальное значение: `-1` для случайного seed
- Default: `-1`
- **Важно**: Опциональный параметр! Число для управления генерацией видео! Используйте -1 для случайного seed! Default: `-1` (случайный seed)! Диапазон: -1 до 2147483647!

#### `enable_safety_checker`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)! Default: `true`! Safety checker всегда включен в Playground, может быть отключен только установкой false через API!

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Особенности модели

| Параметр | bytedance/v1-pro-image-to-video | bytedance/v1-lite-image-to-video |
|----------|----------------------------------|----------------------------------|
| `prompt` | ✅ Обязательный (max 10000) | ✅ Обязательный (max 10000) |
| `image_url` | ✅ Обязательный (max 10MB) | ✅ Обязательный (max 10MB) |
| `resolution` | ✅ Есть (enum: 480p/720p/1080p) | ✅ Есть (enum: 480p/720p/1080p) |
| `duration` | ✅ Есть (enum: "5"/"10") | ✅ Есть (enum: "5"/"10") |
| `camera_fixed` | ✅ Есть (boolean) | ✅ Есть (boolean) |
| `seed` | ✅ Есть (может быть -1, default -1) | ✅ Есть (может быть -1, default -1) |
| `enable_safety_checker` | ✅ Есть (boolean) | ✅ Есть (boolean) |
| `end_image_url` | ❌ Нет | ✅ Есть (string, default "") |
| `fps` | ❌ Нет | ❌ Нет |
| `with_audio` | ❌ Нет | ❌ Нет |
| `width` | ❌ Нет | ❌ Нет |
| `height` | ❌ Нет | ❌ Нет |
| `guidance` | ❌ Нет | ❌ Нет |
| `steps` | ❌ Нет | ❌ Нет |
| `negative_prompt` | ❌ Нет | ❌ Нет |
| `motion` | ❌ Нет | ❌ Нет |
| `style` | ❌ Нет | ❌ Нет |
| `strength` | ❌ Нет | ❌ Нет |

**ВАЖНО**: 
- Это модель для генерации видео из изображения (image-to-video) - Pro версия!
- Использует `camera_fixed` для управления позицией камеры (уникальный параметр)!
- Использует `duration` как string enum ("5" или "10")!
- НЕТ параметра `end_image_url` (есть в v1-lite-image-to-video)!
- `seed` может быть -1 для случайного seed, default -1!
- `prompt` имеет максимум 10000 символов (больше, чем у большинства других моделей)!
- НЕТ параметров `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `negative_prompt`, `motion`, `style`, `strength` (в отличие от других i2v моделей)!

## 🔧 Реализация

### Валидация

Функция `_validate_bytedance_v1_pro_image_to_video()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано, не пустое, максимум 10000 символов
2. **Обязательность `image_url`**: Поле должно быть указано, не пустое, string
3. **Значение `resolution`**: Должно быть одним из 3 допустимых значений (опциональный)
4. **Значение `duration`**: Должно быть одним из 2 допустимых значений (опциональный, string enum)
5. **Значение `camera_fixed`**: Должно быть boolean (опциональный)
6. **Значение `seed`**: Должно быть числом в диапазоне от -1 до 2147483647, может быть -1 для случайного (опциональный)
7. **Значение `enable_safety_checker`**: Должно быть boolean (опциональный)

### Нормализация

1. **`_normalize_boolean()`**: Нормализует `camera_fixed` и `enable_safety_checker` (boolean) - переиспользуется из других моделей
2. **`duration`**: Нормализуется в строку ("5" или "10") если передан как число
3. **`seed`**: Нормализуется в число, проверяется диапазон от -1 до 2147483647

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'resolution' not in normalized_input:
    normalized_input['resolution'] = "720p"  # Default согласно документации
if 'duration' not in normalized_input:
    normalized_input['duration'] = "5"  # Default согласно документации
if 'camera_fixed' not in normalized_input:
    normalized_input['camera_fixed'] = False  # Default согласно документации
if 'seed' not in normalized_input:
    normalized_input['seed'] = -1  # Default согласно документации (случайный seed)
if 'enable_safety_checker' not in normalized_input:
    normalized_input['enable_safety_checker'] = True  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "bytedance/v1-pro-image-to-video",
  "input": {
    "prompt": "A golden retriever dashing through shallow surf at the beach, back angle camera low near waterline, splashes frozen in time, blur trails in waves and paws, afternoon sun glinting off wet fur, overcast day, dramatic clouds",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755179021328w1nhip18.webp",
    "resolution": "720p",
    "duration": "5",
    "camera_fixed": false,
    "seed": -1,
    "enable_safety_checker": true
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Введите текстовое описание для генерации видео |
| `Поле 'prompt' не может быть пустым` | `prompt` пустой | Введите непустое текстовое описание |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 10000 символов | Сократите описание до 10000 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения для генерации видео |
| `Поле 'image_url' не может быть пустым` | `image_url` пустой | Укажите непустой URL изображения |
| `Поле 'resolution' должно быть одним из...` | Неверное значение `resolution` | Используйте одно из: 480p, 720p, 1080p |
| `Поле 'duration' должно быть одним из...` | Неверное значение `duration` | Используйте одно из: 5, 10 |
| `Поле 'camera_fixed' должно быть boolean...` | Неверное значение `camera_fixed` | Используйте true/false |
| `Поле 'seed' должно быть в диапазоне...` | Неверное значение `seed` | Используйте число от -1 до 2147483647 |
| `Поле 'enable_safety_checker' должно быть boolean...` | Неверное значение `enable_safety_checker` | Используйте true/false |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("bytedance/v1-pro-image-to-video")
user_payload = {
    "prompt": "A golden retriever dashing through shallow surf...",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1755179021328w1nhip18.webp",
    "resolution": "720p",
    "duration": "5",
    "camera_fixed": False,
    "seed": -1,
    "enable_safety_checker": True
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Параметры уже есть в схеме `i2v` (camera_fixed, enable_safety_checker)
- [x] Реализована функция `_validate_bytedance_v1_pro_image_to_video()`
- [x] Переиспользованы функции нормализации из других моделей
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=bytedance/v1-pro-image-to-video TYPE=i2v INPUT_KEYS=['prompt', 'image_url', 'resolution', 'duration', 'camera_fixed', 'seed', 'enable_safety_checker'] 
INPUT_PREVIEW={'prompt': 'A golden retriever...', 'image_url': 'https://...', 'resolution': '720p', 'duration': '5', 'camera_fixed': False, 'seed': -1, 'enable_safety_checker': True}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео (максимум 10000 символов)
2. **image_url обязательный** - обязателен для генерации видео! Не может быть пустым! Максимум 10MB! Форматы: jpeg/png/webp!
3. **resolution опциональный** - не обязательный! Только 3 значения, default "720p"! 480p для быстрой генерации, 720p для баланса, 1080p для более высокого качества!
4. **duration опциональный** - не обязательный! Только 2 значения (string enum: "5", "10"), default "5"! Длительность видео в секундах!
5. **camera_fixed опциональный** - не обязательный! Boolean (true/false), default false! Определяет, фиксировать ли позицию камеры! Уникальный параметр для этой модели!
6. **seed опциональный** - не обязательный! Число для управления генерацией видео! Используйте -1 для случайного seed! Default: -1 (случайный seed)! Диапазон: -1 до 2147483647!
7. **enable_safety_checker опциональный** - не обязательный! Boolean (true/false), default true! Safety checker всегда включен в Playground, может быть отключен только установкой false через API!
8. **Уникальные параметры** - модель использует `camera_fixed` вместо стандартных `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `negative_prompt`, `motion`, `style`, `strength`!
9. **НЕТ стандартных параметров** - модель НЕ поддерживает `fps`, `with_audio`, `width`, `height`, `guidance`, `steps`, `negative_prompt`, `motion`, `style`, `strength` (в отличие от других i2v моделей)!
10. **prompt max 10000** - `prompt` имеет максимум 10000 символов (больше, чем у большинства других моделей)!
11. **Отличия от v1-lite-image-to-video** - НЕТ параметра `end_image_url` (есть в v1-lite-image-to-video)!

