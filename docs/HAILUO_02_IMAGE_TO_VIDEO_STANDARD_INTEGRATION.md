# Интеграция hailuo/02-image-to-video-standard

## 📋 Обзор

Документация по интеграции модели `hailuo/02-image-to-video-standard` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `hailuo/02-image-to-video-standard`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"hailuo/02-image-to-video-standard"` |
| `input.prompt` | string | Текстовый промпт, описывающий видео для генерации | Обязательный, максимум 1500 символов |
| `input.image_url` | string | URL изображения для использования в качестве первого кадра видео | Обязательный string (не массив!), максимум 10MB, форматы: JPEG, PNG, WebP |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.end_image_url` | string | Опциональный URL изображения для использования в качестве последнего кадра видео | `"https://file.aiquickdraw.com/custom-page/akr/section-images/1758521423357w8586uq8.png"` |
| `input.duration` | string | Длительность видео в секундах. 10 секундные видео не поддерживаются для 1080p разрешения | `"10"` |
| `input.resolution` | string | Разрешение сгенерированного видео | `"768P"` |
| `input.prompt_optimizer` | boolean | Использовать ли оптимизатор промпта модели | `true` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 1500 символов
- **Важно**: Не может быть пустым! Максимум 1500 символов (меньше чем у других моделей hailuo)!

#### `image_url`
- **Тип**: string (не массив!)
- **Обязательный**: да
- **Описание**: URL изображения для использования в качестве первого кадра видео
- **Ограничения**:
  - Максимум 10MB на файл
  - Форматы: JPEG, PNG, WebP
  - Должен быть валидным URL (начинается с `http://` или `https://`)
- **Default**: `"https://file.aiquickdraw.com/custom-page/akr/section-images/17585207681646umf3lz8.png"`
- **ВАЖНО**: Это string, а не массив! (в отличие от других i2v моделей, где используется `image_urls`)

#### `end_image_url`
- **Тип**: string
- **Обязательный**: нет (опциональный!)
- **Описание**: Опциональный URL изображения для использования в качестве последнего кадра видео
- **Ограничения**:
  - Максимум 10MB на файл
  - Форматы: JPEG, PNG, WebP
  - Должен быть валидным URL (начинается с `http://` или `https://`)
- **Default**: `"https://file.aiquickdraw.com/custom-page/akr/section-images/1758521423357w8586uq8.png"`
- **Важно**: Опциональный параметр! Если пустая строка, параметр не передаётся в API

#### `duration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"6"` - 6 секунд
  - `"10"` - 10 секунд (default)
- **Важно**: Опциональный параметр! Только 2 значения!
- **Примечание**: 10 секундные видео не поддерживаются для 1080p разрешения (но здесь нет 1080p, только 512P и 768P)

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"512P"` - 512P
  - `"768P"` - 768P (default)
- **Важно**: Опциональный параметр! Только 2 значения (в отличие от 2-3 версии, где есть 1080P)!
- **Поддерживаемые алиасы**:
  - `"512"`, `"512p"`, `"512P"` → `"512P"`
  - `"768"`, `"768p"`, `"768P"` → `"768P"`

#### `prompt_optimizer`
- Тип: boolean
- Обязательный: нет (опциональный!)
- Default: `true`
- **Важно**: Опциональный параметр! Boolean (true/false)!
- **Описание**: Использовать ли оптимизатор промпта модели
- **Поддерживаемые значения**:
  - `true`, `1`, `"true"`, `"yes"`, `"y"`, `"on"` → `true`
  - `false`, `0`, `"false"`, `"no"`, `"n"`, `"off"` → `false`

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Отличия от других моделей Hailuo

| Параметр | hailuo/02-image-to-video-standard | hailuo/02-image-to-video-pro | hailuo/2-3-image-to-video-pro |
|----------|----------------------------------|------------------------------|-------------------------------|
| `prompt` max | 1500 символов | 1500 символов | 5000 символов |
| `image_url` | ✅ Обязательный string | ✅ Обязательный string | ✅ Обязательный string |
| `end_image_url` | ✅ Есть (default URL) | ✅ Есть (default "") | ❌ Нет |
| `duration` | ✅ Есть ("6" \| "10", default "10") | ❌ Нет | ✅ Есть ("6" \| "10", default "6") |
| `resolution` | ✅ Есть ("512P" \| "768P", default "768P") | ❌ Нет | ✅ Есть ("768P" \| "1080P", default "768P") |
| `prompt_optimizer` | ✅ Есть (boolean) | ✅ Есть (boolean) | ❌ Нет |

**ВАЖНО**: 
- `prompt` максимум 1500 символов (меньше чем у 2-3 версии)!
- `image_url` - это string, а не массив! Если передан `image_urls`, он будет удалён с предупреждением
- Есть параметр `end_image_url` с default значением (в отличие от pro версии, где default "")!
- Есть параметры `duration` и `resolution` (в отличие от pro версии)!
- `duration` default "10" (в отличие от 2-3 версии, где default "6")!
- `resolution` только "512P" | "768P" (в отличие от 2-3 версии, где есть "1080P")!

## 🔧 Реализация

### Валидация

Функция `_validate_hailuo_02_image_to_video_standard()` проверяет:

1. **Обязательность `prompt`**: Поле должно быть указано
2. **Длина `prompt`**: Максимум 1500 символов
3. **Обязательность `image_url`**: Поле должно быть указано (string, не массив!)
4. **Формат `image_url`**: Должен быть валидным URL
5. **Формат `end_image_url`**: Если указан, должен быть валидным URL (опциональный)
6. **Значение `duration`**: Должно быть "6" или "10" (опциональный)
7. **Значение `resolution`**: Должно быть "512P" или "768P" (опциональный)
8. **Значение `prompt_optimizer`**: Должно быть boolean (опциональный)

### Нормализация

1. **`_normalize_resolution_for_hailuo_02_standard()`**: Нормализует resolution ("512P" | "768P")
2. **`_normalize_duration_for_hailuo_2_3_pro()`**: Нормализует duration ("6" | "10") - переиспользуется из 2-3 версии
3. **`_normalize_boolean()`**: Нормализует prompt_optimizer (boolean) - переиспользуется из других моделей

### Дефолтные значения

Если параметры не указаны, применяются:
```python
if 'image_url' not in normalized_input:
    normalized_input['image_url'] = "https://file.aiquickdraw.com/custom-page/akr/section-images/17585207681646umf3lz8.png"
if 'end_image_url' not in normalized_input:
    normalized_input['end_image_url'] = "https://file.aiquickdraw.com/custom-page/akr/section-images/1758521423357w8586uq8.png"
if 'duration' not in normalized_input:
    normalized_input['duration'] = "10"  # Default согласно документации
if 'resolution' not in normalized_input:
    normalized_input['resolution'] = "768P"  # Default согласно документации
if 'prompt_optimizer' not in normalized_input:
    normalized_input['prompt_optimizer'] = True  # Default согласно документации
```

## 📊 Пример использования

### Запрос

```json
{
  "model": "hailuo/02-image-to-video-standard",
  "input": {
    "prompt": "Epic aerial shot: A lone samurai stands atop a jagged mountain peak.",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17585207681646umf3lz8.png",
    "end_image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1758521423357w8586uq8.png",
    "duration": "10",
    "resolution": "768P",
    "prompt_optimizer": true
  }
}
```

### Ответ

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## 🚨 Ошибки валидации

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Поле 'prompt' обязательно...` | `prompt` не указан | Укажите текстовое описание видео |
| `Поле 'prompt' слишком длинное...` | `prompt` больше 1500 символов | Сократите описание до 1500 символов |
| `Поле 'image_url' обязательно...` | `image_url` не указан | Укажите URL изображения |
| `Поле 'image_url' должно быть валидным URL...` | URL не начинается с `http://` или `https://` | Используйте полный URL с протоколом |
| `Поле 'end_image_url' должно быть валидным URL...` | URL не начинается с `http://` или `https://` | Используйте полный URL с протоколом |
| `Поле 'duration' должно быть одним из...` | Неверное значение `duration` | Используйте "6" или "10" |
| `Поле 'resolution' должно быть одним из...` | Неверное значение `resolution` | Используйте "512P" или "768P" |
| `Поле 'prompt_optimizer' должно быть boolean...` | Неверное значение `prompt_optimizer` | Используйте true/false |

## 📝 Интеграция в код

Модель автоматически валидируется при вызове `build_input()`:

```python
from app.services.kie_input_builder import build_input
from app.kie_catalog import get_model

model_spec = get_model("hailuo/02-image-to-video-standard")
user_payload = {
    "prompt": "Epic aerial shot: A lone samurai stands atop a jagged mountain peak.",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/17585207681646umf3lz8.png",
    "end_image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1758521423357w8586uq8.png",
    "duration": "10",
    "resolution": "768P",
    "prompt_optimizer": True
}

input_dict, error = build_input(model_spec, user_payload)
if error:
    print(f"Ошибка: {error}")
else:
    print(f"Input: {input_dict}")
```

## ✅ Чеклист интеграции

- [x] Реализована функция `_normalize_resolution_for_hailuo_02_standard()`
- [x] Реализована функция `_validate_hailuo_02_image_to_video_standard()`
- [x] Добавлена валидация в `build_input()`
- [x] Добавлены дефолтные значения
- [x] Переиспользованы функции нормализации из других моделей
- [x] Реализовано автоматическое удаление `image_urls` (если передан)
- [x] Создана документация

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=hailuo/02-image-to-video-standard TYPE=i2v INPUT_KEYS=['prompt', 'image_url', 'end_image_url', 'duration', 'resolution', 'prompt_optimizer'] 
INPUT_PREVIEW={'prompt': 'Epic aerial shot...', 'image_url': 'https://...', 'end_image_url': 'https://...', 'duration': '10', 'resolution': '768P', 'prompt_optimizer': True}
```

Если передан параметр `image_urls`, будет предупреждение:
```
Parameter 'image_urls' is not supported for hailuo/02-image-to-video-standard (use 'image_url' as string), removing it
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt обязательный** - обязателен для генерации видео
2. **prompt максимум 1500 символов** - меньше чем у 2-3 версии (5000)!
3. **image_url обязательный** - обязателен для генерации видео! Это string, а не массив!
4. **end_image_url опциональный** - не обязательный! Имеет default значение (в отличие от pro версии)!
5. **duration опциональный** - не обязательный! Только 2 значения: "6" или "10", default "10"!
6. **resolution опциональный** - не обязательный! Только 2 значения: "512P" или "768P", default "768P"!
7. **prompt_optimizer опциональный** - не обязательный! Boolean (true/false), default true!
8. **Отличия от pro версии** - есть параметры `duration` и `resolution`, `end_image_url` имеет default значение!
9. **Отличия от 2-3 версии** - другие значения resolution (нет 1080P), default duration "10" (не "6")!

