# 🚨 СТРОГИЕ ПРАВИЛА ИНТЕГРАЦИИ МОДЕЛЕЙ KIE AI

## ⚠️ КРИТИЧЕСКИ ВАЖНО: ОБЯЗАТЕЛЬНОЕ СОБЛЮДЕНИЕ

**ВСЕ инструкции по моделям, которые предоставляются пользователем, являются ОБЯЗАТЕЛЬНЫМИ и СТРОГИМИ правилами работы.**

## 📋 ОСНОВНОЕ ПРАВИЛО

> **ВСЕ модели ДОЛЖНЫ работать СТРОГО согласно предоставленной документации API.**
> 
> **НИКАКИХ отклонений, НИКАКИХ предположений, НИКАКИХ "улучшений" без явного указания пользователя.**

## ✅ ЧТО ДОЛЖНО БЫТЬ ЗАФИКСИРОВАНО

### 1. Параметры запроса

**СТРОГО по документации:**
- ✅ Обязательные параметры — ДОЛЖНЫ быть обязательными в валидации
- ✅ Опциональные параметры — ДОЛЖНЫ быть опциональными
- ✅ Типы данных — ДОЛЖНЫ соответствовать документации (string, boolean, number, array)
- ✅ Значения по умолчанию — ДОЛЖНЫ быть установлены согласно документации
- ✅ Ограничения (максимальная длина, диапазоны) — ДОЛЖНЫ быть проверены

### 2. Валидация параметров

**СТРОГО по документации:**
- ✅ Проверка обязательности полей
- ✅ Проверка типов данных
- ✅ Проверка ограничений (длина, диапазоны, enum значения)
- ✅ Проверка форматов (URL, base64, и т.д.)
- ✅ Сообщения об ошибках должны быть понятными для пользователя

### 3. Нормализация параметров

**СТРОГО по документации:**
- ✅ Приведение типов согласно документации
- ✅ Нормализация значений (например, "6" | "10" для duration)
- ✅ Обработка алиасов (если указаны в схеме)
- ✅ Удаление недопустимых параметров (whitelist подход)

### 4. Дефолтные значения

**СТРОГО по документации:**
- ✅ Если в документации указан default — ДОЛЖЕН быть установлен
- ✅ Если default не указан — НЕ устанавливать произвольные значения
- ✅ Если параметр опциональный и default не указан — оставить как есть

### 5. Документация

**СТРОГО по документации:**
- ✅ Создать файл `docs/{MODEL_NAME}_INTEGRATION.md` для каждой модели
- ✅ Указать все параметры с их типами и ограничениями
- ✅ Указать отличия от других моделей (если есть)
- ✅ Указать примеры использования
- ✅ Указать возможные ошибки валидации
- ✅ Обновить `docs/OFFICIAL_API_DOCUMENTATION_INDEX.md`

## 🔧 ПРОЦЕСС ИНТЕГРАЦИИ

### Шаг 1: Анализ документации

1. ✅ Прочитать ВСЮ документацию модели
2. ✅ Выделить обязательные параметры
3. ✅ Выделить опциональные параметры
4. ✅ Выделить ограничения (длина, диапазоны, enum)
5. ✅ Выделить дефолтные значения
6. ✅ Выделить отличия от других моделей

### Шаг 2: Обновление схемы входных параметров

1. ✅ Проверить тип модели в `app/kie_catalog/input_schemas.py`
2. ✅ Добавить недостающие параметры в схему (если нужно)
3. ✅ Обновить `REQUIRED_FIELDS` (если нужно)

### Шаг 3: Создание функции валидации

1. ✅ Создать функцию `_validate_{model_id}()` в `app/services/kie_input_builder.py`
2. ✅ Проверить обязательность всех обязательных параметров
3. ✅ Проверить типы данных
4. ✅ Проверить ограничения (длина, диапазоны, enum)
5. ✅ Нормализовать значения (если нужно)
6. ✅ Вернуть понятное сообщение об ошибке при невалидных данных

### Шаг 4: Добавление дефолтных значений

1. ✅ Добавить блок установки дефолтов в `build_input()`
2. ✅ Использовать ТОЛЬКО значения из документации
3. ✅ НЕ устанавливать произвольные значения

### Шаг 5: Интеграция в build_input()

1. ✅ Добавить вызов функции валидации в `build_input()`
2. ✅ Убедиться, что валидация вызывается ДО отправки в API
3. ✅ Убедиться, что ошибки валидации возвращаются пользователю

### Шаг 6: Создание документации

1. ✅ Создать `docs/{MODEL_NAME}_INTEGRATION.md`
2. ✅ Описать все параметры
3. ✅ Описать отличия от других моделей
4. ✅ Привести примеры
5. ✅ Описать возможные ошибки
6. ✅ Обновить `docs/OFFICIAL_API_DOCUMENTATION_INDEX.md`

### Шаг 7: Тестирование

1. ✅ Проверить валидацию обязательных параметров
2. ✅ Проверить валидацию опциональных параметров
3. ✅ Проверить валидацию ограничений
4. ✅ Проверить установку дефолтных значений
5. ✅ Проверить обработку ошибок

## ❌ ЧТО ЗАПРЕЩЕНО

### ❌ НЕ ДОПУСКАЕТСЯ:

1. **Игнорировать параметры из документации**
   - ❌ "Этот параметр не нужен" — НЕТ! Если в документации есть — ДОЛЖЕН быть!

2. **Добавлять параметры, которых нет в документации**
   - ❌ "Это улучшит работу" — НЕТ! Только то, что в документации!

3. **Изменять типы данных**
   - ❌ "Лучше сделать number вместо string" — НЕТ! Строго по документации!

4. **Игнорировать ограничения**
   - ❌ "Максимум 5000, но можно и больше" — НЕТ! Строго 5000!

5. **Устанавливать произвольные дефолты**
   - ❌ "По умолчанию будет 10" — НЕТ! Только если указано в документации!

6. **Предполагать поведение**
   - ❌ "Наверное, это работает так" — НЕТ! Только то, что явно указано!

7. **Упрощать валидацию**
   - ❌ "Проверю только обязательность" — НЕТ! Все ограничения из документации!

## ✅ ПРИМЕРЫ ПРАВИЛЬНОЙ ИНТЕГРАЦИИ

### Пример 1: Обязательный параметр с ограничением

**Документация:**
```
prompt: string, Required: Yes, Max Length: 1500 characters
```

**Правильная реализация:**
```python
# Валидация prompt: обязательный, максимум 1500 символов
prompt = normalized_input.get('prompt')
if not prompt:
    return False, "Поле 'prompt' обязательно для генерации видео"
if not isinstance(prompt, str):
    prompt = str(prompt)
prompt_len = len(prompt.strip())
if prompt_len == 0:
    return False, "Поле 'prompt' не может быть пустым"
if prompt_len > 1500:
    return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 1500)"
```

### Пример 2: Опциональный параметр с enum значениями

**Документация:**
```
duration: string, Required: No, Options: "6" | "10", Default: "6"
```

**Правильная реализация:**
```python
# Валидация duration: опциональный, "6" | "10", default "6"
duration = normalized_input.get('duration')
if duration is not None:
    normalized_duration = _normalize_duration(duration)
    if normalized_duration is None:
        valid_values = ["6", "10"]
        return False, f"Поле 'duration' должно быть одним из: {', '.join(valid_values)}"
    normalized_input['duration'] = normalized_duration

# Дефолтное значение
if 'duration' not in normalized_input:
    normalized_input['duration'] = "6"  # Default согласно документации
```

### Пример 3: Обязательный параметр с типом boolean

**Документация:**
```
prompt_optimizer: boolean, Required: No, Default: true
```

**Правильная реализация:**
```python
# Валидация prompt_optimizer: опциональный boolean
prompt_optimizer = normalized_input.get('prompt_optimizer')
if prompt_optimizer is not None:
    normalized_bool = _normalize_boolean(prompt_optimizer)
    if normalized_bool is None:
        return False, f"Поле 'prompt_optimizer' должно быть boolean (true/false)"
    normalized_input['prompt_optimizer'] = normalized_bool

# Дефолтное значение
if 'prompt_optimizer' not in normalized_input:
    normalized_input['prompt_optimizer'] = True  # Default согласно документации
```

## 📝 ЧЕКЛИСТ ДЛЯ КАЖДОЙ МОДЕЛИ

При интеграции новой модели проверьте:

- [ ] Все обязательные параметры из документации добавлены в валидацию
- [ ] Все опциональные параметры из документации обработаны
- [ ] Все ограничения (длина, диапазоны, enum) проверяются
- [ ] Все дефолтные значения установлены согласно документации
- [ ] Типы данных соответствуют документации
- [ ] Сообщения об ошибках понятны для пользователя
- [ ] Создана документация `docs/{MODEL_NAME}_INTEGRATION.md`
- [ ] Обновлен `docs/OFFICIAL_API_DOCUMENTATION_INDEX.md`
- [ ] Функция валидации добавлена в `build_input()`
- [ ] Дефолтные значения добавлены в `build_input()`

## 🎯 ЦЕЛЬ

**Обеспечить 100% соответствие работы бота официальной документации KIE AI API для каждой модели.**

**НИКАКИХ исключений. НИКАКИХ упрощений. ТОЛЬКО строгое следование документации.**

---

**Дата создания:** 2025-01-22  
**Статус:** АКТИВНО  
**Приоритет:** КРИТИЧЕСКИЙ

