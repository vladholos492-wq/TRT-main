# BetBoom Live Scanner

Автономный сканер live-настольного тенниса на BetBoom без установленного Python.

## 🚀 Быстрый запуск

1. **Скачайте папку** `betboom_scanner`
2. **Дважды кликните** на `start_scanner.bat`
3. **Готово!** При первом запуске автоматически установятся все зависимости

## 📋 Требования

- Windows 10/11
- Интернет-соединение (для загрузки зависимостей и работы сканера)
- ~200 МБ свободного места (для Python и браузера)

## 🎯 Что делает сканер

Сканирует live-настольный теннис на BetBoom и ищет сигналы, когда:

1. ✅ Есть явный фаворит по матчу (коэффициент ≤ 1.20)
2. ✅ Фаворит ведёт 2:0 по сетам
3. ✅ 3-й сет ещё не начался (0:0) или только начался (1:0 / 0:1)
4. ✅ На победу в 3-м сете фаворита коэффициент ≥ 1.90

При обнаружении сигнала:
- 🔊 Громкий звуковой сигнал
- 🔔 Windows toast уведомление
- 📝 Запись в `app_data/signals.csv` и `app_data/signals.sqlite`

## 📁 Структура проекта

```
betboom_scanner/
├── app/                  # Основной код
│   ├── main.py          # Точка входа + GUI
│   ├── scanner.py       # Playwright сканер
│   ├── models.py        # Модели данных
│   ├── storage.py       # CSV + SQLite
│   ├── notify.py        # Звук + toast
│   └── config.py        # Конфигурация
├── tools/
│   ├── bootstrap_windows.ps1  # Автоустановка uv + Python
│   └── build_exe.ps1          # Сборка .exe (опционально)
├── start_scanner.bat    # Запуск приложения
├── pyproject.toml       # Зависимости
└── README.md
```

## 🔧 Что происходит при первом запуске

1. **Bootstrap скрипт** (`bootstrap_windows.ps1`):
   - Скачивает `uv` (менеджер Python)
   - Устанавливает Python 3.12+ в `.runtime/python`
   - Создаёт виртуальное окружение `.venv`
   - Устанавливает зависимости (playwright, winotify)
   - Устанавливает браузер Chromium

2. **Запуск приложения**:
   - Открывается окно Tkinter
   - Запускается Playwright браузер
   - Начинается сканирование каждые 2-5 секунд

## 📊 Интерфейс

- **Таблица матчей**: показывает все live матчи с коэффициентами и счётом
- **Кнопки**:
  - `Start` — начать сканирование
  - `Stop` — остановить сканирование
  - `Open signals.csv` — открыть файл с сигналами
  - `Open logs folder` — открыть папку с логами
- **Статус-бар**: показывает количество матчей, сигналов и время последнего скана

## 🔍 Как работает парсинг

1. **Приоритет**: перехват сетевых запросов (response interception)
   - Подписка на `page.on("response")`
   - Фильтрация JSON ответов
   - Извлечение данных матчей из API

2. **Fallback**: парсинг DOM (если API недоступен)
   - Поиск элементов матчей на странице
   - Извлечение данных из HTML

## ⚙️ Настройки

Настройки можно изменить в `app/config.py`:

- `MAX_MATCH_ODDS = 1.20` — максимальный коэффициент фаворита
- `MIN_SET3_ODDS = 1.90` — минимальный коэффициент на 3-й сет
- `SCAN_INTERVAL_MIN/MAX` — интервал сканирования (2-5 сек)

## 📝 Логи

- **Сигналы**: `app_data/signals.csv` и `app_data/signals.sqlite`
- **Ошибки**: `app_data/errors.log`

## 🛠️ Сборка .exe (опционально)

Если хотите собрать один .exe файл:

```powershell
.\tools\build_exe.ps1
```

Результат: `dist/BetBoomScanner.exe`

## ⚠️ Автовосстановление

При падении браузера или ошибках:
- Автоматический перезапуск браузера
- Retry механизм (до 3 попыток)
- Логирование всех ошибок

## 🔒 Безопасность

- Работает гостем (без логина)
- Не хранит пароли
- Все данные локально
- Использует только официальный Playwright

## 🐛 Решение проблем

**Проблема**: Bootstrap не скачивает uv
- **Решение**: Проверьте интернет-соединение, запустите PowerShell от администратора

**Проблема**: Playwright не устанавливается
- **Решение**: Запустите вручную: `.venv\Scripts\python.exe -m playwright install chromium`

**Проблема**: Нет данных в таблице
- **Решение**: Проверьте, что на BetBoom есть live-матчи, обновите страницу в браузере

## 📞 Поддержка

Все логи сохраняются в `app_data/errors.log` — проверьте их при проблемах.

