# Структура проекта BetBoom Live Scanner

## 📁 Полная структура файлов

```
betboom_scanner/
│
├── app/                          # Основной код приложения
│   ├── __init__.py              # Инициализация модуля
│   ├── main.py                  # Точка входа + Tkinter GUI
│   ├── scanner.py               # Playwright сканер с network interception
│   ├── models.py                # Dataclasses для матчей и сигналов
│   ├── storage.py               # CSV + SQLite хранилище
│   ├── notify.py                # Звуковые и toast уведомления
│   └── config.py                # Конфигурация (пороги, URL, интервалы)
│
├── tools/                       # Вспомогательные скрипты
│   ├── bootstrap_windows.ps1    # Автоустановка uv + Python + зависимости
│   └── build_exe.ps1            # Сборка .exe через PyInstaller
│
├── start_scanner.bat            # ⭐ ГЛАВНЫЙ ФАЙЛ ДЛЯ ЗАПУСКА
│
├── pyproject.toml               # Зависимости проекта
├── README.md                    # Подробная документация
├── QUICK_START.txt              # Быстрый старт
├── STRUCTURE.md                 # Этот файл
└── .gitignore                   # Игнорируемые файлы
```

## 🔄 Поток данных

```
start_scanner.bat
    ↓
bootstrap_windows.ps1 (если первый запуск)
    ↓
app/main.py
    ├── ScannerGUI (Tkinter)
    └── BetBoomScanner
        ├── Playwright Browser
        ├── Network Interception (response.on)
        ├── DOM Fallback Parser
        └── Signal Detection Logic
            ↓
        Storage (CSV + SQLite)
        Notify (Sound + Toast)
```

## 🎯 Ключевые компоненты

### 1. Bootstrap (`tools/bootstrap_windows.ps1`)
- Скачивает `uv` (менеджер Python)
- Устанавливает Python 3.12+ в `.runtime/python`
- Создаёт venv в `.venv`
- Устанавливает зависимости (playwright, winotify)
- Устанавливает Chromium браузер

### 2. GUI (`app/main.py`)
- Tkinter окно с таблицей матчей
- Кнопки управления (Start/Stop)
- Статус-бар с метриками
- Интеграция с async scanner через asyncio event loop

### 3. Scanner (`app/scanner.py`)
- Playwright browser manager
- Network response interception для парсинга API
- DOM fallback parser
- Автоматическое восстановление при ошибках
- Retry механизм

### 4. Models (`app/models.py`)
- `MatchData` - данные матча
- `MatchOdds` - коэффициенты
- `MatchScore` - счёт по сетам
- `Signal` - обнаруженный сигнал
- `check_signal_conditions()` - логика проверки

### 5. Storage (`app/storage.py`)
- CSV файл для всех сигналов
- SQLite база данных
- Подсчёт сигналов за день

### 6. Notify (`app/notify.py`)
- Windows системный звук (winsound)
- Windows toast уведомления (winotify)

## 🚀 Требования к запуску

1. **Windows 10/11**
2. **Интернет-соединение** (для bootstrap и сканирования)
3. **~200 МБ свободного места** (Python + браузер)
4. **БЕЗ установленного Python** (устанавливается автоматически)

## 📊 Данные

Все данные сохраняются в `app_data/`:
- `signals.csv` - CSV с сигналами
- `signals.sqlite` - SQLite база
- `errors.log` - логи ошибок

## 🔧 Конфигурация

Настройки в `app/config.py`:
- `MAX_MATCH_ODDS = 1.20` - порог фаворита
- `MIN_SET3_ODDS = 1.90` - минимальный кф на 3-й сет
- `SCAN_INTERVAL_MIN/MAX` - интервал сканирования (2-5 сек)
- `LIVE_URL` - URL страницы live-матчей

## 🐛 Отладка

Логи:
- `app_data/errors.log` - все ошибки приложения
- Консольный вывод (если запускается из командной строки)

Проверка:
- Откройте браузер Playwright (headless=False) чтобы увидеть страницу
- Проверьте network tab для API запросов
- Используйте DOM fallback если API недоступен

