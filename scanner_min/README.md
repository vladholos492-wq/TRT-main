# BetBoom Live Scanner (Minimal)

Минимальный надёжный парсер live-настольного тенниса на BetBoom.

## 🚀 Быстрый старт

```bash
# 1. Создать виртуальное окружение
python -m venv .venv

# 2. Активировать (Windows)
.venv\Scripts\activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Установить браузер Playwright
playwright install chromium

# 5. Запустить
python app.py
```

## 📋 Что делает

Сканирует live-настольный теннис на BetBoom и выводит матчи, где:
- Фаворит (min(P1, P2) <= 1.20) ведёт 2:0 по сетам
- 3-й сет ещё 0:0 (или 1:0 или 0:1)
- Коэффициент на 3-й сет фаворита >= 1.90

## 📊 Вывод

Каждые 2-5 секунд выводится таблица с найденными сигналами:

```
TIME     | MATCH_URL                                          | SETS        | GAME3   | MATCH_ODDS      | SET3_ODDS       | SIGNAL
----------------------------------------------------------------------------------------------------------------------------
12:34:56 | https://betboom.ru/...                            | 2:0, 3:1    | 0:0     | 1.15,4.20       | 2.10,1.85       | OK (P1 1.15 -> 2.10)
```

Если сигналов нет, выводится:
```
NO SIGNALS | Total matches scanned: 5
```

## 🔧 Технические детали

- **Python 3.8+** с asyncio
- **Playwright** для браузера (headless=False по умолчанию)
- **Network interception** для парсинга API (приоритет)
- **DOM fallback** если API недоступен
- **Автоматическое восстановление** при ошибках
- **Защита от падений**: try/except, таймауты, перезапуск браузера

## 📁 Структура

```
scanner_min/
├── app.py              # Основной цикл и точка входа
├── bb.py               # Парсер BetBoom (network + DOM)
├── requirements.txt    # Зависимости
└── README.md          # Этот файл
```

## ⚙️ Настройки

Все настройки в коде:
- URL: `https://betboom.ru/sport/table-tennis?period=all&type=live`
- Интервал сканирования: 2-5 секунд (случайный)
- Пороги: 1.20 (фаворит), 1.90 (3-й сет)

## 🐛 Решение проблем

**Браузер не запускается:**
- Проверьте установку Playwright: `playwright install chromium`
- Запустите от администратора (если нужно)

**Нет данных:**
- Проверьте интернет-соединение
- Убедитесь, что на странице есть live-матчи
- Попробуйте открыть браузер вручную (headless=False)

**Ошибки парсинга:**
- Скрипт автоматически перезапускает браузер при ошибках
- Проверьте логи в консоли

