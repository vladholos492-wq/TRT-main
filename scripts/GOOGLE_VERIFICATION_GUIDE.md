# 🔐 Руководство по подтверждению через Google/YouTube

## Проблема

При авторизации на KIE AI может потребоваться подтверждение через Gmail/YouTube (двухфакторная аутентификация или OAuth).

## ✅ Решение

Используйте **интерактивный режим** скрипта:

```bash
python scripts/parse_kie_documentation.py --interactive
```

## 📋 Пошаговая инструкция

### 1. Запуск в интерактивном режиме

```bash
python scripts/parse_kie_documentation.py --interactive
```

### 2. Процесс авторизации

1. **Браузер откроется автоматически**
   - Вы увидите страницу логина KIE AI
   - Логин и пароль будут введены автоматически (если указаны в `.env`)

2. **Если появится запрос на подтверждение:**
   - Откройте Gmail или YouTube на другом устройстве
   - Найдите код подтверждения (обычно 6-значный)
   - Или подтвердите через приложение Google Authenticator

3. **Введите код в браузере:**
   - Введите код в поле подтверждения
   - Или нажмите "Подтвердить" в приложении Google

4. **Ожидание автоматического продолжения:**
   - Скрипт автоматически обнаружит успешное подтверждение
   - Или нажмите Enter в консоли после подтверждения

### 3. Сохранение состояния

После успешной авторизации:
- Состояние сессии сохраняется в `.cache/kie_storage_state.json`
- При следующих запусках подтверждение **не потребуется**
- Можно использовать обычный режим: `python scripts/parse_kie_documentation.py`

## 🎯 Альтернативные варианты

### Вариант 1: Видимый браузер (без интерактивного режима)

```bash
python scripts/parse_kie_documentation.py --no-headless
```

Браузер будет видимым, но скрипт не будет ждать подтверждения автоматически.

### Вариант 2: Полностью ручная авторизация

1. Запустите скрипт с `--no-headless`
2. Выполните авторизацию вручную в браузере
3. После успешной авторизации нажмите Enter в консоли

## ⚠️ Важные моменты

1. **Время ожидания:** Скрипт ждёт подтверждения до 5 минут
2. **Автоматическое обнаружение:** Скрипт автоматически определяет, когда подтверждение завершено
3. **Один раз:** После первого успешного подтверждения, состояние сохраняется
4. **Безопасность:** Состояние сессии хранится локально в `.cache/`

## 🔧 Устранение проблем

### Проблема: Скрипт не ждёт подтверждения

**Решение:** Используйте `--interactive` флаг:
```bash
python scripts/parse_kie_documentation.py --interactive
```

### Проблема: Браузер закрывается до подтверждения

**Решение:** Используйте `--no-headless`:
```bash
python scripts/parse_kie_documentation.py --no-headless
```

### Проблема: Код подтверждения не приходит

**Решение:**
1. Проверьте Gmail/YouTube на другом устройстве
2. Проверьте спам-папку
3. Убедитесь, что номер телефона/email правильный
4. Попробуйте запросить новый код

### Проблема: Состояние не сохраняется

**Решение:**
1. Убедитесь, что директория `.cache/` существует и доступна для записи
2. Проверьте права доступа к файлу `.cache/kie_storage_state.json`
3. Попробуйте удалить старый файл и авторизоваться заново

## 📝 Пример полного процесса

```bash
# 1. Установите переменные окружения
export KIE_EMAIL=your_email@example.com
export KIE_PASSWORD=your_password

# 2. Запустите в интерактивном режиме
python scripts/parse_kie_documentation.py --interactive

# 3. В консоли увидите:
# 🔐 Требуется подтверждение через Google/YouTube
# 💡 Ожидание ручного подтверждения...
#    Пожалуйста, выполните подтверждение в открытом браузере
#    После успешного подтверждения нажмите Enter здесь...

# 4. В браузере:
# - Введите код из Gmail/YouTube
# - Или подтвердите через Google Authenticator

# 5. Скрипт автоматически продолжит работу:
# ✅ Подтверждение завершено!
# ✅ Авторизация успешна!
# 💾 Состояние сохранено в .cache/kie_storage_state.json

# 6. При следующих запусках:
python scripts/parse_kie_documentation.py
# Подтверждение не потребуется!
```

## 🎉 Готово!

После успешной авторизации скрипт продолжит парсинг всей документации автоматически.

