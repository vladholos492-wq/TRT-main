# 🚀 Быстрый старт: Парсинг документации KIE AI

## Шаг 1: Установка зависимостей

```bash
pip install playwright python-dotenv
playwright install chromium
```

## Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
KIE_EMAIL=your_email@example.com
KIE_PASSWORD=your_password
```

Или установите переменные окружения:

**Windows (PowerShell):**
```powershell
$env:KIE_EMAIL="your_email@example.com"
$env:KIE_PASSWORD="your_password"
```

**Linux/Mac:**
```bash
export KIE_EMAIL=your_email@example.com
export KIE_PASSWORD=your_password
```

## Шаг 3: Запуск

### Обычный режим (без подтверждения):
```bash
python scripts/parse_kie_documentation.py
```

### Интерактивный режим (для подтверждения через Google/YouTube):
```bash
python scripts/parse_kie_documentation.py --interactive
```

Или с видимым браузером:
```bash
python scripts/parse_kie_documentation.py --no-headless
```

**Важно:** Если у вас включено подтверждение через Gmail/YouTube, используйте `--interactive` или `--no-headless`!

## 📁 Результаты

Все данные будут сохранены в:
- `data/kie_documentation/full_documentation_*.json` - полный дамп
- `data/kie_documentation/documentation_structured.json` - структурированные данные
- `data/kie_documentation/models_text/` - текстовые версии для каждой модели

## ⚠️ Важно

- Скрипт автоматически сохраняет состояние авторизации в `.cache/kie_storage_state.json`
- При следующем запуске авторизация будет автоматической
- Парсинг может занять несколько минут (зависит от количества моделей)

### 🔐 Подтверждение через Google/YouTube

Если при авторизации требуется подтверждение через Gmail/YouTube:
1. Используйте флаг `--interactive` или `--no-headless`
2. Браузер откроется видимым
3. Выполните подтверждение в браузере (введите код из Gmail/YouTube)
4. После подтверждения нажмите Enter в консоли
5. Скрипт продолжит работу автоматически

## 🔧 Если что-то не работает

1. **Ошибка авторизации**: Проверьте логин и пароль
2. **Playwright не установлен**: Выполните `playwright install chromium`
3. **Таймауты**: Увеличьте задержки в коде или проверьте интернет

## 📚 Подробная документация

См. `scripts/README_PARSE_KIE_DOCS.md` для полной документации.

