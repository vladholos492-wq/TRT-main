# Парсинг документации KIE AI

Скрипт для полного парсинга всей документации, инструкций и знаний по всем моделям KIE AI с сайта.

## 🚀 Установка

1. Установите зависимости:
```bash
pip install playwright python-dotenv
```

2. Установите браузер для Playwright:
```bash
playwright install chromium
```

## 📋 Настройка

### Вариант 1: Переменные окружения

Создайте файл `.env` в корне проекта или установите переменные окружения:

```bash
export KIE_EMAIL=your_email@example.com
export KIE_PASSWORD=your_password
```

### Вариант 2: Интерактивный ввод

Если переменные окружения не установлены, скрипт запросит их интерактивно при запуске.

## 🎯 Использование

### Базовый запуск:
```bash
python scripts/parse_kie_documentation.py
```

### Интерактивный режим (для подтверждения через Google/YouTube):
```bash
python scripts/parse_kie_documentation.py --interactive
```

### С видимым браузером (для отладки):
```bash
python scripts/parse_kie_documentation.py --no-headless
```

### Все опции:
```bash
python scripts/parse_kie_documentation.py --help
```

## 📊 Что парсится

Скрипт собирает:

1. **Главная страница документации** (`docs.kie.ai`)
   - Структура документации
   - Ссылки на все модели
   - Общая информация

2. **Market Documentation** (`docs.kie.ai/market`)
   - Все модели по категориям (Image, Video, Audio)
   - Ссылки на документацию каждой модели

3. **Документация каждой модели**
   - Quickstart guides
   - API Reference
   - Code Samples
   - Параметры и схемы
   - Полный текст документации

4. **Данные из API**
   - Список всех моделей через KIE API
   - Детали моделей и их modes

## 💾 Результаты

Все данные сохраняются в `data/kie_documentation/`:

- `full_documentation_YYYYMMDD_HHMMSS.json` - полный дамп всех данных
- `documentation_structured.json` - структурированные данные (без HTML)
- `models_text/` - текстовые версии документации для каждой модели

## 🔐 Авторизация

Скрипт автоматически:
- Авторизуется на сайте KIE AI (если указаны логин/пароль)
- Сохраняет состояние сессии в `.cache/kie_storage_state.json`
- Использует сохранённое состояние при следующих запусках

### Подтверждение через Google/YouTube

Если при авторизации требуется подтверждение через Gmail/YouTube (2FA/OAuth):

1. **Используйте интерактивный режим:**
   ```bash
   python scripts/parse_kie_documentation.py --interactive
   ```

2. **Процесс:**
   - Браузер откроется видимым
   - Введите логин и пароль (или они будут введены автоматически)
   - Если появится запрос на подтверждение через Google/YouTube:
     - Откройте Gmail/YouTube на другом устройстве
     - Введите код подтверждения в браузере
     - Или подтвердите через приложение Google
   - После успешного подтверждения нажмите Enter в консоли
   - Скрипт продолжит работу автоматически

3. **Автоматическое определение:**
   - Скрипт автоматически определяет, требуется ли подтверждение
   - Если требуется, он переходит в режим ожидания
   - Периодически проверяет, завершилось ли подтверждение

4. **Сохранение состояния:**
   - После успешной авторизации состояние сохраняется
   - При следующих запусках подтверждение не потребуется

## ⚙️ Настройки

Можно изменить в коде:

- `headless=True` → `headless=False` - для отладки (покажет браузер)
- `await asyncio.sleep(1)` - задержка между запросами (можно увеличить)
- `timeout=30000` - таймаут загрузки страниц

## 📝 Пример вывода

```
🚀 Запуск парсера документации KIE AI...
🔐 Попытка авторизации...
✅ Авторизация успешна!
💾 Состояние сохранено в .cache/kie_storage_state.json
📚 Парсинг главной страницы документации...
📚 Парсинг Market Documentation...
📡 Получение списка моделей из API...
✅ Получено 47 моделей из API
📚 Парсинг документации для 47 моделей...
  [1/47] z-image
  [2/47] nano-banana-pro
  ...
💾 Сохранение результатов...
✅ Полный дамп сохранён: data/kie_documentation/full_documentation_20241219_120000.json
✅ Структурированные данные сохранены: data/kie_documentation/documentation_structured.json
✅ Текстовые версии сохранены в data/kie_documentation/models_text/

================================================================================
📊 СТАТИСТИКА ПАРСИНГА
================================================================================
Всего моделей обработано: 47
Моделей из API: 47
Моделей с Quickstart: 45
Моделей с API Reference: 47
Моделей с Code Samples: 42

Файлы сохранены в: data/kie_documentation
================================================================================
```

## 🔧 Устранение проблем

### Ошибка авторизации

Если авторизация не работает:
1. Проверьте правильность логина и пароля
2. Попробуйте запустить с `headless=False` для визуальной отладки
3. Удалите `.cache/kie_storage_state.json` и попробуйте снова

### Таймауты

Если страницы не загружаются:
- Увеличьте `timeout` в коде
- Проверьте интернет-соединение
- Увеличьте задержки между запросами

### Не все модели спарсились

- Проверьте логи на ошибки
- Некоторые модели могут иметь нестандартные URL
- Попробуйте запустить скрипт повторно

## 📚 Использование результатов

Спарсенные данные можно использовать для:

1. **Автоматической генерации валидаторов** - на основе API Reference
2. **Обновления документации бота** - на основе Quickstart guides
3. **Проверки соответствия** - сравнение с текущей реализацией
4. **Добавления новых моделей** - автоматическое извлечение параметров

## 🔄 Обновление

Для обновления документации просто запустите скрипт снова. Он перезапишет старые файлы новыми данными.

