# 🚀 Быстрый запуск BB TT Scanner

## Вариант 1: Готовый .exe (рекомендуется)

1. Перейдите в папку `dist/`
2. Запустите `BB_TT_Scanner.exe`
3. Готово! Приложение автоматически начнёт работу

**При первом запуске:**
- Откроется браузер
- Если нужно — войдите в BetBoom
- После входа сканирование начнётся автоматически

## Вариант 2: Из исходников

### Шаг 1: Установка Python
```powershell
# Проверьте версию (нужна 3.11+)
python --version
```

### Шаг 2: Установка зависимостей
```powershell
cd bb_tt_scanner
python -m pip install -r requirements.txt
python -m playwright install chromium
```

### Шаг 3: Запуск
```powershell
python run.py
# или
python -m app.main
```

## Сборка .exe

Если хотите собрать .exe самостоятельно:

```powershell
cd bb_tt_scanner
.\scripts\build_win.ps1
```

После сборки создайте ярлык:
```powershell
.\scripts\make_desktop_shortcut.ps1
```

## Что делать если пусто?

1. **Проверьте браузер**: Откройте "Open Live" — должна загрузиться страница BetBoom
2. **Проверьте логин**: Убедитесь, что вы залогинены
3. **Проверьте матчи**: На странице должны быть live-матчи настольного тенниса
4. **Подождите**: Данные могут появиться через 10-30 секунд после загрузки страницы

## Настройка автозапуска

Приложение автоматически запускает сканирование при старте. Кнопка "Start" уже нажата.

Чтобы отключить автозапуск, измените в `app/main.py`:
```python
# Закомментируйте эту строку:
# QTimer.singleShot(1000, lambda: asyncio.create_task(run_scanner(scanner, window)))
```

---

**Вопросы?** См. полный README.md



