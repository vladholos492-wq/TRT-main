# BB TT Scanner

**BetBoom Table Tennis Live Scanner** — Windows desktop приложение для сканирования live-настольного тенниса на BetBoom с алертами по стратегии TT_LIVE_V1.

## 🚀 Быстрый старт (2 минуты)

### Вариант 1: Готовый .exe

1. **Скачайте** `BB_TT_Scanner.exe` из папки `dist/`
2. **Запустите** двойным кликом
3. **Готово!** Приложение автоматически:
   - Откроет браузер
   - Перейдёт на страницу live-настолки
   - Начнёт сканирование

**Первый запуск:**
- Если требуется логин, приложение откроет браузер — войдите в BetBoom
- После логина сканирование продолжится автоматически
- Сессия сохранится в `./bb_profile` (больше логиниться не нужно)

### Вариант 2: Из исходников

1. **Установите Python 3.11+**
   ```powershell
   # Проверка версии
   python --version
   ```

2. **Установите зависимости**
   ```powershell
   cd bb_tt_scanner
   python -m pip install -r requirements.txt
   python -m playwright install chromium
   ```

3. **Запустите приложение**
   ```powershell
   python -m app.main
   ```

## 📦 Сборка .exe

Если хотите собрать .exe самостоятельно:

```powershell
# Из корня проекта bb_tt_scanner
.\scripts\build_win.ps1
```

После сборки создайте ярлык на рабочем столе:

```powershell
.\scripts\make_desktop_shortcut.ps1
```

## 🎯 Как работает

1. **Автозапуск**: При запуске приложение автоматически начинает сканирование
2. **Network capture**: Перехватывает XHR/fetch запросы с данными о матчах
3. **Нормализация**: Адаптивно парсит разные структуры JSON
4. **Стратегия**: Проверяет условия TT_LIVE_V1 (REVERSAL_NOT_END)
5. **Алерты**: При сигнале — звук, уведомление в трее, всплывающее окно

## 📊 Интерфейс

### Левая панель: Таблица матчей
- **Match**: Игроки
- **League**: Турнир
- **Sets**: Счёт по сетам
- **Points**: Очки в текущем сете
- **Odds Main/Hedge**: Коэффициенты и рынки
- **Status**: WATCH / CANDIDATE / SIGNAL / STALE
- **Reason**: Причина сигнала
- **Updated**: Время последнего обновления

**Цвета:**
- 🔴 **SIGNAL**: Красный фон — обнаружен сигнал
- 🟡 **CANDIDATE**: Жёлтый фон — близко к сигналу
- ⚪ **STALE**: Серый фон — нет обновлений >60 сек

### Правая панель: Последний сигнал
- Информация о сигнале
- P&L калькулятор (сценарии A/B/C/D)
- Блок "Почему это НЕ конец сета"
- Кнопки: "Copy bet text", "Export CSV"

### Верхняя панель
- **Start/Stop**: Запуск/остановка сканирования
- **Open Live**: Открыть страницу в браузере
- **Settings**: Настройки стратегии
- **Health**: Статус подключения, активные матчи, события/мин, время обновления

## ⚙️ Настройки

В диалоге Settings можно изменить:
- **Min/Max diff**: Диапазон разницы очков для сигнала (по умолчанию 2-4)
- **Odds min/max**: Диапазон коэффициентов для MAIN (по умолчанию 1.70-2.60)
- **Unit**: Базовая единица ставки в рублях (по умолчанию 30₽)

## 🎲 Стратегия TT_LIVE_V1

### Условия сигнала REVERSAL_NOT_END:

1. ✅ **Не конец сета**: `max(points_p1, points_p2) <= 8`
2. ✅ **Отставание**: Один игрок отстаёт на 2-4 очка
3. ✅ **Моментум**: За последние 3 тика разница улучшилась минимум на 2 в сторону отстающего
4. ✅ **Коэффициент**: Кф на отстающего в диапазоне 1.70-2.60
5. ✅ **Live**: Матч реально live (счёт обновляется)

### Выбор рынков:

**MAIN** (приоритет):
1. "Победа в текущем сете" на отстающего
2. "Победа в матче" на отстающего

**HEDGE** (если доступно):
1. "Фора по очкам в текущем сете +X.5/+Y.5" на отстающего (кф 1.35-1.80)
2. "Тотал очков в текущем сете Больше" (кф 1.35-1.80, только если счёт >= 6:6 или сумма >= 12)

### Суммы:
- С HEDGE: MAIN = 2*unit, HEDGE = 1*unit
- Без HEDGE: MAIN = 1*unit
- Максимум на матч: 3*unit

### P&L сценарии:
- **A**: MAIN win, HEDGE lose → `profit = MAIN*(Kmain-1) - Hedge`
- **B**: MAIN lose, HEDGE win → `profit = Hedge*(Khedge-1) - Main`
- **C**: BOTH win → `profit = MAIN*(Kmain-1) + Hedge*(Khedge-1)` (если применимо)
- **D**: BOTH lose → `profit = -(MAIN + Hedge)`

## 🔔 Алерты и анти-спам

- **При SIGNAL**: Звук (beep) + уведомление в трее + всплывающее окно
- **Cooldown**: 1 сигнал на match_id не чаще 1 раза в 180 секунд (если reason не изменился)
- **System tray**: Приложение работает в фоне, можно свернуть в трей

## 💾 Журнал сигналов

Все сигналы сохраняются в SQLite базу данных (`bb_scanner.db`):
- Timestamp
- Match ID, название
- Рынки, коэффициенты, суммы
- P&L данные
- Raw данные события

**Экспорт**: Кнопка "Export CSV" в правой панели

## 🛠️ Структура проекта

```
bb_tt_scanner/
├── app/
│   ├── main.py              # Точка входа
│   ├── gui/                 # GUI компоненты
│   │   ├── main_window.py   # Главное окно
│   │   └── models.py        # Модель таблицы
│   ├── scanner/             # Сканер
│   │   ├── browser.py       # Playwright браузер
│   │   └── scanner.py       # Основной сканер
│   ├── engine/              # Движок
│   │   ├── normalizer.py    # Нормализация событий
│   │   └── strategy.py      # Стратегия TT_LIVE_V1
│   └── storage/             # Хранилище
│       └── database.py      # SQLite база данных
├── scripts/
│   ├── build_win.ps1        # Сборка .exe
│   └── make_desktop_shortcut.ps1  # Ярлык на Desktop
├── dist/                    # Готовый .exe (после сборки)
├── logs/                    # Логи приложения
├── bb_profile/              # Профиль браузера (сессия)
├── requirements.txt         # Зависимости
└── README.md               # Этот файл
```

## 🐛 Решение проблем

### Приложение не запускается
- Убедитесь, что Python 3.11+ установлен
- Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
- Установите браузеры Playwright: `python -m playwright install chromium`

### Пустая таблица (нет матчей)
1. Проверьте, что страница BetBoom загрузилась в браузере
2. Убедитесь, что вы залогинены
3. Проверьте, что на странице есть live-матчи
4. Включите Debug режим (если доступен) для просмотра перехваченных данных

### Браузер не открывается
- Проверьте, что Playwright установлен: `python -m playwright install chromium`
- Попробуйте удалить профиль `./bb_profile` и перезапустить

### Сессия не сохраняется
- Убедитесь, что папка `./bb_profile` доступна для записи
- Проверьте права доступа к папке

## 📝 Логи

Логи сохраняются в `./logs/app.log` с ротацией (10 MB, 7 дней).

## ⚠️ Важно

- **Нет автоставок**: Приложение только сканирует и выдаёт сигналы
- **Не гарантия**: Стратегия — это фильтр, не гарантия выигрыша
- **Тестирование**: Протестируйте на демо-счёте перед реальным использованием

## 📄 Лицензия

Для личного использования.

---

**Версия**: 1.0.0  
**Автор**: BB TT Scanner Team



